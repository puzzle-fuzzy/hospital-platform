# 当前实施 TODO：支付、医保异常与迁移收口

更新时间：2026-09-19

## 这份文件怎么使用

本文件只保留“当前尚未闭环、下一步可以执行、完成后能够验收”的事项，不再追加逐日执行流水。历史过程仍可从 Git 提交 `059c8d80` 以及各专题文档中追溯；新的调查记录应写入对应专题文档，`TODO.md` 只更新结论、优先级、负责人、证据位置和未验证项。

状态口径：

- “本阶段主流程可用”只表示已经达到当前测试阶段，不表示所有异常、退款、补偿、真机、Provider、微信、HIS 和生产验收均已完成。
- “代码存在”“测试通过”“HTTP 200”“微信客户端回调成功”都不是支付或 HIS 结算最终成功的充分证据。
- 未拿到真实材料时必须写“未验证”或“待确认”，不能从字段名、历史样例或客户端表现推断结论。
- 本文件不保存患者隐私、支付密钥或完整原始报文；需要排障时按[日志规范](docs/日志规范.md)和[支付日志一键导出](docs/操作手册/支付日志一键导出.md)受控交付。
- 真实支付、退费、Provider、医保和 HIS 写入必须单独确认范围；没有用户最终确认时只允许源码、本地测试和只读查询。

当前 TODO 复选框总数为 14 项，其中已完成 0 项、未完成 14 项。
另按标题优先级统计未完成项为：P0 3、P1 6、P2 5、P3 0。

## 当前阶段结论

| 业务范围 | 当前状态 | 已知边界与下一步 |
| --- | --- | --- |
| 医保挂号 | 本阶段主流程可用 | 由本轮实施反馈确认；正常路径暂不作为重构对象。中途退出、网络中断、重复进入、未知结果、现金腿失败、HIS 回写失败和号源释放等异常仍要补齐 |
| 医保门诊 | 本阶段主流程可用 | 由本轮实施反馈确认；正常支付路径暂不作为重构对象。异常恢复、费用明细来源、退款和最终状态仍未完整闭环 |
| 微信支付缴费 | 当前有问题，P0 | 刚完成的测试已暴露异常，但本次整理尚未绑定具体业务入口、订单、错误码和失败阶段；必须先取得同一笔订单的新证据，再判断是纯微信缴费、医保混合支付现金部分、客户端调起、服务端查单还是 HIS 回写问题 |
| 支付中途退出 | 部分能力存在，未完整验收 | 已有部分取消/退出和查单能力；应用被杀、页面销毁、回调丢失、支付成功后未返回以及未知状态下的恢复仍需统一 |
| 退费 | 有局部模型与管理能力，业务闭环未完成 | 退款申请、微信退款结果、医保撤销/退费、HIS 状态、预约或费用状态必须形成同一条可追溯链路 |
| 医保结算页费用明细 | 临时来源可用，正式接口待确认 | 当前只能考虑复用同一患者、同一费用记录的待缴费列表数据；仍需向 HIS/众阳确认是否存在按正式业务 ID 查询单笔订单或处方明细的接口 |
| 旧服务迁移 | 已有广度台账，业务等价仍未全部证明 | 入口有落点不等于真实业务已迁完；需要按领域复核接口、状态、写入、外部主体和当前运行包 |
| 用户体验 | 尚未完成全链路审查 | 重点检查加载、空态、错误、确认中、恢复、重复点击、患者切换、返回路径和人工处理入口 |

### 当前实施原则

1. 先保护已经可用的医保挂号和医保门诊正常路径，再针对单一异常做最小改动；不得为了修复微信缴费问题顺带重写医保公共核心。
2. 先确定业务类型：`registration/RegPay`、`outpatient/DiagPay`、纯医保、医保加现金或普通微信自费，禁止用一个业务的样例替代另一个业务的合同。
3. 先查已有订单，再决定是否允许重试；结果未知时不能创建新订单、重新扣费、重复执行 6201/6202，或释放可能已经支付的号源/费用。
4. 金额以服务端已持久化事实为准，统一使用整数分，并核对总额、医保支付、个人账户、现金、优惠/减免、退款金额及各分项守恒。
5. 客户端支付调起成功只是过程证据。必须继续核对服务端查单、平台订单、Provider/医保状态，以及相应业务需要的 HIS 最终回写。

## 当前执行顺序

1. 先处理 P0-01，定位刚测试暴露的微信缴费问题，并冻结相关订单证据。
2. 在不破坏正常路径的前提下，分别补齐 P0-02 医保挂号和 P0-03 医保门诊异常矩阵。
3. 把 P1-01 的医保变更核对标准作为后续所有支付改动的强制门禁。
4. 再依次处理退出恢复、退款、费用明细和用户体验；迁移剩余项并行做只读审查，不与支付修复混改。

## P0：当前阻塞与高风险异常

### P0-01 定位并修复刚测试暴露的微信缴费问题

- [ ] 完成同一笔测试订单从小程序、API、Worker、微信查单到 Provider/HIS 结果的定位，确认根因并修复；在证据收齐前保持“问题已确认存在、具体原因未验证”。

调查必须按以下顺序执行：

1. 记录测试时间、业务入口、支付方式和用户看到的现象；敏感身份信息不写入本文件。
2. 重新获取这次测试对应的最新订单证据，确认 `orderId/businessId/out_trade_no/mix_trade_no` 等标识是否属于同一笔业务。需要完整日志时使用 `pnpm payment:last` 重新采集，不能复用旧快照。
3. 分开判断客户端调起结果、微信官方查单结果、平台本地订单状态、Worker 补偿状态、Provider 回包和 HIS 最终状态，不能因为任意一层成功就宣布整条链路成功。
4. 明确问题属于普通微信自费、医保混合支付现金部分还是支付后的医院结算；没有定位前不得修改医保正常路径。
5. 检查重复点击、回调丢失、用户返回、网络超时、服务重启、查单延迟和金额不一致是否会产生重复订单或重复扣费。

完成标准：有可复现步骤、根因、最小修复、失败与成功回归、微信服务端查单证据、平台订单状态、对应 Provider/HIS 最终状态、当前运行包验证以及回滚方案。若外部环境仍无法验证，必须明确列出缺少哪一层，不能标记完成。

### P0-02 补齐医保挂号异常矩阵，保护已可用正常路径

- [ ] 在保持医保挂号正常流程不回退的前提下，覆盖授权取消、6201/6202 失败或结果未知、客户端退出、医保查单非终态、现金支付失败、HIS 回写失败和重复进入等异常。

重点核对：

- 预约、患者、号源、医保订单和微信订单的归属必须一致；患者切换或页面重开后不能串单。
- 6201、6202、6301、微信支付和后置 HIS 命令分别记录状态；仅在对应流程适用时执行后置 `.32`、现金分支和 `.5`，不能把所有订单硬套成同一路径。
- 支付状态未知时保留号源和待确认订单，交给查单/补偿/人工复核；只有权威未支付或终态失败证据满足释放规则时才能取消。
- 重试必须复用稳定业务键、订单号和幂等键；不可重放命令不得因超时自动再次执行。
- 验收至少覆盖：首次成功、主动取消、应用被杀、支付成功未返回、回调先到/后到、查单超时、重复点击、服务重启、金额不一致、患者切换和人工恢复。

完成标准：正常路径回归仍通过，异常矩阵逐项有期望状态、用户文案、可操作按钮、补偿责任方、最终证据和回滚结论；不得只以客户端页面或单元测试作为完成依据。

### P0-03 补齐医保门诊异常矩阵，分离账单与结算最终性

- [ ] 在保持医保门诊正常流程不回退的前提下，覆盖待缴费列表变化、处方归属变化、6201/6202 未知、混合支付部分成功、支付后结算失败、重复进入和退款中的异常。

重点核对：

- 门诊 `recordId` 是平台受控引用，不得反向当作 HIS 处方号、收费单号或 Provider 订单号；页面不能提交 Provider 原始标识覆盖服务端事实。
- 支付前服务端重新确认患者、记录、费用状态和金额版本；待缴费条目变化、已缴费、已作废或金额不守恒时停止支付并提示刷新。
- 6202 的金额事实、微信/医保支付结果和 HIS 最终结算分层保存；部分成功必须进入确认中或人工复核，不能显示成全部成功，也不能立即重新支付。
- 同一订单多处方、多项目、零现金、现金差额、退款中和已退款都要有明确状态，不得通过前端重新合计推导权威支付金额。
- 验收至少覆盖：纯医保、医保加现金、待缴费快照变化、支付后返回失败、网络中断、重复操作、已支付后再次打开、退款后打开和患者切换。

完成标准：门诊正常路径回归仍通过，费用、支付和结算三层状态可追溯，异常恢复不重复扣费、不串患者、不丢处方，并形成真机/Provider/微信/HIS 分层证据。

## P1：支付闭环与医保变更强制标准

### P1-01 落实医保流程修改强制核对门禁

- [ ] 将下列核对表实际用于每一次医保相关改动，并在变更说明或专题验收文档中逐项给出“通过、失败或未验证”，禁止只写“测试通过”。

任何触及医保订单、6201/6202/6301、`RegPay/DiagPay`、微信医保调起、混合现金支付、退款或 HIS 回写的改动，都必须执行以下检查：

1. **变更边界**：写清只影响挂号、门诊还是公共核心；只影响纯医保、医保加现金还是普通自费；列出明确不改的正常路径。
2. **变更前基线**：先保存当前可用流程的测试结果、关键状态转换和运行包版本，确保修复后能证明没有回退。
3. **完整时序**：画出或文字列出授权、费用上传、结算、支付调起、服务端查单、Provider/HIS 后置命令、结果页和补偿 Worker 的先后关系；区分可重试与不可重放步骤。
4. **标识映射**：逐项核对 `userId/patientId/businessId/orderId/recordId/out_trade_no/mix_trade_no/serial_no` 等标识的来源、归属和持久化位置，禁止页面自行拼接或跨患者复用。
5. **金额守恒**：只使用服务端权威金额和整数分，核对总额、医保基金、个人账户、现金、医院负担、其它支付、优惠和退款；任何不一致都应 fail-closed。
6. **状态与最终性**：明确每一状态的权威来源、是否终态、允许的用户操作和下一责任方；HTTP 200、6202 返回、客户端回调或收银台成功不能替代服务端查单与业务最终状态。
7. **幂等与恢复**：核对重复点击、超时、乱序回调、服务重启、Worker 重试、页面重开和人工接管；未知结果只能查单和收敛，不能盲目重建订单。
8. **退出、取消与退款**：明确什么时候只退出页面、什么时候关闭未支付订单、什么时候释放号源、什么时候必须退款或医保撤销，以及部分成功时谁负责补偿。
9. **安全与隐私**：所有读写校验当前登录用户和当前就诊人归属；Provider 原始标识、医保凭证、微信密钥和完整原始报文不得下发前端或写入普通日志。
10. **证据分层**：分别报告源码/单测、API/Worker、生成运行包、DevTools/真机、微信、Provider、HIS 和生产证据；缺少哪一层就明确保留为未验证。
11. **发布与回滚**：说明数据库迁移、配置、API/Worker 版本、灰度顺序、监控指标、停止条件和回滚方案；不能用一次脚本成功代替进程与业务健康检查。

以下情况必须停止合并或发布：业务类型不清、订单标识对不上、金额不守恒、存在未知状态却会重新扣费、不可重放步骤没有保护、正常路径回归失败、运行包不是当前源码生成，或缺少可执行回滚方案。

### P1-02 统一中途退出、未知结果和防重复支付

- [ ] 建立挂号、门诊、普通微信和医保支付共用的退出/恢复规则，解决用户中途离开但挂号或缴费实际已经成功的问题。

实施要求：

- 用户返回、关闭页面、切后台或应用被杀时，不以客户端生命周期直接判定失败；再次进入先查服务端订单，再决定继续等待、展示成功、允许重试或进入人工复核。
- 支付按钮必须防抖和服务端幂等；订单结果未知时禁用“重新支付/重新挂号”，提供“查询结果”和明确说明。
- 回调丢失时由服务端主动查单和 Worker 收敛；已支付但 HIS 未完成时显示“支付结果确认中”，不能显示“未支付”诱导再次扣款。
- 只有权威未支付且满足关闭条件时才能关单或释放号源；任何可能已支付的订单都先查单。

完成标准：退出场景矩阵、状态机、幂等测试、Worker 恢复测试、真机返回/杀进程验证和至少一条受控同链证据齐全。

### P1-03 建立退费完整闭环

- [ ] 打通退款申请、微信退款、医保撤销/退费、HIS 回写、业务状态恢复和用户结果展示，明确普通自费、纯医保与医保混合支付的差异。

实施要求：

- 退款前重新核对原订单归属、可退金额、已退金额、支付渠道、业务状态和幂等键；禁止前端指定权威退款金额。
- 区分“退款申请受理”“渠道处理中”“渠道成功”“医保/HIS 已完成”和“人工复核”，不把接口受理当作最终退费成功。
- 混合支付要分别记录医保部分和现金部分，定义部分退款失败时的补偿、重试与用户文案。
- 退款回调、主动查单、重复申请、超时、乱序和服务重启均不得重复退款。
- 患者端提供可理解的退款进度；管理端保留审计、人工接管和不可重放保护。

完成标准：状态机和金额守恒、权限与幂等回归、渠道查单、医保/HIS 最终证据、结果页、告警、人工处理和回滚方案齐全。

### P1-04 补齐医保结算页费用明细/处方明细来源

- [ ] 先提供安全的临时明细方案，同时向 HIS/众阳确认正式的按业务 ID 查询单笔订单或处方明细接口；未确认前不得把列表数据描述成“按 ID 查询的 HIS 处方详情”。

当前事实与临时方案：

- 结算页已有 6202 的总额、医保金额和现金金额，但当前没有正式的项目级处方明细接口。
- 现有单笔 `recordId` 查询本质仍来自待缴费列表读取和服务端匹配；`recordId` 是平台 opaque 标识，不是 HIS 订单号。
- 临时方案只能由服务端在同一登录用户、同一患者、同一费用记录和同一版本下，复用待缴费列表的白名单项目或保存本次快照；页面不能只相信客户端缓存。
- 明细仅用于展示和核对，支付权威金额继续来自 6202。明细缺项、版本变化、身份冲突或金额不一致时提示“费用明细已更新，请刷新确认”并停止支付。

必须向 HIS/众阳确认：

1. 是否能按 `outTradeOrderId/mainId/chargeId/chargeCode/presCode/visitRecordId` 或正式 HIS 就诊号查询单笔处方/费用明细。
2. 接口版本、认证方式、患者引用、业务状态、查询时间窗口、金额单位、舍入规则、退款/作废语义和稳定标识。
3. 返回能否关联 6201、6202、微信/医保订单及 HIS 最终结算，并提供多处方、多项目和结算后重开页面的稳定结果。
4. 至少取得一笔受控脱敏样例和字段字典；接口 HTTP 200 不代表合同已经确认。

完成标准：正式接口材料或经批准的降级合同、服务端归属校验、明细与 6202 金额核对、空/变更/冲突/退款场景测试、运行包及真机展示证据齐全。

### P1-05 审查并优化支付与服务全链路体验

- [ ] 按真实状态重新检查医保挂号、医保门诊和微信缴费的页面、文案、按钮权限、返回路径和恢复入口，消除“看起来失败但其实成功”或“看起来成功但后台未完成”的误导。

审查重点：

- 统一加载、空数据、失败、取消、确认中、成功、部分成功、退款中和人工复核的文案与操作权限。
- 支付方式选择只更新选择状态，必须由用户明确确认后才发起支付；防止双击、多页面并发和无意重复授权。
- 结果页展示服务端可信金额和业务状态；患者切换、会话过期或返回重进时先恢复服务端状态。
- 对可恢复错误给“重试查询”，对未知结果给“不要重复支付”，对需要人工处理的订单给明确入口和可关联编号。
- 用低敏 `requestId/traceId/orderId/providerRequestId` 串联耗时、重试和状态变化，不在普通日志记录敏感原文。

完成标准：状态—文案—按钮映射表、关键交互截图或录屏、可用性测试矩阵、恢复耗时和问题闭环；体验优化不能掩盖尚未确认的业务状态。

### P1-06 建立支付变更的分层验收与发布证据

- [ ] 为每次支付/医保变更形成可复查的证据包，并在发布前确认 API、Worker、数据库迁移、运行包和外部系统使用的是同一版本与同一订单事实。

最低证据层级：

1. 相关 domain、adapter、API、Worker 和小程序定向测试。
2. `pnpm typecheck`，以及按改动范围执行 `pnpm --filter @hospital/api test`、`pnpm --filter @hospital/worker test`、`pnpm --filter @hospital/miniprogram test`。
3. 小程序 `build:dev` 后执行 `runtime:verify:dev`，确认 DevTools/真机加载的是当前生成包而非旧目录。
4. 同一订单的客户端、API、Worker、微信、Provider 和 HIS 关联证据；敏感原文受控存放，不进入 Git。
5. 发布后的进程版本、工作目录、配置、数据库迁移、健康检查、关键状态指标和回滚备份。

完成标准：上述各层分别写明通过、失败或未验证；不得把本地测试通过写成真实支付、Provider 或生产验收完成。

## P2：迁移完整性与非支付剩余范围

### P2-01 收口患者与预约领域

- [ ] 复核患者绑定/目录、预约科室与医生、号源、确认、记录、详情、爽约和我的医生，补齐正式 Provider 合同、归属、锁号生命周期、补偿和真机证据。

当前已有大量页面、服务和本地回归，但仍要区分页面可达、只读列表可用、真实 Provider 写入、号源占用和跨系统最终状态。没有受控测试账号和明确写入确认时继续 fail-closed。

### P2-02 收口报告、门诊记录、病案与住院领域

- [ ] 复核检验/检查/PACS/体检报告、门诊病历、住院 episode、住院预缴和出院相关能力，确认真实正文、附件授权、患者归属、Provider 合同和支付边界。

页面外壳、安全子集或兼容摘要不能写成完整临床业务迁移；涉及临床正文、附件、费用或住院写入的能力必须单独验收。

### P2-03 收口健康内容、问卷与临床辅助领域

- [ ] 复核健康知识、健康计算器、风险评估、预问诊、问诊问卷、智能导诊和出院随访，明确内容审核、算法边界、外部主体、临床责任和关闭态。

静态内容、演示数据、规则型自测和真实临床能力必须分开；没有审核 bundle、正式合同或责任主体时保持安全子集或关闭态。

### P2-04 收口便民、外部入口与后台运营领域

- [ ] 复核便民服务、快递、反馈/表扬、外部 WebView/小程序、患者签名、互联网医院入口和后台管理，确认哪些是旧端真实能力、静态替代、外部主体能力或明确排除项。

旧端本身是空壳、本地假保存或 TODO 的功能不应凭空扩建；外部 URL、登录态、患者身份传递、回跳和数据保留规则未确认时不得标记为业务等价。

### P2-05 重做旧服务到新服务的完整性审查

- [ ] 以旧服务页面、路由、接口、后台任务和外部跳转为输入，重新核对新服务的 contract、adapter、domain、persistence、API、小程序、运行包和真实验收，找出尚未迁移完整的内容。

审查输出至少包含：

- 旧入口到新入口的映射，以及 `complete/partial/replaced/surface-only/blocked/excluded` 的可核验证据。
- 旧 API、WebSocket、支付调用、外部小程序/WebView、二维码/公众号和后台任务的对应关系。
- 已有页面但缺业务合同、已有 API 但缺真实 Provider、已有源码但未进入当前运行包、已有运行包但未真机验收的差异。
- 明确不迁移的旧假功能，以及需要新立项而不能伪装成“迁移遗漏”的新增需求。
- 当前候选运行包、DevTools/真机、Provider、外部主体和生产证据分别处于什么状态。

完成标准：更新[原生页面迁移状态](docs/迁移/原生页面迁移状态.md)、[API 矩阵](docs/迁移/API矩阵.md)和[剩余迁移清单](docs/迁移/剩余迁移清单.md)，运行迁移与文档门禁，并且所有“完成”均能追溯到当前版本证据。

## 每项完成时必须回填的内容

每个 TODO 完成时，在对应专题文档或变更说明中记录：

1. 原问题、影响范围和明确未改范围。
2. 修改前后流程、状态机、标识与金额规则。
3. 源码落点、数据库/配置变化和幂等策略。
4. 成功、取消、失败、超时、乱序、重复、重启、患者切换和越权场景。
5. 单测、API/Worker、小程序运行包、DevTools/真机、Provider、微信、HIS 和生产的分层结果。
6. 原始证据的受控位置、时间窗口、哈希和完整性结论；本文件仅保留低敏摘要。
7. 仍未验证的事项、监控与人工接管方式、回滚步骤。

勾选前需要同时满足：目标行为已实现；相关正常路径没有回退；异常路径有明确状态；不存在已知重复扣费/退款风险；证据与当前源码和运行版本一致。只完成其中一部分时更新进展说明，但保持未勾选。

## 维护与校验

- 修改本文件后执行 `pnpm todo:audit` 和 `pnpm docs:audit`。
- 支付或医保代码变更按 P1-06 执行分层测试，并重新生成、校验实际使用的小程序运行包。
- 排查“刚测试的最后一笔支付”时必须重新执行 `pnpm payment:last`，记录采集窗口和完整性；不得把历史订单日志当作当前测试证据。
- 过程记录超过一屏时移入 `docs/` 下的专题文档，只在本文件保留结论和链接，避免再次变成时间流水账。

## 参考基线

- [医保统一核心与业务入口](docs/架构决策/0005-医保统一核心与业务入口.md)
- [支付契约](docs/迁移/支付契约.md)
- [公共 API v2](docs/公共API-v2.md)
- [三个支付按钮业务说明](docs/miniprogram-pay-三个支付按钮业务说明.md)
- [支付日志一键导出](docs/操作手册/支付日志一键导出.md)
- [非支付存量连续性复核](docs/迁移/非支付存量连续性复核-2026-09-16.md)
