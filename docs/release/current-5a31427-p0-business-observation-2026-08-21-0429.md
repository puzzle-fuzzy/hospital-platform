# `5a31427` 当前 P0 业务空窗口观察（2026-08-21 04:29 CST）

## 观察范围

本记录来自服务器 `192.168.112.172` 的只读 SSH 检查，时间约为
`2026-08-21 04:29:26 CST`。只读取新 API 的 systemd 状态、监听端口和
当前 release 的低敏 journald 聚合；没有修改配置、重启服务、调用 Provider，
也没有写入 MySQL/Redis。旧 Python 服务保持原状态。

| 项目 | 结果 |
| --- | --- |
| 新 API systemd | `active` |
| 当前 release | `5a31427` |
| 新 API 监听 | `10.0.0.3:18081` |
| 旧 Python 监听 | `0.0.0.0:8001` |
| 观察窗口 | 最近约 30 分钟 |
| 聚合输入 | 4 行 |
| 成功解析 | 3 条 |
| 解析错误 | 0 |
| systemd 警告 | 0 |
| HTTP 完成 | 3 条，全部为 `200` |
| 业务事件 | 没有 `auth.*`、`patient.*`、`appointment.*` 或 `outpatient.payment.*` |

## 结论

当前新旧服务共存和日志解析正常，但本窗口没有新的真机业务请求。因此不能把
“服务 active”或基础 HTTP `200`解释成微信登录、患者同步、显式切换、预约历史、
爽约记录或门诊费用验收成功，也不能据此判断 Provider 失败。

当前候选固定为服务端 `5a31427` 与小程序 `acf5a85`，运行包来源为
`acf5a8596e70e1fb2b8d220a0b41eb69418ae086`。下一次业务验收必须使用该候选重新生成
二维码，并在同一时间窗口保留页面、客户端 `traceId/requestId` 和服务端低敏业务事件。

支付、医保、预约写入、报告 Provider、患者绑定、二维码和 HIS 回写继续保持关闭；
旧 Python `8001` 不属于本次改动范围。
