# 当前公网只读复核（2026-08-19 17:23 CST）

## 复核基线

| 项目 | 来源 |
| --- | --- |
| 服务端 | `398be8e`，公网入口 `https://test-hp.meiyi.pro/api/v2` |
| 小程序候选 | `474b044`，完整来源 `474b0444736599c848a4cef9f47fd930884e401d` |
| 复核方式 | 无会话、无患者参数、只读 HTTP GET |
| 影响范围 | 未修改服务器、旧 Python、数据库或 Redis；未调用 Provider |

## 结果

| 路由 | HTTP | 结论 |
| --- | ---: | --- |
| `/health/live` | 200 | 新 API 进程存活，返回 `hospital-api` |
| `/health/ready` | 200 | `database`、`redis`、`schema` 均为 `ok` |
| `/system/ping` | 200 | 当前 `/api/v2` 路由前缀可用 |
| `/me` | 401 | 未携带会话时正确拒绝 |
| `/patients` | 401 | 未携带会话时正确拒绝 |

## 验收边界

本次只证明公网运行层、依赖 readiness 和未授权边界正常，不证明微信登录、患者同步、
`patInfosFind`、预约历史、门诊费用、报告或真机页面已经完成。下一步仍需用户在当前
`474b044` 小程序候选上扫码，取得新的真实微信 session 后，再按“患者同步/显式切换 →
预约历史 → 门诊费用”的只读顺序采集页面、HTTP 和低敏日志三层证据。

支付、医保授权、退款、预约写入、二维码和 HIS 回写继续保持关闭。
