> 当前配套小程序运行包（2026-08-27）：本地 live `dist` 的 sourceRevision 为 `413cbea13f022831f63e9c750661eeabbffc68d5`（`413cbea`），共 40 个页面；当前没有运行中的微信开发者工具或真机会话，九个真机证据域仍为 `pending`。本文下方历史候选仅作追溯。

> 当前配套小程序运行包来源（2026-08-27）：`413cbea13f022831f63e9c750661eeabbffc68d5`（`413cbea`）；当前没有开发者工具或真机会话，九个真机证据域仍为 `pending`。本文下方更早候选仅作历史追溯。

> 当前服务端配套发布更新（2026-08-24 13:01 CST）：线上服务端 release 为 `28a5c0c131794ce9dcc5f94bd3809402188ac87a`；当前小程序运行包来源仍为 `13f597ea9ee3f65b9be858117826d948339d904a`（提交 `13f597e`）。本轮为服务端独立只读 adapter 发布，未重建小程序运行包；下方历史候选仅供追溯，本行优先。
> 历史配套小程序构建来源（2026-08-26）：`0be59f966de2c3a0861cb44e9a526a1ef557f6c7`，仅表示当时本地 live 候选，未证明微信线上版本或真机业务已验收；当前入口以当前项目基线为准。
> 当前线上服务端 release（2026-08-27）：`b44421cd321ff9ff23eeb49b12641d1772d2bdc1`，已完成候选 preflight、隔离 smoke、原子切换和公网 runtime smoke；该运行层证据不等价于真实 Provider 或支付业务成功。
> 当前发布基线更新（2026-08-24 13:01 CST）：线上服务端 release 为 `28a5c0c131794ce9dcc5f94bd3809402188ac87a`；当前小程序运行包来源为 `13f597ea9ee3f65b9be858117826d948339d904a`（提交 `13f597e`）。本轮为服务端独立只读 adapter 发布，真机业务三层证据仍待。
> 本段优先于本文下方旧日期、旧 release 或旧运行包叙述；旧值只作为历史记录，不作为当前验收入口。
> 下方 2026-08-22 的 release 与运行包只作历史追溯；当前执行使用顶部 `28a5c0c1` 服务端 + `13f597e` 小程序分层基线。
> 当前小程序配套运行包来源（2026-08-27）：`413cbea13f022831f63e9c750661eeabbffc68d5`（`413cbea`）；本文中更早候选和真机窗口仅作历史追溯，当前无真机/开发者工具会话。

> 当前运行层复核（2026-08-24）：线上 `28a5c0c1` 使用 `NODE_ENV=production`，新 API `10.0.0.3:18081` 与旧 Python `8001` 共存，内外网健康探针和依赖 ready 均通过；当前本地未发布小程序候选为 `356705e`，不能直接当作线上运行包。journald 因 `sudo` 权限需要密码未形成新的业务事件计数，不能由此推断业务请求为零。详见 [`current-runtime-coexistence-readonly-2026-08-24.md`](current-runtime-coexistence-readonly-2026-08-24.md)。

# 当前候选下一步审计（2026-08-22）
> 当前服务端 release 为 `28a5c0c131794ce9dcc5f94bd3809402188ac87a`，小程序来源为 `13f597ea9ee3f65b9be858117826d948339d904a`。本记录只保留运行层发布已完成、真机业务证据待补的边界。

> 历史开发者工具会话（2026-08-22 18:04 CST）曾使用服务端 `84370077` 与小程序 `a64fe023`；该记录不属于当前候选。
> 本文当前业务边界绑定服务端 release `28a5c0c131794ce9dcc5f94bd3809402188ac87a` 与小程序来源
> `13f597ea9ee3f65b9be858117826d948339d904a`（`13f597e`），
> 不把本地测试通过误写成真实微信业务完成。旧 Python 服务、旧数据库和旧 Redis 不在本轮修改范围内。

> 2026-08-22 的 SSH 拒绝记录只作历史追溯；当前受控发布通道已恢复，`28a5c0c1` 已完成切换。

> 2026-08-22 21:22 CST 页面迁移完整性复核：`pnpm migration:audit` 确认 14 个新页面、64 个旧页面和 195 个旧端挂载路由均有台账记录。
> 对旧端剩余页面逐项复核后，没有发现可在不扩大 Provider/HIS/微信/临床 contract 的前提下安全新增的业务入口；患者绑定、报告详情/附件、
> 病历、住院、健康内容、外部入口、支付和医保继续保持关闭或迁移提示，不使用旧端字段猜测成功路径。

## 当前候选

| 项目 | 当前值 |
| --- | --- |
| 服务端 release | `28a5c0c131794ce9dcc5f94bd3809402188ac87a`（`28a5c0c1`） |
| 小程序提交 | `13f597e` |
| 运行包来源 | `13f597ea9ee3f65b9be858117826d948339d904a` |
| 页面数量 | 14 |
| `dist/` 测试脚本 | `*.test.js` / `*.spec.js` 均为 0 |
| 运行包校验 | `runtime:verify` 通过 |
| 小程序门禁 | 全量测试 `221 pass / 0 fail / 1640 expect()`、typecheck、build、runtime:verify 均通过 |
| 真机连接 | 当前二维码/手机业务连接仍待现场记录 |
| 服务端候选发布 | `28a5c0c1` 已完成线上切换；旧 Python `8001` 继续共存 |

历史 release 的服务端低敏观察曾在 `2026-08-22 10:42–14:42 CST` 窗口内形成微信授权、就诊人读取/同步、预约科室/排班/历史和门诊费用只读链路；
该记录不属于当前 `28a5c0c1` 验收证据，不能替代当前候选的真机页面和客户端 requestId。当前候选最近的低敏观察没有新的真机业务流量，
详见 [`current-business-observation-2026-08-22-1510.md`](current-business-observation-2026-08-22-1510.md)。

## 本轮业务审计结论

### 患者上下文

- 首页的“新增/更换就诊人”统一进入独立患者选择页，不在首页直接写入患者 ID。
- 首次没有本地选择时，只能默认第一位 `clinicalAccess=ready` 的患者。
- 已保存患者失效、目录为空或临床映射不可用时，不会静默切换到另一位患者，必须由用户显式选择。
- 目录读取完成不等于 HIS 临床映射完成；同步期间不能返回业务页，也不能展示上一轮“当前”标记。
- `other` 是服务端关系枚举，前端只将其映射为“其他”；关系缺失使用“关系未提供”，不能混淆两者。
- 卡号展示使用服务端脱敏 contract，最多保留前五位和后四位；小程序不从完整证件号自行推算展示值。

### 患者范围只读页面

预约历史、爽约、门诊费用和报告目录已统一为：

1. 依当前会话重新取得 owner-scoped 患者目录；
2. 只使用服务端返回的内部 `patientId` 发起查询；
3. 依页面实例、会话代际和当前选择丢弃过期响应；
4. 依赖失败、401、网络错误和 Provider 错误进入独立错误态，不落入“暂无数据”空态；
5. 提供“重新加载”和“选择就诊人”恢复入口。

这几类页面的本地分批渲染只解决页面性能，不代表 Provider 分页，也不改变服务端查询窗口和结果语义。

## 仍未形成的证据

当前缺少的是同一个候选来源下的真实手机三层证据：

- 页面确实显示了结果；
- 小程序客户端 requestId 和 HTTP 结果与页面动作对应；
- 服务端 Pino 日志存在同链、低敏、成功或明确失败事件。

因此微信登录、患者显式切换、预约历史、爽约、门诊费用和报告目录仍不能标记为“真实完成”。支付、医保、退款、报告详情/附件和 HIS 写回继续保持关闭。

## 真机人工交接

微信开发者工具当前应导入：

```text
E:\__Super_Core__\hospital-platform\apps\miniprogram
```

不要导入旧的 `mp-weixin`，也不要把 `dist/` 之外的 TypeScript 源目录作为小程序运行根目录。若仍出现
`dist/services/single-flight.test.js`，应关闭旧真机调试、清理文件/编译缓存、重新打开上述项目并普通编译；不得手工复制测试脚本到 `dist/`。

## 2026-08-22 18:04 CST 运行包错误恢复与二维码交接

本轮针对真机报告的 `dist/services/single-flight.test.js` ENOENT 完成了当前候选恢复：

- 重新执行 `pnpm --filter @hospital/miniprogram build` 和 `runtime:verify`，运行包来源为 `171a874`，14 个页面入口齐全；
- 复核 `dist/services/single-flight.js` 存在，`single-flight.test.js` 以及全部 `*.test.js`/`*.spec.js` 均不存在；
- 只关闭并重新打开新项目 `miniprogram` 窗口，旧项目 `mp-weixin` 窗口未操作；
- 新项目普通编译完成，构建面板列出 `services/single-flight.js`，没有同一路径 ENOENT 或页面脚本缺失；
- 重新生成当前候选 iOS/局域网真机二维码，等待手机扫码，尚未形成真机三层业务证据；成功请求的客户端 requestId 已纳入低敏观测，可与服务端 Pino 日志对齐。

当前尚无与 `171a874` 配套的二维码现场记录；重新生成二维码后再补充页面、客户端和服务端三层证据。

结论仍是开发者工具旧增量模块索引/文件句柄导致的历史路径请求，不是业务代码缺少 `single-flight.test.js`。禁止将测试脚本复制到
`dist/`，后续真机必须扫描本轮重新生成的二维码。

## 下一步顺序

1. 人工确认正确项目并从 `4ba492a` 当前运行包重新生成、扫描调试二维码；
2. 扫码确认微信会话建立；
3. 刷新患者目录并显式切换到另一位可用患者；
4. 依次验收预约历史、爽约和门诊费用只读；
5. 取得三层证据后再评估报告 Provider 是否具备开放条件。

在 Provider 文档、字段脱敏、权限、状态映射和回滚证据齐全前，不切换到支付、医保或 HIS 写回。
> 当前发布基线更新（2026-08-24 19:54 CST）：线上服务端 release 已切换为 `8eb51b5ffe85b0b8f8a032783f893117d3df549d`；小程序运行包来源仍为 `13f597ea9ee3f65b9be858117826d948339d904a`（提交 `13f597e`）。本轮只重启新 API，旧 Python `8001` 未修改；普通资料 PUT、支付、医保和 Provider 真机证据仍待。
> 当前统一发布基线补充（2026-08-27）：服务端 release 为 `b44421cd321ff9ff23eeb49b12641d1772d2bdc1`；小程序本地 live 运行包来源为 `413cbea13f022831f63e9c750661eeabbffc68d5`，共 40 个页面。本文更早版本仅作历史追溯，真机证据仍为 pending；旧 Python `8001` 未修改。
