# 新旧小程序真机调试会话边界记录（2026-08-18）

本文记录重启后对微信开发者工具窗口的只读核对结果，目的不是验收旧端，而是防止把旧项目的真机连接误当成新原生小程序的 P0 证据。
本文不记录真实患者姓名、卡号、openid、Provider 患者号、请求参数或原始响应。

## 当前服务端基线（2026-08-19 00:50 CST）

当前服务端 release 为 `b7c9451`，新 API 监听 `10.0.0.3:18081`，旧 Python 继续监听 `0.0.0.0:8001`；
当前本地配套小程序构建来源为 `5fdc740e3450c8773a81d1d13c8c55d5288d9259`（尚未上传线上）。本节之后涉及的旧二维码、旧模拟器和历史
`687690e` 观察都只用于追溯，不能覆盖当前真机验收基线。

## 当前观测（2026-08-18 17:09 CST）

已在新 `miniprogram` 项目窗口重新打开 iOS“二维码真机调试”并生成当前候选二维码；截至本观测时刻，
窗口仍显示二维码，未出现新设备连接。该事实只说明真机验收入口已准备好，不代表微信会话、患者目录或任何只读业务已经成功。

## 重启后复核（不计入真机验收）

重启后再次按窗口标题选择新 `miniprogram` 窗口时，返回的窗口句柄与可见画面出现不一致：
标题仍指向新候选项目，但画面无法确认是该项目的二维码或页面。由于项目上下文不能只靠标题推断，
本次没有点击、输入、重新生成二维码或继续扫码，也没有把该画面中的任何业务日志作为证据。
后续必须先关闭并重新打开新候选项目，确认项目资源树为 `dist/` 且二维码弹窗与资源树属于同一窗口后，
再由用户扫码建立新设备连接。

## 最终候选重新编译后的复核（2026-08-18 18:36 CST，不计入真机验收）

`07d3988` 候选重新编译后，开发者工具新 `miniprogram` 项目的模拟器首页能够正常渲染，
此前 `appService.js: define is not defined` 导致的 2 个错误已经消失，仅保留微信基础库提示。
最终候选二维码已经重新生成；截至本次窗口枚举仍没有新设备连接，列表中活动的“真机调试”仍属于旧
`mp-weixin` 项目。因此本次只证明候选运行包和模拟器启动边界已恢复，不增加微信会话、患者目录或
任何只读业务的真机验收证据。

## 继续工作后的只读窗口复核（不计入真机验收）

本次重启后的窗口列表仍同时存在旧 `mp-weixin` 和新 `miniprogram` 两个开发者工具项目；
新窗口标题可识别为当前候选项目，但窗口元数据没有提供新的扫码连接状态。为避免把标题存在、
窗口存在或旧窗口在线误记为新包真机成功，本次只完成窗口枚举，没有激活、点击、输入、扫码或
读取窗口业务画面。当前仍必须由用户在确认资源树为 `dist/` 的新项目二维码弹窗中重新扫码。

## 重启后新候选窗口复核（2026-08-18 18:40 CST，不计入真机验收）

本次按窗口标题和资源树重新核对后，目标窗口确实属于新 `miniprogram` 项目：资源树包含 `dist/`，
二维码弹窗显示当前候选代码包约 607 KB，模拟器首页正常显示患者卡片和服务入口；调试器状态为 0 个
错误、3 条微信基础库提示。窗口仍没有新的手机连接状态，二维码有效期显示至 18:59；因此本次只恢复
了新候选的二维码与模拟器上下文，不增加微信登录、患者目录或任何只读业务的真机证据。

## 重启后继续工作的最新复核（2026-08-18 21:33 CST，不计入真机验收）

重新激活标题为 `miniprogram - 微信开发者工具 Stable v2.01.2510290` 的窗口后，资源树仍明确包含
`dist/`，模拟器仍能显示首页患者卡片和服务入口，问题面板显示总计 0 个问题。二维码弹窗仍显示代码包
约 607 KB，但有效期仍为 `8/18 18:59`；本机时间为 21:33，二维码已经过期。重新关闭并打开“真机调试”
入口没有刷新该有效期，因此本次没有引导扫码，也没有新增微信会话、患者目录或业务真机证据。

后续必须先在该新项目窗口重新编译并确认二维码有效期晚于当前时间，再由用户扫码；仅有 `dist/`、模拟器
首页和二维码弹窗都不能替代有效设备连接。

## 重新编译后的有效二维码复核（2026-08-18 21:35 CST，不计入真机验收）

在新 `miniprogram` 项目窗口触发一次普通编译后，开发者工具重新生成了代码包约 606 KB 的二维码，
有效期更新为 `8/18 21:59`。资源树仍为 `dist/`，模拟器首页正常，问题面板显示总计 0 个问题。
截至本记录仍未出现新的手机连接；本次只证明当前候选已有可扫码二维码，不增加微信登录、患者目录、
预约、门诊费用或其它业务真机证据。

后续由用户在该新窗口扫码；连接成功后必须先记录设备连接归属，再按只读业务验收候选采集页面、HTTP
trace 和历史 `687690e` 服务端低敏日志三层证据。

## 重启后继续工作的当前窗口复核（2026-08-18 21:40 CST，不计入真机验收）

重启后再次读取新 `miniprogram` 窗口，确认资源树仍为 `dist/`；模拟器首页继续显示患者卡片、服务入口和
固定底部导航，问题面板为 0 个问题，调试器为 0 个错误和 3 条微信基础库提示。当前窗口没有新的手机连接，
也没有可归属于新项目的微信会话、HTTP 业务 trace 或服务端业务日志，因此本次只证明候选窗口恢复正常，
不增加登录、患者切换、预约历史或门诊费用的真机证据。

当前应继续由用户在新项目窗口点击“真机调试”并扫码；若二维码过期，先普通编译重新生成，再以设备连接、
页面请求和 `687690e` 低敏日志三者同一时间窗口为准。旧 `mp-weixin` 窗口仍不得作为新端验收入口。

## 模拟器会话恢复时序复核（2026-08-18 21:41 CST，不计入真机验收）

新 `miniprogram` 模拟器控制台出现一条 `GET /api/v2/me` 的 `401`，随后从当前线上低敏日志按同一时间窗口
核对到完整恢复链：旧会话 `/me` 被拒绝后，`/auth/wechat` 成功，新的 `/me` 返回 200，患者目录读取返回 1 条，
随后患者目录同步返回 1 条患者和 1 条 `his-patient` 临床映射。该时序符合客户端 `requestWithSession` 的一次
重新登录边界；调试器把预期的非 2xx 记录为红色，并不等于后续会话恢复失败。

这次证据只来自开发者工具模拟器和服务端日志，未出现新手机设备连接，不能计入真实微信登录、患者切换或其它
只读业务的真机验收。真实设备仍需重新扫码，并且必须将设备连接、页面请求和 `687690e` 日志按同一时间窗口关联。

## 重启后重新生成二维码复核（2026-08-18 21:49 CST，不计入真机验收）

继续工作期间重新打开新 `miniprogram` 项目的“真机调试”入口，编译状态完成后二维码代码包显示约 `606 KB`；
资源树仍为 `dist/`，模拟器首页仍正常显示。当前弹窗没有出现新的手机设备连接或新微信会话，因此这次只证明
二维码入口已恢复，不增加真实微信登录、患者切换、预约历史、门诊费用或其它只读业务的真机证据。

用户后续扫码时，必须先确认连接归属于新 `miniprogram` 项目，再用同一时间窗口关联设备连接、页面请求和当前
`687690e` 服务端低敏日志；旧 `mp-weixin` 项目的既有连接继续排除在验收之外。

## 普通资料页模拟器复核（2026-08-18 21:52 CST，不计入真机验收）

在新 `miniprogram` 模拟器中从“我的”页进入普通资料页，页面成功渲染昵称、性别、年龄、邮箱、资料边界和保存按钮；
性别选择器能够打开并关闭，未点击保存，未产生资料写入。页面可见状态不能替代真机结果。

本次开发者工具控制台仍显示一条 `GET /api/v2/me 401`（属于已记录的会话恢复边界）、`clickCheckTask` 的内部错误，
以及 `undefined is not iterable` 渲染层错误。当前错误没有项目源码调用栈，且资料页和选择器仍可完成上述只读交互，
因此暂不能证明是业务代码缺陷，也不能把它标成已解决；必须在真实手机上复核性别选择器和页面返回。如果真机复现，
再根据可关联的页面请求和源码栈定位，禁止先加入无依据的兼容分支。

同一窗口重新打开“真机调试”后，二维码显示代码包约 `606 KB`，有效期至 `8/18 22:13`；仍未出现新手机连接。

## 1. 核对结果

| 项目 | 观察结果 | 结论 |
| --- | --- | --- |
| 新项目窗口 | 资源树包含 `dist/`，二维码弹窗显示约 607 KB，模拟器首页正常，调试器 0 errors / 3 warnings；未取得新的扫码连接元数据 | 当前候选仍未取得可复核的新包真机连接 |
| 另一个开发者工具窗口 | `mp-weixin - 微信开发者工具 Stable v2.01.2510290`，资源树包含旧端 `pages/`、`pagesB/`、`stores/`、`utils/` | 这是旧项目，不属于 `apps/miniprogram` |
| “真机调试”窗口 | 存在活动 iOS 设备连接，状态显示服务正常 | 连接归属于旧 `mp-weixin` 调试上下文，不能计入新端验收 |
| 旧窗口 Network | 出现旧端 Provider 请求和旧端业务链路 | 仅作为识别旧上下文的低敏特征，不复制、不重放、不纳入新端业务证据 |

本次没有结束旧端真机调试、没有点击旧端业务入口、没有读取或保存旧端敏感响应，也没有修改旧项目代码或服务端。

## 2. 验收边界

新端真机验收必须同时满足以下条件：

1. 操作窗口标题和项目资源树明确对应 `hospital-platform/apps/miniprogram`，运行根目录为 `dist/`；
2. 真机连接是在新项目的“二维码真机调试”弹窗扫码后产生，而不是复用已有 `mp-weixin` 会话；
3. 页面、HTTP `requestId/traceId` 和当前服务端 `b7c9451` 的低敏业务日志属于同一时间窗口；
4. 不出现旧端 Provider 直连、旧端患者标识或支付/医保/HIS 写入请求。

只看到设备在线、旧项目页面可用或旧 Network 有 HTTP 200，都不能证明新原生小程序完成登录、患者切换、预约历史或门诊费用验收。

## 3. 下一步

先重新打开并确认新 `miniprogram` 窗口的项目资源树与二维码属于同一上下文，再扫码连接当前候选包：
服务端 `b7c9451`，小程序构建来源 `5fdc740e3450c8773a81d1d13c8c55d5288d9259`。
连接成功后先完成微信会话和患者目录，再按
[`miniprogram-readonly-acceptance-candidate-2026-08-18.md`](miniprogram-readonly-acceptance-candidate-2026-08-18.md)
采集只读业务三层证据；在此之前不开放全部挂号、预约写入、支付、医保、报告详情或 HIS 回写。
