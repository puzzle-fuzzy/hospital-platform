> 历史现场记录（2026-08-22）：本记录对应小程序运行包来源 `a64fe023bc34fe6e44f93846c39e202fe02d64a5`（提交 `a64fe023`），不代表当前候选；当前候选为服务端 `0e2a366e` 与小程序 `171a874`，真实真机证据仍待。

# 小程序运行包 ENOENT 复核记录（2026-08-22 18:30 CST）
> 记录时服务端发布基线（2026-08-22 18:55 CST）：`0e2a366efcca8da25d7edd4a286781f2d3dfdbec`；本记录对应的历史小程序来源为 `a64fe023bc34fe6e44f93846c39e202fe02d64a5`。本记录只证明运行包缓存恢复，不证明真机业务验收。

> 本记录针对真机调试报错 `dist/services/single-flight.test.js` 不存在。
> 它记录的是当前源码和运行包复核结果，不把开发者工具旧增量缓存产生的错误
> 误判成业务代码缺陷，也不代表手机端微信登录、患者或只读业务已经验收。

## 1. 当前运行包证据

| 项目 | 结果 |
| --- | --- |
| 小程序项目 | `E:\\__Super_Core__\\hospital-platform\\apps\\miniprogram` |
| 运行根目录 | `dist/` |
| 历史运行包来源 | `a64fe023bc34fe6e44f93846c39e202fe02d64a5`（提交 `a64fe023`） |
| 记录时服务端配套 release | `84370077024762d92050cf077c27f3c60302e8f8`（提交 `84370077`） |
| 构建时间 | `2026-08-22T10:30:45.312Z`（18:30:45 CST） |
| 页面入口 | 14 个，全部生成对应 `.js/.json/.wxml/.wxss` |
| `single-flight.js` | 存在 |
| `single-flight.test.js` | 不存在，属于预期结果 |
| `*.test.js` / `*.spec.js` | 0 个 |
| 测试模块相对引用 | 0 个 |
| `runtime:verify` | 通过 |

本次执行的命令为：

```powershell
pnpm --filter @hospital/miniprogram build
pnpm --filter @hospital/miniprogram runtime:verify
```

构建使用 `tsconfig.build.json` 排除 `src/**/*.test.ts` 和 `src/**/*.spec.ts`，发布器
还会在 staging 阶段阻断测试运行文件、缺失相对 `require` 和 workspace 裸模块引用。
因此不能通过复制一个空的 `single-flight.test.js` 来“修复”错误；那会破坏测试源码和
真机运行包的边界。

## 2. 根因判断与恢复顺序

当前 `dist/` 没有测试脚本、没有测试引用，且开发者工具普通编译面板显示 `Errors: 0`。
因此这条 ENOENT 的可验证结论是：当前候选运行包没有该文件；若真机仍请求它，优先按
开发者工具保留旧增量模块图处理。

本次历史运行包对应记录时已验证的服务端 release `84370077024762d92050cf077c27f3c60302e8f8`；
这只用于发布基线关联，不代表已经完成微信、患者、预约或费用真机业务验收。

恢复顺序如下：

1. 结束当前真机调试会话，暂时不要继续操作业务页面。
2. 确认打开的是 `apps/miniprogram/`，且 `project.config.json` 的 `miniprogramRoot`
   仍为 `dist/`；不要打开旧项目的 `mp-weixin`，也不要把 `src/` 作为小程序根目录。
3. 关闭所有指向新项目的微信开发者工具窗口，等待进程完全退出后重新打开新项目。
4. 普通编译一次，再确认“构建”无错误、`dist/services/single-flight.js` 存在。
5. 重新生成二维码后再扫码；不要继续使用旧二维码。

如果仍然出现同一个路径，才在新项目窗口执行“工具 → 清除缓存 → 清除文件缓存”，
随后再次普通编译。不要清除登录、授权、网络或业务数据，除非另有明确验收目的。

## 3. 停止条件与边界

- 如果 `runtime:verify` 失败，停止真机验收，先修复运行包，不进入业务操作。
- 如果错误路径变成真实存在的页面 `.js` 缺失，停止并记录完整路径、候选来源和工具项目根目录。
- 如果继续请求 `single-flight.test.js`，不要在 `dist/` 手工补文件；应保留错误现场，
  重新检查开发者工具是否仍运行旧项目或旧增量缓存。
- 本次只重建了新项目运行包并复核开发者工具新项目窗口，没有操作旧 `mp-weixin`，
  没有修改旧 Python 服务、数据库或 Redis。
