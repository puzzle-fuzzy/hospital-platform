# 当前新旧服务与 P0 日志只读观察（2026-08-20 20:36 CST）

## 运行层

本次通过 SSH 只读检查服务器监听和新 API readiness，没有重启、部署、配置写入、数据库/Redis 写入，
也没有修改旧项目。

| 检查项 | 结果 |
| --- | --- |
| 新服务 | `hospital-platform-api-v2.service=active` |
| 新 API 监听 | `10.0.0.3:18081` |
| 旧 Python 监听 | `0.0.0.0:8001`，仍在监听 |
| 新 API readiness | `database=ok`、`redis=ok`、`schema=ok` |

## 最近 15 分钟低敏日志摘要

只在服务器端按固定 `event` 和 HTTP 状态码做计数，没有把原始日志、traceId、requestId、Authorization、
用户标识、患者标识或 Provider 报文复制到本地或输出到终端。

| 维度 | 结果 |
| --- | --- |
| JSON 解析错误 | `0` |
| `http.request.completed` | `4` |
| `http.request.failed` | `2` |
| HTTP 200 | `4` |
| HTTP 401 | `2` |
| 微信登录/患者目录/患者同步 | `0` |
| 预约/报告/门诊费用/普通资料 | `0` |

这 4 次 200 和 2 次 401 与本轮公网健康检查及未登录认证边界复核相符；没有新的微信真机业务请求。

## 验收结论

- 新旧服务仍然共存，旧 Python `8001` 没有被本轮操作影响。
- 运行层和未登录认证边界保持正常，但不能据此标记微信登录、患者同步、多患者切换、预约历史、
  门诊费用或普通资料为真机已验收。
- 下一步仍是使用当前 `8f80b3e` 候选二维码扫码；扫码后必须重新采集页面、客户端 HTTP 和服务端低敏日志
  三层证据，不能用本观察窗口的健康请求替代。

