# 小程序真机二维码会话记录（2026-08-22 18:25 CST）
> 当前服务端发布基线（2026-08-22 18:55 CST）：`0e2a366efcca8da25d7edd4a286781f2d3dfdbec`；本二维码对应的小程序来源为 `a64fe023bc34fe6e44f93846c39e202fe02d64a5`。本记录仍无手机业务证据。

> 本记录是 `single-flight.test.js` 真机 `ENOENT` 的缓存清理后现场。
> 它只证明当前候选在正确项目中完成文件缓存清理、普通编译并重新生成真机入口，
> 不代表手机已经扫码，也不代表微信登录、患者、预约或费用业务已经验收。

## 1. 当前候选与工具状态

| 项目 | 结果 |
| --- | --- |
| 微信开发者工具项目 | `E:\\__Super_Core__\\hospital-platform\\apps\\miniprogram` |
| 开发者工具窗口 | `miniprogram - 微信开发者工具 Stable v2.01.2510290` |
| 小程序运行根目录 | `dist/` |
| 小程序运行包来源 | `a64fe023bc34fe6e44f93846c39e202fe02d64a5`（提交 `a64fe023`） |
| 服务端配套 release | `84370077024762d92050cf077c27f3c60302e8f8`（提交 `84370077`） |
| 普通编译结果 | 14 个页面，开发者工具 `Errors: 0`、`Warnings: 4` |
| 二维码平台/网络 | iOS / 局域网模式 |
| 二维码有效期 | 至 `2026-08-22 18:40 CST` |
| 手机扫码状态 | 现场记录时尚未检测到手机连接 |

## 2. ENOENT 恢复动作

1. 只在新 `miniprogram` 项目窗口结束当前真机调试弹窗。
2. 通过开发者工具“工具 → 清除缓存”只执行“清除文件缓存”。
3. 未执行“清除数据缓存”“清除授权数据”“清除网络缓存”“清除登录状态”或“全部清除”。
4. 使用普通编译重新建立当前 `dist/` 的开发者工具模块索引，再生成新的真机二维码。

当前运行包边界仍保持：

- `dist/services/single-flight.js` 存在；
- `dist/services/single-flight.test.js` 不存在，这是预期结果；
- `dist/` 中 `*.test.js` 和 `*.spec.js` 数量为 0；
- `runtime:verify` 通过；不应复制测试脚本来绕过错误。

因此，若手机扫描本记录二维码后仍请求 `services/single-flight.test.js`，应记录新的完整时间、
候选来源和开发者工具项目路径，再停止真机业务操作；不能把错误归因于 `single-flight.js` 缺失。

## 3. 验收边界

手机扫码后必须按“微信登录 → 患者目录 → 明确切换另一位就诊人 → 我的挂号/爽约 → 门诊费用只读”
顺序取页面、客户端脱敏 `requestId` 和服务端 Pino 同链证据。任何患者归属、状态、日期、金额或
服务端 trace 不一致时立即停止当前业务域；支付、医保、预约写入、退款和 HIS 写回继续关闭。

本轮没有操作旧 `mp-weixin` 项目，没有修改旧 Python 服务、数据库或 Redis。
