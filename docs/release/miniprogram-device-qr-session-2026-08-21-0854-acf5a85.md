# 当前小程序真机二维码会话（2026-08-21 08:54 CST）

> 本记录只描述开发者工具中当前打开的真机调试会话，不代表手机已经扫码，也不代表微信登录、患者同步或任何业务域已经验收。

## 会话边界

| 项目 | 结果 |
| --- | --- |
| 服务端配套 release | `5a31427` |
| 小程序候选 | `acf5a85` |
| 小程序运行包来源 | `acf5a8596e70e1fb2b8d220a0b41eb69418ae086` |
| 开发者工具项目 | `miniprogram` |
| 运行根目录 | `apps/miniprogram/dist/` |
| 页面数量 | 14 |
| 代码包大小 | 约 608 KB |
| 真机模式 | iOS + 局域网 |
| 工具当前显示的二维码有效期 | 2026-08-21 09:06 CST |
| `dist/services/single-flight.js` | 存在 |
| `dist/services/single-flight.test.js` | 不存在，符合生产运行包边界 |
| `dist/` 测试运行脚本 | `*.test.js` / `*.spec.js` 均为 0 |
| 手机是否扫码 | 待记录 |

## 当前操作要求

请只扫描开发者工具当前显示的二维码。扫码后按“微信登录 → 患者同步 → 显式选择就诊人 → 预约历史/爽约 → 门诊缴费只读 → 普通资料”的顺序操作；每个业务域都必须同时保留页面结果、脱敏客户端 HTTP 和服务端低敏事件。

如果再次出现 `ENOENT .../dist/services/single-flight.test.js`，立即停止扫码会话，关闭并重新打开 `apps/miniprogram/` 项目后再普通编译；不要把测试脚本复制到 `dist/`。

支付、医保授权、退款、预约写入、患者绑定、报告 Provider 详情和 HIS 回写继续保持关闭；旧 Python 服务、线上 API、数据库和 Redis 未因本地构建或二维码操作被修改、部署或重启。
