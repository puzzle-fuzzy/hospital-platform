# 小程序当前候选 `f085d06` 本地构建记录（2026-08-21）

> 本记录锁定当前待真机验收的本地运行包，不代表小程序已经上传线上，也不代表微信、患者、Provider 或支付业务已经验收。

## 候选边界

| 项目 | 结果 |
| --- | --- |
| 服务端 release | `5a31427` |
| 小程序客户端 | `f085d06` |
| 小程序构建来源 | `f085d06d8e4b9695274f93b5a6b56f2af3faac91` |
| 运行包目录 | `apps/miniprogram/dist/` |
| 页面入口 | 14 个 |
| `dist/build-info.json` | `sourceRevision` 与上述完整来源一致；当前生成时间 `2026-08-21T01:12:57.854Z`（09:12:57 CST） |
| 运行包测试脚本 | 0 个 `*.test.js` / `*.spec.js` |
| `single-flight.js` | 存在 |
| `single-flight.test.js` | 不存在，符合运行包边界 |

## 本次业务状态机修复

普通资料页在资料 PUT 已返回但会话代际已经变化时，会在异步 `reLaunch` 真正卸载前先释放 `saving`，并由统一的资料上下文清理函数再次保证 `saving=false`。这样旧账号的返回结果不能提交到新账号页面，也不会把保存按钮卡在“保存中”。

对应的资料页静态验收门禁、API 个人资料服务/应用测试、持久化测试和小程序全量测试均通过；本次没有修改服务端、Provider、数据库、Redis 或旧 Python 服务。

## ENOENT 运行包边界

`pnpm check` 已通过构建，生成的 `dist/` 不包含任何 `*.test.js` / `*.spec.js`，因此不能通过复制测试脚本处理开发者工具的旧增量索引错误。若开发者工具再次报告 `single-flight.test.js`，应关闭旧项目窗口、清理文件/编译缓存、确认 `miniprogramRoot=dist/` 后重新普通编译。

## 当前验收边界

本提交后必须重新生成与 `f085d06d8e4b9695274f93b5a6b56f2af3faac91` 对应的真机二维码；此前 `acf5a85` 二维码和真机窗口不能用于本候选业务验收。真实微信登录、患者显式切换、预约历史、门诊费用、普通资料首次写入/409 冲突仍需页面、客户端请求和服务端低敏日志三层证据。

支付、医保授权、退款、预约写入、患者绑定、报告 Provider 详情和 HIS 写回继续保持关闭；旧 Python 服务、线上 API、数据库和 Redis 未因本地构建被修改或重启。
