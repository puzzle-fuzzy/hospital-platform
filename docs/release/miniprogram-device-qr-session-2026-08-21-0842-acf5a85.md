# 当前小程序真机二维码会话（2026-08-21 08:42 CST）

> 本记录只描述开发者工具中当前打开的真机调试会话，不代表手机已经扫码，也不代表微信登录、患者同步或任何业务域已经验收。

## 会话边界

| 项目 | 结果 |
| --- | --- |
| 服务端配套 release | `5a31427` |
| 小程序候选 | `acf5a85` |
| 小程序运行包来源 | `acf5a8596e70e1fb2b8d220a0b41eb69418ae086` |
| 开发者工具项目 | `miniprogram` |
| 运行根目录 | `apps/miniprogram/dist/` |
| 页面数量 | 14 |
| 真机模式 | iOS + 局域网 |
| 工具当前显示的二维码有效期 | 2026-08-21 08:54 CST |
| `dist/services/single-flight.js` | 存在 |
| `dist/services/single-flight.test.js` | 不存在，符合生产运行包边界 |
| `dist/` 测试运行脚本 | `*.test.js` / `*.spec.js` 均为 0 |
| 手机是否扫码 | 待记录 |

## 本次恢复动作

针对真机调试曾报告的 `ENOENT .../dist/services/single-flight.test.js`，已重新执行小程序构建和运行包校验，并在正确的 `miniprogram` 项目中关闭后重新打开真机调试入口。构建、运行包校验和页面数量均符合当前候选基线。

该错误不能通过把测试脚本复制到 `dist/` 解决；若手机再次报告同一文件，应停止当前扫码会话，关闭并重新打开 `apps/miniprogram/` 项目后再普通编译，避免继续沿用开发者工具旧增量模块索引。

## 真机验收要求

二维码扫码后必须记录页面结果、客户端脱敏 requestId/HTTP 状态和服务端低敏事件三层证据。若出现患者归属不一致、旧患者残留、未配置依赖、未知状态或 HTTP/日志链不一致，立即停止对应业务域。

支付、医保授权、退款、预约写入、患者绑定、报告 Provider 详情和 HIS 写回继续保持关闭；旧 Python 服务、线上 API、数据库和 Redis 未因本次本地构建或二维码操作被修改、部署或重启。
