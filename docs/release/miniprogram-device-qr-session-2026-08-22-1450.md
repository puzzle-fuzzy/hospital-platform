# 小程序真机二维码会话记录（2026-08-22 约 14:50 CST）

## 当前状态

已在正确的微信开发者工具 `miniprogram` 项目中，为当前候选重新完成编译上传并生成 iOS/局域网真机调试二维码。
本记录只证明当前二维码和运行包已经进入扫码阶段，不宣称微信登录、患者、预约或费用业务完成。

| 项目 | 结果 |
| --- | --- |
| 微信开发者工具项目 | `E:\__Super_Core__\hospital-platform\apps\miniprogram` |
| 小程序运行包来源 | `41c708e1adf864ef6fef1f788e97aa8fb4371227`（`41c708e1`） |
| 服务端配套 release | `1e58bb66bf24021d2b680eb5fd03abfec467989a` |
| 代码包大小 | 约 `624 KB` |
| 注册页面数 | `14` |
| 真机平台 | iOS |
| 网络模式 | 局域网模式 |
| 二维码有效期 | 开发者工具界面显示至 `2026-08-22 15:16 CST` |
| 扫码状态 | 等待用户扫码 |
| 旧项目 `mp-weixin` / 旧 Python 服务 | 未操作、未重启 |

## 开发者工具错误边界

当前调试控制台显示 `Errors: 1, Warnings: 3`。已只读展开错误，唯一错误为微信开发者工具注入的
`WAServiceMainContext.js` 定时器访问 `__subPageFrameEndTime__` 时的 `TypeError`，堆栈位于微信基础库
`WeChatLib 3.17.1`，没有指向 `dist/` 页面脚本、业务 service 或当前 API 请求。

该工具内部错误不作为业务成功，也不阻止扫码；若手机出现页面脚本错误、接口错误或页面内容异常，必须按真实业务证据重新记录，
不能把本条开发者工具告警当作手机验收结果。

## 扫码后的证据顺序

1. 记录手机实际页面：微信登录结果、患者目录、显式切换后的当前就诊人，以及预约历史/门诊费用页面。
2. 记录客户端脱敏 HTTP：请求路径、状态码、requestId/traceId 和页面动作，不记录 token、完整身份证或完整卡号。
3. 在同一时间窗口读取服务端 Pino 低敏日志，确认业务 `requested`、结果、HTTP 状态和 Provider 关联链一致。
4. 若患者归属、状态、金额、日期或关联链不一致，立即停止当前业务域，不进入下一个域。

二维码、模拟器页面、单个 HTTP 200 或服务端健康检查都不能替代上述三层证据。
