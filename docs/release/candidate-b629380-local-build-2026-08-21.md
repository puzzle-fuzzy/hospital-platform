# 小程序当前候选 `b629380` 本地构建记录（2026-08-21）

> 本记录锁定当前待真机验收的本地运行包，不代表小程序已经上传线上，也不代表微信、患者、Provider 或支付业务已经验收。

## 候选边界

| 项目 | 结果 |
| --- | --- |
| 服务端 release | `5a31427` |
| 小程序客户端 | `b629380` |
| 小程序构建来源 | `b629380162aa8275418b643e10a16e96a65d0b36` |
| 运行包目录 | `apps/miniprogram/dist/` |
| 页面入口 | 14 个 |
| `dist/build-info.json` | `sourceRevision` 与上述完整来源一致；最终构建时间 `2026-08-21T02:52:54.710Z`（10:52:54 CST） |
| 运行包测试脚本 | 0 个 `*.test.js` / `*.spec.js` |
| `single-flight.js` | 存在 |
| `single-flight.test.js` | 不存在，符合运行包边界 |

## 本次运行包门禁修正

本候选在原有 Unicode 资料 contract 修正基础上，统一隔离 `*.test.ts` 与 `*.spec.ts`：TypeScript 运行构建、来源指纹和运行包文件门禁现在使用同一套排除语义。这样未来新增规格测试文件时，不会进入微信 `dist/`，也不会因测试文件改动伪造业务运行包来源。

该修正只影响小程序构建和验收工具，不改变微信登录、患者上下文、预约、费用、Provider、数据库、Redis 或旧 Python 服务边界。

## ENOENT 运行包边界

`pnpm --filter @hospital/miniprogram build` 和 `pnpm --filter @hospital/miniprogram runtime:verify` 均已通过，生成的 `dist/` 不包含任何 `*.test.js` / `*.spec.js`。若开发者工具再次报告 `single-flight.test.js`，应关闭旧项目窗口、清理文件/编译缓存、确认 `miniprogramRoot=dist/` 后重新普通编译；不能复制测试脚本进入运行包。

## 本地验证证据

| 检查 | 结果 |
| --- | --- |
| 原生小程序定向验收/来源测试 | 98 项通过，1123 个断言 |
| 原生小程序全量测试 | 174 项通过，1378 个断言 |
| 小程序类型检查 | 通过 |
| 小程序构建 | 通过 |
| 运行包验证 | 通过，14 个页面齐全，测试运行脚本数量为 0 |

## 当前验收边界

本候选必须重新生成真机二维码；此前其他候选的二维码和真机窗口不能用于本候选业务验收。真实微信登录、患者显式切换、预约历史、门诊费用、普通资料首次写入/409 冲突仍需页面、客户端请求和服务端低敏日志三层证据。支付、医保授权、退款、预约写入、患者绑定、报告 Provider 详情和 HIS 写回继续保持关闭。

本轮没有上传服务器或修改线上配置；旧 Python 服务、线上 API、数据库和 Redis 未因本地构建被修改或重启。
