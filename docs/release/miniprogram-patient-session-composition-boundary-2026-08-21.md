# 小程序患者页面会话组合边界审计（2026-08-21）

> 当前候选：服务端 release `5a31427`；小程序运行包来源 `7a6f4df34fac5975c6012a30d2c137953a892059`（提交 `7a6f4df`）。

> 当前配套服务端 release 为 `5a31427`；当前小程序候选为 `7a6f4df`，完整运行包来源为 `7a6f4df34fac5975c6012a30d2c137953a892059`。本轮只修改本地新项目代码和文档，未部署、未重启旧 Python 服务或新 API，未调用 Provider、医保、支付或 HIS。

## 发现的问题

预约历史、爽约、报告目录和门诊费用页面原先已经具备页面请求令牌、当前患者 ID 回查和服务端 owner/patient 校验，但页面组合边界仍不完整：

```text
/me 成功（会话 A）
  -> 患者目录成功（会话 B）
  -> 业务列表成功（会话 B）
  -> 页面把三段结果拼成一张卡片
```

当另一个页面在请求之间完成微信会话轮换时，每个单独的 GET 可能都返回 200，但它们不再属于同一个 owner 快照。单纯检查 `isCurrentSelectedPatient` 只能确认本地选择字符串，没有证明 `/me`、患者目录和业务列表属于同一会话代际。

报告详情还有一条深链风险：旧页面栈或手工 URL 可以携带 `patientId/reportId`，如果详情页不重新读取当前 owner 的患者目录，就会把旧本地选择当作当前患者证明。服务端仍会做最终授权，但客户端应在发起详情请求前先收敛自己的上下文。

## 修正规则

新增 `services/session-boundary.ts` 的 `assertSessionGeneration`：

- 患者范围页面在 `/me` 成功后捕获当前会话代际；
- 患者目录返回、业务列表返回和页面提交前重新校验代际；
- 代际变化统一抛出 `session-changed`，沿用已有中文错误文案和页面清理语义；
- 不记录 token、openid、患者号或非法上下文，代际只存在进程内内存；
- 不自动把旧患者请求重放到新账号，必须由下一轮完整页面加载建立新快照。

报告详情额外执行：

```text
当前 owner /me
  -> 当前 owner 患者目录
  -> 当前显式选择与 URL patientId 一致
  -> 报告详情引用请求
```

这不是服务端授权替代品。服务端仍必须校验 owner、patientId、reportId 和短期引用 TTL；客户端门禁只负责阻断旧页面栈和混合读模型。

## 代码覆盖

| 页面 | 组合边界 |
| --- | --- |
| `appointment-records` | `/me`、当前患者、预约历史提交绑定同一代际 |
| `missed-appointments` | `/me`、当前患者、爽约筛选结果提交绑定同一代际 |
| `report-directory` | `/me`、当前患者、报告目录提交绑定同一代际 |
| `outpatient-payment` | `/me`、当前患者、待缴/已缴费用结果提交绑定同一代际；标签切换也捕获当前代际 |
| `report-detail` | 先重新确认 owner 和患者目录，再请求详情，并在详情提交前复核代际 |

## 验证证据

| 检查 | 结果 |
| --- | --- |
| 小程序全量测试 | 176 pass / 0 fail / 1399 expects |
| 会话代际单元测试 | `session-boundary.test.ts` 通过 |
| 小程序 typecheck | 通过 |
| Biome | 通过 |
| 小程序 build | 通过；14 个页面运行脚本存在 |
| `runtime:verify` | 通过；来源 `7a6f4df34fac5975c6012a30d2c137953a892059`，不含测试脚本 |
| 真机、Provider、公网业务 | 尚未由本轮证明 |

## 后续停止条件

真实微信验收时，如果出现 `session-changed`、患者卡片和列表不一致、旧患者详情仍能打开、未知业务状态或服务端 trace 无法关联，应停止当前业务域，保存页面、客户端 requestId 和服务端低敏日志，再回到会话/患者 contract 审计。支付、医保授权、预约写入、报告 Provider 详情和 HIS 回写继续最后处理。
