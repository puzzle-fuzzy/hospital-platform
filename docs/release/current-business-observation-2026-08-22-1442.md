# 当前服务端只读业务观察（2026-08-22 10:42–14:42 CST）

> 本记录只保存服务端低敏聚合结果，不包含患者姓名、证件号、完整卡号、订单号、原始 Provider 报文、token 或 requestId 原文。
> 本次通过 SSH 读取当前新 API 日志，没有修改旧 Python 服务、旧数据库或旧 Redis，也没有手工调用支付、医保或 HIS 写回接口。

## 1. 观察基线

| 项目 | 结果 |
| --- | --- |
| 服务端 release | `1e58bb66bf24021d2b680eb5fd03abfec467989a` |
| 小程序配套运行包来源 | `41c708e1adf864ef6fef1f788e97aa8fb4371227` |
| 日志窗口 | 约 `2026-08-22 10:42–14:42 CST` |
| 聚合输入 | 140 行 |
| 成功解析 | 134 条 |
| 解析错误 | 0 |
| systemd 告警 | 0 |
| 平台 trace 数量 | 62 |
| Provider request ID 数量 | 12 |

聚合器只输出计数和固定枚举，未输出原始日志正文。全窗口 HTTP 结果为 `200=57`、`401=2`、`404=3`；失败枚举为
`unauthorized=2`、`NOT_FOUND=3` 和 `PersistenceUnavailableError=1`。这些失败不能从成功业务链中删除，必须保留在当前窗口结论里。

## 2. P0 业务证据门禁

| 业务域 | requested | 明确成功 | 同链 HTTP 2xx | failure | 结论 |
| --- | ---: | ---: | ---: | ---: | --- |
| 微信授权登录 | 2 | 2 | 2 | 0 | 服务端链路通过 |
| 就诊人读取 | 16 | 16 | 16 | 0 | 服务端链路通过 |
| 就诊人同步 | 6 | 6 | 6 | 0 | 服务端链路通过 |
| 预约科室目录 | 1 | 1 | 1 | 0 | 服务端链路通过 |
| 预约排班目录 | 1 | 1 | 1 | 0 | 服务端链路通过 |
| 预约历史记录 | 1 | 1 | 1 | 0 | 服务端链路通过 |
| 门诊费用只读 | 1 | 1 | 1 | 0 | 服务端链路通过 |
| 报告目录 | 0 | 0 | 0 | 0 | 当前没有请求，继续关闭 |
| 普通资料读取 | 0 | 0 | 0 | 0 | 当前没有请求 |
| 普通资料更新 | 0 | 0 | 0 | 0 | 当前没有请求 |

这里的“服务端链路通过”只表示当前 API 记录了同一 trace 内的请求、业务结果和 HTTP 完成事件；不表示手机页面已经显示正确，
也不表示客户端 requestId、Provider 字段或真实用户操作已经完成三层配对。

## 3. 排班快照持久化降级

窗口内出现 1 次 `appointment.schedule_snapshots.failed`。当前排班目录仍完成了同一 trace 的读取和返回，原因是只读排班目录允许快照
持久化在依赖暂时不可用时降级：本次请求返回 Provider 已校验的只读目录，并记录 `snapshotPersistenceStatus=unavailable`。

这不是预约写入的放行条件。未来任何锁号、预约提交、取消或其它写操作都必须把快照持久化和幂等状态改为严格成功后再继续，不能沿用本次只读降级规则。

## 4. 当前结论

- 当前 release 的服务端授权、就诊人、预约只读和门诊费用只读均取得了低敏同链证据。
- 这批证据仍然是“服务端-only”，没有证明真机页面显示、客户端 requestId 与页面动作的对应关系。
- 报告目录、普通资料读取/更新在本窗口没有请求，不能据此宣称完成；报告 Provider 继续关闭。
- 微信支付、医保授权/结算、退款、预约写入、取消、锁号和 HIS 写回继续关闭。
- `401`、`404` 及一次持久化暂不可用必须继续作为独立失败边界观察，不能被空列表或成功统计掩盖。

## 5. 下一步取证

使用当前 `41c708e1` 运行包重新生成二维码并扫码，按页面动作依次采集：

1. 微信登录与就诊人目录；
2. 显式切换就诊人后的预约历史/爽约；
3. 门诊费用待缴/已缴只读页面；
4. 客户端脱敏请求路径、HTTP 状态和 requestId；
5. 与该页面动作同一 trace 的服务端低敏日志。

只有页面、客户端和服务端三层证据同一动作配对后，才能把相应只读业务从“服务端链路通过”提升为“真机已验收”。
