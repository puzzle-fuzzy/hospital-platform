# 当前只读业务迁移审计（2026-08-25）

> **当前事实（2026-08-27）**：本文主体记录的是早期 `eef55001/b587c7ea` 审计窗口，不能作为当前运行包证据。当前线上服务端 release 为 `0aaa13b53cb6e21b59b332dbd4e2b982a5aba1e7`，本地 live 小程序运行包来源为 `413cbea13f022831f63e9c750661eeabbffc68d5`；当前九个真机业务域的证据状态以 [`device-evidence-413cbea1-pending.json`](device-evidence-413cbea1-pending.json) 和 [`../migration/current-execution-checkpoint-2026-08-27.md`](../migration/current-execution-checkpoint-2026-08-27.md) 为准。

> **历史本地源码候选（2026-08-25）**：本文主体审计窗口曾使用 `eef550012e071d2891f990035a9b40041f314e5d`、20 个页面和 `259 pass / 0 fail / 2525 expect()`；该窗口只作历史交接。当前 live 候选和证据清单以第一段的 `413cbea` 与 `device-evidence-413cbea1-pending.json` 为准。

> `eef55001` 历史窗口只补齐报告详情的显式重试和会话/患者边界复核，不改变报告真实 Provider、附件、支付或医保的关闭状态；全量迁移仍按 64 个旧入口台账和六个批次队列推进。

> **历史候选事实（2026-08-25，提交 `99c7e8fd`）**：小程序 pending 曾扩展为 20 个页面并接入健康百科三个只读页面；该候选及其 `259 pass / 0 fail / 2525 expect()` 记录仅作历史追溯。当前 live `dist` 仍为旧候选 `fcc6630e`，健康内容 bundle 未发布前继续 fail-closed。

> 历史事实源（2026-08-25）：早期 pending 运行包曾来源于 `b587c7ea8479e38d47055f3f5b672263f32aec41`；下方 `baa31df0`、`c4dc00b9`、`b3436c24`、`90d5ab03` 仅作历史交接。当前运行包来源以第一段和全量交接单为准。

> 当前小程序只读验收入口：`apps/miniprogram/dist/`，当前 live 运行包来源为 `413cbea13f022831f63e9c750661eeabbffc68d5`，当前没有 pending 目录。四个主 Tab 使用微信原生
> `tabBar`；上一候选 custom-tab-bar 因真机未呈现已撤回，当前原生候选仍需扫码普通编译后验收。线上服务端为 `b44421cd`，旧 Python `8001` 继续共存。

> **当前服务端发布门禁补充（2026-08-27）**：服务端 release `0aaa13b5` 已通过
> `pnpm release:baseline:audit`，当前 API 运行时代码与线上 release 没有漂移；旧 Python `8001` 未修改、未停止。

## 审计结论

当前新项目已经具备患者端只读纵向切片的代码和运行边界，但还没有把代码测试、健康检查或服务端配置状态误写成真实业务完成。下一阶段继续按“真实证据优先、支付医保最后”的顺序推进。

本次审计没有修改旧项目、旧 Python 服务、服务器配置、MySQL、Redis，也没有触碰并行会话负责的众阳自动化代码。

## 已核对的代码边界

| 业务域 | 当前实现 | 当前限制 |
| --- | --- | --- |
| 微信登录与当前用户 | `/auth/wechat`、`/me`、Redis 会话和 owner 校验 | 需要当前运行包、客户端 requestId、服务端日志和真实微信身份的同链证据 |
| 就诊人目录 | owner-scoped `GET /patients`、幂等 `POST /patients/sync`、独立选择页 | 新增/绑卡、实名字段、provider 患者号和二维码仍关闭 |
| 预约目录 | 科室、排班只读；日期和筛选在服务端收窄 | 锁号、预约写入、取消、挂号费和 HIS 写回未注册 |
| 我的挂号/爽约 | `scope=online|all` 的预约历史；爽约只派生明确的 `missed` | 真实 provider/公网/真机四方证据仍待，不能由空列表推断没有历史 |
| 门诊缴费 | owner-scoped 待缴/已缴查询，服务端固定最近 30 个中国标准时间日窗口 | 费用详情、支付调起、医保授权、结算、退费和 HIS 回写关闭 |
| 报告目录 | 报告目录与受限 LIS opaque 详情骨架 | 报告 gate、真实 LIS/PACS/ECG 数据、附件下载和体检报告仍待独立合同/验收 |
| 普通资料 | owner/version 的 GET/PUT、未知字段拒绝和 409 冲突边界 | 真实受控 PUT、冲突复现和真机页面证据仍待 |

这些页面都必须经过当前会话和当前 opaque `patientId` 校验；页面切换患者或会话代际变化时，旧响应不能回写新页面。加载、错误和空结果使用不同状态语义，不能把 Provider/持久化故障显示成“暂无记录”。

## 当前未注册或有意关闭的能力

- 门诊病历目录、正文、结构化病历和附件；旧端病历接口缺少可验证的患者归属、字段白名单和资源授权合同。
- 患者新增、绑卡、实名资料、二维码；旧端二维码包含完整卡号，不能直接复用，必须先冻结扫码字段、签名、TTL、撤销和设备验收。
- 健康自测、风险评估、AI 导诊和报告解读；内容审核、版本和免责声明尚未形成可发布 bundle。健康百科页面和路由已接入，但正式 bundle 未发布前保持 fail-closed。
- 预约写入、锁号、取消、微信支付、医保 1101/6201/6202/6301、退款和 HIS 回写；这些能力需要独立状态机、幂等、金额守恒、查单和补偿证据。

## 运行层只读证据

2026-08-25 只读检查结果：

- 新 `hospital-platform-api-v2.service` 为 `active/running`，监听 `10.0.0.3:18081`。
- `/health/ready` 返回 `database=ok`、`redis=ok`、`schema=ok`。
- 旧 Python Gunicorn 继续监听 `0.0.0.0:8001`，本次没有停止、重启或修改旧服务。
- 该结果只证明新服务运行层和依赖就绪，不证明真实患者、预约、费用或报告业务成功。

### 2026-08-25 02:48 CST 日志窗口

对新 API 最近 30 分钟 journald 做了只读、低敏事件计数：

- 微信登录、患者目录、患者同步、预约目录/历史、门诊费用和普通资料事件均为 `0`。
- 通用 `http.request.completed` 为 `2`。
- 当前没有客户端 requestId、服务端业务 traceId 和 Provider 低敏 requestId 可以组成业务证据链。

因此本窗口只能记录为“尚未产生真实业务请求”，不能推断为业务成功、失败或空数据；扫描二维码后的下一次操作必须重新建立窗口并保存四方关联证据。

代码门禁同时通过：架构边界 68 条、原生页面迁移登记 40 页、旧页面映射 64 页、旧路由登记 195 条、日志事件登记 84 条、Provider 文档接收审计 4 份/31 个 documentId。架构边界新增了“登录链路禁止隐式资料授权、独立资料模块必须由显式手势触发”以及“生产扫描排除测试 fixture”的校验；当前功能候选和 live 运行包来源为 `413cbea`，旧端 64 页面广度台账、健康百科路径回归、互联网医院安全壳分类回归、预约记录分批展示、跨会话授权拒绝回归和自动化验收通过；真机证据仍需采集。

当前工作树已重新执行全量测试：工具测试 `108 pass / 0 fail / 764 expect()`，API 测试 `216 pass / 0 fail / 907 expect()`，
小程序测试 `340 pass / 0 fail / 3726 expect()`；`release:baseline:audit` 已确认服务端与小程序当前来源一致。以上仍是代码、运行包和发布层证据，不能替代真机业务证据。

> 当前功能执行入口和运行包执行入口均为 `413cbea`；旧的 `39cbf021`、`99c7e8fd`、`b587c7ea`、`baa31df0`、`485c0892`、`b3436c24`、`90d5ab03`、`8bc649f`、`1404a03` 文字仅作历史交接。当前工作树复跑为 `340 pass / 0 fail / 3726 expect()`，仍需从 live 运行包重新普通编译并取得真机证据。

## 下一步顺序

1. 在有微信开发者工具和真机会话时，从已发布的 `apps/miniprogram/dist/` 运行包普通编译并生成新二维码，完成微信原生四 Tab、登录、就诊人显式切换和健康百科关闭态验收。
2. 在同一受控身份上采集预约历史在线/全部、爽约和门诊缴费只读的页面、客户端 requestId、服务端 Pino、Provider 低敏 requestId 四方关联。
3. 在获得测试资料授权后验证普通资料首次 PUT、重复提交和 409 版本冲突；不使用真实敏感资料做回归。
4. 重新核对报告 Provider 文档和 gate，具备完整来源证据后再做报告目录只读验收。
5. 最后单独设计病历、患者绑定、二维码、支付和医保 contract；在 contract 未冻结前不加兼容转发、不加假数据、不开放按钮成功路径。

## 证据停止条件

任何请求只出现健康检查、401、依赖未配置或空结果而没有对应业务成功事件时，均只能记为“未完成验收”。发现 owner、patient 映射、状态枚举、日期窗口或 Provider trace 不一致时，整批 fail-closed，先修复合同或证据，不通过前端文案掩盖。
