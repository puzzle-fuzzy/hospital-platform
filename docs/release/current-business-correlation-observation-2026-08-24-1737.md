# 当前只读业务关联观察（2026-08-24 17:37 CST）

> 本文只记录通过 SSH 对生产新 API journald 做的只读、脱敏关联审计。没有重启、配置写入、数据库/Redis 写入，也没有修改旧 Python 服务。服务器当前 release、端口共存和 production readiness 事实以 [`current-runtime-coexistence-readonly-audit-2026-08-24-1727.md`](current-runtime-coexistence-readonly-audit-2026-08-24-1727.md) 为准。

## 1. 证据边界

- 服务器：`ps@192.168.112.172`；
- 新 API：`28a5c0c131794ce9dcc5f94bd3809402188ac87a`，监听 `10.0.0.3:18081`；
- 旧 Python：`0.0.0.0:8001`，本次未触碰；
- 日志窗口：2026-08-24 12:00–18:00 CST；
- 聚合方式：只保留稳定业务事件、公共路径和 HTTP 完成事件，交给 `p0-log-aggregate` 与 `p0-business-evidence-audit`；输出不包含 trace、requestId、患者、金额、手机号或 Provider 原文；
- 当前本地小程序候选：`ce3d4b964546317f6c3a658b1b14b31ace39ed94`。服务器日志本身不携带小程序运行包来源，因此不能仅凭本观察把这些请求归属于该候选。

整日全量聚合的关联桶超过工具的安全上限，工具正确标记 `correlation-truncated`。因此下面对每个业务域使用稳定事件/公共路径做有界过滤，再检查同一关联链和 HTTP 2xx；不能把整日的全量计数直接当成通过。

## 2. 通过的只读链路

| 业务域 | 时间窗口 | requested | success | 同链 | 同链 HTTP 2xx | 解析/系统告警 | 结论 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| 预约历史 | 12:00–18:00 | 8 | 8 | 8 | 8 | 0 / 0 | 服务器链路通过 |
| 预约科室目录 | 12:00–18:00 | 1 | 1 | 1 | 1 | 0 / 0 | 服务器链路通过 |
| 预约排班目录 | 12:00–18:00 | 1 | 1 | 1 | 1 | 0 / 0 | 服务器链路通过 |
| 门诊费用只读 | 13:00–14:00 | 4 | 4 | 4 | 4 | 0 / 0 | 服务器链路通过 |

“服务器链路通过”只表示日志中同一关联链同时出现请求、业务成功和最终 HTTP 2xx；它不证明手机页面展示正确，也不证明在线/全部/爽约标签、待缴/已缴标签和患者切换使用了正确的客户端操作。

## 3. 仍未形成证据的域

- 报告目录：本窗口没有 `report.directory.*` 请求/成功事件；继续保持报告深层 Provider gate 的关闭态；
- 普通资料更新：本窗口没有 `user.profile.update.*` 请求；不能把资料读取成功推导为 PUT/409 已验收；
- 支付、医保、退款、HIS 写回、预约写入/取消：没有开放或没有发生请求，继续保持停止条件；
- 页面层、客户端 requestId、当前二维码/运行包来源：本次 SSH 审计没有这些证据，不能把四条服务器链路标记为真机完成。

## 4. 下一步

使用当前本地 `ce3d4b96` 运行包重新普通编译并扫码，在手机上按“患者选择 → 我的挂号在线/全部 → 爽约 → 门诊缴费待缴/已缴”的顺序操作；每个域保存页面截图、客户端无查询参数的公共路径/状态码/requestId，再与服务器同链日志对照。若客户端没有请求，或请求属于旧运行包，则保持 `pending`，不通过补日志或改旧服务伪造成功。
