# 当前运行包与公网只读复核（2026-08-20 18:39 CST）

> 本记录只覆盖新项目运行包、自动化门禁和公网运行层；不把模拟器、健康探针或未登录 `401` 当作微信真机业务完成证据。旧 Python 服务、旧 `8001`、数据库业务数据和 Redis 业务数据不属于本次修改范围。

## 1. 当前候选

| 项目 | 事实 |
| --- | --- |
| 服务端 release | `0e360d3` |
| 小程序候选提交 | `767ed9c` |
| 小程序运行包来源 | `767ed9c225bf4d329761f6abed7668015a2626b2` |
| 小程序运行根目录 | `apps/miniprogram/dist/` |
| 注册页面 | 14 个 |
| 线上新 API | `10.0.0.3:18081`，公网通过 `/api/v2` 前缀访问 |
| 旧 Python API | `0.0.0.0:8001`，继续共存 |

## 2. 本轮验证结果

### 运行包

- 重新执行 `pnpm --filter @hospital/miniprogram build`，构建成功；
- `pnpm --filter @hospital/miniprogram runtime:verify` 通过；
- `dist/` 中没有 `*.test.js` 或 `*.spec.js`；
- `dist/services/single-flight.js` 存在，`dist/services/single-flight.test.js` 不存在；
- 开发者工具必须打开 `apps/miniprogram/`，并由 `project.config.json` 使用 `dist/` 作为 `miniprogramRoot`。

真机报错 `ENOENT ... dist/services/single-flight.test.js` 的原因是开发者工具增量缓存仍引用旧测试产物，不是业务模块缺失。不能通过把测试脚本复制进 `dist/` 解决；重新构建后关闭并重新导入项目，才会清理这条旧模块引用。

本次再次复现了真机调试窗口短暂显示 `end / 重试` 的现象：第一次生成二维码和点击一次“重试”后仍显示该状态；关闭真机调试窗口、重新打开并等待编译完成后，二维码恢复正常显示。恢复后的开发者工具“问题”面板为 0，控制台只有工具通用提示，服务端同一时间窗口没有收到微信登录请求。因此该现象目前定位为开发者工具真机调试会话状态，不是小程序业务接口错误；遇到时按“关闭 → 重新打开 → 等待编译完成 → 再扫码”处理，不改服务端兼容逻辑。

### 全量工程门禁

执行 `pnpm check` 通过，包含：

- 架构边界、迁移清单、Provider 文档接收、Markdown 链接和 release 基线审计；
- Biome 格式与 lint；
- 工具测试、9 个 workspace 包的类型检查和测试；
- Turbo 全量构建及小程序运行包发布。

当前 release 基线审计仍要求服务端 `0e360d3` 与小程序来源 `767ed9c225bf4d329761f6abed7668015a2626b2` 成对使用。

### 公网只读回归

通过 `https://test-hp.meiyi.pro/api/v2` 访问：

| 路径 | 结果 | 说明 |
| --- | --- | --- |
| `/health/live` | `200` | API 进程可响应 |
| `/health/ready` | `200` | `database=ok`、`redis=ok`、`schema=ok` |
| `/me` | `401` | 未携带会话时按预期拒绝 |
| `/patients` | `401` | 未携带会话时按预期拒绝 |
| `/appointments/records` | `401` | 未携带会话时按预期拒绝 |
| `/payments/outpatient/records` | `401` | 未携带会话时按预期拒绝 |

线上新旧端口均保持监听；本次没有修改旧服务、没有重启旧服务、没有写入 MySQL/Redis，也没有调用 Provider 业务接口。

## 3. 尚未完成的事实

本轮没有新的有效微信会话请求，因而以下内容仍不能标记为真实完成：

1. 真机微信登录、`/me` 会话恢复和失效重登；
2. 患者目录真实同步、第二位就诊人显式切换和失效恢复；
3. 我的挂号、爽约记录、门诊费用只读的页面、HTTP、服务端日志三层证据；
4. 普通资料首次更新、双设备 `409` 冲突和真机写入页面；
5. 报告 Provider、病历、患者新增/绑卡、二维码、支付、医保和 HIS 回写。

下一步仍应使用当前 `767ed9c` 运行包生成有效二维码，在真实微信会话中按同一时间窗口采集页面结果、客户端 `traceId/requestId` 和低敏服务端事件。支付、医保和 HIS 继续最后专项处理。
