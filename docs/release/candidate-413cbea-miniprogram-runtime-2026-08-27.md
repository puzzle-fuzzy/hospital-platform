# 当前本地小程序运行包候选 `805c54e`（2026-08-27）

> 本文只记录原生微信小程序本地 live 运行包，不表示已经上传微信线上版本，也不表示真机业务已验收。当前没有运行中的微信开发者工具或真机会话，九个真机证据域继续为 `pending`。

## 构建与验证

| 项目 | 值 |
| --- | --- |
| 运行输入来源 | `805c54ea9fa943385ad6feebed1401d521fbad3c`（`805c54e`） |
| live 目录 | `apps/miniprogram/dist/` |
| 页面数量 | 40 |
| 测试结果 | `341 pass / 0 fail / 3728 expect()` |
| 运行包校验 | `pnpm runtime:verify` 通过 |
| 服务端配套 | `b44421cd321ff9ff23eeb49b12641d1772d2bdc1`，仅表示 API 分层发布配套 |

运行包来自已提交、无运行输入脏改动的工作树；页面脚本、根文件、相对依赖和测试脚本隔离均通过构建器门禁。运行目录不包含 `*.test.js` 或 `*.spec.js`，避免开发者工具再次加载测试模块。

## 下一步

## 真机边界

下一次真机调试必须直接导入当前 `apps/miniprogram/`，普通编译后核对 `dist/build-info.json` 的完整 `sourceRevision`，再从同一 live 目录生成二维码。不能使用历史二维码、历史微信线上包或旧候选证据拼接当前业务结论。

当前可以继续验收四个主 Tab、显式切换就诊人、预约目录/历史、门诊费用只读和普通资料的页面状态；Provider、患者写入、外部入口、二维码真实生成、支付、医保和 HIS 仍按迁移准入目录保持关闭或 pending。
