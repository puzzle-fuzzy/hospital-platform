# `356705e` 原生小程序本地候选构建记录（2026-08-24）

## 1. 候选定位

这是当前本地未发布的原生 TypeScript 小程序候选，来源为：

- 源码提交：`356705e41852e585b07296c5e6e3dec52bce1381`（`356705e`）；
- 工程目录：`apps/miniprogram/`；
- 运行目录：`apps/miniprogram/dist/`；
- 页面入口：14 个；
- 运行包中测试脚本：0 个。

该候选没有上传、替换或覆盖线上小程序运行包，也没有修改旧 Python 服务、旧服务端口、MySQL、Redis、支付、医保或 HIS 配置。线上当前仍使用服务端 `28a5c0c131794ce9dcc5f94bd3809402188ac87a` 和小程序运行包 `13f597ea9ee3f65b9be858117826d948339d904a`，两者不能在真机验收时混用。

## 2. 本轮页面与业务边界

本候选收口了患者端已经有明确契约的页面体验：

1. “我的挂号”使用稳定的卡片层级展示状态、就诊人、时间、科室和操作，不再把每条记录压成难以扫描的长列表；
2. 爽约记录是独立的历史查询入口，不因缺少当前就诊人上下文自动打开“选择就诊人”模块；只有用户主动更换就诊人时才进入选择页；
3. 预约记录、报告目录、门诊费用等查询页面保持固定的查询状态容器。加载、成功空结果、失败和有数据状态在同一个容器内切换，避免“正在加载”先占用较小高度、空状态图片随后把卡片突然撑高；
4. 在线/全部预约记录的请求范围在进入页面时形成快照，避免标签切换或页面重渲染把旧患者、旧范围的结果写回当前页面。

对应的视觉与状态契约见
[`appointment-records-visual-state-shell-2026-08-24.md`](appointment-records-visual-state-shell-2026-08-24.md)。

## 3. 本地验证

- 小程序测试：`228` 项通过，`1706` 个断言通过；
- 构建校验：页面脚本、页面样式、`services/single-flight.js` 均在 `dist/`，测试脚本未进入运行包；
- 运行包来源校验：`dist/build-info.json` 的 source revision 为 `356705e41852e585b07296c5e6e3dec52bce1381`；
- 服务器与线上运行包：本记录不证明线上已使用本候选，也不替代真机页面截图、服务端同链日志和 Provider 返回证据。

## 4. 真机准入

真机测试必须在开发者工具中重新导入
`E:\__Super_Core__\hospital-platform\apps\miniprogram`，以当前 `dist/` 为运行根目录，并确认开发者工具没有复用旧项目的增量索引。扫码后需要同时收集：

1. 小程序页面与操作结果；
2. 新 Bun 服务的同一 `traceId` 请求日志；
3. Provider 只读返回或明确的受控失败结果。

在这三层证据齐全前，本候选只算本地可回归版本，不算正式发布版本。
