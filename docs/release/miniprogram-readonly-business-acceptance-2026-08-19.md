# 小程序只读业务链路验收记录（2026-08-19）

## 验收范围

本次仅验收不产生费用、不改变医院业务数据的只读链路：

1. 我的挂号和爽约记录；
2. 门诊缴费查询；
3. 报告目录的 Provider 未配置 fail-closed 边界；
4. 普通资料读取；
5. 更换就诊人并进入独立的就诊人选择页。

支付调起、医保授权、退费和结算回写不在本次范围内，也没有为了模拟成功而添加假数据。

> 历史观察窗口：本记录使用的是 `b7c9451` 切换前的服务端 `c26e696`，保留当时的页面、HTTP
> 和低敏日志事实；它不能替代当前 `b7c9451` + `5348715` 候选的真机或 Provider 验收。

## 验收环境

- 小程序：`hospital-platform/apps/miniprogram`，微信开发者工具 Stable `2.01.2510290`。
- 服务端：线上新服务 `hospital-platform-api-v2.service`，生产模式，当前仍通过原有域名转发。
- 旧服务：未修改、未停止、未切换端口。
- 数据与缓存：使用线上现有 MySQL、Redis 和中台配置，未清理数据、未写入业务测试数据。
- 当前账号：使用受控微信会话；患者姓名、卡号和内部患者标识不写入验收文档。

## 结果

### 1. 我的挂号

- 页面能够从首页进入 `pages/appointment-records/appointment-records`。
- 页面显示当前就诊人、当前院区、在线挂号/全部挂号两个标签以及“更换”入口。
- 服务端日志显示：`GET /api/v1/appointments/records` 返回 `200`。
- 本次线上中台返回 60 条记录，状态统计为 `cancelled: 60`，因此页面显示“暂无挂号记录”。这是后端真实状态映射后的空状态，不是前端吞掉数据。
- “全部挂号”仍保持迁移门禁，未把在线挂号结果伪装成全渠道历史。

### 2. 门诊缴费查询

- 页面能够从首页进入 `pages/outpatient-payment/outpatient-payment`。
- 页面显示当前就诊人、待缴费/已缴费标签、查询说明以及支付能力未开放提示。
- 服务端日志显示：`GET /api/v1/payments/outpatient/records` 返回 `200`，查询状态为 `unpaid`，返回 `itemCount: 0`。
- 服务端日志记录的时间窗口为 `2026-07-20 00:00:30` 至 `2026-08-19 00:00:30`（Asia/Shanghai），证明窗口由服务端生成，前端没有自行决定账期。
- 支付调起、医保授权、退费和结算回写仍然关闭，页面没有展示“立即支付”或医保按钮。

### 3. 更换就诊人

- 从门诊缴费页点击“更换就诊人”后，确实进入独立的 `pages/patient-select/patient-select` 页面。
- 当前线上账号返回 1 位就诊人；页面标记当前患者，并提供“刷新就诊人”按钮。
- 关系字段显示“其他”，来自中台关系值在适配层的未知值归一化，不是伪造的亲属关系。
- 卡号由前端展示模型基于服务端已脱敏值保持首 5 位和末 4 位；没有在页面恢复身份证或卡号原文，具体卡号不写入验收记录。
- 新增/绑定就诊人仍显示后续接入真实医院接口的提示，未开放空壳提交。

## 本轮追加观察（2026-08-19）

- 本地运行包来源为 `93a3c720dc137162ff469ec745359775b08f84ab`；微信开发者工具重新普通编译成功，14 个页面编译完成，调试器显示 `Errors: 0`。
- 模拟器从首页进入 `pages/appointment-records/appointment-records`，展示当前院区、在线/全部标签、空记录态和更换就诊人入口；没有执行预约写入、取消、支付或医保操作。
- 服务器当前 release `c26e696` 的最近 10 分钟低敏聚合：`appointmentRecords` 请求 `1`、成功 `1`、失败 `0`，`parseErrors=0`、`systemdWarningCount=0`；该结果证明服务端预约历史读链被触发，不证明真机页面或 Provider 数据正确。
- 模拟器随后从“我的”页进入 `pages/missed-appointments/missed-appointments`，页面显示当前就诊人、过去 90 天查询范围和合法空状态；爽约结果只按服务端归一化的 `missed` 状态派生，没有执行任何写入。
- 针对该爽约查询窗口的低敏聚合为 `appointmentRecords` 请求 `1`、成功 `1`、失败 `0`，`parseErrors=0`、`systemdWarningCount=0`；这证明爽约页触发了预约历史只读链，不证明真实 Provider 字段或真机验收完成。
- 模拟器随后进入 `pages/outpatient-payment/outpatient-payment`，展示待缴费/已缴费切换、合法空状态以及支付/医保能力关闭提示；没有触发支付、退费、医保授权或任何费用写入。
- 同一最近 10 分钟低敏聚合中，`outpatientPaymentRecords` 请求 `1`、成功 `1`、失败 `0`，`parseErrors=0`、`systemdWarningCount=0`；该结果只证明门诊费用查询读链被触发并完成服务端门禁，不证明真实费用数据或真机页面验收。
- 模拟器随后进入 `pages/report-directory/report-directory`，页面正确展示报告服务未配置提示、摘要范围说明和合法空状态；未打开报告详情、下载或文件资源。
- 同一最近 10 分钟低敏聚合中，`reportDirectory` 请求 `1`、成功 `0`、失败 `1`，`parseErrors=0`、`systemdWarningCount=0`；对应公网请求为 `503 dependency-not-configured`。这证明报告路由和 Provider 未配置时的 fail-closed 错误边界已生效，不把未接入能力伪装成成功空列表，也不证明真实 Provider 数据或真机页面验收。
- 模拟器随后从“我的”页进入 `pages/profile/profile`，普通资料只读加载完成，页面展示昵称、性别、年龄、邮箱和资料边界提示；本轮没有点击保存，没有触发 `PUT /api/v2/me/profile`，也没有写入生产资料。
- 同一最近 10 分钟低敏聚合中，`profileRead` 请求 `2`、成功 `2`、失败 `0`，`parseErrors=0`、`systemdWarningCount=0`；该结果证明普通资料读取链完成，不代表首次更新、版本冲突 `409` 或真机验收完成。
- 普通资料写入契约完成本地分层审计：API 49 项/268 个断言、持久化 42 项/152 个断言、小程序全套 128 项/1075 个断言均通过；覆盖 owner 隔离、首次 `version=0` 插入、条件版本更新、显式清空、非法字段拒绝、版本上限和 409 页面边界。全仓 `pnpm check` 也通过（架构 66 条、9 个 workspace typecheck/test/build）；本地小程序运行包 revision 为 `93a3c72`，未因此部署服务端或写入生产资料。
- 本轮继续前通过 SSH 只读复核：`hospital-platform-api-v2.service` 仍为 `active`，当前 release 仍为 `c26e696`；新 API `18081` 与旧 Python `8001` 同时监听，临时端口 `18082` 不存在。公网 live/ready 均为 `200`，ready 的 `database/redis/schema` 均为 `ok`；本轮没有重启、migration、Redis 清理或业务写入。
- 本轮仍未检测到手机连接，以上模拟器和日志结果不能升级为真实微信真机验收。

## 本轮追加观察（2026-08-19 00:41 CST）

- 在同一开发者工具模拟器运行包中，从爽约记录返回“我的”再返回首页，首页先显示“微信已登录/无就诊卡”的短暂加载态；患者目录读取完成后恢复为当前就诊人姓名和首 5 位+末 4 位脱敏卡号。这证明加载中的空展示没有被当成服务端空目录，页面等待异步读模型收敛后才绘制患者事实。
- 点击首页患者卡片的“更换就诊人”进入独立 `pages/patient-select/patient-select`，目录同步完成后展示当前患者、关系“其他”和脱敏电子就诊卡；本轮没有点击“刷新就诊人”或新增绑定。关系文本来自适配层的未知关系归一化，不是小程序根据姓名猜测。
- 当前线上 `c26e696` 同一低敏窗口的 P0 关联审计：`patientRead` 请求 `9`、成功 `9`、失败 `0`；`patientSync` 请求 `3`、成功 `3`、失败 `0`；两项均为 `parseErrors=0`、`systemdWarningCount=0`，且请求、业务成功和 HTTP 2xx 位于同一关联链。选择页的既有页面生命周期会自动完成目录同步，本轮没有预约、报告、费用、支付或医保业务写入。
- 本条仍是“模拟器页面 + 线上低敏日志”的配对证据，不证明真机、多患者显式切换或新增绑定已经完成；患者切换仍需真实多患者账号和手机端验收。

## 开发者工具错误边界

模拟器控制台持续出现 1 个相同的错误：

```text
Cannot read property '__subPageFrameEndTime__' of null
at <setInterval callback function>
at http://127.0.0.1:41227/appservice/__dev__/WAServiceMainContext.js
```

当前证据表明它来自微信开发者工具基础库 `WeChatLib 3.17.1` 的内部脚本：

- 堆栈没有指向 `apps/miniprogram/src` 或业务页面构建文件；
- 在小程序源代码和构建产物中搜索不到 `__subPageFrameEndTime__`；
- 页面切换、接口请求和页面渲染仍然完成，服务端请求均有明确的 200 日志。

因此本次不修改业务代码来“消除”该错误。它只能暂记为开发者工具/模拟器问题，仍需在真实微信客户端真机调试中复核；如果真机出现同样堆栈，再单独按基础库版本和页面生命周期排查。

## 尚未完成的验收

- 真机扫码后的页面操作仍需真实微信客户端完成；模拟器结果不能替代真机结论。
- 患者选择目前线上账号只有 1 位患者，尚未具备真实多患者账号来验收切换后上下文隔离。
- 支付、医保授权、退费、结算回写继续保持关闭，等待独立业务契约和医院侧联调证据。

## 预约目录追加观察（2026-08-19 00:42 CST）

- 从首页进入医院静态前置页，再进入预约目录，模拟器成功展示真实 Provider 返回的两列级联：左侧科室列表，右侧当前科室的日期、医生、时段和剩余号源；日期窗口由服务端固定为中国标准时间未来 7 天。
- 连续快速点击两个不同科室后，页面最终只保留最后一次选择对应的“风湿免疫门诊”排班，旧科室响应没有覆盖当前右栏；其中一次返回 13 个排班，页面按本地批次先展示 12 条并保留后续加载边界，未把本地批次误报为 Provider 分页。
- 当前线上 `c26e696` 同一低敏窗口事件计数为：`appointment.directory.departments.requested=1`、`departments.synced=1`、`schedules.requested=3`、`schedules.synced=3`、`appointment.schedule_snapshots.persisted=3`；`parseErrors=0`、`systemdWarningCount=0`。快照持久化只证明短期观察事实已保存，不代表已经获得锁号、预约写入或支付授权。
- 本轮未点击号源确认、预约下单、取消、支付或医保入口；预约写入和全部渠道历史仍保持关闭，以上仍是模拟器 + 服务端低敏日志证据，不替代手机真机验收。
