# `5a31427` P0 业务日志空窗口观察（2026-08-21 04:09 CST）

> 本文是当前 release 的线上只读运行观察。它证明服务正常、日志解析正常和新旧服务共存，
> 不证明微信、患者、预约、门诊费用或普通资料业务已经成功；因为观察窗口没有新的业务请求，
> 业务证据门禁必须保持未通过。

## 1. 观察范围与运行状态

| 项目 | 结果 |
| --- | --- |
| 观察服务器时间 | `2026-08-21 04:09:20 CST` |
| 观察窗口 | `2026-08-21 03:54:00 CST` 至取证时刻 |
| 当前 release | `5a314275e9bae43730eab5b32638a8baecda5869` |
| 新 API | `10.0.0.3:18081`，`active` |
| 旧 Python | `0.0.0.0:8001`，继续监听；未修改、未停止 |

本次只执行 SSH 只读命令：读取当前 release 指针、systemd 状态、监听端口和新 API journald，
没有重启服务、写入 MySQL/Redis、调用 Provider 或改变 Nginx 转发。

## 2. 日志聚合结果

使用当前 release 自带的 `apps/worker/dist/p0-log-aggregate.js` 解析 journald，再使用同一 release
的 `p0-business-evidence-audit.js` 检查业务链。聚合摘要为：

| 指标 | 结果 |
| --- | ---: |
| 输入记录 | `16` 行 |
| 有效解析记录 | `10` 条 |
| 解析错误 | `0` |
| systemd 控制消息 | `5` 条，已按规则忽略 |
| systemd warning | `0` |
| HTTP 完成事件 | `7` 条，均为 `200` 健康检查 |
| P0 业务请求/成功 | `0/0` |

微信登录、患者目录读取/同步、预约科室/排班/历史、门诊费用、报告目录和普通资料读写的
`requested`、`success`、同 trace 请求/成功及同 trace HTTP `2xx` 均为 `0`。P0 审计返回
`passed=false` 是正确的“证据缺失”结果，不是线上服务异常。

## 3. 下一步

必须由当前 `6e6604f` 小程序运行包重新扫码产生真实业务请求，再按以下顺序取证：

1. 微信登录与 `/me` 会话恢复；
2. “更换就诊人”中的显式患者选择与返回后的上下文清理；
3. “我的挂号”与爽约记录；
4. 门诊费用待缴/已缴只读页面；
5. 普通资料读取、受控更新和双会话 `409`。

每个业务域必须同时满足页面结果、客户端 requestId/traceId、服务端同链成功和 HTTP `2xx` 四项证据。
支付、医保、预约写入、报告详情/附件、患者新增绑定、病历和 HIS 回写继续保持关闭。
