# 当前真机微信登录与患者同步观察（2026-08-24）

## 1. 观察范围

本记录只保存当前真机调试窗口和服务端低敏日志能够证明的事实，不把它扩展为预约、门诊费用、支付或医保验收。

- 小程序项目：`E:\__Super_Core__\hospital-platform\apps\miniprogram`；
- 真机运行根目录：`dist/`；
- 本地运行包来源：`356705e41852e585b07296c5e6e3dec52bce1381`（`356705e`）；
- 配套线上服务端：`28a5c0c131794ce9dcc5f94bd3809402188ac87a`（`28a5c0c1`）；
- 真机调试窗口显示：iPhone 17 Pro、iOS 26.5.2、微信 8.0.75、基础库 3.17.1、4G；
- 观察时间：2026-08-24 15:50–15:52 CST。

真机调试窗口曾显示连接正常并产生 13 条网络请求；随后设备连接断开，但调试网络列表和服务端 journald 低敏事件仍可用于本次观察。没有上传小程序、切换线上运行包或修改旧 Python `8001`。

## 2. 已证实的请求链

服务端当前 release 的 Pino 事件按时间顺序出现了以下业务事实：

| 时间（CST） | 请求/事件 | 结果 | 证据含义 |
| --- | --- | --- | --- |
| 15:50:44 | `GET /api/v1/me` | `401 unauthorized` | 本地会话未被服务端接受，属于登录前受控失败 |
| 15:50:45 | `POST /api/v1/auth/wechat` | `200` | 微信 code 兑换和平台会话签发成功 |
| 15:50:45 | `GET /api/v1/me` | `200` | 新会话通过 owner 校验 |
| 15:50:45 | `GET /api/v1/patients` | `200`，`itemCount=1` | 当前账号的患者目录读取成功 |
| 15:50:46–15:50:48 | `POST /api/v1/patients/sync` | `200` | Provider `zhongyang` 同步成功，`patientCount=1`、`activePatientCount=1`、`hisPatientReferenceCount=1` |
| 15:50:48 | 同步后的 `GET /api/v1/patients` | `200`，`itemCount=1` | 同步结果重新进入当前患者读模型 |

服务端事件含有独立的 `requestId`/`traceId` 和 Provider request id；本记录只保留事件类型、状态和数量，不记录用户标识、患者姓名、卡号、身份证号、Authorization 或 Provider 原文。

## 3. 观察到的客户端行为

- `/me` 首次 `401` 后进入微信登录，再以新会话读取 `/me` 和患者目录；没有把 `401` 当作“永久退出”直接卡死；
- 患者同步成功后再次读取患者目录，服务端看到的操作 `attemptCount=1`，没有发现同一操作的自动重放冲突；
- 调试网络面板中的 `SdkReport` 为微信开发者工具/基础库的遥测请求，不属于医院业务接口，也不参与登录、患者同步或业务成功判断；
- 当前没有预约历史、爽约、门诊费用或报告请求，因此这些业务不能由本记录标记为真机完成。

## 4. 当前验收等级

| 业务域 | 当前证据 | 结论 |
| --- | --- | --- |
| 微信登录 | 服务端 Pino + 真机调试网络请求序列 | 已取得本次登录链路观察，仍缺稳定复现的页面截图/操作记录 |
| 患者目录/同步 | 服务端 Pino + Provider request id + 真机调试网络请求序列 | 已取得一次受控成功观察，仍缺显式更换第二患者和失效恢复 |
| 预约历史/爽约 | 无当前请求 | 未验收 |
| 门诊费用 | 无当前请求 | 未验收 |
| 报告目录 | 无当前请求 | 未验收 |
| 支付/医保/HIS | 运行闸门关闭 | 按计划最后处理，不能通过只读链路推导 |

## 5. 下一步操作

保持当前 `miniprogram` 项目和服务端版本不变，重新扫码后按以下顺序采集：

1. 在首页确认当前患者卡片，再主动进入患者选择页并选择另一条可用患者（若测试账号只有一条患者，记录为“无第二患者样例”，不得伪造切换成功）；
2. 返回“我的挂号”，分别点击在线、全部和爽约入口，保存页面状态和调试网络请求；
3. 进入门诊缴费，分别观察待缴、已缴和空结果状态；
4. 每个页面都用服务端同一时间窗口的低敏 `traceId`/Provider request id 对照；
5. 不点击预约提交、取消、支付、医保授权、结算或 HIS 回写入口。

只有页面操作、客户端请求和服务端日志三层同时具备时，才把对应只读业务域从“代码已实现”提升为“真机已验收”。
