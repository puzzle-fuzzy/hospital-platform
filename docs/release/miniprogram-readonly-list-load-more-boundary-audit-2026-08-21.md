> 当前服务端配套发布更新（2026-08-24 13:01 CST）：线上服务端 release 为 `28a5c0c131794ce9dcc5f94bd3809402188ac87a`；当前小程序运行包来源仍为 `13f597ea9ee3f65b9be858117826d948339d904a`（提交 `13f597e`）。本轮为服务端独立只读 adapter 发布，未重建小程序运行包；下方历史候选仅供追溯，本行优先。
> 当前发布基线更新（2026-08-24 13:01 CST）：线上服务端 release 为 `28a5c0c131794ce9dcc5f94bd3809402188ac87a`；当前小程序运行包来源为 `13f597ea9ee3f65b9be858117826d948339d904a`（提交 `13f597e`）。本轮为服务端独立只读 adapter 发布，真机业务三层证据仍待。
> 本段优先于本文下方旧日期、旧 release 或旧运行包叙述；旧值只作为历史记录，不作为当前验收入口。
> 下方旧 release 与运行包只作历史追溯；当前执行使用顶部 `28a5c0c1` 服务端 + `13f597e` 小程序分层基线。

# 小程序只读列表本地加载窗口审计（2026-08-21）
> 历史服务端发布基线（2026-08-22）：`0e2a366efcca8da25d7edd4a286781f2d3dfdbec`；小程序来源为 `171a8743185fb4ecc1696851662659c1a0ee7ebf`。本审计不把本地加载窗口当作 Provider 分页证据。

> 当前验收基线：服务端 `28a5c0c131794ce9dcc5f94bd3809402188ac87a`；小程序运行包来源
> `13f597ea9ee3f65b9be858117826d948339d904a`（提交 `13f597e`）。下方历史测试数字只表示当时的代码审计结果。

> 本记录只覆盖本地页面状态边界，不代表 Provider 或真机业务验收完成。

## 发现的问题

预约历史、爽约记录、报告目录和门诊费用都把服务端一次完整只读结果保存到页面，再用“加载更多”控制首屏渲染数量。旧 WXML 事件可能在刷新、切换患者、切换费用状态或会话代际变化后抵达；如果页面只依赖按钮当时是否显示，事件就可能继续修改已经失去业务上下文的本地窗口。

## 修正规则

四个页面的 `onLoadMore` 现在统一遵循以下顺序：

1. 加载中或没有当前患者时直接忽略事件；
2. 患者或会话代际已经变化时，重新建立当前 `/me` → 患者目录 → 业务读模型组合，不展开旧快照；
3. 当前结果确实还有未展示记录时才计算下一窗口；
4. 下一展示数量必须严格大于旧数量，并限制在本次完整结果总数内。

这项规则只保护客户端渲染生命周期，不新增 Provider 分页语义、不改变金额/预约状态、不触发支付、医保、预约写入或 HIS 回写。

## 验证

- 小程序测试：180 项通过，1438 个断言；
- 小程序 typecheck、Biome、build 和 `runtime:verify` 通过；
- 运行包包含 14 个页面，`dist/` 中不存在 `*.test.js` 或 `*.spec.js`；
- 旧 Python 项目、服务器、线上配置、MySQL、Redis 和并行众阳自动化代码未修改。

## 尚未完成

真实微信会话、患者显式切换、预约历史/爽约/门诊费用页面三层证据、Provider 报告详情、支付、医保、退款、预约写入和 HIS 回写仍按各自 contract 与真机门禁处理。
