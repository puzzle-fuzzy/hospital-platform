# `37735af1` 原生小程序本地候选构建记录（2026-08-24）

> 本文只记录本地原生小程序运行包，不代表已经上传微信开发者工具、部署服务端或完成当前线上 release 的真机验收。
> 本轮不修改旧 Python 服务、不重启旧服务、不写数据库/Redis，不触发支付、医保、预约写入、退款或 HIS 回写。

## 来源与构建结果

| 项目 | 值 |
| --- | --- |
| 原生小程序运行输入提交 | `37735af1525e4ff23e96d21bbaecb5dedf5ae557` |
| 当前仓库文档提交 | `b405cb0e1c7768259bdfb253a45ab216b588fbc5` |
| 构建入口 | `apps/miniprogram/` |
| 运行包目录 | `apps/miniprogram/dist/` |
| 页面数量 | 14 |
| 测试脚本数量 | 0 |
| 本地构建时间 | `2026-08-24T06:41:30.387Z` |
| 配套线上服务端 | `28a5c0c131794ce9dcc5f94bd3809402188ac87a`，本轮未修改 |

本地 `typecheck`、小程序构建和 `runtime:verify` 均通过。`build-info.json` 的 `sourceRevision`
固定为 `37735af1525e4ff23e96d21bbaecb5dedf5ae557`，14 个 `app.json` 页面脚本全部存在，
`dist/` 不包含 `*.test.js` 或 `*.spec.js`。

## 本候选包含的安全修正

1. 患者同步期间进入选择页时，统一导航入口返回明确结果；爽约页被同步门禁拦截时退出跳转 loading，进入可重试错误态。
2. 报告详情的加载、错误、无检测项和无云影像状态共享稳定的 `360rpx` 状态容器，减少读模型收敛时的页面跳动。
3. 相关中文注释、业务正确性规则和静态验收断言已同步更新；不改变报告 Provider、患者引用、附件授权或任何副作用能力。

## 证据边界

当前线上正式真机基线仍是服务端 `28a5c0c1` + 小程序 `13f597e`。因此正式
`device-evidence-audit` 的 `13f597e` 清单不能填入本地 `37735af1` 的页面或 requestId；
否则来源校验会拒绝，拒绝是正确行为。若使用本包做本地视觉/逻辑验收，必须单独标记为
`37735af1` 候选观察，不能回写为当前线上 `13f597e` 的通过证据。

如需将本候选纳入正式真机验收，必须先按受控发布手册完成候选来源确认、重新生成二维码，
并重新建立服务端 release、客户端 requestId、Pino/Provider 日志的同链证据；旧 Python `8001`
仍必须保持监听和不变。
