> 当前配套小程序运行包（2026-08-27）：本地 live `dist` 的 sourceRevision 为 `62cdb8f82b4169dd1b9a6ed3403e3be2f7422328`（`62cdb8f`），共 40 个页面；当前没有运行中的微信开发者工具或真机会话，九个真机证据域仍为 `pending`。本文下方历史候选仅作追溯。

> 当前配套小程序运行包来源（2026-08-27）：`f1b8b61609e0560d3da3fe176f62ab3585b6ee98`（`f1b8b61`）；当前没有开发者工具或真机会话，九个真机证据域仍为 `pending`。本文下方更早候选仅作历史追溯。

> 当前服务端配套发布更新（2026-08-24 13:01 CST）：线上服务端 release 为 `28a5c0c131794ce9dcc5f94bd3809402188ac87a`；当前小程序运行包来源仍为 `13f597ea9ee3f65b9be858117826d948339d904a`（提交 `13f597e`）。本轮为服务端独立只读 adapter 发布，未重建小程序运行包；下方历史候选仅供追溯，本行优先。
> 历史配套小程序构建来源（2026-08-26）：`0be59f966de2c3a0861cb44e9a526a1ef557f6c7`，仅表示当时本地 live 候选，未证明微信线上版本或真机业务已验收；当前入口以当前项目基线为准。
> 当前线上服务端 release（2026-08-27）：`1bc8b0a85f21cb58205a99ce4de0de6afe9bf240`，已完成候选 preflight、隔离 smoke、原子切换和公网 runtime smoke；该运行层证据不等价于真实 Provider 或支付业务成功。
> 当前服务端 release 为 `28a5c0c131794ce9dcc5f94bd3809402188ac87a`；当前小程序运行包来源为 `13f597ea9ee3f65b9be858117826d948339d904a`（提交 `13f597e`）；真实真机页面证据仍待。
> 当前小程序配套运行包来源（2026-08-27）：`f1b8b61609e0560d3da3fe176f62ab3585b6ee98`（`f1b8b61`）；本文中更早候选和真机窗口仅作历史追溯，当前无真机/开发者工具会话。

# “我的”页面与未迁移入口审计（2026-08-22）
> 当前执行基线（2026-08-24）：线上服务端为 `28a5c0c131794ce9dcc5f94bd3809402188ac87a`；
> 小程序来源为 `13f597ea9ee3f65b9be858117826d948339d904a`（提交 `13f597e`）。未迁移写入、支付和外部入口继续关闭。

> 当前执行对象为线上服务端 `28a5c0c1` 与小程序 `13f597e` 的分层配套；文中更早候选只作历史追溯。
> 当前完整小程序来源：`13f597ea9ee3f65b9be858117826d948339d904a`。
> 本文只记录当前原生小程序的页面结构、入口语义和迁移边界。旧项目
> `G:\\fuck\\hospital` 仅作为只读对照，本轮没有修改旧 Python 服务、旧端口、数据库或 Redis。

## 1. 对照结论

当前小程序源码基线为 `13f597e`。`apps/miniprogram/src/pages/my/`
已经按旧端 `src/pages/user/user.vue` 和 `src/jsonData/userNavData.json` 保留了页面骨架：

- 顶部本地背景图、头像区和昵称入口；
- 蓝色“家庭成员管理”卡片，点击进入独立就诊人选择页；
- 三个“我的订单”分组，顺序、标题、四列网格和本地 SVG 图标与旧端一致；
- 当前页面使用微信原生 `app.json.tabBar`，四项文字、图标和选中资源由平台统一渲染；
- 页面底部为平台 TabBar 预留空间，业务内容使用独立滚动视口，避免页面整体滚动造成底栏闪动或选中态丢失。

当前没有发现需要为了“更现代”而改变旧端视觉层级的理由，也没有发现可以安全恢复旧 WebView 或旧接口直连的业务缺口。

## 2. 入口迁移状态

| 旧端入口 | 当前处理 | 原因与下一步 |
| --- | --- | --- |
| 家庭成员管理 | 已迁移到原生患者选择页 | 目录由当前 owner 读取；患者切换是显式操作，不默认把当前微信用户当作任意就诊人 |
| 我的挂号 | 已迁移为预约历史只读页 | 使用新服务内部患者 ID 和已确认渠道 3；详情、取消和重新预约仍未开放 |
| 爽约记录 | 已迁移为标准化 `missed` 只读页 | 只展示服务端明确归一化的爽约状态，不在页面猜 Provider 状态码 |
| 意见反馈 | 已迁移为安全静态帮助页 | 在线反馈提交还没有新 contract；客服电话入口保留在静态页面 |
| 个人资料 | 已迁移为平台资料页 | 当前 contract 只支持展示名、性别、年龄和邮箱，不接收旧端头像、openid 或患者字段 |
| 我的问诊 | 明确提示迁移中 | 旧端依赖问诊订单、聊天和外部页面，尚未完成 owner、消息和详情 contract |
| 门诊病历 | 明确提示迁移中 | 门诊病历不是检验报告，不能复用报告目录路由；待单独冻结患者范围和详情引用 |
| 电子导诊单 | 明确提示迁移中 | 旧端有独立电子导诊业务，不把它降级成静态成功或报告列表 |
| 我的医生 | 明确提示迁移中 | 旧端包含关注/取消关注和医生关系接口，当前没有新读写 contract |
| 智能客服 | 明确提示迁移中 | 旧端是外部 WebView，域名、登录态和隐私边界尚未在新项目确认 |
| 医保电子凭证 | 明确提示需要独立授权 | 不能从“我的”页直接复用旧小程序跳转；医保授权和支付最后单独验收 |

旧端 `userNavData.json` 中曾被注释掉的“我的快递”和“消息订阅”不应悄悄重新出现在新菜单：没有真实入口和 contract 时，保留缺席比添加不可用的幽灵功能更准确。

## 3. 关键视觉复核

### 3.1 背景和头像

- 旧端背景使用 `background-image`，新端使用 `<image>`，因为微信 WXSS 不能可靠读取本地 `background-image`。
- 新端使用本地 `legacy-user-background.png`，固定原始设计的 `566rpx` 顶部高度并采用 `widthFix`，避免真机下重复、裁切或远程资源失败。
- 新资料 contract 没有头像 URL，因此头像使用本地默认头像；不能把旧端可能返回的头像字段未经 contract 校验带回页面。

### 3.2 分组和图标

- 三个分组标题都保留旧端事实值“我的订单”，不是排版遗漏；菜单数据来自旧端 JSON 的逐项对照。
- 菜单项使用标题作为 WXML 稳定 key。未迁移入口仍然有明确 action 和提示，不使用空 action 作为 key，避免微信渲染层复用错位。
- 四列网格保留 `20rpx` 的行列间距、`50rpx` 图标和 `20rpx` 图标文字间距。

### 3.3 底部导航

- “我的”页和首页使用同一组 tabbar 资源；“我的”激活第四项。
- 导航栏使用 `position: fixed` 固定在视口底部，业务栏高度固定为 `130rpx`，安全区只增加底部留白。
- 未迁移的“就诊”和“互联网医院”点击只提示迁移中，不跳转旧页面或伪造已完成能力；首页入口和“我的”入口仍保持可用。

## 4. 不应直接迁移的旧逻辑

旧端页面通过标题直接跳转到多个 `pagesB` 路由，其中包含外部 WebView、医保小程序、患者绑定、医生关系和问诊订单。新端不应复用这些 URL 或 Provider 参数，具体原因：

1. 旧路由的登录态与新 Elysia 会话不是同一 contract；
2. 旧页面可能直接使用 `patId`、用户信息或订单号，无法证明当前 owner 归属；
3. 医保电子凭证和微信支付存在授权、域名、商户和状态机约束，必须最后单独验收；
4. 页面显示“迁移中”比跳转到不可控旧服务更能避免用户误以为新链路已完成。

## 5. 当前未完成清单

按业务风险排序，后续应依次处理：

1. 对已迁移的预约和门诊费用只读页补真实 Provider 非空响应、线上低敏日志关联和真机页面证据；
2. 单独冻结门诊病历和电子导诊单的公开读模型，再实现详情和列表；
3. 评估我的问诊、我的医生、智能客服的 owner/外部会话边界，不能只做页面壳；
4. 设计门诊费用详情、微信支付、医保授权、6202/6301、支付通知、退款和 HIS 写回状态机；
5. 最后再处理“就诊/互联网医院”底部 tab 的真实页面及旧服务共存切流。

真机验收仍必须使用重新构建的当前 `dist/`，确认没有 `*.test.js`/`*.spec.js`，并关闭旧的“真机调试”会话和增量缓存；不能把开发者工具旧窗口显示的 ENOENT 或旧二维码当成当前源码证据。
> 当前发布基线更新（2026-08-24 19:54 CST）：线上服务端 release 已切换为 `8eb51b5ffe85b0b8f8a032783f893117d3df549d`；小程序运行包来源仍为 `13f597ea9ee3f65b9be858117826d948339d904a`（提交 `13f597e`）。本轮只重启新 API，旧 Python `8001` 未修改；普通资料 PUT、支付、医保和 Provider 真机证据仍待。
> 当前统一发布基线补充（2026-08-27）：服务端 release 为 `1bc8b0a85f21cb58205a99ce4de0de6afe9bf240`；小程序本地 live 运行包来源为 `f1b8b61609e0560d3da3fe176f62ab3585b6ee98`，共 40 个页面。本文更早版本仅作历史追溯，真机证据仍为 pending；旧 Python `8001` 未修改。
