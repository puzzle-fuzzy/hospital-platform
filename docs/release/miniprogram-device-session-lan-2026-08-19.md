# 原生小程序局域网真机调试重试记录（2026-08-19）

> 本文只记录微信开发者工具的真机传输重试状态。二维码生成、局域网模式勾选和代码包编译都不能单独证明手机已连接、微信登录成功或业务验收完成。

## 当前候选

- 服务端生产候选：`398be8e`
- 小程序代码来源：`474b044`
- 小程序完整来源：`474b0444736599c848a4cef9f47fd930884e401d`
- 开发者工具项目：`miniprogram`
- 运行包根目录：`dist/`
- 代码包大小：约 `643 KB`
- 调试系统：`iOS`
- 局域网模式：已勾选
- 工具界面观察时间：约 `2026-08-19 17:58 CST`
- 工具显示二维码失效时间：`2026-08-19 18:23 CST`

## 18:15 CST 追加观察

- 二维码仍可见，iOS 系统和“局域网模式”仍为选中状态；截至本次观察没有手机连接窗口、微信 `code`、平台 session 或业务 HTTP 请求。
- 开发者工具额外弹出“模拟器长时间没有响应”的工具提示，调试面板持续出现 `getDevCodeByFileList-miniProgram` 日志。
- 该提示与真机二维码连接发生在同一开发者工具层，当前没有证据表明请求已经到达新 API；因此仍归类为开发者工具/真机传输层问题，不能当作微信授权、患者同步或 Provider 失败。

## 观察结果

本次在当前 `miniprogram` 窗口重新打开“二维码真机调试”，代码包编译和上传完成，并生成二维码。当前已确认：

- 模拟器仍为匿名患者，没有真实微信身份；
- 局域网模式复选框为选中状态；
- 尚未观察到手机设备连接窗口；
- 尚未取得微信 `code`、平台 session、患者目录或任何业务 HTTP 请求；
- 因此不能把本次状态计入微信登录、患者同步、预约、报告或门诊费用验收。

此前独立的真机调试窗口曾出现 `websocket error RSV2 and RSV3 must be clear`，并显示服务已结束、收发流量为 0。该错误发生在业务请求之前，只能归类为真机调试传输层观察，不能归因于新 API、微信登录接口或 Provider 业务。

## 下一步固定顺序

1. 手机扫描本记录对应的二维码，并确认打开的是 `miniprogram` 候选，而不是旧的 `mp-weixin` 窗口；
2. 先确认真机调试服务变为已连接，再观察是否出现微信登录请求；
3. 登录完成后只验证 `/me`、患者同步和显式切换；
4. 只有前一步同时具备页面结果、API 状态/追踪标识和服务端低敏日志，才进入预约历史和门诊费用只读验收；
5. 任一出现传输错误、会话失效、患者串用或持久化异常，立即停止业务操作，不点击挂号写入、支付、医保或二维码。

## 共存与安全边界

- 本次只操作微信开发者工具真机调试入口，没有修改旧 Python 服务、线上新 API、MySQL、Redis 或 Provider 配置。
- 没有触发预约写入、支付、医保授权、二维码生成或 HIS 回写。
- 不在文档、日志或截图中保存微信令牌、openid、session_key、身份证号、手机号、完整卡号或 Provider 原始档案。
- 用户已有的 `apps/miniprogram/project.config.json` 修改保持未暂存、未提交。
