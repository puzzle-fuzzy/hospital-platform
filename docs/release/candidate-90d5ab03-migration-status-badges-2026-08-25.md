# 小程序候选 `90d5ab03`：全入口迁移状态护栏（2026-08-25）

> 候选来源：`90d5ab0323b33fb620c83b86a8248b31c0ae3341`。
> 本文只记录新项目候选，不代表线上小程序已经切换，也不改变旧 Python 服务、数据库或 Redis。

## 本轮内容

- 统一状态页不再把所有未开放能力显示成笼统“功能迁移中”，改为展示实际阻塞类型：
  `待 provider contract`、`待临床审核`、`待支付与回写 contract`、`待患者绑定 contract`、
  `待外部入口 contract`；
- 首页、“我的”和二级动作继续只使用固定 `FeatureKey`，新增入口若没有对应状态目录会由小程序验收测试失败；
- 更新批次 B 读模型迁移矩阵，集中登记门诊病历、问诊/导诊单、住院、健康内容、实时就诊和互联网医院；
- 修正 Obsidian 中新增就诊人和服务栅格仍描述为默认弹窗/Toast 的过时记录。

## 构建与运行包边界

| 项目 | 结果 |
| --- | --- |
| 运行包来源 | `90d5ab0323b33fb620c83b86a8248b31c0ae3341` |
| 注册页面 | 17 个 |
| pending 位置 | `.local/hospital-miniprogram/pending/` |
| 当前 live `dist` | `fcc6630ebfa7b0697cbd03a5e376ce6765d1643b` |
| 构建 | staging 成功；原子发布因微信开发者工具占用 `dist/` 返回 `EBUSY` |
| 处理方式 | 保留旧 live，保留新 pending；关闭工具后执行 `pnpm --filter @hospital/miniprogram runtime:publish-pending` |

## 自动化证据

- 小程序类型检查通过；
- 小程序测试：`251 pass / 0 fail / 2069 expect()`；
- Biome 定向 lint/format 通过；
- 文档链接审计：668 个文档，无断链；
- 迁移台账：17 个原生页面、64 个旧页面、195 个旧端挂载路由、87 个旧客户端接口字面量一致。

## 未改变的业务边界

本候选没有打开门诊病历、住院、健康百科、自测、实时就诊、互联网医院 WebView、患者绑定、
二维码、预约写入、支付、医保、退款或 HIS 回写；这些能力继续由各自 contract 和真实验收证据控制。
本候选没有部署服务器，也没有修改旧 Python `8001`、MySQL、Redis 或另一会话负责的众阳自动化文件。
