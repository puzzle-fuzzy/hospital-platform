# 公网新 API 运行层与普通资料鉴权复核（2026-08-21 11:36 CST）

> 本次使用本地构建的 smoke 工具访问当前公网 `https://test-hp.meiyi.pro`，只携带工具生成的关联标识，
> 不携带 Bearer、微信 code、患者标识、Provider 凭证或支付数据。没有写入数据库/Redis，也没有修改或重启服务。
> 结果只证明公网运行层和路由边界，不证明微信真机或 Provider 业务完成。

## 1. 结果

| 检查 | 结果 | 说明 |
| --- | --- | --- |
| `health-live` | `200` | 进程存活 |
| `health-ready` | `200`，连续 `3/3` | 生产 readiness 稳定 |
| `system-ping` | `200` | 服务身份正确 |
| `auth-boundary` | `401/unauthorized` | `/me`、普通资料、患者目录、预约历史、报告和门诊费用均先经过认证边界 |
| `closed-boundary` | `404/not-found` | 患者新增、门诊病历、医保授权、预约写入/占号/取消继续未注册 |

smoke 输出的运行环境为 `production`。其中普通资料单独检查 `GET /api/v2/me/profile`，确认它不会因为
缺少查询参数而进入 validation，也不会在未登录时返回空资料或 200。

## 2. 结论

公网入口、生产模式、普通资料鉴权和未开放能力边界均符合当前 fail-closed 设计；本次没有调用任何 Provider，
不能据此判断资料读取、资料写入、预约历史、门诊费用或患者切换已经有真实业务数据。

当前线上 release 仍为 `5a31427`，本地小程序候选仍为 `c86a788`。下一步必须由该候选重新生成二维码并采集页面、
客户端 requestId/traceId 和服务端低敏业务事件；若没有业务事件，应先排查运行包与开发者工具项目路径，不打开支付、医保或未冻结 contract。
