# 原生小程序开发者工具项目前置检查（2026-08-21）

## 结论

当前代码和运行包已经通过发布前门禁；本轮桌面检查发现微信开发者工具只打开了旧项目 `mp-weixin`，没有打开新项目 `miniprogram`。因此不能在旧项目窗口中继续真机调试，否则开发者工具可能继续使用旧的增量模块索引并再次报告已经不存在的 `dist/services/single-flight.test.js`。

## 当前候选证据

| 项目 | 结果 |
| --- | --- |
| 小程序候选提交 | `03d9d25` |
| 运行包来源指纹 | `03d9d25d80d5a5d872a9137c7df0aa19a91ba38f` |
| 运行包目录 | `E:\\__Super_Core__\\hospital-platform\\apps\\miniprogram\\dist` |
| `services/single-flight.js` | 存在 |
| `services/single-flight.test.js` | 不存在 |
| `*.test.js` / `*.spec.js` | 0 个 |
| `app.json` 页面脚本 | 14 个 |
| `runtime:verify` | 通过 |

## 桌面窗口观察

2026-08-21 当前只读观察到微信开发者工具窗口标题为 `mp-weixin - 微信开发者工具 Stable v2.01.2510290`；没有发现标题为 `miniprogram` 的新项目窗口。旧项目窗口不属于新候选验收范围，本轮没有点击、编译、扫码或修改旧项目。

## 正确恢复顺序

1. 关闭旧的 `mp-weixin` 真机调试会话；不要在该窗口继续编译新候选。
2. 关闭并重新打开 `E:\\__Super_Core__\\hospital-platform\\apps\\miniprogram`。
3. 确认项目配置的 `miniprogramRoot` 是 `dist/`，而不是 `src/` 或旧端目录。
4. 在新项目窗口先执行一次普通“编译”，确认 14 个页面入口都加载。
5. 使用新项目窗口重新生成二维码，再开始微信登录、患者目录和页面验收。

如果仍出现 `single-flight.test.js`、页面脚本 404 或旧页面路径，先停止真机调试并重新建立新项目窗口；不得把测试脚本复制进 `dist/`，也不得用旧项目二维码作为当前候选证据。

## 尚未形成的证据

本检查只证明本地运行包边界和开发者工具项目选择，不能代替以下三层配对证据：

- 真机页面结果；
- 同一操作对应的 HTTP `traceId/requestId`；
- 同一时间窗口的低敏服务端业务日志。

因此微信登录、患者显式切换、预约历史、爽约记录和门诊费用仍不能标记为当前候选的真机业务完成；支付、医保、HIS 写回和 Provider 未冻结能力继续保持关闭。
