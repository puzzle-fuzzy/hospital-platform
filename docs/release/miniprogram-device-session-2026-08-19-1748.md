# 原生小程序真机调试入口记录（2026-08-19 17:48 CST）

> 本文只记录微信开发者工具的候选入口，不把二维码存在误认为手机已经连接、微信登录成功或业务验收完成。

## 当前候选

- 服务端生产候选：`398be8e`
- 小程序代码来源：`474b044`
- 小程序完整来源：`474b0444736599c848a4cef9f47fd930884e401d`
- 开发者工具项目：`miniprogram`
- 运行包根目录：`dist/`
- 代码包大小：约 `643 KB`
- 调试系统：`iOS`
- 当前二维码生成时间：`2026-08-19 17:48 CST`
- 开发者工具显示失效时间：`2026-08-19 18:13 CST`

## 入口状态

本次通过当前 `miniprogram` 窗口重新打开“真机调试”，完成代码包编译并生成新的二维码。生成时：

- 模拟器仍显示匿名患者，未携带真实微信会话；
- 没有观察到手机连接、微信 `code`、平台 session 或业务请求；
- 没有取得患者、预约、报告、门诊费用、资料或其它 Provider 业务证据。

因此，本记录只能证明“当前候选存在有效扫码入口”。后续必须由用户扫描该二维码，并在同一个窗口关联设备、页面操作、HTTP `traceId/requestId` 与服务端低敏日志，才能开始真机业务验收。

## 安全与共存边界

- 该操作是微信开发者工具的真机调试代码包上传，不是小程序线上版本发布。
- 没有修改旧 Python 服务、线上新 API、MySQL、Redis、Provider 配置或业务数据。
- 没有触发预约写入、支付、医保、二维码业务或任何 HIS 回写。
- 用户已有的 `apps/miniprogram/project.config.json` 修改保持原样，未暂存、未提交。

## 扫码后的固定顺序

1. 先确认手机连接显示的是本次 `miniprogram` 候选，而不是旧的 `mp-weixin` 窗口；
2. 完成微信登录并确认 `/me` 会话恢复；
3. 刷新患者目录，再显式切换另一位可用就诊人；
4. 依次验证“我的挂号”“爽约记录”“门诊待缴/已缴”；
5. 每一步同时保存页面结果、HTTP 状态/追踪标识和当前 release 的低敏业务日志；
6. 任一出现患者串用、持久化错误、Provider 字段异常或成功事件缺失，立即停止该业务域验收。

完整停止条件见 [`miniprogram-real-device-acceptance-checklist-2026-08-19.md`](miniprogram-real-device-acceptance-checklist-2026-08-19.md)。
