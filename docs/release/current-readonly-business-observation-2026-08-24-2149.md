# 2026-08-24 21:19–21:49 线上只读业务窗口复核

## 结论

本次通过阿里云中转机和内网 inspection key 只读复核当前线上运行层。新 Bun API、旧 Python 服务和依赖状态均正常；窗口内出现了微信登录及患者目录同步/读取链，但没有预约历史、预约目录、门诊费用或普通资料业务事件。

这只能证明当前 release 收到了微信/患者请求，不能证明小程序正在运行本地未发布候选，也不能把预约、费用和资料页面标记为已验收。当前线上小程序仍是 `13f597ea9ee3f65b9be858117826d948339d904a`，本地候选仍是 `39b50d5c4287f54ecc24e8564e2dc811a55c1d1b`。

## 运行层

| 检查项 | 结果 |
| --- | --- |
| 服务端 release | `8eb51b5ffe85b0b8f8a032783f893117d3df549d` |
| 新 API | `active`，监听 `10.0.0.3:18081` |
| 旧 Python | 继续监听 `0.0.0.0:8001` |
| Worker | `inactive` |
| live | 200，`status=ok` |
| ready | 200，database/redis/schema 均为 `ok` |
| system ping | 200 |
| 旧服务/数据 | 未修改、未重启、未写入 MySQL/Redis |

## 低敏日志聚合

时间窗口为 `2026-08-24 21:19:36 +08:00` 至 `21:49:36 +08:00`，使用当前 release 的 `p0-log-aggregate` 只保留计数，不输出 trace、Authorization、患者标识、卡号、金额或 Provider 原文。

| 指标 | 结果 |
| --- | ---: |
| 输入日志行 | 48 |
| 解析记录 | 47 |
| 解析错误 | 0 |
| HTTP 2xx 完成 | 19 |
| HTTP 401 | 1 |
| HTTP 404 | 1 |
| 微信登录事件 | 2（requested + succeeded） |
| 患者目录读取事件 | 12（6 requested + 6 loaded） |
| 患者目录同步事件 | 3 次完整同步链 |
| trace 链 | 21，缺失关联 0 |
| Provider request id | 4 |

业务域结果：

| 业务域 | 请求/成功事件 | 当前结论 |
| --- | --- | --- |
| 预约历史 | 0 / 0 | 未产生请求，未验收 |
| 预约目录 | 0 / 0 | 未产生请求，未验收 |
| 门诊费用只读 | 0 / 0 | 未产生请求，未验收 |
| 普通资料读写 | 0 / 0 | 未产生请求，未验收 |

本窗口的 404 来自操作员第一次把公网版本前缀误用于内网健康路径的探针，不是业务页面请求；随后正确的内网 `/health/live`、`/health/ready` 和 `/api/v1/system/ping` 均为 200。该误探针不能被解释成预约或费用故障。

## 下一步

1. 在正确的 `apps/miniprogram/` 工程普通编译后，先记录控制台完整 `revision`，明确手机运行的是 `13f597e` 还是本地 `39b50d5c`。
2. 登录后显式进入就诊人选择页；若只有一位可用患者，记录为“无第二患者样例”，不能伪造切换成功。
3. 依次打开“我的挂号”在线/全部、爽约记录、门诊缴费待缴/已缴，收集页面状态、客户端 requestId 和服务端低敏事件。
4. 看到预约提交、取消、支付、医保授权、结算或 HIS 写回时立即停止；这些能力仍保持关闭。
