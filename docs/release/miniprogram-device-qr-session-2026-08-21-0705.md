# 小程序历史真机二维码会话（2026-08-21 07:05 CST，已过期）

> 本记录只证明当前候选已经在正确的 `miniprogram` 项目中重新编译并打开真机调试入口，不代表手机已经完成微信登录、患者同步或任何业务验收。
> 旧 Python 服务、线上新 API、线上数据库和 Redis 均未因本次本地操作被修改或重启。

## 当前候选

| 项目 | 结果 |
| --- | --- |
| 服务端配套 release | `5a31427` |
| 小程序运行包来源 | `93408462f3eeadffed172f1ea3b10c043d461b1b`（`9340846`） |
| 开发者工具项目 | `miniprogram` |
| 运行根目录 | `apps/miniprogram/dist/` |
| 普通编译 | 已通过；工具日志出现 `analyzing codes success`，14 个页面完成编译 |
| 页面入口 | 14 个 |
| 代码包大小 | 工具显示约 607 KB |
| 真机模式 | iOS + 局域网 |
| 二维码有效期 | 工具最后复核显示至 2026-08-21 07:23 CST |
| `dist/services/single-flight.js` | 存在 |
| `dist/services/single-flight.test.js` | 不存在，符合运行包边界 |
| `dist/` 中测试脚本 | `*.test.js` / `*.spec.js` 均为 0 |

## ENOENT 处理结论

`single-flight.test.js` 是测试源码对应的编译输入，不是微信运行时模块。构建使用 `tsconfig.build.json` 排除测试源码，
并在 staging 发布前再次扫描运行目录；不能通过手工复制测试文件消除错误。

本次提交格式整理后重新构建运行包，并在开发者工具中重新执行普通编译、关闭旧二维码面板后重新打开真机调试入口。
若手机仍显示旧的 `single-flight.test.js` ENOENT，必须停止该扫码会话并重新普通编译、重新打开二维码，不能复制测试文件到
`dist/`，也不能把模拟器页面当作真机验收。

## 当前状态

本记录中的二维码有效期为 07:23 CST，已经不能用于当前候选验收。重新构建并验证 `9340846` 运行包不会自动延长本记录二维码的有效期；必须在开发者工具中重新普通编译并重新打开“真机调试”生成新二维码。

## 下一步真机操作

请先生成新的二维码，再扫描当前面板，并依次记录微信登录、患者目录同步、切换就诊人、我的挂号、爽约记录和门诊缴费只读列表的页面、客户端 HTTP
及服务端低敏日志三层证据。任一页面出现患者归属不一致、旧患者残留、未配置依赖或未知状态时，停止该业务域并保留时间和 `requestId`。
