# 当前小程序真机二维码会话（2026-08-21 06:58 CST）

> 本记录只证明当前候选已经在正确的 `miniprogram` 项目中重新编译并打开真机调试入口，不代表手机已经完成微信登录、患者同步或任何业务验收。
> 旧 Python 服务、线上新 API、线上数据库和 Redis 均未因本次本地操作被修改或重启。

## 当前候选

| 项目 | 结果 |
| --- | --- |
| 服务端配套 release | `5a31427` |
| 小程序运行包来源 | `c08378bebed493b6f3094d30d8ad4e27031e7037`（`c08378b`） |
| 开发者工具项目 | `miniprogram` |
| 运行根目录 | `apps/miniprogram/dist/` |
| 普通编译 | 已通过；构建面板显示上传完成、初始化调试环境和页面/TS 准备流程 |
| 页面入口 | 14 个 |
| 代码包大小 | 工具显示约 607 KB |
| 真机模式 | iOS + 局域网 |
| 二维码有效期 | 工具显示至 2026-08-21 07:11 CST |
| `dist/services/single-flight.js` | 存在 |
| `dist/services/single-flight.test.js` | 不存在，符合运行包边界 |
| `dist/` 中测试脚本 | `*.test.js` / `*.spec.js` 均为 0 |

## ENOENT 处理结论

`single-flight.test.js` 是测试源码对应的编译输入，不是微信运行时模块。构建使用
`tsconfig.build.json` 排除测试源码，并在 staging 发布前再次扫描运行目录；因此不能通过手工复制测试文件来消除错误。

本次先关闭旧的真机面板，再在同一正确项目中重新打开当前候选的真机调试二维码，使开发者工具重新使用已经发布的
`dist/` 运行包。若手机仍显示旧的 `single-flight.test.js` ENOENT，必须停止该扫码会话并重新普通编译、重新打开二维码，不能复制测试文件到
`dist/`，也不能把模拟器页面当作真机验收。

## 下一步真机操作

请扫描当前面板中显示的二维码，并按下列顺序记录三层证据：

1. 微信登录成功，确认客户端 `/auth/wechat`、`/me` 和服务端 `auth.*` 日志处于同一请求链；
2. 首页患者目录同步、进入选择就诊人页并切换患者，确认页面、HTTP 参数和低敏日志中的内部患者上下文一致；
3. 依次打开“我的挂号”、爽约记录和门诊缴费的待缴/已缴只读列表，分别记录页面结果、客户端请求及服务端业务日志。

任一页面出现 `single-flight.test.js`、患者归属不一致、旧患者残留、未配置依赖或未知状态时，停止该业务域，保留错误时间和
`requestId`，不要把测试文件复制到 `dist/`，也不要把空列表当作业务成功。
