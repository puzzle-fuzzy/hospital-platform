# 当前公网只读运行层复核（2026-08-21 20:55 CST）

## 1. 范围与限制

本次只通过公网 HTTPS 对新 API 做匿名 GET 探针，没有携带微信会话、Bearer、患者标识或 Provider 参数；没有 SSH 写入、部署、重启、数据库/Redis 写入，也没有调用众阳或医保接口。旧 Python 服务没有修改。

本记录只证明公网代理到新 API 的运行层和未登录鉴权边界，不证明微信登录、患者同步、多患者切换、预约、门诊费用、报告、真机页面或任何支付/医保业务已经完成。

## 2. 探针结果

| 请求 | HTTP | 低敏结果 | 结论 |
| --- | ---: | --- | --- |
| `GET /api/v2/health/live` | 200 | `status=ok`、`service=hospital-api`、`version=0.1.0` | 进程存活 |
| `GET /api/v2/health/ready` | 200 | `status=ready`；`database=ok`、`redis=ok`、`schema=ok` | 当前公网 readiness 通过 |
| `GET /api/v2/system/ping` | 200 | `service=hospital-api`、`apiVersion=0.1.0` | 版本化系统探针通过 |
| `GET /api/v2/me/profile`（无会话） | 401 | `error.code=unauthorized` | 资料读取未绕过会话 |
| `GET /api/v2/patients`（无会话） | 401 | `error.code=unauthorized` | 患者目录未绕过会话 |

匿名鉴权响应的 request id 已由公网响应头返回，可用于服务端日志关联：

- `/me/profile`：`3e662d64-262e-4b99-8d30-e0fdf92aaaed`
- `/patients`：`788633a8-fed4-4b99-b344-6d8d0e59cdbe`

request id 只用于本次低敏链路关联，不代表对应请求产生了业务成功事件；本次请求没有业务会话，因此不应在日志中出现微信登录、患者同步或 Provider 成功事件。

## 3. 运行包来源边界

本地全量门禁在同一时间窗口后重新构建了小程序运行包，构建输出为 `revision=24d8409`、14 个页面脚本齐全；该运行包只是本地构建结果，未因本次公网探针自动上传或切换线上。

当前发布基线文档仍锁定服务端 `c8eef370` 与已登记的小程序候选 `f488c6f3`；本次注释安全修正不会把本地 `24d8409` 自动宣称为已验收候选。真机操作必须重新从明确登记的候选生成二维码，并同时采集页面、客户端 HTTP 和服务端低敏日志三层证据。

## 4. 下一步

1. 保持旧服务和支付/医保/HIS/患者绑定/二维码等高风险边界不变。
2. 使用当前登记候选重新建立微信真机连接，再按“登录与 `/me` → 患者目录/显式切换 → 我的挂号 → 门诊费用只读”的顺序采集三层证据。
3. 任一业务域出现患者归属、HTTP trace、页面结果或日志成功事件不一致时，停止该域，回到对应 contract 审计；不能用本次健康探针替代业务验收。
