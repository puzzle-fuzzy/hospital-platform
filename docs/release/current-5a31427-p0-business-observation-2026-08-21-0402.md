# `5a31427` P0 业务日志空窗口观察（2026-08-21 04:02 CST）

> 本文只记录线上新 API 的低敏 P0 日志聚合结果，不代表微信、患者、预约、门诊费用或普通资料业务失败。
> 观察窗口从 `2026-08-21 03:54:00 CST` 开始；窗口内没有新的业务请求，因此证据门禁缺少业务事件是预期结果。

## 1. 运行边界

| 项目 | 结果 |
| --- | --- |
| 当前 release | `5a314275e9bae43730eab5b32638a8baecda5869` |
| 新 API | `10.0.0.3:18081`，`active` |
| 旧 Python | `0.0.0.0:8001`，继续监听，未修改、未停止 |
| Worker | `inactive` |
| readiness | database/Redis/schema 均为 `ok` |
| 当前小程序候选来源 | `acf5a8596e70e1fb2b8d220a0b41eb69418ae086`（`acf5a85`） |
| 日志解析错误 | `0` |
| systemd warning | `0` |

## 2. P0 聚合结果

本次使用当前 release 内置的 `p0-log-aggregate.js` 和
`p0-business-evidence-audit.js`，只消费 journald 的安全事件计数与同 trace
关联摘要，不输出原始日志、token、患者标识、Provider 报文或金额。

| 业务域 | requested | success | 同 trace 请求/成功 | 同 trace HTTP 2xx | 结论 |
| --- | ---: | ---: | ---: | ---: | --- |
| 微信登录 | 0 | 0 | 0 | 0 | 尚未发生请求 |
| 患者目录读取 | 0 | 0 | 0 | 0 | 尚未发生请求 |
| 患者目录同步 | 0 | 0 | 0 | 0 | 尚未发生请求 |
| 预约科室目录 | 0 | 0 | 0 | 0 | 尚未发生请求 |
| 预约排班目录 | 0 | 0 | 0 | 0 | 尚未发生请求 |
| 预约历史 | 0 | 0 | 0 | 0 | 尚未发生请求 |
| 门诊费用只读 | 0 | 0 | 0 | 0 | 尚未发生请求 |
| 报告目录 | 0 | 0 | 0 | 0 | 尚未发生请求 |
| 普通资料读取 | 0 | 0 | 0 | 0 | 尚未发生请求 |
| 普通资料更新 | 0 | 0 | 0 | 0 | 尚未发生请求 |

P0 审计工具返回 `passed=false` 是正确行为：每个业务域都缺少
`requested`、`success`、同 trace 关联和同 trace HTTP 2xx。它不表示线上服务异常，
只表示当前窗口不能升级为业务验收证据。

## 3. 下一次真机取证顺序

必须在当前 `9340846` 小程序运行包重新普通编译并生成新二维码后，由同一微信会话依次执行：

1. 微信登录，并确认首页会话状态；
2. 进入“更换就诊人”，确认当前目录和显式患者选择；
3. 进入“我的挂号”和“爽约记录”；
4. 分别打开门诊费用“待缴费”和“已缴费”；
5. 记录页面结果、客户端 HTTP `traceId/requestId` 和服务端低敏事件。

只有页面、HTTP 和服务端日志属于同一会话、同一患者和同一时间窗口时，才允许把对应域标记为真实已验收。
支付、医保、预约写入、报告资源、患者新增绑定、病历和 HIS 回写继续关闭。
