# 当前小程序候选模拟器只读观察（2026-08-19）

> 本记录只证明开发者工具模拟器中的页面和只读请求链，不替代手机真机、服务端低敏日志、Provider 结果或多患者验收。

## 1. 候选来源

| 项目 | 值 |
| --- | --- |
| 服务端候选 | `b7c9451`，线上旧 Python 服务继续保持运行 |
| 小程序提交 | `d2086d8` |
| 运行包来源 | `d2086d819b3e393da2e8c5c39d7704012854214b` |
| 开发者工具项目 | 新 `miniprogram` 项目，资源树指向 `dist/` |
| 操作范围 | 只读页面和刷新；未执行保存、预约、支付、医保、绑定或其它写入 |

## 2. 已观察页面

1. 首页：患者卡片、二维码视觉入口、顶部服务入口、服务分类和固定底部导航正常显示。
2. 选择就诊人：独立页面可加载当前 owner-scoped 目录，关系枚举 `other` 在展示层显示为“其他”；点击“刷新就诊人”后仍能回到同一目录快照。
3. 我的：背景、家庭成员管理卡片、旧端三组菜单和底部导航正常显示。
4. 普通资料：只读读取成功，页面显示平台默认普通资料状态；没有点击保存，不产生 PUT。
5. 我的挂号：先显示加载态，随后提交当前就诊人上下文并显示预约记录空态；没有进入“全部挂号”写入或详情能力。

页面观察期间未将内部 `patientId`、Provider 患者号、身份证或完整卡号写入页面参数；患者卡片只显示服务端脱敏读模型。

本轮在当前 `d2086d8` 运行包上重新核对首页到独立就诊人选择页的只读导航，并点击“刷新就诊人”：页面路径收敛到
`pages/patient-select/patient-select`，目录仍显示当前就诊人、关系“其他”和首 5 位+末 4 位脱敏卡号，刷新后目录仍能稳定回填。
这条观察只证明当前候选的页面来源和只读刷新链，不扩展为第二位真实就诊人或真机结论。

## 2.1 当前窗口的连续业务页复核（2026-08-19，开发者工具显示 01:40–01:41 CST）

本轮继续从选择页返回首页，按真实页面入口依次进入 `pages/appointment-records/appointment-records` 和
`pages/outpatient-payment/outpatient-payment`：

- “我的挂号”展示已确认的当前就诊人、单院区“高平市人民医院”、在线/全部两个标签和合法空记录态；页面没有把空结果改写成错误，也没有开放详情、取消或预约写入。
- “门诊缴费”展示当前就诊人、待缴费/已缴费两个状态；两个状态均能收敛到各自的合法空态，页面同时保留支付、退费、医保授权和结算回写关闭提示，没有调起支付或产生写入。
- 本轮点击了“刷新就诊人”和“已缴费”标签，操作期间没有进入绑定、资料保存、预约下单、支付或医保流程。
- 当前模拟器会话只返回一位可用就诊人，因此本轮不能证明第二位患者的显式切换、切换后旧结果淘汰或失效恢复。

开发者工具调试面板仍显示 `0 errors` 和若干微信基础库提示；本轮未观察到指向新小程序业务源文件的调用栈，不能因此加入未经证实的兼容代码。

## 2.2 公网未登录边界复核（2026-08-19 01:45 CST）

从工作区对当前公网入口 `https://test-hp.meiyi.pro/api/v2` 发起只读 GET：

| 路由 | HTTP | 结果 |
| --- | ---: | --- |
| `/health/live` | 200 | `status=ok`，服务可达 |
| `/health/ready` | 200 | `database=ok`、`redis=ok`、`schema=ok` |
| `/system/ping` | 200 | `service=hospital-api`、`apiVersion=0.1.0` |
| `/me` | 401 | `error.code=unauthorized` |
| `/patients` | 401 | `error.code=unauthorized` |
| `/me/profile` | 401 | `error.code=unauthorized` |
| `/appointments/records?page=1&pageSize=10` | 401 | `error.code=unauthorized` |
| `/payments/outpatient/records?page=1&pageSize=10` | 401 | `error.code=unauthorized` |

本次没有携带 Bearer 会话，没有读取患者、资料、预约或费用正文，也没有触发 Provider、写入、支付或医保流程。
这条证据只确认当前公网运行层和未登录认证边界正常，不能代替微信真机、多患者切换、Provider 字段或业务写入验收。

## 2.3 本地完整门禁复核（2026-08-19，继续工作窗口）

针对本候选工作树执行 `pnpm check`，结果全部通过：架构边界 66 条、迁移台账、Provider 接收审计、183 份文档链接、发布基线、Biome 格式与 lint、工具测试 17 项、9 个 workspace 类型检查、9 个 workspace 测试和 9 个 workspace 构建。
原生小程序构建重新生成 14 个注册页面脚本，来源仍为 `d2086d8` / `d2086d819b3e393da2e8c5c39d7704012854214b`。
该门禁只证明当前代码、文档和构建产物一致，不扩大为真机、Provider、支付、医保或 HIS 业务完成；用户已有的
`apps/miniprogram/project.config.json` 未修改、未暂存、未提交。

## 3. 工具层异常边界

开发者工具 Console 出现 `clickCheckTask`、`undefined is not iterable` 和 `webviewScriptError`，调用栈落在本机
微信开发者工具的 `appservice`/基础库内部，没有指向 `apps/miniprogram/src` 的项目源码调用栈。页面仍可完成上述只读导航和
请求收敛，因此本记录不把它们改写成业务成功，也不加入未证实的兼容代码；必须在新项目手机真机上再次观察，区分工具版本问题和真实渲染层问题。

## 4. 未完成边界

- 本次没有第二位真实就诊人，不能证明多患者显式切换、失效恢复或跨患者异步隔离。
- 没有保存普通资料，因此没有证明真实 PUT、版本冲突或微信真机资料验收。
- 没有报告、二维码、病历、患者新增绑定、预约写入、支付、医保或 HIS Provider 材料；这些入口继续保持 fail-closed。
- 本记录没有服务端 P0 日志聚合结果，不能用模拟器画面单独宣称公网业务三层证据完成。
