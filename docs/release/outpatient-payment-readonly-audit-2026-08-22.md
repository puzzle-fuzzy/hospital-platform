> 当前服务端配套发布更新（2026-08-24 13:01 CST）：线上服务端 release 为 `28a5c0c131794ce9dcc5f94bd3809402188ac87a`；当前小程序运行包来源仍为 `13f597ea9ee3f65b9be858117826d948339d904a`（提交 `13f597e`）。本轮为服务端独立只读 adapter 发布，未重建小程序运行包；下方历史候选仅供追溯，本行优先。
> 历史配套小程序构建来源（2026-08-26）：`0be59f966de2c3a0861cb44e9a526a1ef557f6c7`，仅表示当时本地 live 候选，未证明微信线上版本或真机业务已验收；当前入口以当前项目基线为准。
> 当前线上服务端 release（2026-08-27）：`1107a78a47ac2fbe0557958251d66da9effc66de`，已完成候选 preflight、隔离 smoke、原子切换和公网 runtime smoke；该运行层证据不等价于真实 Provider 或支付业务成功。
> 当前服务端 release 为 `28a5c0c131794ce9dcc5f94bd3809402188ac87a`；当前小程序运行包来源为 `13f597ea9ee3f65b9be858117826d948339d904a`（提交 `13f597e`）。门诊费用仍只读，真实 Provider 非空和真机验收仍待。
> 当前小程序配套运行包来源（2026-08-27）：`6f47c6408fe5b62025bd74fa66893f306eb7b9aa`（`6f47c64`）；本文中更早候选和真机窗口仅作历史追溯，当前无真机/开发者工具会话。

# 门诊缴费只读当前候选审计（2026-08-22）

> 本文记录当前新项目的门诊费用查询边界。线上执行基线为服务端 `28a5c0c1` 与小程序来源 `13f597e` 的分层配套；
> 进行业务验证时必须绑定本 release 的日志和客户端来源；
> 文中更早提交只作历史追溯。它不是支付、医保结算或真实 Provider 非空业务验收报告。
> 本轮没有修改旧 Python 项目、旧端口 `8001`、旧数据库或旧 Redis，也没有打开微信支付、医保授权、6202/6301
> 结算、退款、通知或 HIS 写回。
> 本轮 `recordId` 作用域修正和稳定身份字段校验已随 `28a5c0c1` 独立发布到新 API；真实 Provider 非空和微信真机
> 门诊费用验收仍未完成，旧 Python 服务不受影响。

## 1. 当前候选与结论

- 小程序运行包来源：`13f597ea9ee3f65b9be858117826d948339d904a`（提交 `13f597e`）。
- 线上已验证服务端 release：`28a5c0c131794ce9dcc5f94bd3809402188ac87a`。
- 当前页面入口：`/pages/outpatient-payment/outpatient-payment`。
- 当前 API：`GET /api/v2/payments/outpatient/records?patientId=<platform-patient-id>&status=unpaid|paid`。

结论：门诊缴费已经具备可审计的只读查询链路，但不能标记为“门诊缴费业务全部完成”。查询结果只允许进入费用展示；支付、医保、退费和结算仍按停止条件关闭。

## 2. 当前已确认的业务不变量

### 2.1 患者和会话边界

- 小程序只提交平台内部 `patientId`，不提交众阳 `patId`、卡号、身份证号、订单号或金额。
- 服务端先按当前登录用户解析 owner-scoped 的 `his-patient` 映射，再把 Provider 患者引用交给 adapter。
- 患者切换、刷新、费用状态切换和页面卸载都会使旧请求或旧卡片失去回写资格。
- 患者上下文未确认、会话代际变化或 owner 映射失效时，页面清空上一轮费用，不能把旧金额留在新患者页面。

### 2.2 状态、时间和金额

- 公共状态只有 `unpaid` 和 `paid`；服务端分别固定查询 Provider `tradeStatus=1` 和 `tradeStatus=3`。
- Provider 缺失或错配状态、未知状态、重复费用引用、非法日期、窗口外账单和越过资源上限的结果整批失败，不过滤坏行伪装成功。
- 费用 `recordId` 的服务端哈希包含 Provider 患者引用作为不可见作用域，避免不同患者返回相同费用字段时产生相同公开引用；Provider 身份字段出现空白文本时按格式异常拒绝，不静默降级为缺失。
- 服务端以 `Asia/Shanghai` 计算最近 30 个自然日窗口，不能继承 systemd 进程时区。
- Provider 元金额通过十进制整数换算为分，小程序按安全整数拆分展示，不使用浮点 `toFixed` 重新计算。
- 公共响应只暴露展示字段：记录引用、状态、科室、医生、账单时间和金额；Provider 交易号、医保金额、患者原始字段不进入客户端。

### 2.3 列表和事件

- API 返回本次已校验的完整只读快照，小程序首批只渲染 10 条；“加载更多”只展开本地快照，不冒充 Provider 分页。
- 费用卡片使用当前渲染批次的 `viewKey` 回查，不能直接相信 WXML 事件携带的状态或索引。
- 页面保留旧端的“就诊人/当前院区”两行选择区域；就诊人进入独立选择页，当前院区是已核对的单院区展示配置。
- 旧端的选择框、底部“去支付”栏和外部“药去哪取”入口没有恢复：这些入口会携带订单、医保或外部咨询语义，当前没有冻结的新 contract。

## 3. 与旧端的差异处理

旧端 `hospital-app/src/pagesB/health/outpatient_pay.vue` 将 Provider 原始 `patId`、`outTradeOrderId`、医保字段和支付参数直接组织在页面，并同时承担支付调起、医保分流、外部问诊入口和费用查询。该结构不能原样迁移，原因是：

1. 页面提交 Provider 患者号和订单号会绕过新服务的 owner-scoped 归属边界；
2. `waitPayAmount || amount` 和浮点 `toFixed` 不能作为新支付金额事实；
3. 微信支付参数、医保授权和 6202/6301 结算属于不同状态机，不能由费用列表点击直接推导；
4. 外部电子问诊入口的域名、登录态和业务归属尚未在新项目 contract 中确认。

因此当前采用“结构保留、能力收窄”：保留患者/院区选择、待缴/已缴标签、费用卡片和原有橙色提示区域的视觉层级；费用卡片点击只提示迁移状态，不发起支付或写回。

## 4. 日志和错误边界

- 服务端使用现有日志包记录 `outpatient.payment.records.requested`、`loaded`、`failed` 事件，并保留平台 `traceId` 与经过校验的 Provider request ID。
- 日志不记录 Provider 原始响应、患者原始身份字段、订单号、金额明细或错误正文；失败原因使用固定低敏枚举。
- Provider 失败、依赖未配置、患者映射缺失和会话失效在页面侧使用不同的公共文案，不把内部错误原文展示给患者。
- “服务暂未配置”不能被降级为空列表；空列表只表示一次成功且完整的查询没有记录。

## 5. 本地验证证据

| 范围 | 结果 |
| --- | --- |
| API 门诊费用 service | `15 pass / 0 fail / 55 expect()` |
| 众阳门诊费用 adapter | `20 pass / 0 fail / 46 expect()` |
| 门诊费用领域校验 | `3 pass / 0 fail / 5 expect()` |
| 小程序全量验收与运行包门禁 | `221 pass / 0 fail / 1640 expect()` |
| 文档链接审计 | `531` 个 Markdown 文档，无断链 |

## 6. 当前线上发布与验收状态

- 线上 `6db3217b` 已完成生产运行层验收；本地 `13f597ea` 的八个生产 bundle 已完成本地/远端 SHA-256 对照，真实生产 env 的 MySQL、Redis、schema preflight 通过。
- `13f597ea` 已在 `127.0.0.1:18082` 完成隔离 smoke，但尚未原子切换 `current`；切换前不能把候选代码证据写成线上业务证据。
- 旧 Python `8001` 的 Gunicorn PID 集合保持不变，Worker 仍为 `inactive`；没有执行 migration、支付、医保、HIS 写回或 Provider 门诊费用请求。
- 线上当前窗口仍缺门诊费用 requested/success 的真机三层业务证据。因此当前状态是“只读代码已实现，线上业务证据待补”，不是“门诊缴费已完成”。详见 [`6db3217b-production-acceptance-2026-08-24.md`](6db3217b-production-acceptance-2026-08-24.md)。

## 7. 当前 `dist/` 运行包门禁

当前 `dist/` 运行包仍没有 `*.test.js` 或 `*.spec.js`；真机调试必须关闭旧调试会话、清理文件/编译缓存后重新编译当前 `dist/`，不能使用旧二维码或旧增量缓存。

## 8. 后续停止条件

门诊费用只读链路完成真实 Provider/真机验收后，才进入下一阶段。支付和医保仍必须最后单独设计并验收：

1. 冻结费用详情、订单报价和订单幂等 contract；
2. 冻结微信 JSAPI 参数来源、商户签名、支付通知和订单状态机；
3. 冻结医保授权、6202 结算与 6301 查询/对账的状态和金额边界；
4. 证明支付通知、退款和 HIS 写回在重试、超时和重复消息下幂等；
5. 通过真实 Provider 非空样例、线上低敏日志关联、真机取消/成功/重试和旧服务共存验收。

在这些证据到齐前，不通过兼容字段、模拟支付、空列表降级或直接复用旧端支付请求来宣称迁移完成。
> 当前发布基线更新（2026-08-24 19:54 CST）：线上服务端 release 已切换为 `8eb51b5ffe85b0b8f8a032783f893117d3df549d`；小程序运行包来源仍为 `13f597ea9ee3f65b9be858117826d948339d904a`（提交 `13f597e`）。本轮只重启新 API，旧 Python `8001` 未修改；普通资料 PUT、支付、医保和 Provider 真机证据仍待。
> 当前统一发布基线补充（2026-08-27 13:12 CST）：服务端 release 为 `1bc8b0a85f21cb58205a99ce4de0de6afe9bf240`；小程序本地 live 运行包来源为 `90d8910bdc54d48dde66c4ff03a7434c182ebd92`，共 40 个页面。本文更早版本仅作历史追溯，真机证据仍为 pending；旧 Python `8001` 未修改。
