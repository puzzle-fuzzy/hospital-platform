# `2385f90` 原生小程序本地候选构建记录（2026-08-24）

> 本文只记录重制项目的本地运行包，不代表已经上传微信开发者工具、部署服务端或完成真机验收。
> 本轮不修改旧 Python 服务、不重启旧服务、不调用真实 Provider，不触发支付、医保、预约写入或 HIS 回写。

## 来源与构建结果

| 项目 | 值 |
| --- | --- |
| 原生小程序提交 | `2385f90b7d187df77bf94c7c67571022fef56c62` |
| 构建入口 | `apps/miniprogram/` |
| 运行包目录 | `apps/miniprogram/dist/` |
| 页面数量 | 14 |
| 测试脚本数量 | 0 |
| 服务端配套 | 线上 `28a5c0c131794ce9dcc5f94bd3809402188ac87a`，本轮未修改 |
| 构建工具 | Bun `1.4.0-canary.1` + TypeScript + pnpm workspace |

本地 `typecheck`、`build` 和 `runtime:verify` 均通过。运行包中的 `build-info.json` 固定上述完整来源，
页面脚本全部存在，且没有把 `*.test.js` 或 `*.spec.js` 带入 `dist/`。

当前正式 `device-evidence-audit` 仍绑定线上小程序基线 `13f597e`，因此会主动拒绝本地未部署的
`2385f90` 证据文件；这是正确的发布门禁，不是本候选构建失败。候选发布后必须重新生成与新服务端/小程序来源
一致的正式证据清单，不能复用本地构建时间或旧二维码。

## 本候选包含的业务安全修正

1. “我的挂号”卡片按业务类型、服务端状态、科室、就诊日期/时段和操作区重新分层；不改变预约记录查询范围、状态映射或操作副作用边界。
2. 爽约页遇到明确的患者上下文失效时直接进入统一就诊人选择页；空态只代表当前已确认患者没有 `missed` 记录，不再把选择就诊人模块当作查询结果。
3. 预约、爽约、报告、门诊费用和患者选择页统一 loading/空数据/错误容器的高度与图片尺寸，避免状态切换造成页面跳动。
4. 相关业务边界、中文注释和静态回归门禁已同步更新；旧服务与并行众阳自动化不在本候选范围内。

## 下一步人工验收

如果要使用本候选进行真机验收，应先在微信开发者工具中确认项目路径为
`E:\__Super_Core__\hospital-platform\apps\miniprogram`，普通编译后重新生成二维码；不能继续使用旧的 `13f597e` 二维码。
验收顺序仍为：微信登录 → 患者目录同步 → 显式切换就诊人 → 我的挂号在线/全部与爽约 → 门诊缴费只读。

页面截图、客户端 requestId 和服务端 Pino 同链日志必须来自同一二维码会话；在这些证据产生前，所有业务域继续标记为 `pending`。
