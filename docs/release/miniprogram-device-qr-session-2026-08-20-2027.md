# 小程序真机二维码会话（2026-08-20 20:27）

## 会话固定来源

| 项目 | 值 |
| --- | --- |
| 小程序工程 | `E:/__Super_Core__/hospital-platform/apps/miniprogram` |
| 开发者工具运行根目录 | `apps/miniprogram/dist/` |
| 小程序来源提交 | `8f80b3e30385fe3655f871673d8616cd2d31faaa` |
| 小程序短提交 | `8f80b3e` |
| 服务端候选 | `0e360d3`，新服务 `10.0.0.3:18081`，公网 `/api/v2` |
| 真机系统 | iOS |
| 开发者工具模式 | 局域网模式 |
| 二维码生成时间 | 2026-08-20 20:27 CST |
| 二维码预计失效时间 | 2026-08-20 20:52 CST |

## 本次运行包确认

- 开发者工具构建日志显示已编译 14 个页面。
- `apps/miniprogram/dist/build-info.json` 的 `sourceRevision` 与上表完整提交一致。
- `dist/` 内不存在 `*.test.js` 或 `*.spec.js`；测试文件只存在于 TypeScript 源码和测试执行环境。
- 本二维码对应当前 `8f80b3e` 候选，不是此前已经失效的 `3a89312` 二维码。

## 扫码验收边界

本记录只证明当前候选已在微信开发者工具中重新编译并生成二维码，不证明真机业务已经成功。扫码后应按以下顺序记录证据：

1. 微信登录会话建立，并确认客户端显示低敏 `requestId`/`traceId`。
2. 患者目录刷新完成；患者同步请求必须遵循 `/me` owner 会话证明后再调用 `POST /patients/sync`。
3. 进入患者选择页，显式切换至少一个非当前患者，再验证首页快照和“我的挂号”使用的是选中患者。
4. 继续验收预约历史、爽约记录、门诊费用只读链路。

若二维码过期、开发者工具仍报告 `dist/services/*.test.js` 不存在或出现旧页面，先关闭本次真机调试窗口，再重新编译 `apps/miniprogram/dist/`，不要将测试文件复制进运行包。

## 旧服务边界

本次只操作新项目本地小程序构建和开发者工具二维码；旧 Python 服务 `8001` 未修改、未重启，线上旧服务继续保持原状。
