# `968af78` 重启后运行层与业务日志复核（2026-08-19 15:50 CST）

本记录保存重启后的 SSH 只读复核结果。它只证明当前发布指针、双服务共存、公网健康探针和基础依赖状态，没有把运行层健康误判成微信、Provider 或小程序业务成功。

## 1. 只读核对结果

| 检查项 | 结果 |
| --- | --- |
| 服务器 | `192.168.112.172`；仅执行 SSH 只读命令 |
| 复核时间 | `2026-08-19 15:50:19 CST` |
| 当前新服务 release | `/home/ps/code/hospital-platform/releases/968af78` |
| `hospital-platform-api-v2.service` | `active`；`MainPID=703078`；`ExecMainStatus=0` |
| 新 API 监听 | `10.0.0.3:18081` |
| 旧 Python API 监听 | `0.0.0.0:8001` |
| 公网 live | `https://test-hp.meiyi.pro/api/v2/health/live` 返回 `200`，状态 `ok` |
| 公网 ready | 返回 `200`；`database=ok`、`redis=ok`、`schema=ok` |
| 最近 20 分钟业务事件 | 未观察到新的登录、患者、预约、门诊费用或报告事件 |

## 2. 安全边界

- 本次没有修改旧 Python 项目、旧服务进程、Nginx、数据库、Redis、环境文件或 systemd 配置。
- 本次没有执行 migration、写入业务数据、调用 Provider 业务接口或使用真实微信会话。
- 新旧端口同时监听，且公网健康探针通过，只能证明运行层和流量隔离仍然正常。
- 当前服务端仍为 `968af78`；小程序候选 `48ba22f` 仍是本地构建来源，尚未上传或替换线上小程序包。
- 没有业务事件不代表业务失败，只表示本次观察窗口没有可供验收的真实业务链。

## 3. 下一步

使用当前小程序候选按以下顺序取证，并为每个业务保存页面、HTTP 和低敏服务端日志三层证据：

1. 微信登录、`/me`、患者目录读取和完整同步；
2. 显式更换就诊人，确认仍使用服务端 opaque `patientId`，不把目录 `thirdPatientId` 或 HIS `patId` 交给小程序；
3. “我的挂号”在线记录和爽约派生记录的只读查询；
4. 门诊费用待缴/已缴只读查询；
5. 只有以上链路在当前 release 和当前小程序包稳定通过后，才评估报告只读域。

预约写入、锁号、取消、微信支付、医保授权、退款、二维码和 HIS 回写继续保持关闭。
