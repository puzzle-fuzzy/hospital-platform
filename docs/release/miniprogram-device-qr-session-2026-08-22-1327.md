# 当前候选真机调试二维码会话（2026-08-22 13:27 CST）

> 本记录只证明开发者工具为当前候选生成了新的真机调试入口，不包含二维码内容，也不代表手机已经扫码或微信业务已经验收。

## 二维码生成现场

| 项目 | 结果 |
| --- | --- |
| 开发者工具项目 | `E:\__Super_Core__\hospital-platform\apps\miniprogram` |
| 项目窗口 | `miniprogram - 微信开发者工具 Stable v2.01.2510290` |
| 运行根目录 | `dist/` |
| 小程序来源 | `41c708e1adf864ef6fef1f788e97aa8fb4371227` |
| 配套服务端 release | `9f479c9a` |
| 代码包大小 | 约 624 KB |
| 目标系统 | iOS，局域网模式 |
| 二维码有效期 | `2026-08-22 13:54 CST` |
| 编译错误 | 0 |

本次只在新 `miniprogram` 项目中重新打开“真机调试”并生成二维码；旧 `mp-weixin` 项目未操作。二维码没有保存、复制、上传或写入仓库，避免短期调试凭证形成长期泄露面。

## 真机交接顺序

扫码后只按以下顺序验收：

1. 微信登录和会话建立；
2. 患者目录刷新；
3. 显式选择另一位可用就诊人（若账号确实存在第二位）；
4. 预约历史、爽约、门诊费用只读页面；
5. 报告目录仅在 Provider 配置和同链日志可确认时继续。

每个业务动作必须同时取得手机页面结果、小程序 `/api/v2/` 请求的 `requestId/traceId` 和服务端低敏日志；不能用模拟器、二维码生成或健康探针替代真机业务证据。预约写入、患者绑定、支付、医保、退款和 HIS 回写继续关闭。

本次没有修改旧 Python 项目、旧服务、数据库、Redis、Provider 或业务数据。
