# 小程序真机二维码会话记录（2026-08-22 18:04 CST）
> 当前服务端发布基线（2026-08-22 18:55 CST）：`0e2a366efcca8da25d7edd4a286781f2d3dfdbec`；本二维码对应的小程序来源为 `a64fe023bc34fe6e44f93846c39e202fe02d64a5`。本记录仍无手机业务证据。

> 本记录只证明当前候选已在正确的微信开发者工具项目中生成新的真机调试入口，
> 不代表手机已经扫码，也不代表微信登录、患者、预约或费用业务已经验收。

## 当前候选与工具现场

| 项目 | 结果 |
| --- | --- |
| 微信开发者工具项目 | `E:\__Super_Core__\hospital-platform\apps\miniprogram` |
| 开发者工具窗口 | `miniprogram - 微信开发者工具 Stable v2.01.2510290` |
| 小程序运行根目录 | `dist/` |
| 小程序运行包来源 | `a64fe023bc34fe6e44f93846c39e202fe02d64a5`（提交 `a64fe023`） |
| 服务端配套 release | `84370077024762d92050cf077c27f3c60302e8f8`（提交 `84370077`） |
| 页面入口 | 14 个 |
| 代码包大小 | 约 `625 KB` |
| 真机平台 | iOS |
| 网络模式 | 局域网模式 |
| 二维码有效期 | 至 `2026-08-22 18:14 CST` |
| 手机扫码状态 | 本记录生成时尚未检测到手机连接，等待用户扫码 |

## 运行包边界

- `dist/services/single-flight.js` 存在。
- `dist/services/single-flight.test.js` 不存在，这是预期结果；测试脚本不能进入微信运行包。
- `dist/` 中 `*.test.js` 和 `*.spec.js` 数量为 0。
- 本轮只操作新 `miniprogram` 项目窗口；旧 `mp-weixin` 项目窗口未操作，旧 Python 服务、数据库和 Redis 未触碰。

## 扫码后的取证顺序

1. 记录手机页面是否完成微信会话建立，并保留当前候选来源与扫码时间。
2. 依次验证患者目录刷新、显式切换另一位可用就诊人、我的挂号/爽约和门诊费用只读页面。
3. 对每次页面动作记录客户端脱敏路径、HTTP 状态和 `requestId`，不得记录 token、AppSecret、完整身份证或完整卡号。
4. 在同一时间窗口读取服务端 Pino 日志，确认 HTTP `requestId`、业务事件和 Provider requestId 能够配对。
5. 任一层出现患者归属、状态、金额、日期或关联号不一致时，立即停止当前业务域；不进入支付、医保或 HIS 写回。

二维码生成、模拟器显示、单个 HTTP `200` 或服务端健康检查都不能替代上述三层证据。
