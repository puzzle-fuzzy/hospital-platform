# 原生小程序真机入口状态复核（2026-08-19 16:51 CST）

> 本记录只确认当前候选是否形成可扫码、可连接的真机入口，不把二维码存在、模拟器页面或已结束的调试窗口写成微信登录或业务验收成功。

## 1. 固定候选

| 项目 | 值 |
| --- | --- |
| 服务端 | `398be8e`，公网入口 `https://test-hp.meiyi.pro/api/v2` |
| 小程序提交 | `48ba22f` |
| 小程序完整来源 | `48ba22f16cb0f1d1098895772e660a3ed96761bb` |
| 构建结果 | 14 个页面，`runtime:verify` 通过，代码包约 643 KB |
| 开发者工具项目 | 新 `miniprogram` 窗口，资源根为 `dist/` |

## 2. 当前观察

2026-08-19 16:49 CST 左右，新 `miniprogram` 窗口重新生成了 iOS 真机调试二维码，开发者工具标记有效至 17:14 CST。
当前窗口没有手机连接；已存在的“真机调试”子窗口显示“连接方式：未连接”“服务状态：已结束”“流量：0”，因此不属于当前有效设备会话。

本次没有获得微信 session、页面截图、HTTP `traceId/requestId` 或服务端 `auth.wechat.*`、`patient.directory.*` 业务链，也没有点击患者、预约、报告或门诊费用页面。

## 3. 边界与下一步

- 不把当前二维码或模拟器首页当作微信登录成功。
- 不复用已结束窗口、旧 `mp-weixin` 窗口或历史 token。
- 用户扫描当前新 `miniprogram` 窗口二维码并看到设备连接后，才开始按登录 → 患者同步/显式切换 → 我的挂号 → 门诊费用只读顺序取证。
- 旧 Python、线上新 API、数据库和 Redis 本次均未修改。
