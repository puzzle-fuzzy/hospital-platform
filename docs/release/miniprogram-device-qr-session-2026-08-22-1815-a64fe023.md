> 历史现场记录（2026-08-22）：本记录对应小程序运行包来源 `a64fe023bc34fe6e44f93846c39e202fe02d64a5`（提交 `a64fe023`），不代表当前候选；当前候选为服务端 `0e2a366e` 与小程序 `171a874`，真实真机证据仍待。

# 小程序真机二维码会话记录（2026-08-22 18:15 CST）
> 记录时服务端发布基线（2026-08-22 18:55 CST）：`0e2a366efcca8da25d7edd4a286781f2d3dfdbec`；本二维码对应的历史小程序来源为 `a64fe023bc34fe6e44f93846c39e202fe02d64a5`。本记录仍无手机业务证据。

> 本记录针对 `dist/services/single-flight.test.js` 的真机 `ENOENT` 现场，
> 只证明当前候选已重新编译并在正确的微信开发者工具项目中生成新的真机调试入口；
> 不代表手机已经扫码，也不代表微信登录、患者、预约或费用业务已经验收。

## 当前候选与工具现场

| 项目 | 结果 |
| --- | --- |
| 微信开发者工具项目 | `E:\\__Super_Core__\\hospital-platform\\apps\\miniprogram` |
| 开发者工具窗口 | `miniprogram - 微信开发者工具 Stable v2.01.2510290` |
| 小程序运行根目录 | `dist/` |
| 小程序运行包来源 | `a64fe023bc34fe6e44f93846c39e202fe02d64a5`（提交 `a64fe023`） |
| 服务端配套 release | `84370077024762d92050cf077c27f3c60302e8f8`（提交 `84370077`） |
| 页面入口 | 14 个 |
| 代码包大小 | 约 `625 KB` |
| 真机平台 | iOS |
| 网络模式 | 局域网模式 |
| 开发者工具普通编译 | 14 个页面 JSON、`analyzing codes success`，调试器 `Errors: 0`、`Warnings: 4` |
| 二维码有效期 | 至 `2026-08-22 18:39 CST` |
| 手机扫码状态 | 本记录生成时尚未检测到手机连接，等待用户扫码 |

## 运行包边界与 ENOENT 结论

- `dist/services/single-flight.js` 存在。
- `dist/services/single-flight.test.js` 不存在，这是预期结果；测试脚本不能进入微信运行包。
- `dist/` 中 `*.test.js` 和 `*.spec.js` 数量为 0。
- `runtime:verify` 已通过；它会阻止测试脚本、缺失相对模块和 workspace 裸模块进入运行包。
- 当前错误路径指向开发者工具旧增量模块索引或旧真机调试会话，不应通过复制测试脚本到 `dist/` 解决。
- 本轮只操作新 `miniprogram` 项目窗口；旧 `mp-weixin` 项目窗口未操作，旧 Python 服务、数据库和 Redis 未触碰。

## 扫码与继续验收

1. 只扫描本记录生成的二维码，不使用过期二维码或上一轮二维码。
2. 若扫码后仍请求 `services/single-flight.test.js`，结束真机调试，关闭并重新打开
   `apps/miniprogram/`，普通编译一次后再生成二维码；不要在 `dist/` 手工创建测试脚本。
3. 记录手机页面是否完成微信会话建立，再依次验证患者目录刷新、显式切换另一位可用就诊人、
   我的挂号/爽约和门诊费用只读页面。
4. 对每次页面动作记录客户端脱敏路径、HTTP 状态和 `requestId`，不得记录 token、AppSecret、
   完整身份证或完整卡号；随后在同一时间窗口配对服务端 Pino 日志。
5. 任一层出现患者归属、状态、金额、日期或关联号不一致时，立即停止当前业务域；不进入支付、医保或 HIS 写回。

二维码生成、模拟器显示、单个 HTTP `200` 或服务端健康检查都不能替代上述三层证据。
