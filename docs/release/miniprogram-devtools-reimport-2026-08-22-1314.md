# 小程序开发者工具重新导入与普通编译证据（2026-08-22 13:14 CST）

> 本记录只证明当前候选已经重新建立开发者工具运行索引并进入真机准入，不代表微信登录、患者切换、预约、报告或门诊费用业务已经取得真机三层证据。

## 现场范围

| 项目 | 结果 |
| --- | --- |
| 开发者工具项目 | `E:\__Super_Core__\hospital-platform\apps\miniprogram` |
| 开发者工具窗口 | `miniprogram - 微信开发者工具 Stable v2.01.2510290` |
| 资源树根目录 | `MINIPROGRAM` |
| 小程序运行根目录 | `dist/` |
| 小程序来源 | `41c708e1adf864ef6fef1f788e97aa8fb4371227` |
| 配套服务端 release | `9f479c9a` |
| 注册页面 | 14 个 |
| `services/single-flight.js` | 存在 |
| `services/single-flight.test.js` | 不存在 |
| `*.test.js` / `*.spec.js` | 0 个 |

## 已完成动作

1. 只关闭并重新打开新 `miniprogram` 项目，未操作旧 `mp-weixin` 项目。
2. 在新项目中执行一次“普通编译”。
3. 编译后开发者工具调试器显示 `Errors: 0`；剩余 warning 不属于运行包缺失错误。
4. 从新项目重新打开“真机调试”，生成新的局域网二维码；二维码显示有效期至 `2026-08-22 13:38 CST`。

## 结论与停止条件

`dist/services/single-flight.test.js` 不是当前运行包应该拥有的文件。再次出现该路径时，仍应按开发者工具旧增量索引或旧真机调试会话处理，不能手工复制测试脚本到 `dist/`。

当前二维码只能作为真机准入入口。必须由同一次扫码会话继续记录手机页面、客户端 `/api/v2/` requestId/traceId 和服务端低敏 Pino 事件；只有三层证据配对后，才能把微信登录、患者目录、显式切换或只读业务标记为当前候选已验收。

本次没有修改旧 Python 项目、旧服务、数据库、Redis 或 Provider，也没有写入业务数据。
