# 2026-08-24 21:20–21:36 线上只读运行与业务证据观察

## 结论

本次只读复核确认线上新旧服务仍然共存，且受控 SSH 通道已经恢复：

- 阿里云中转机 `root@8.130.127.184` 登录成功；
- 通过中转机登录内网 `ps@10.0.0.3` 成功；
- 当前 release 为 `8eb51b5ffe85b0b8f8a032783f893117d3df549d`；
- `hospital-platform-api-v2.service` 为 `active`，Worker 为 `inactive`；
- 新 API 监听 `10.0.0.3:18081`，旧 Python 继续监听 `0.0.0.0:8001`；
- 内网 live、ready 和 system ping 均返回成功，ready 的 database、Redis、schema 均为 `ok`；
- 没有重启服务、切换 release、修改配置、写入 MySQL/Redis 或读取患者/Provider 原始数据。

健康探针必须请求 `10.0.0.3:18081`。服务没有绑定 `127.0.0.1:18081`，因此在内网主机上直接请求
loopback 得到连接拒绝不代表新 API 故障；监听地址和探针地址必须按当前 systemd 配置记录。

## 低敏日志窗口

通过当前 release 的 `p0-log-aggregate` 对 `hospital-platform-api-v2.service` 的 journald 窗口做安全聚合，
只保留计数，不输出 trace、患者标识、Authorization、金额或 Provider 原文：

| 指标 | 结果 |
| --- | ---: |
| 输入日志行 | 34 |
| 解析记录 | 33 |
| 解析错误 | 0 |
| 忽略空行 | 1 |
| systemd 告警 | 0 |
| HTTP 2xx 完成 | 14 |
| HTTP 401 | 1 |
| 微信登录事件 | 2（requested + succeeded） |
| 患者目录事件 | 16 |
| trace 链数量 | 15 |
| Provider request id 数量 | 3 |

该窗口只出现微信登录和患者目录链，没有出现预约历史、预约目录、门诊费用或普通资料事件。对安全摘要执行业务域门禁的结果如下：

| 业务域 | requested | success | 同链 HTTP 2xx | 当前结论 |
| --- | ---: | ---: | ---: | --- |
| 预约历史 | 0 | 0 | 0 | 未产生请求，未验收 |
| 门诊费用只读 | 0 | 0 | 0 | 未产生请求，未验收 |
| 普通资料读取 | 0 | 0 | 0 | 未产生请求，未验收 |

“没有日志”在这里表示该时间窗口没有对应业务请求，不表示 Provider 故障，也不表示页面已经成功。
同样不能用 readiness、健康检查或微信登录成功推导预约、费用或资料业务完成。

## 当前版本边界

- 线上小程序仍为 `13f597ea9ee3f65b9be858117826d948339d904a`（`13f597e`）；
- 本地未发布候选为 `39b50d5c4287f54ecc24e8564e2dc811a55c1d1b`（`39b50d5c`）；
- 本地候选没有上传微信，也没有替换线上运行包；
- 新旧服务、数据库、Redis 和线上小程序本次均未修改。

因此，后续真机验收必须先明确手机实际运行的是线上 `13f597e` 还是本地 `39b50d5c`，再把页面操作、客户端
HTTP requestId 和服务端低敏 trace/Provider request id 放进同一个时间窗口。不能把两个小程序来源混在一条证据链里。

## 下一步和停止条件

1. 在正确的 `apps/miniprogram/` 工程普通编译当前候选；若验证线上版本，则使用已发布的 `13f597e`，不要用旧二维码。
2. 登录后主动进入选择就诊人页；如果测试账号只有一位可用患者，记录“无第二患者样例”，不能伪造切换成功。
3. 依次打开“我的挂号”的在线/全部、爽约记录和门诊缴费的待缴/已缴，只读观察页面状态、HTTP 请求和低敏服务端事件。
4. 看到支付、医保授权、结算、退费、预约提交或取消入口时立即停止，不触发任何资金或 HIS 写回动作。
5. 在没有页面和客户端 requestId 的情况下，继续保持“代码已实现、线上业务未验收”，不通过服务端空日志补全业务结论。

