# 原生小程序真机微信登录与患者同步验收记录（2026-08-20）

## 1. 验收边界

本记录只覆盖当前新服务与当前小程序候选的微信会话、当前用户读取、患者目录读取和患者同步入口。
它不覆盖患者新增/绑卡、患者多选切换、预约写入、报告 Provider、门诊缴费 Provider、支付、医保、退费或 HIS 写入。

| 项目 | 本次固定值 |
| --- | --- |
| 新服务 | `0e360d3` |
| 小程序候选 | `d772f09` |
| 运行包来源指纹 | `d772f0966bbb0e369c61646528e8205eaf73825f` |
| 公网入口 | `https://test-hp.meiyi.pro/api/v2` |
| 真机链路 | 微信开发者工具真机调试，iPhone 微信 WebView |

## 2. 低敏公网证据

通过阿里云 Nginx 访问日志按同一真机 User-Agent 和当前时间窗口复核，观测到：

| 时间（中国标准时间） | 请求 | 状态 |
| --- | --- | --- |
| 16:10:12 | `GET /api/v2/me` | `401`，随后触发重新建立会话 |
| 16:10:13 | `POST /api/v2/auth/wechat` | `200` |
| 16:10:13 | `GET /api/v2/me` | `200` |
| 16:10:13 | `GET /api/v2/patients` | `200` |
| 16:10:18 | `POST /api/v2/patients/sync` | `200` |
| 16:10:32–16:14:53 | `/me`、`/patients`、`patients/sync` 重复恢复/同步 | `200` |

### 2.1 真实瞬态故障与恢复

同一真机在 16:24:07 发起患者目录读取时，新 API 记录了 `patient.directory.read.requested`；
16:24:08 以 `503` 结束，安全诊断字段为 `PersistenceUnavailableError`、
`persistenceOperation=read`、`persistenceErrorCode=PROTOCOL_CONNECTION_LOST`。
这说明用户偶尔看到“数据服务暂时不可用，请稍后重试”确实有一次真实数据库连接层瞬断作为依据，
不是微信授权弹窗缺失，也不是把旧服务路由到了新服务。

随后 16:58:49 的存活探针、16:58:51 的 readiness 探针和系统 ping 均返回 `200`；readiness 明确显示
数据库、Redis、schema 均为 `ok`。新服务进程自 13:42 启动后持续运行，没有因本次故障重启；旧 Python
服务没有修改或重启。

当前行为保持 fail-closed：MySQL 仓储只对幂等读做固定的短退避恢复尝试，仍无法取得连接时返回 503；
小程序清理未经本轮确认的患者卡片并保留可重试入口，不能把这次故障降级成空目录，也不能无限自动重放。
因此，本次瞬态已定位但不等于数据库稳定性问题根治，后续仍需观察同类 `PROTOCOL_CONNECTION_LOST` 是否复发。

这些请求均通过公网域名进入新 API；日志中的 User-Agent 为微信 iPhone WebView。记录不保存 access token、
openid、临时 code、患者标识、身份证号、原始响应或请求体。

## 3. 当前结论

### 已通过：服务端真实链路的部分验收

- 真机调试连接已在开发者工具显示为绿色“正常”。
- 真实微信 `wx.login` code 兑换请求到达新服务并返回 `200`。
- 会话建立后 `/me`、`/patients` 和 `/patients/sync` 均返回 `200`。
- 旧 Python 服务未修改、未重启；本次请求走新服务 `/api/v2` 路由。

### 尚未通过：完整真机页面与业务验收

当前开发者工具的真机连接窗口后来显示为“未连接”，页面仍显示“匿名 / 就诊卡：----”，且本次人工点击前没有出现新的登录请求。因此，上述日志只能证明
服务端链路在该时间窗口可用，不能证明当前页面已经完成登录态渲染，也不能证明当前账号取得了可展示的患者目录。

下一步必须由真机手动点击首页“新增就诊人/更换就诊人”，取得同一时间窗口的：

1. 页面从首页进入患者选择页；
2. `/patients` 或 `/patients/sync` 的请求号、状态和页面结果；
3. 当前患者目录是否为空、单患者还是多患者；
4. 返回首页后卡片是否匹配显式选择；
5. 若存在第二位患者，切换前后预约/报告/费用查询是否使用新的 opaque `patientId`。

## 4. 关于“微信授权弹窗”

本项目使用 `wx.login()` 的静默登录方式，只兑换一次性 code，不读取头像、昵称或手机号。因此真机扫码或点击登录时没有“获取用户信息”
授权弹窗是预期行为，不是授权失败。若产品后续需要头像/昵称，必须另行设计用户主动触发的资料授权流程，不能混入医疗身份登录。

## 5. 停止条件

在取得页面、客户端 HTTP 和服务端日志三层同链证据前，不扩大预约、报告、门诊费用或任何支付/医保能力的开放范围；
遇到 `401`、`503`、`persistence-temporarily-unavailable`、患者目录为空但页面仍展示旧患者，立即停止该业务链路并保留日志请求号。
