# 当前 `c26e696` P0 业务日志只读观察

更新时间：2026-08-18 23:40 CST

## 1. 观察范围

本次只读取服务器 `hospital-platform-api-v2.service` 的 journald，窗口从 `2026-08-18 22:56:00 CST` 开始；当前实际 release 为 `c26e696`，API service 为 active，Worker 为 inactive。日志聚合使用未切换候选 `b7c9451` 的安全聚合器和同链审计器离线处理，**不代表 `b7c9451` 已经运行在线上**。

聚合器只输出事件计数、HTTP 状态计数、低敏错误类型和 SHA-256 关联指纹，不输出 openid、患者标识、token、金额或原始 request/trace 值。

## 2. P0 业务门禁结果

| 业务域 | 请求事件 | 成功事件 | 同一 trace/request 链 | 结论 |
| --- | ---: | ---: | ---: | --- |
| 微信登录 | 0 | 0 | 0 | 未形成证据 |
| 患者目录读取 | 12 | 12 | 12 | 服务端日志链通过 |
| 患者目录同步 | 6 | 6 | 6 | 服务端日志链通过 |
| 预约历史 | 0 | 0 | 0 | 未形成证据 |
| 门诊费用只读 | 0 | 0 | 0 | 未形成证据 |
| 报告目录 | 0 | 0 | 0 | 未形成证据 |
| 普通资料读取 | 0 | 0 | 0 | 未形成证据 |
| 普通资料更新 | 0 | 0 | 0 | 未形成证据 |

聚合摘要为：`parseErrors=0`、`systemdWarningCount=0`、`traceIdCount=41`、`providerRequestIdCount=6`、HTTP `200=38`、HTTP `401=3`、关联链未截断。由于其他业务域缺少请求/成功/同链证据，整体 P0 审计结果仍为 `passed=false`，不能把患者目录的成功链扩展解释为其他业务已完成。

## 3. 证据边界

患者读取和同步已经具备当前服务端的低敏日志同链证据，说明对应请求进入了当前 API，并形成了明确的读取/同步成功事件；这仍然不是完整真机验收，原因是本窗口没有配套保存：

- 微信开发者工具或真实手机的页面截图、页面状态和操作时间线；
- 同一操作的客户端 HTTP 响应语义与 `x-request-id` 对照；
- 多就诊人切换、失效会话恢复和患者归属的设备侧结果。

因此当前状态只能写成“患者目录读取/同步的服务端日志证据已恢复”，不能写成“微信登录、患者切换或小程序 P0 业务全部验收通过”。此外，当前线上仍是 `c26e696`，新同链门禁候选 `b7c9451` 尚未切换，后续若切换必须重新采集切换窗口的日志并保存候选来源。

## 4. 下一步

1. 在有效二维码和真实微信会话下重新记录登录事件、页面操作和 HTTP trace；
2. 先完成多就诊人选择/切换及失效恢复的三层证据，再核对预约历史和门诊费用只读链；
3. 报告目录继续保持关闭，预约写入、支付、医保、退款和 HIS 回写继续保持关闭；
4. 只有业务请求/成功在同一关联链、页面结果和 HTTP 语义同时对齐后，才更新对应迁移项。
