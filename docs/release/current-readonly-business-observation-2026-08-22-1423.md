# 当前只读业务窗口（2026-08-22 13:23–14:23 CST）

> 本记录只保存服务器端的低敏聚合结果，不包含患者姓名、证件号、完整卡号、订单号、原始 Provider 报文或 requestId 原文。
> 本次通过 SSH 只读执行，没有重启、发布、migration、数据库/Redis 写入或 Provider 手工调用，也没有修改旧 Python 服务。

## 1. 当前运行边界

| 项目 | 观察结果 |
| --- | --- |
| 服务端 release | `9f479c9a` |
| 小程序配套候选 | `41c708e1` / `41c708e1adf864ef6fef1f788e97aa8fb4371227` |
| 新 API | `hospital-platform-api-v2.service=active`，监听 `10.0.0.3:18081` |
| 旧 Python API | 继续监听 `0.0.0.0:8001` |
| 日志窗口 | `2026-08-22 13:23:00` 至 `14:23:07` CST |
| 读取方式 | 当前 release 自带 P0 聚合器和业务证据审计，服务器端处理后只输出计数 |

## 2. P0 聚合与业务门禁

聚合结果为 `inputLines=60`、`parsedRecords=59`、`parseErrors=0`、`systemdWarningCount=0`，没有发现日志解析错误或停机超时告警。
窗口内共有 1 条门诊费用只读业务链，业务证据审计结果如下：

| 业务域 | requested | 明确成功 | 同链 HTTP 2xx | failure | 结果 |
| --- | ---: | ---: | ---: | ---: | --- |
| 门诊费用只读 | 1 | 1 | 1 | 0 | 服务端门禁通过 |

该结果说明当前 release 的 `outpatient.payment.records.requested → outpatient.payment.records.loaded →
http.request.completed(2xx)` 在同一安全关联链上成立。它只证明服务端已经完成一次只读业务响应，不能推导页面显示、金额字段正确或支付/医保可用。

窗口还观察到患者目录读取/同步事件，但没有预约历史、预约科室/排班、报告或普通资料的完整业务门禁结果；这些域不能用总请求数或其它域的成功链代替。

## 3. 当前结论与下一步

当前可以把门诊费用推进到“服务端只读链已有当前窗口证据”，但仍不能标记为真实完成。完整验收必须在同一
`41c708e1` 小程序候选上同时取得：

1. 手机页面显示待缴/已缴结果或合法空态；
2. 客户端脱敏路径、HTTP 状态和 requestId/Provider requestId；
3. 服务端同链 Pino 事件及低敏聚合结果。

支付、医保授权/结算、退款、费用详情和 HIS 回写仍保持关闭。预约历史、爽约、报告和普通资料继续按各自独立的
三层证据清单验收，不因本窗口门诊费用成功而跨域放行。
