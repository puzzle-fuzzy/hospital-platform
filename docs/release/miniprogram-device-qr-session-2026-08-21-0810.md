# 当前小程序真机二维码会话（2026-08-21 08:10 CST）

> 本记录只证明当前候选已经在正确的 `miniprogram` 项目中重新编译并打开真机调试入口；二维码生成时尚未取得手机业务结果，不代表微信登录、患者同步或任何 P0 业务已经验收。
> 本地操作没有修改旧 Python 服务、线上新 API、MySQL 或 Redis。

## 扫码前固定事实

| 项目 | 结果 |
| --- | --- |
| 服务端配套 release | `5a31427` |
| 小程序候选 | `6677671` |
| 小程序运行包来源 | `667767123efdb5b3a0bedbe423ab1797f16b1247` |
| 开发者工具项目 | `miniprogram` |
| 运行根目录 | `apps/miniprogram/dist/` |
| `dist/build-info.json` | `pageCount=14`，构建时间 `2026-08-21T00:01:16.926Z` |
| 普通编译 | 通过；工具显示 14 个页面、约 608 KB |
| 真机模式 | iOS + 局域网 |
| 二维码生成时间 | `2026-08-21 08:10 CST` |
| 二维码有效期 | 工具显示至 `2026-08-21 08:34 CST` |
| `dist/services/single-flight.js` | 存在 |
| `dist/services/single-flight.test.js` | 不存在 |
| `dist/` 测试脚本 | `*.test.js` / `*.spec.js` 均为 0 |
| 手机是否扫码 | 待记录 |

## 当前状态

状态为 `not-run`。扫码后必须使用同一二维码会话记录：

1. 微信登录、`/me` owner 验证和患者目录读取/同步；
2. 患者显式切换（如果当前账号存在多位可用就诊人）；
3. “我的挂号”、爽约记录、门诊缴费待缴/已缴只读查询；
4. 每个业务域同时保存页面结果、客户端脱敏 HTTP 关联和服务端低敏事件链。

任一业务域出现患者归属不一致、旧患者残留、未配置依赖、未知状态或 HTTP/日志链不一致，立即停止该域，不用另一条重试链覆盖原证据。支付、医保、预约写入、退款、HIS 写回和二维码协议仍保持关闭。

