# 当前小程序候选构建记录（`d4d65b7`，2026-08-21）

本候选由提交 `d4d65b735da8630e9b6795d9e105192713297474` 生成，包含门诊费用页患者
会话代际竞态修正。服务端、旧 Python 服务、线上配置、MySQL 和 Redis 均未因本次本地
构建而修改或重启。

| 基线项目 | 值 |
| --- | --- |
| 服务端 release | `5a31427` |
| 小程序客户端 | `d4d65b7` |
| 小程序构建来源 | `d4d65b735da8630e9b6795d9e105192713297474` |
| 运行包目录 | `apps/miniprogram/dist/` |

## 本地验证

| 项目 | 结果 |
| --- | --- |
| 小程序全量测试 | 177 项通过，0 项失败，1403 个断言 |
| API 预约/门诊费用/报告定向测试 | 62 项通过，0 项失败，252 个断言 |
| `pnpm --filter @hospital/miniprogram typecheck` | 通过 |
| `pnpm --filter @hospital/miniprogram build` | 通过；14 个页面运行脚本已发布 |
| `pnpm --filter @hospital/miniprogram runtime:verify` | 通过 |
| `pnpm docs:audit` | 通过；374 个文档，无断链 |
| `dist/services/single-flight.js` | 存在 |
| `dist/services/single-flight.test.js` | 不存在 |
| `dist/` 中 `*.test.js` / `*.spec.js` | 0 个 |

## 本轮业务边界修正

门诊费用页现在把患者卡片绑定到当前会话代际。状态 tab 发现会话轮换或本地显式
患者选择已经与卡片不一致时，只保留用户最后点击的状态意图，重新执行 `/me` →
患者目录 → 当前状态费用查询；旧患者对象不会在请求前直接交给费用 API。费用查询
发出前和回写前均继续保留会话、页面请求令牌和当前患者三重门禁。

这项修正不开放支付、医保结算、退款、预约写入、患者绑定、HIS 回写或二维码协议。

## 真机验收边界

本候选只证明本地运行包、来源、类型、测试和文档门禁正确，尚未证明手机扫码后的
微信登录、患者同步、预约历史、爽约或门诊费用已经完成真机三层验收。继续验收时
必须关闭旧真机调试会话，普通编译当前 `dist/`，再生成与 `d4d65b7` 对应的新二维码，
同时记录页面结果、客户端 `requestId` 和服务端低敏日志。若再次出现
`dist/services/single-flight.test.js`，不要补测试文件，应按 ENOENT 恢复记录刷新
开发者工具模块索引。
