# `5a31427` 当前 P0 业务空窗口观察（2026-08-21 04:51 CST）

## 观测范围

本记录来自服务器 `192.168.112.172` 的只读 SSH 检查，服务器时间为
`2026-08-21T04:51:41+08:00`。本次只读取新 API 的 systemd 状态、监听端口、当前 release
和最近 30 分钟的低敏业务事件；没有修改配置、重启服务、调用 Provider，也没有写入 MySQL/Redis。
旧 Python 服务保持原状态。

| 项目 | 结果 |
| --- | --- |
| 新 API systemd | `active` |
| 当前 release | `5a31427` |
| 新 API 监听 | `10.0.0.3:18081` |
| 旧 Python 监听 | `0.0.0.0:8001` |
| 当前 release 目录 | `/home/ps/code/hospital-platform/releases/5a31427` |
| 最近 30 分钟业务事件筛选 | 无输出 |

## 结论

当前新旧服务共存和运行层正常，但本窗口没有新的 `auth.*`、`patient.*`、`appointment.*`、
`outpatient.payment.*` 或 `user.profile.*` 业务请求。因此不能把“服务 active”、端口监听或健康检查
解释成微信登录、患者同步、显式切换、预约历史、爽约、门诊费用或普通资料验收成功，也不能据此判断
Provider 失败。

下一步仍必须使用当前小程序候选 `1d161b7`（完整来源
`1d161b701384ad8073ed907fe14f611ab3166fcd`）重新生成二维码并完成真实设备操作，同时保留：

1. 真机页面结果；
2. 客户端 HTTP 的脱敏 `traceId/requestId`；
3. 服务端同一时间窗口的低敏 `requested`、明确成功事件和 HTTP `2xx` 关联链。

在三层证据形成前，不开放预约写入、全部挂号、报告 Provider、患者绑定、支付、医保或 HIS 回写；
旧 Python `8001` 不属于本次改动范围。
