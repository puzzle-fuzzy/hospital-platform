# 当前 `b7c9451` 运行层与 P0 业务观察（2026-08-19 10:36 CST）

## 1. 结论

本次通过 SSH 对线上中转服务器做只读核对，没有重启、切换、迁移、Redis 清理、环境修改或业务写入。
当前新服务和旧 Python 服务仍然共存；新服务的生产日志出现一条“会话失效 → 微信登录 → 当前用户 → 患者读取 →
患者同步”的完整链，但开发者工具新 `miniprogram` 窗口仍未显示手机连接，因此这只能作为生产运行层与模拟器触发的
P0 观察，不能升级为真机验收。

## 2. 双服务共存与健康检查

| 项目 | 只读结果 |
| --- | --- |
| 当前新服务 release | `/home/ps/code/hospital-platform/releases/b7c9451` |
| 新服务 systemd | `hospital-platform-api-v2.service=active/running` |
| 新服务启动时间 | `2026-08-19 00:48:11 CST` |
| 新 API 监听 | `10.0.0.3:18081` |
| 旧 Python/Gunicorn 监听 | `0.0.0.0:8001`，仍有旧服务进程 |
| `/health/live` | `200`，`status=ok` |
| `/health/ready` | `200`，`database/redis/schema=ok` |

本次只读结果证明重启后旧端口没有被新服务替换，新服务也没有因为依赖探针成功而停止旧 Python；没有执行任何会改变
旧服务状态的命令。

### 2.1 重启后的再次只读复核（10:44 CST）

为排除本地开发者工具或会话重启造成的误判，`2026-08-19 10:44 CST` 再次通过 SSH 只读核对：

- `hospital-platform-api-v2.service` 仍为 `active`；
- 新 Bun API 仍由同一 release 目录启动并监听 `10.0.0.3:18081`；
- 旧 Gunicorn 仍监听 `0.0.0.0:8001`，旧 Python 进程仍在运行；
- `/health/live` 与 `/health/ready` 均成功，ready 继续报告 `database/redis/schema=ok`。

本次仍未执行重启、切换、配置写入、数据库写入或 Redis 清理，因此不能把这次核对解释为一次发布操作。

## 3. 当前日志链

在 `2026-08-19 10:35 CST` 左右，当前生产 journald 出现以下低敏业务顺序：

1. 旧会话访问 `/me` 返回预期的 `401/unauthorized`；
2. `/auth/wechat` 返回 `200`，并记录 `auth.wechat.login.succeeded`；
3. `/me` 返回 `200`；
4. `/patients` 返回 `200`，读取到 `1` 条患者目录记录；
5. `/patients/sync` 返回 `200`，快照提交并完成同步，`patientCount=1`、`activePatientCount=1`、
   `hisPatientReferenceCount=1`。

日志字段显示 `environment=production`，请求完成事件带有同值 `requestId/traceId` 关联链；原始用户 ID、患者号、临时 code
和 Provider 报文不写入本记录。

这条链可以证明当前生产 API 的微信会话恢复、患者目录读取和单患者临床映射同步仍能完成；它不能证明第二位患者、显式切换、
失效恢复、页面展示或真实手机渲染已经完成。

## 4. 开发者工具归属

随后只读核对标题为 `miniprogram` 的新开发者工具窗口：资源树指向 `dist/`，模拟器首页正常，二维码仍为当前 iOS 候选包；
窗口没有出现手机设备连接。窗口列表中的 `mp-weixin` 仍是旧项目，不能把旧项目设备或旧 Network 请求纳入新端证据。

因此本窗口的证据等级为：

| 证据层 | 状态 |
| --- | --- |
| 生产运行层 | 已确认，`b7c9451`、双服务监听、live/ready 和依赖状态正常 |
| 服务端 P0 日志链 | 已确认，微信登录、患者读取、同步各有成功 HTTP 事件 |
| 新原生小程序模拟器 | 已观察，能渲染首页并触发上述链 |
| 新原生小程序真机 | 未确认，没有新设备连接 |
| 多患者切换/预约历史/爽约/门诊费用/报告/普通资料 | 本窗口未取得对应业务事实 |

## 5. 后续动作

下一步仍按以下顺序执行：

1. 用户在新 `miniprogram` 窗口扫码，先确认设备连接归属；
2. 真机登录后只读确认患者目录和选择页；
3. 取得第二位患者或失效/恢复样例后，再验收显式切换；
4. 在同一设备、HTTP trace 和生产低敏日志时间窗口内依次验收预约历史、爽约和门诊费用；
5. 报告详情、病历、绑定、支付、医保、HIS 回写仍保持原有 fail-closed 边界。

在真实设备连接前，不把这次生产日志成功链当成真机业务完成，也不打开任何新的 Provider 或写入 gate。
