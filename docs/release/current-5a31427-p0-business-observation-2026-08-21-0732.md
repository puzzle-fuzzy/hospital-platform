# 当前 `5a31427` P0 业务观测（2026-08-21 07:32 CST）

> 本文只记录当前 release 的 SSH 只读运行状态、开发者工具候选和低敏业务事件窗口。
> “没有请求”不等于业务失败；健康检查也不等于微信、患者或 Provider 业务完成。
> 本次没有修改配置、重启服务、调用 Provider、写入 MySQL/Redis 或触碰旧 Python 服务。

## 1. 当前线上运行状态

| 项目 | 结果 |
| --- | --- |
| 观测时间 | `2026-08-21 07:32:10 CST` |
| 当前服务端 release | `5a31427`，`hospital-platform-api-v2.service=active` |
| 新 API 监听 | `10.0.0.3:18081`，Bun/Elysia |
| 旧 Python 监听 | `0.0.0.0:8001`，继续共存 |
| Worker | `inactive`，符合支付/医保/HIS 回写尚未开放的边界 |
| 内网 readiness | `database=ok`、`redis=ok`、`schema=ok` |

## 2. 当前小程序候选与二维码

微信开发者工具当前加载的是 `miniprogram` 项目，而不是另一个同名旧项目。当前候选来源为：

- 小程序源码提交：`9340846`；
- 运行包来源：`93408462f3eeadffed172f1ea3b10c043d461b1b`；
- 运行包：14 个页面，约 607 KB；
- `dist/` 测试脚本数量：0；
- 当前 iOS/局域网二维码在工具中显示有效至 `07:56`。

这只证明扫码前的运行包和工具状态。服务器在本观测窗口没有看到新的微信登录、患者、预约、门诊费用或普通资料请求，因此还没有形成当前候选的真机三层证据。

## 3. 低敏日志结果

`07:22–07:32 CST` 的新 API journald 没有以下业务事件：

- `auth.*`；
- `patient.*`；
- `appointment.*`；
- `outpatient.payment.*`；
- `user.profile.*`。

没有 requestId/traceId 就不能建立“真机页面 → HTTPS 请求 → 服务端日志”的同链关系。因此本窗口不推进微信登录、患者切换、预约历史或门诊费用的业务验收结论。

## 4. 下一步门禁

请使用开发者工具当前二维码扫码，并按以下顺序操作：

1. 微信登录完成后确认首页会话状态；
2. 进入就诊人选择页，确认目录和当前选择；
3. 进入“我的挂号”，先验收已确认的在线历史渠道；
4. 再进入门诊费用，只做已确认状态的只读查询。

每一步都必须同时保留真机页面结果、小程序请求状态和服务端低敏 requestId/traceId。出现会话失效、患者归属不一致、Provider 字段不完整或依赖暂不可用时，立即停止该业务域，不使用空列表或旧接口兼容来伪造成功。
