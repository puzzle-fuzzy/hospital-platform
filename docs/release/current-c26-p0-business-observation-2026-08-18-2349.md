# 当前 `c26e696` P0 业务日志观察（HTTP 完成门禁）

更新时间：2026-08-18 23:49 CST

## 1. 观察范围

本次只读取 `hospital-platform-api-v2.service` 的 journald，窗口从 `2026-08-18 22:56:00 CST` 开始；查询时服务器实际 `current` 为 `c26e696`，API service 为 active，监听 `10.0.0.3:18081`，旧 Python 继续监听 `0.0.0.0:8001`，Worker 未启动。

日志使用未切换候选 `387b4a3` 的新版安全聚合器和业务审计器离线处理；候选工具不代表已经在线上运行。本次输出只保留业务计数、同链结果和安全 HTTP 状态事实，不保存原始 trace、患者标识、token、金额或 Provider 报文。

## 2. 新门禁结果

| 业务域 | 请求 | 成功 | 同一关联链 | 同链 HTTP 2xx | 结论 |
| --- | ---: | ---: | ---: | ---: | --- |
| 微信登录 | 1 | 1 | 1 | 1 | 服务端日志链通过 |
| 患者目录读取 | 14 | 14 | 14 | 14 | 服务端日志链通过 |
| 患者目录同步 | 7 | 7 | 7 | 7 | 服务端日志链通过 |
| 预约历史 | 0 | 0 | 0 | 0 | 未形成证据 |
| 门诊费用只读 | 0 | 0 | 0 | 0 | 未形成证据 |
| 报告目录 | 0 | 0 | 0 | 0 | 未形成证据 |
| 普通资料读取 | 0 | 0 | 0 | 0 | 未形成证据 |
| 普通资料更新 | 0 | 0 | 0 | 0 | 未形成证据 |

全局 `parseErrors=0`、`systemdWarningCount=0`。整体 P0 审计仍为 `passed=false`，因为其余业务域缺少请求、成功和同链 HTTP `2xx`；不能由微信登录或患者同步的成功链推导预约、费用、报告或普通资料已完成。

## 3. 证据边界

本窗口已经出现有效的微信登录及患者目录读写服务端链，说明当前 API 收到了对应活动并完成了服务端业务结果；但现有日志没有配套的手机/开发者工具页面截图、用户操作时间线、页面展示字段核对和患者显式切换记录，因此仍只能标记为“服务端日志证据”，不能标记为“真机三层验收完成”。

下一次真实验收仍必须把同一操作的页面结果、HTTP 响应语义和服务端低敏链对齐；如果患者切换过程中发生 stale、失效恢复或映射缺失，必须单独记录并停止该业务域，不得用当前患者目录成功数覆盖。

## 4. 下一步

1. 在当前有效会话下完成一次明确的多就诊人刷新/切换，并保存页面、HTTP 和服务端链；
2. 依次触发“我的挂号”“爽约记录”“门诊待缴/已缴”，只读查询成功后再记录对应业务域；
3. 报告详情、预约写入、支付、医保、退款和 HIS 回写继续保持关闭，直到 contract、金额/状态机和真机证据分别通过。
