# 旧 Python 服务 6201 日志路由观察（2026-08-20）

## 结论

旧服务的 `/common/mbs-fsi/6201` 本次确实被调用过，但不能只查看
`/home/ps/code/Hospital-Backend/logs/all.log` 判断是否调用成功。该请求的原始记录实际落在
`logs/info_2026-08-19.log`，而不是当前 `all.log`；因此“`all.log` 没有 6201”不等于“接口没有被调用”。

本记录只保存低敏的日志路由结论，不复制医保请求体、响应体、令牌、患者信息或第三方原始报文。

## 只读证据

本次通过 SSH 对阿里云中转机及其内网旧服务进行只读检查，未修改旧项目、未重启旧服务、未主动发送
`6201` 请求。

| 检查项 | 结果 |
| --- | --- |
| 6201 原始日志 | `logs/info_2026-08-19.log` 中存在 1 条，时间为 `2026-08-20 12:32:48.812`，处理函数为 `service.py:forward_6201:674` |
| `logs/all.log` | 本次聚合窗口内未匹配到 6201 原始记录 |
| 超时日志 | `logs/error.log` 与 `logs/info_2026-08-19.log` 均出现过对应超时观察 |
| 旧服务进程 | Gunicorn 1 个 master、4 个 worker，旧 API 仍为 `8001` |
| 文件描述符 | 不同 worker 分别持有 `all.log`、`info_2026-08-19.log` 以及轮转后带 `(deleted)` 的文件 |

## 原因判断

旧项目的 `app/core/logger.py` 为 `all.log` 和 `error.log` 配置了按时间轮转的文件 handler，
同时旧服务使用多 worker Gunicorn。每个 worker 都会创建自己的 handler；多个进程对同一个日志文件
进行轮转时，文件名、打开的文件描述符和实际写入位置可能短暂或持续不一致。

所以旧服务当前存在以下可观察现象：

- 同一次请求可能出现在 `all.log`、日期轮转文件或某个已被轮转但仍被 worker 持有的 `(deleted)` 文件中；
- `all.log` 不是旧多 worker 服务的完整日志索引；
- `error.log` 也不能替代完整请求日志，只能作为错误级别的一部分观察来源；
- 日志文件中出现超时，只能证明某次外部请求超时，不能单独证明医保业务最终状态。

## 新服务的边界

新 Elysia 服务不依赖旧项目的 `all.log` 查业务链路。新服务使用 Pino 输出结构化 JSON 日志，
由 `requestId`、`traceId`、业务事件和低敏内部引用关联请求；生产环境通过 systemd/journald 统一收集，
再使用与 release 一致的聚合工具生成安全摘要。新增业务必须同时核对 HTTP 响应、页面结果、业务持久化状态
和同一时间窗口的结构化日志，不能用单个日志文件或单个 200 响应替代业务验收。

## 旧服务后续建议

旧服务日志治理应作为独立维护窗口处理，不与新业务迁移混做。推荐后续按以下顺序评估：

1. 让 Gunicorn worker 输出 JSON 或文本日志到 stdout/stderr，由 systemd/journald 负责收集和轮转；
2. 若必须保留文件日志，改为单一日志写入进程或 `QueueHandler`/`QueueListener`，禁止多个 worker 独立轮转同一文件；
3. 在变更前保留旧 `8001` 的健康检查、`6201/6202` 低敏计数和公网回归证据；
4. 变更后验证旧服务仍可用，再逐步迁移日志查询脚本，不把旧日志文件直接接入新服务的业务审计链。

在没有单独变更窗口和回滚证据前，不应为了让 `all.log` 看起来完整而重启或改动旧服务。
