# 2026-08-24 21:37–22:07 线上只读业务窗口复核

## 结论

本次通过阿里云中转机和内网 inspection key，只读复核当前线上新 API 的运行状态和低敏日志计数。新 Bun API、旧 Python 服务以及数据库、Redis、schema readiness 均正常；窗口内只有会话校验和患者目录同步/读取链，没有普通资料、预约历史、预约目录或门诊费用请求。

这只能证明当前 release 正常接收了患者上下文相关请求，不能证明本地未发布小程序候选已经在手机上运行，也不能把普通资料、预约或费用页面标记为已验收。支付、医保、患者新增/绑定和报告 Provider 自动化继续保持冻结。

## 运行层

| 检查项 | 结果 |
| --- | --- |
| 服务端 release | `8eb51b5ffe85b0b8f8a032783f893117d3df549d` |
| 新 API | `active`，监听 `10.0.0.3:18081` |
| 旧 Python | 继续监听 `0.0.0.0:8001` |
| Worker | `inactive`，不影响当前同步请求链 |
| live | 200，`status=ok` |
| ready | 200，database/redis/schema 均为 `ok` |
| system ping | 200 |
| 旧服务/数据 | 未修改、未重启、未写入 MySQL/Redis |

当前线上小程序运行包仍是 `13f597ea9ee3f65b9be858117826d948339d904a`；本地未发布候选仍是 `39b50d5c4287f54ecc24e8564e2dc811a55c1d1b`，两者不能混作同一条真机证据。

## 低敏日志聚合

窗口约为 `2026-08-24 21:37:44` 至 `22:07:44 +08:00`。服务器只输出事件名、HTTP 方法/路径、状态和计数，没有输出 Authorization、requestId、患者标识、卡号、金额、Provider 原文或资料正文。

| 指标 | 结果 |
| --- | ---: |
| HTTP 完成事件 | 51 |
| HTTP 失败事件 | 1 |
| `/api/v1/me` 成功 | 18 |
| `/api/v1/patients` 成功 | 9 |
| `/api/v1/patients/sync` 成功 | 9 |
| 患者目录读取链 | 18 次 requested + 18 次 loaded |
| 患者同步链 | 9 次完整 requested → started → snapshot → synced |
| 正常健康探针 | live 11 次、ready 2 次、system ping 2 次 |

窗口内唯一的 404 是操作员探针请求了错误的 `/api/v2/health/ready` 内网路径，不是小程序业务请求；正确的内网健康路径均返回 200。该探针不能解释为预约、资料或费用故障。

业务域计数如下：

| 业务域 | 请求/成功事件 | 当前结论 |
| --- | ---: | --- |
| 普通资料读写 | 0 / 0 | 未产生请求，未验收 |
| 预约历史 | 0 / 0 | 未产生请求，未验收 |
| 预约目录 | 0 / 0 | 未产生请求，未验收 |
| 门诊费用只读 | 0 / 0 | 未产生请求，未验收 |

## 下一步执行顺序

1. 使用正确的 `apps/miniprogram/` 工程普通编译并记录完整运行包 revision，确认手机不是沿用旧二维码或线上历史包。
2. 在同一真实会话下先进入普通资料页，完成一次页面读取；若要执行保存，必须使用用户明确提供且可恢复的测试值，保存后立即重新读取 canonical 资料，不修改患者档案字段。
3. 再依次打开“我的挂号”在线/全部、爽约记录和门诊缴费待缴/已缴，分别记录页面状态、客户端 requestId 和服务端低敏事件；没有三层配对证据时不修改日期窗口或状态映射。
4. 任何预约提交、取消、支付、医保授权、结算、退款或 HIS 写回入口一旦出现，立即停止该条验收；这些副作用仍未开放。
