# 当前 `5a31427` P0 业务观测（2026-08-21 05:54 CST）

> 本文只记录 SSH 只读运行状态和低敏业务事件窗口，不把“没有请求”解释成业务失败，也不把健康检查解释成业务完成。
> 本次未修改配置、未重启服务、未调用 Provider、未写入 MySQL/Redis，旧 Python 服务未触碰。

## 1. 观测范围

| 项目 | 结果 |
| --- | --- |
| 当前服务端 release | `5a31427`，`hospital-platform-api-v2.service=active` |
| 新 API 监听 | `10.0.0.3:18081` |
| 旧 Python 监听 | `0.0.0.0:8001`，继续共存 |
| Worker | `inactive`，符合当前不启用异步支付/医保回写的边界 |
| readiness | `database=ok`、`redis=ok`、`schema=ok` |
| 最近 30 分钟业务事件 | 没有 `auth.*`、`patient.*`、`appointment.*`、`outpatient.payment.*` 或 `user.profile.*` |

## 2. 业务结论

本窗口只证明当前服务端运行层健康，以及新旧服务没有因本次只读检查发生冲突。没有新的微信登录、患者同步、显式切换、预约历史、门诊费用或普通资料请求，因此当前候选仍没有新的真机三层业务证据。

下一次验收必须使用小程序候选 `6ce1272` 的新二维码，并在同一时间窗口同时保留：

1. 真机页面结果；
2. 小程序发出的 HTTPS 请求与响应状态；
3. 新 API journald 中按 `traceId` 关联的低敏业务事件。

只有三层证据同时对应同一候选、同一会话和同一患者，才能推进“微信登录 → 患者显式切换 → 预约历史 → 门诊费用只读”的下一道门禁。
