# 当前 c26e696 重启后运行与 P0 业务观察

> 观察时间：2026-08-18 23:52–23:54（Asia/Shanghai）
> 观察方式：SSH 只读核对、内网/公网健康探针、当前 journald 离线聚合
> 当前 release：`c26e696`
> 结论：运行层正常；微信登录和患者目录链有服务端证据；预约历史、门诊费用、报告、普通资料和真机三层验收仍未完成

## 1. 重启影响核对

本次核对没有发现操作系统重启：服务器 `who -b` 仍显示最近系统引导为
`2026-02-10 13:32`，系统 uptime 约 189 天。新 API 单元的当前进程仍为
`hospital-platform-api-v2.service=active/running`，`current` 仍指向
`/home/ps/code/hospital-platform/releases/c26e696`，没有发生 release 漂移。

运行边界保持不变：

- 新 Bun/Elysia API 监听 `10.0.0.3:18081`；
- 旧 Python/Gunicorn 继续监听 `0.0.0.0:8001`；
- `18082` 临时候选端口没有残留监听；
- `hospital-platform-worker-v2.service` 仍为 `inactive`，没有因本次核对被启动；
- 没有停止、重启、修改旧 Python 服务，也没有执行 migration、清空 Redis 或业务写入。

## 2. 运行层探针

内网 `10.0.0.3:18081/health/live` 和 `/health/ready` 均返回 HTTP 200；ready 依赖为
`database=ok`、`redis=ok`、`schema=ok`。公网
`https://test-hp.meiyi.pro/api/v2/health/live` 和 `/api/v2/health/ready` 也均返回
HTTP 200，响应包含 `Cache-Control: no-store`，公网 `/api/v2` 仍正确转发到新 API。

这只证明当前进程、依赖和代理路由可用，不证明任一患者端业务已经完成真实验收。

## 3. P0 日志门禁结果

使用未切换线上、仅用于验收工具的 `387b4a3` bundle，对当前 `c26e696` 自
`2026-08-18 22:56:00` 起的 journald 做离线聚合。聚合器只输出事件计数和关联摘要，
不输出患者号、Provider 患者号、token、订单号或原始报文。

| 业务域 | requested | success | 同链 HTTP 2xx | 结论 |
| --- | ---: | ---: | ---: | --- |
| 微信登录 | 1 | 1 | 1 | 通过服务端门禁 |
| 患者目录读取 | 14 | 14 | 14 | 通过服务端门禁 |
| 患者目录同步 | 7 | 7 | 7 | 通过服务端门禁 |
| 预约历史 | 0 | 0 | 0 | 缺少业务链 |
| 门诊费用只读 | 0 | 0 | 0 | 缺少业务链 |
| 报告目录 | 0 | 0 | 0 | 缺少业务链 |
| 普通资料读取/更新 | 0 | 0 | 0 | 缺少业务链 |

全局结果为 `parseErrors=0`、`systemdWarningCount=0`，但整体 P0 审计仍为失败，原因是
预约历史、门诊费用、报告和普通资料没有同一关联链内的业务请求、业务成功与 HTTP 2xx
完成事件。该结果不能用来宣称页面已打开、患者已切换或 Provider 已返回真实业务数据。

## 4. 当前未完成与下一步

下一次真实验收必须使用当前有效的小程序运行包和微信会话，按同一内部
`patientId` 保存页面截图/操作时间、HTTP trace 和服务端低敏日志：

1. 先确认患者选择页展示当前用户的全部有效就诊人，并显式切换到另一名就诊人；
2. 在切换后的页面打开“我的挂号”，分别观察全部/在线或旧端对应的状态筛选；
3. 打开门诊费用，分别触发待缴费和已缴费只读查询，核对最近 30 个中国标准时间日窗口、状态、金额和空态；
4. 继续验收报告目录、普通资料和页面回到首页后的患者上下文是否仍一致；
5. 任何一步出现 owner 映射、Provider 包络、日期窗口、状态或 HTTP 失败，立即保留失败证据并停止扩大范围。

预约写入、挂号取消、微信支付、医保授权/结算、退款和 HIS 回写仍然关闭。它们需要独立的
Provider/院方 contract、金额与状态机、幂等、查单/回调和失败补偿证据，不能由本次只读观察顺带开放。
