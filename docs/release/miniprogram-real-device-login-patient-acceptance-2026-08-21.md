# 小程序真机微信登录与患者同步验收（2026-08-21）

## 结论

> 版本边界：本文记录的是 `2026-08-21 03:22–03:23 CST`、服务端 release `6038560` 的历史真机窗口。
> 当前线上服务端已切换为 `5a31427`；虽然本轮主要变化是只读 Provider trace/日志保留，历史业务请求仍不能
> 自动升级为当前 release 的验收证据。当前 release 必须重新在匹配的 `9340846` 运行包上采集页面、HTTP 和服务端日志。

当时的小程序候选 `6e6604f` 使用新二维码完成了一次真实微信设备会话，服务端 `6038560`
确认完成微信登录、当前用户读取、患者目录读取和患者目录同步。此次证据只覆盖登录与患者同步，
不代表患者显式切换、我的挂号、爽约、门诊费用或普通资料已经在真机完成。

旧 Python `8001` 未修改、未重启；本次没有执行预约写入、支付、医保、报告详情、病历或 HIS 写入。

## API 前缀边界

日志中出现的 `/api/v1` 是新 Elysia 服务的内部路由，不是小程序绕过公网隔离访问旧服务：

```text
小程序/公网: https://test-hp.meiyi.pro/api/v2/*
        -> 阿里云 Nginx 精确转发
新 API 内部: http://10.0.0.3:18081/api/v1/*
```

因此 journald 中记录 `/api/v1/auth/wechat`、`/api/v1/me` 和 `/api/v1/patients` 属于预期现象。
小程序 `app.ts` 的生产配置仍为 `apiPrefix: "/api/v2"`，不能仅因内部日志路径而修改客户端前缀。

## 三层链路证据

以下时间为服务端日志 UTC 时间；页面只保留低敏结果，不记录 token、openid、手机号、姓名、卡号、
身份证号或服务商原始报文。

| 时间（UTC） | 业务事件 | HTTP 结果 | 低敏关联信息 |
| --- | --- | --- | --- |
| 19:22:56.451 | 首次 `GET /me` 无会话 | 401 | `errorCode=unauthorized`，属于自动登录前的预期探测 |
| 19:22:57.362–19:22:58.136 | 微信临时 code 兑换平台会话 | 成功 | `auth.wechat.login.requested` → `auth.wechat.login.succeeded` |
| 19:22:58.137 | `POST /auth/wechat` | 200 | `idempotencyKeyPresent=false` 的 HTTP 完成事件 |
| 19:22:58.396 | 当前用户读取 `GET /me` | 200 | 当前会话已被服务端接受 |
| 19:22:58.590–19:22:58.680 | 患者目录读取 `GET /patients` | 200 | `patient.directory.read.loaded`，`itemCount=1` |
| 19:22:58.891 | 同步前再次读取 `/me` | 200 | 患者同步前 owner 会话证明成功 |
| 19:22:59.111–19:23:01.803 | `POST /patients/sync` | 200 | 请求、操作开始、快照提交和同步成功事件均存在 |
| 19:23:01.804–19:23:01.848 | 同步后重新读取患者目录 | 200 | `itemCount=1`，作为页面最终读模型 |

患者同步成功事件包含 `patientCount=1`、`activePatientCount=1`、`deactivatedPatientCount=0` 和
`hisPatientReferenceCount=1`；这些是数量级和状态计数，不把患者内部 ID 写入本文。

## 当前已证明/未证明

### 已证明

- 当前二维码能够建立真实微信设备会话；
- 新 API 的微信身份配置、Redis 会话和 MySQL 持久化链路可用；
- 登录后 `/me` 能返回当前平台用户；
- 患者目录读取和同步在同一会话内成功；
- 同步前后都执行了 owner 会话及目录读取，未使用本地缓存冒充服务端事实；
- journald 具备请求、业务开始、业务成功和 HTTP 完成四层低敏日志。

### 尚未证明

- 用户在手机上看到的患者卡片与当前服务端读模型完全一致的截图证据；
- 点击“更换就诊人”后的显式选择、返回和旧页面清理；
- “我的挂号”预约历史及爽约筛选的当前 release Provider 非空样例；
- 门诊费用待缴/已缴的真实金额和页面展示；
- 普通资料读取、更新和双设备 `409`；
- 报告 Provider、支付、医保、退款、预约写入和 HIS 回写。

## 下一步操作

继续使用同一真机候选和有效会话，按以下顺序操作：

1. 在首页点击“更换就诊人”，确认进入选择页而不是直接默认当前用户；
2. 选择一个明确患者并返回首页，核对旧患者卡片不会残留；
3. 进入“我的挂号”，记录页面结果、客户端 requestId 和服务端同一时间窗口事件；
4. 再进入爽约记录和门诊费用只读页；
5. 任一域出现患者、状态、日期、金额或 trace 不一致，立即停止该域，不用兼容逻辑掩盖问题。

本记录的代码依据和前缀说明见 [`wechat-auth-login.md`](../wechat-auth-login.md)、
[`candidate-6e6604f-local-build-2026-08-21.md`](candidate-6e6604f-local-build-2026-08-21.md)
及 [`next-business-gates-2026-08-20.md`](next-business-gates-2026-08-20.md)。
