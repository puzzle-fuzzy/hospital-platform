# `5a31427` P0 线上共存与业务空窗口观察（2026-08-21 05:45 CST）

> 本文来自服务器 `192.168.112.172` 的只读 SSH 复核，只记录运行层和低敏业务事件结果。
> 本次没有部署、重启、修改配置、调用 Provider，也没有写入 MySQL/Redis 或旧 Python 服务。
> 运行层正常不等于微信、患者、预约、门诊费用或普通资料业务已经验收。

## 1. 运行边界

| 项目 | 结果 |
| --- | --- |
| 当前 release | `5a31427`，目录 `/home/ps/code/hospital-platform/releases/5a31427` |
| 新 API systemd | `active` |
| 新 API 监听 | `10.0.0.3:18081` |
| 旧 Python 监听 | `0.0.0.0:8001`，继续共存，未修改、未停止 |
| 正确 readiness 地址 | `http://10.0.0.3:18081/health/ready` |
| readiness | `database=ok`、`redis=ok`、`schema=ok` |
| 当前小程序候选来源 | `acf5a8596e70e1fb2b8d220a0b41eb69418ae086`（`acf5a85`） |

新 API 只绑定 WireGuard 内网地址 `10.0.0.3`，不绑定 `127.0.0.1`；对回环地址执行探针得到连接拒绝，
这是监听地址不匹配，不是服务停止。后续线上探针必须使用实际监听地址或经过确认的公网代理路径，不能把错误探针地址的失败写成业务故障。

## 2. 低敏业务事件

最近 30 分钟的 journald 业务事件筛选没有出现以下事件域：

- `auth.*`
- `patient.*`
- `appointment.*`
- `outpatient.payment.*`
- `user.profile.*`

最近 10 分钟可见的服务日志只有 readiness `HTTP 200`，对应日志时间为
`2026-08-20T21:44:54.563Z`（北京时间 `2026-08-21 05:44:54`）。因此本窗口只能证明服务和依赖可用，
不能证明真机扫码、患者同步、显式切换、预约历史、爽约、门诊费用或普通资料读写成功，也不能据此判断 Provider 失败。

## 3. 下一步

必须使用当前 `9340846` 运行包重新生成二维码，由同一微信会话取得页面、客户端 HTTP `traceId/requestId` 和服务端低敏事件三层证据，
顺序保持为：

1. 微信登录与 `/me` 会话恢复；
2. 患者目录同步和显式切换就诊人；
3. “我的挂号”和爽约记录；
4. 门诊费用待缴/已缴只读目录；
5. 普通资料读取、受控更新和双会话 `409`。

支付、医保、预约写入、报告详情/附件、患者新增绑定、门诊病历和 HIS 回写继续保持关闭。
