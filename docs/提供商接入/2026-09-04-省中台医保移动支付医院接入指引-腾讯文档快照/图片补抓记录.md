# 腾讯文档图片补抓记录

## 处理方法

腾讯文档的内嵌原图链接在浏览器资源打包时有部分跨域失败，但并不是图片不存在。对主文档资源清单中的 `docimg*.docs.qq.com` URL 使用原页面 `Referer` 和 Chrome `User-Agent` 重新请求后，原图可以正常返回 PNG。

这套方法已经批量执行，不需要逐张手动打开新窗口或使用 DevTools 的 Capture full size screenshot。原始链接、资源 ID、文件名和本地路径保存在 [assets/manifest.json](./assets/manifest.json)。

## 结果

- 原页面资源清单：66 项图片、2 项内嵌 SVG、4 项其他资源；
- 已落盘的主文档 PNG：25 项（资源清单中的 25 项 `docimg` 图片全部恢复）；
- 已落盘的其他资源：1 项 GIF、10 项 SVG（其中 2 项为内嵌 SVG，8 项为网络探测图）；
- 主文档图片中的流程图、参数表、界面截图均以原始 PNG 保存，未通过 OCR 或猜测改写内容；
- 关联文档 `DV3lxV3hSbXFudVBE` 的主体内容由腾讯文档画布绘制，页面资源清单未暴露可直接下载的正文图片，因此使用真实 Chrome 定位章节并保存关键章节的高分辨率画布截图，截图位于关联文档目录。

## 复核边界

浏览器打包阶段的 42 项失败记录仍保留在 [腾讯文档资源清单-70项.json](./腾讯文档资源清单-70项.json)，这是首次资源打包的原始诊断结果；其中主文档的 25 个 PNG 已通过原图补抓恢复，不应再视为当前无法读取的图片。

## 预览层补充图片（2026-09-04）

本轮复核发现，主文档第 23/61 页的《国家局医保移动支付医疗机构接入时序图（微信）》由腾讯文档画布的图片预览层动态加载，未出现在首次观察到的 70 项静态资源清单中。因此，不能仅凭静态资源清单判断画布中的所有图片都已导出。

- 操作方式：在 Chrome 中打开图片预览，将鼠标移动到预览底部，显示预览工具栏后点击下载按钮。
- CSS 选择器：`span.dui-preview-bar-button.dui-preview-download-button`。用户给出的选择器中两个 class 之间需要补充 `.`。
- 预览原图地址：`https://docimg8.docs.qq.com/image/AgAABdlcCDaNjaxUrB5IX7ZIL5EY-KS_.png?w=1280&h=1900.9418282548477`。
- 本地文件：[assets/AgAABdlcCDaNjaxUrB5IX7ZIL5EY-KS_.png](./assets/AgAABdlcCDaNjaxUrB5IX7ZIL5EY-KS_.png)，实际尺寸 `2888×4289`，已用 `file` 校验为 PNG。
- 原始 70 项资源清单保持不变；本文件和 `assets/manifest.json` 记录这张“预览层补充图片”的来源与落盘位置。

## 全量预览复核结果（61 个位置）

本次从预览位置 `61/61` 反向复核到 `1/61`，并把此前本地没有的预览图片全部下载。预览位置中存在重复图片；完整位置、图片 ID、预览尺寸和本地文件映射见 [预览层图片补抓清单-61项.json](./预览层图片补抓清单-61项.json)。

- 本轮新增唯一图片：28 项；上一轮已补的预览图片：1 项；预览层唯一图片合计：29 项。
- 与静态资源补抓的 25 项 PNG 合并后，本地 `assets/` 中共有 54 项 PNG 图片。
- 新增图片均通过“图片预览 → 鼠标移到预览底部 → 下载按钮”获取；没有用相似文档或猜测内容替代。
- `预览层图片补抓清单-61项.json` 使用腾讯文档预览层实际返回的图片路径和页面位置；原始静态资源清单仍以 [腾讯文档资源清单-70项.json](./腾讯文档资源清单-70项.json) 为准。
