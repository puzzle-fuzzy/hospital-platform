# Provider 文档接收记录：旧端链路流向图与生产拓扑

> 接收日期：2026-09-03（Asia/Shanghai）
> 当前状态：`normalized`（运行拓扑事实已登记，未用于开放任何 gate）
> 业务范围：测试环境全链路拓扑、三通道 HTTP 走向、forward 转发规则、医保支付五段流程、已知运行问题。

## 1. 接收原则

原始材料位于旧项目 `hospital-app/docs/链路流向图.md`，由旧项目维护者基于
`test-hp.meiyi.pro`（3090 后端）与 `gpsrmyy.meiyi.pro`（中转 nginx / Java 拦截器）
的真实日志核对绘制，自评可信度约 98%。2026-09-03 本仓库另做了只读服务器复核：
gpsrmyy→7112 拦截器、test-hp→10.0.0.3:18081、中转机 Redis 无监听、3090 本机
MySQL/Redis、旧后端 test-v3 分叉（4 个未提交文件）均与文档一致或比文档更严重。
本文只登记拓扑与链路事实；事实存在不授权任何支付、医保或写入能力开放。

## 2. 文件证据指纹

| documentId | 原始文件 | 大小 | 材料日期 | SHA-256 | 状态 |
| --- | --- | ---: | --- | --- | --- |
| `legacy-chain-topology-20260903` | `hospital-app/docs/链路流向图.md` | 15994 bytes | 2026-09-03 | `F9DB8AB3E1D10AEF509BFFA707AB758F5FD9128DECBAA3EE7AB39FCCC761F765` | `normalized` |

## 3. 脱敏后的拓扑事实（2026-09-03 服务器复核版）

### 3.1 服务与端口

| 位置 | 组件 | 事实 |
| --- | --- | --- |
| 阿里云中转 `meiyi.pro`（8.130.127.184） | nginx `gpsrmyy.meiyi.pro` | `location / → localhost:7112`，全部流量进 Java 拦截器 |
| 同上 | Java 拦截器（interceptor_core，`:7112`） | 透传/拦截转发；**内置定时任务“获取叫号信息并推送”每 7–20 秒轮询 HIS**；日志在 `/home/admin/inter/logs/interceptor.log` |
| 同上 | nginx `test-hp.meiyi.pro` | `/api/v2/*` → VPN `10.0.0.3:18081`（新 API）；旧 `/api/v1/*` → autossh 隧道 → 3090 `:8001` 旧 Python |
| 同上 | MySQL `:3306` | 旧系统数据库所在 |
| 同上 | Redis | **无监听（故障态）**，仅影响旧系统 |
| 3090（192.168.112.172 / VPN 10.0.0.3） | 新 Elysia API | `10.0.0.3:18081`，MySQL `:3306` 与 Redis `127.0.0.1:6379` 均在本机（与旧系统隔离） |
| 同上 | 旧 Python（test-v3 分支） | `0.0.0.0:8001`，4 个未提交改动文件（见 §3.4） |
| 医院内网 `124.164.248.83` | `:50080` 医保 FSI 网关、`:56380` 众阳 org API（HIS） | 仅可经中转机 forward 到达 |
| 腾讯医保测试中台 | `test-receiver.wecity.qq.com` | 医保授权 `mip-user-query` 的 forward 目标 |

### 3.2 forward 转发协议（旧后端 → 中转 :7112/forward）

请求体 `{method, base_url, path, headers, body}`，按 `base_url` 分流：
`http://124.164.248.83:50080/...`（FSI，通道层 envelope）、
`http://124.164.248.83:56380/org/...`（HIS，6201/6202 移动支付中心）、
`https://test-receiver.wecity.qq.com/...`（腾讯授权）。旧代码硬编码
`Authorization: Bearer main-token`；新仓库 `legacy-fsi-gateway` 已改为显式
`MBS_FORWARD_AUTHORIZATION_TOKEN` 配置。排查口诀：3090 日志看不到 ≠ 没发出，
须对照中转 `access.log` 与 `interceptor.log`。

### 3.3 医保支付五段链（A–E）的关键数据依赖

| 段 | 依赖事实 |
| --- | --- |
| A 发起结算 | 2.6.65.1 `apply-pay-settle` → businessId/businessCode；2.6.65.2 第一次 `pre-order`（payTypeId=2 医保）→ payingId/tradingId |
| B 医保授权 | 国家医保小程序跳转 → authCode → `mip-user-query`（经 forward → wecity）→ `pay_auth_no` |
| C 医保结算 | 1101（FSI）→ psnNo/参保地；**2.27.2.27 `out-insur-settle-infos`（gpsrmyy）= 6201 费用清单来源**；**2.1.9 depts → 医保科室码 `caty`**；**2.1.13 users → 医保医师码**；6201（→56380 `hos/uldFeeInfo`）→ payOrdId/payToken；**2.6.33 `outpatient-child-payment-records` 决定 6202 的 `acctUsedFlag`**；6202 → ordStas + 四项金额 |
| D 插件自费 | 2.6.65.2 第二次 pre-order（payTypeId=50/CREDIT）→ 新流水；`plugin-payment-order`（微信 JSAPI 订单）；`uni.requestPayment`；`plugin-payment-complete` 后端依次执行 2.27.2.29 三方登记 → 2.6.65.15 收款确认 → 2.6.65.5 最终结算 |
| E 确认写回 | 6301 轮询 ordStas 终态后才允许 `medical-settlement-complete`（2.27.2.32 写回 HIS → 2.6.65.5 最终结算）；异常走 2.6.65.6 cancel-settle。旧端缴费页另有前端直连 2.27.2.32 的不一致路径，新端必须统一为服务端编排 |

### 3.4 旧服务分叉（3090 实测，比原文档多）

`test-v3` 分支未提交改动：`mbs_fsi/order_model.py`、`mbs_fsi/order_service.py`
（含“允许空工作站ID”手工修复）、`yunhealth_settle/schema.py`、`env/.env.prod`。
另有未跟踪文件 `=`。旧服务退役前必须收割这些 diff。

## 4. 当前冻结项

- 中转机与拦截器不受本仓库版本控制；其可用性观测与 token 轮换策略另行立项；
- 本文拓扑事实不改变 `MEDICAL_INSURANCE_READY`、`WECHAT_PAYMENT_READY` 或任何
  写入 gate 的关闭状态；
- 服务器凭据、token 与 env 值不进入本记录。

## 5. 下一步执行顺序

1. 按 [`infra/生产链路拓扑.md`](../../infra/生产链路拓扑.md) 固化运维边界与观测建议；
2. F 批次（支付/医保/写入，已按仓库所有者 2026-09-03 决策解除延后）实现时，
   以 §3.3 的数据依赖作为 6201/6202 前置材料清单；
3. crypto 第④层验证按 §3.2 的 forward 协议分段执行（FSI/HIS/wecity 各自确认）。
