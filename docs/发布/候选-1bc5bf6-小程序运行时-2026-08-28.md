# 小程序运行候选 `9354104` 发布记录（2026-08-28）

> 本记录只锁定本地微信开发者工具使用的 TypeScript 原生小程序运行包。它不表示微信线上版本已经上传，也不表示众阳、医保、支付或真实设备业务已经通过验收。

## 候选来源

| 项目 | 值 |
| --- | --- |
| 配套服务端 release | `5738a71e0bcddaa8849106754baf5b296427bed7` |
| 小程序提交 | `9354104` |
| 小程序构建来源 | `935410473e5a7c1be125a85834f957f53a833d8f` |
| 当前 live `dist` | `apps/miniprogram/dist/`，sourceRevision=`935410473e5a7c1be125a85834f957f53a833d8f` |
| 页面与 Tab | 38 个页面、4 个微信原生 Tab |
| 微信线上版本 | 尚未上传，本候选仅供本地/开发者工具验证 |

## 本轮修复

- 修复预约记录查询失败时患者上下文尚未提交的问题，避免 503 被误翻译成“请先选择就诊人”。
- 新增预约记录错误分类，将 provider 临时故障、持久化故障、未配置和无记录分别映射为用户可理解的提示。
- 保持预约历史只读、患者 owner 边界和原生 `tabBar`；没有打开预约写入、支付或医保。

## 本地验证

- 小程序 TypeScript 检查通过。
- 小程序测试：`356 pass / 0 fail / 3826 expect()`。
- `runtime:verify` 通过，38 个页面脚本和根运行文件齐全。
- 当前真机证据清单 ``device-evidence-1bc5bf6-pending.json`` 九个域全部为 `pending`；清单结构通过不等于真实业务通过。

## 外部依赖边界

预约记录对应的众阳接口仍受 `gpsrmyy.meiyi.pro` 过期 HTTPS 证书影响。服务器到该域名的标准 TLS 请求在拿到 HTTP 响应前失败，因此本候选不能把本地构建成功宣称为预约 Provider 成功。证书续期后，必须重新采集页面、客户端 requestId、服务端 trace 和 Provider 低敏 requestId。

支付、医保授权/结算、预约写入/取消、退款、HIS 回写和未知外部 WebView 继续关闭。
