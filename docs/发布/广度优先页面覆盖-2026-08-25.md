# 广度优先页面迁移覆盖证据（2026-08-25，当前复核至 2026-09-16）

> **当前事实源补充（2026-09-16）**：本页下方的旧候选段落保留历史交接信息；当前逐页机器台账以
> `LEGACY_HOSPITAL_ROOT=/Users/yxswy/Documents/GitHub/hospital pnpm migration:audit` 为准：64 个旧页面、47 个原生页面，状态分布为
> `partial=35`、`replaced=10`、`surface-only=17`、`blocked-payment=0`、
> `blocked-provider=0`、`blocked-external=1`、`excluded=1`。
> 这份覆盖证据证明入口没有遗漏，不证明阻塞入口的真实业务已经开放。
> 当前工作区源码已注册 47 个原生页面；当前小程序运行输入的最近一次相关提交为 `6ebaeebc927a14b1f45a33e3d6b378ebb5ca216b`；
> 当前小程序回归为 423 pass / 0 fail，代码候选已完成本地回归；最近一次 release 与 development 运行包仍是更早候选，不能替代本次候选的运行包校验；
> 线上历史运行包和下方旧候选只作历史/运行边界记录，不能作为当前真机证据。
>
> **最近一次已验证运行包构建事实（2026-09-16，早于当前候选提交）**：`pnpm --filter @hospital/miniprogram build:dev` 与
> `runtime:verify:dev` 已通过，开发包位于 `.local/hospital-miniprogram/development/`，
> `baseSourceRevision=cba13c71a74022b756144b9c7a61965cd542b1af`、
> `sourceRevision=workspace-sha256:0d85ea72c0b1518dcdff49d87a0c0ef076e4fc529735ae9430f94573b9dd3eaa`、
> `pageCount=47`、`generatedAt=2026-09-15T19:28:40.112Z`；release 构建与
> `runtime:verify` 已通过，`apps/miniprogram/dist/build-info.json` 为
> `sourceRevision=cba13c71a74022b756144b9c7a61965cd542b1af`、`pageCount=47`、
> `generatedAt=2026-09-15T19:28:46.660Z`。`apps/miniprogram/project.config.json` 的
> `miniprogramRoot` 仍为 `dist/`。
> 当前候选源码为 `6ebaeebc927a14b1f45a33e3d6b378ebb5ca216b`，尚未用本提交重建运行包；因此运行环境、DevTools 和真机验收继续保持 pending。

## 结论

本轮先解决“旧端还有哪些入口、每个入口现在落在哪里”的覆盖问题，没有继续扩大支付、医保、患者绑定、临床问卷或外部 WebView 能力。

旧仓库 `G:\\fuck\\hospital\\hospital-app\\src` 实际扫描到 64 个 Vue 页面；新端逐页台账已覆盖全部 64 个页面，并由 `pnpm migration:audit` 同时核对旧仓库实际文件、Markdown 迁移矩阵和 TypeScript 落点目录。

| 分类 | 数量 | 含义 |
| --- | ---: | --- |
| `replaced` | 10 | 已由原生页面或等价静态能力替换，不代表旧端所有隐含 provider 能力都存在 |
| `partial` | 35 | 已有安全只读/静态子集，详情、写入、支付或外部回写仍关闭 |
| `surface-only` | 17 | 已有原生页面外壳和稳定关闭态，真实业务读取或写入仍未开放 |
| `blocked-payment` | 0 | 当前没有单独以页面状态登记的支付阻塞页；支付/医保 gate 仍由 FeatureKey 管控 |
| `blocked-provider` | 0 | 当前没有单独以页面状态登记的 Provider 阻塞页；未确认的 Provider 能力仍由 FeatureKey 管控 |
| `blocked-external` | 1 | 外部入口、WebView 或实时能力的安全边界未齐，继续保持关闭态 |
| `blocked-clinical`、`blocked-patient-contract` | 0（当前页面台账） | 这些阻塞原因由对应 `featureKey` 和 A–E 批次记录，不再重复作为页面状态枚举 |
| `excluded` | 1 | 旧端开发辅助页，不进入生产小程序 |
| **合计** | **64** | 每个旧页面只有一个明确落点 |

## 机器门禁

- 页面落点台账：[`apps/miniprogram/src/services/legacy-page-catalog.ts`](../../apps/miniprogram/src/services/legacy-page-catalog.ts)
- 页面台账回归：[`apps/miniprogram/src/services/legacy-page-catalog.test.ts`](../../apps/miniprogram/src/services/legacy-page-catalog.test.ts)
- 总迁移审计：`pnpm migration:audit`
- 入口分发广度审计：`pnpm migration:breadth:audit`
- 该审计同时验证首页/“我的” action 分支，以及当前 47 个已注册页面的 WXML 事件方法闭环；它不替代 Provider、真机或真实业务验收。
- 旧端逐页解释：[`../迁移/旧页面矩阵.md`](../迁移/旧页面矩阵.md)

历史候选门禁输出（2026-08-26，仅作追溯）：

```text
Native page migration ledger passed: 40 registered page(s) documented
Legacy page inventory passed: 64 old page(s) match the migration matrix
Native legacy page catalog passed: 64 page(s), partial=23, replaced=8,
excluded=1, surface-only=25, blocked-payment=7
```

## 下一步原则

1. 当前 35 个 `partial` 页面已经具备可验证的只读/静态代码落点，首页和“我的”可见入口也已通过分发审计；下一步是用当前源码输入构建候选，为患者、预约历史/目录、报告目录、门诊费用、住院摘要和普通资料分别保存页面、HTTP requestId、服务端低敏日志三层证据。
2. 新 Provider 文档到达后，按业务域完成 `contract → adapter → domain → API → 小程序 → 日志 → 验收`，不把一个域的字段复用到另一个域。
3. 支付、医保、二维码、患者新增绑定、问卷提交、WebSocket、外部 WebView 和 HIS 写回继续保持关闭；状态页存在只代表入口可解释，不代表业务已完成。

本轮只修改新项目代码与文档，不修改旧 Python 服务、旧数据库、Redis 或线上运行包。
