# 当前候选配套记录（2026-09-16）

> 本文是当前候选的基线配套记录，线上切换须以本次发布后的远端 `current`、服务状态和 readiness 证据为准。旧 Python 服务、旧数据库、旧 Redis 和线上进程不在本地构建动作范围内。

## 候选来源

| 项目 | 值 |
| --- | --- |
| 服务端 release | `ce1e871cfa99faf292203b4bfc2006ff962309b1` |
| 小程序客户端 | `99a0b0b` |
| 小程序构建来源 | `99a0b0bc812911889f1c4895e1b3222fe45cf591` |
| 小程序运行包 | `apps/miniprogram/dist/`，48 个页面 |
| 目标 schema head | `0026_medical_insurance_fee_refs` |
| 真机证据 | pending，使用同源待采集清单 |

## 当前事实

1. 小程序运行包已根据当前已提交运行输入重建，`build-info.json` 与 `app.js` 启动来源均为 `99a0b0bc812911889f1c4895e1b3222fe45cf591`。
2. 本地基线只绑定运行包来源、服务端登记 release、目标 schema 和真机清单，不代表小程序已经上传微信线上环境。
3. `0026_medical_insurance_fee_refs` 是当前基线索引绑定的 migration head；服务端是否已经执行到该版本必须通过真实服务器 readiness/schema 证据确认，不能由本地源码推断。
4. 当前没有微信开发者工具或真机会话，登录、患者、预约、费用、报告和资料的真实页面—客户端 requestId—服务端 trace/Provider requestId 证据仍为 pending。

## 发布边界

- 本候选没有执行线上发布、SSH 写入、服务重启、数据库 migration、Redis 清理、旧服务修改或真实 Provider 业务调用。
- 服务端候选 `ce1e871` 必须完成受控服务端发布，才能与本地小程序候选组成可验收版本；发布后仍需以远端 release、服务状态和 readiness 证据确认运行时一致。
- 支付、医保、退款、预约写入、HIS 回写、临床 Provider、外部 WebView 和真机业务证据不因基线刷新而开放。

## 真机取证入口

使用 [`device-evidence-99a0b0bc812911889f1c4895e1b3222fe45cf591-pending.json`](真机证据-99a0b0bc812911889f1c4895e1b3222fe45cf591-pending.json) 记录当前小程序候选的九个业务域。所有域必须在同一运行包、同一服务端版本和同一微信会话下逐项取得页面、客户端 requestId、服务端低敏日志及 Provider requestId（如有）；pending 模板本身不构成验收完成。

> 当前 live `dist` 运行包来源为 `99a0b0bc812911889f1c4895e1b3222fe45cf591`（提交 `99a0b0b`）；该来源只代表本地运行包指纹，不代表已上传微信线上环境或已取得真机业务证据。

> 当前小程序配套运行包来源（2026-09-16）：本地 live 运行输入为 99a0b0bc812911889f1c4895e1b3222fe45cf591（提交 99a0b0b）。本行只锁定当前候选，历史段落保留用于追溯。
