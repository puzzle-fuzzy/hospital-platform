# 工具链与小程序运行包复现记录（2026-09-16）

> 本记录只核对仓库声明、CI 和本机运行包来源，不执行发布、不启动微信开发者工具、不连接 Provider/生产，也不改变任何支付或医保能力。

## 1. 固定声明与当前执行机

| 工具 | 仓库固定值 | 当前执行机 | 判定 |
| --- | --- | --- | --- |
| Bun | `.bun-version` / `package.json engines.bun` = `1.4.0` | `1.4.0` | 一致 |
| Node.js | `.node-version` / `package.json engines.node` = `24.12.0` | `v24.12.0`（显式使用已安装路径；默认 shell 仍为 `v26.8.1`） | 复现命令一致 |
| pnpm | `package.json packageManager` = `pnpm@11.9.0` | `11.9.0` | 一致 |

`pnpm toolchain:audit` 在 Node 24.12.0、Bun 1.4.0、pnpm 11.9.0 组合下通过，且已完成本地候选复现。CI 使用 `oven-sh/setup-bun@v2` 读取 `.bun-version`、`actions/setup-node@v4` 读取 `.node-version`，并固定 `pnpm/action-setup@v4` 为 11.9.0，证据见 `.github/workflows/ci.yml:27-50` 和 `tools/toolchain-audit.mjs:29-95`。默认 shell 仍会解析到 Node 26.8.1，后续命令必须显式使用 Node 24.12.0。

## 2. 小程序运行包来源快照

| 项目 | 当前值 | 结论 |
| --- | --- | --- |
| `src/app.json` 页面数 | 47 | 仅是源码注册数量 |
| `apps/miniprogram/dist/build-info.json` | sourceRevision=`cba13c71a74022b756144b9c7a61965cd542b1af`，pageCount=47，generatedAt=`2026-09-15T19:28:46.660Z` | 已按当前运行输入重建的 release 包；docs-only 提交不改变运行输入来源 |
| release dist 文件树 | 453 个文件、2,413,086 bytes、tree SHA-256=`2dfff7e24cc15c3249708e5157296d4d7b62771a1d2ede6d7dde7eb5178f9b4d` | 当前 release 包完整性指纹 |
| development build-info | baseSourceRevision=`cba13c71a74022b756144b9c7a61965cd542b1af`，sourceRevision=`workspace-sha256:0d85ea72c0b1518dcdff49d87a0c0ef076e4fc529735ae9430f94573b9dd3eaa`，pageCount=47，generatedAt=`2026-09-15T19:28:40.112Z` | 已按当前工作树运行输入重建的 development 包 |
| development 文件树 | 453 个文件、2,413,381 bytes、tree SHA-256=`944f5963bc1ba74b677edab5edafe8a41aa17a3831dadf137cbbd698a24a3d19` | 当前 development 包完整性指纹 |

最近一次已构建的小程序运行输入源码提交为 `cba13c71a74022b756144b9c7a61965cd542b1af`；当前 HEAD 的 `48868dff` 包含尚未重建到 dist 的小程序客户端校验改动，后续 `0f1fa829` 只更新文档，因此现有运行包仍明确属于 `cba13c71`，不能当作当前源码候选。构建脚本规定正式 release 必须在运行输入干净时记录 Git revision，development 必须记录工作树快照，见 `apps/miniprogram/scripts/runtime-provenance.ts:133-226`；不能手工改 `build-info.json` 或直接在 `dist` 修补页面。

## 3. 可复现发布顺序

本次在 Node 24.12.0、Bun 1.4.0、pnpm 11.9.0 的同一环境中按以下顺序完成并保存输出：

```text
pnpm install --frozen-lockfile
pnpm --filter @hospital/miniprogram build:dev
pnpm --filter @hospital/miniprogram runtime:verify:dev
pnpm --filter @hospital/miniprogram build
pnpm --filter @hospital/miniprogram runtime:verify
```

结果为：`toolchain:audit` 通过；`build:dev` 和 `runtime:verify:dev` 通过；release `build` 和 `runtime:verify` 通过；两种运行包均为 47 页、453 个文件。文件树 SHA-256 按“按相对路径排序，依次写入相对路径、NUL、文件字节、NUL”计算。运行包要交给 DevTools 时，仍须经过 pending 目录和原子发布器；不得把 DevTools 的增量编译结果当作候选来源。

## 4. 当前结论与剩余阻塞

P2-07 的本地工具链和运行包复现已完成：已用已安装的 Node 24.12.0 重新构建 development/release，并通过两个 `runtime:verify`。仍未进行发布、DevTools、Provider、微信真机或生产验收；这些不属于本地工具链复现，必须在独立窗口按候选来源继续采集。

本记录不授权安装系统 Node、切换发布机、上传运行包或变更线上服务；这些动作需要独立的发布窗口和责任人确认。
