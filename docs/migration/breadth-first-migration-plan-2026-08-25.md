# 广度优先迁移计划（2026-08-25）

> 这份文档是当前迁移主线。目标是先让旧端可见入口在新小程序中全部有稳定落点，再按业务域深入 provider contract、数据模型、日志和真机验收。页面进入“功能迁移中”状态不等于业务已经完成，也不能替代真实接口。

## 一、当前基线

| 项目 | 当前事实 |
| --- | --- |
| 旧端页面扫描 | 64 个 Vue 页面 |
| 新端页面注册 | 17 个页面；前 4 个由微信原生 TabBar 管理 |
| 已完成候选 | `90d5ab0323b33fb620c83b86a8248b31c0ae3341` |
| 候选位置 | `.local/hospital-miniprogram/pending/` |
| 当前 live 运行包 | 仍为 `fcc6630ebfa7b0697cbd03a5e376ce6765d1643b`，开发者工具释放后再原子发布 |
| 旧服务 | Python `8001` 未修改、未停止；本轮只修改新项目 |

## 二、入口覆盖层已经完成

本轮增加 `pages/feature-status/feature-status` 和 `services/feature-navigation.ts`，并把二级动作也纳入统一状态路由：

- 首页顶部快捷入口、门诊/住院/便民服务清单中的未完成项目都有固定 action；
- “我的”中的问诊、医生、门诊病历、电子导诊单、智能客服和医保电子凭证都有页面落点；
- 预约下单、挂号详情、预约前预问诊、云影像、报告分享和报告复诊都有固定页面落点；
- 状态页使用代码内固定目录，不接受旧端 URL、provider ID 或任意 query 拼接；
- 页面只解释“正在迁移”和需要的 contract，不调用旧接口、不读旧缓存、不发起支付；
- 已迁移的预约、患者、报告、门诊费用、静态医院卡片、院内导航、公众号说明和反馈帮助不经过状态页。

这一步解决的是入口完整性、用户反馈和 404 风险，不把“能打开页面”误记为“业务已迁移”。

2026-08-25 的广度护栏补充：所有统一状态页现在展示机器可读的准入阻塞类型，分别区分
`待 provider contract`、`待临床审核`、`待支付与回写 contract`、`待患者绑定 contract` 和
`待外部入口 contract`。首页、“我的”和预约/报告/费用二级操作均由固定 `FeatureKey` 目录校验；
新增入口如果没有对应 key 会在小程序验收测试中失败，不能退化成无意义 Toast。这个改动仍然只是
迁移边界可见化，不代表对应业务已经完成。

## 三、下一批按业务域批量推进

### 批次 A：低风险只读与静态能力

优先把不涉及金额、医疗结论和写回的入口形成闭环：

1. 预约目录、预约历史、爽约记录：补齐真实微信页面、客户端 requestId、服务端 Pino 事件和 provider 同链证据。
2. 门诊费用只读：补齐待缴/已缴列表的真实 provider 样例、患者归属和空态，不接支付按钮。
3. 报告目录：先确认 LIS 摘要的真实字段；影像/心电/PEIS、附件和下载继续分开验收。
4. 普通资料：完成真实 GET/PUT/409 页面验收，保持头像授权与服务端资料边界分离。

### 批次 B：已有页面但 contract 尚未冻结的读模型

这批先完成 contract 和只读展示，不跨域复用患者 ID：

- 门诊病历：HIS/EMR 只读资源、字段白名单、owner/患者映射和脱敏失败样例；
- 我的问诊、电子导诊单、我的医生：会话/关系来源、内容保留和患者归属；
- 住院信息：独立住院患者标识、费用清单和权限模型；
- 健康百科：版本化内容、临床审核、搜索和下线审计。

每个域都必须完成 `contract → adapter → domain → persistence（如需要）→ API → 小程序 → 日志 → 验收`，不允许直接从页面调用旧 provider。

### 批次 C：写入与敏感医疗业务

这些能力需要单独的状态机和回滚证据：

- 患者新增、绑定、解绑、协议和签名；
- 二维码/就诊码：确认字段、有效期、签名、受众、防重放和撤销后再开放；
- 预约锁号、下单、取消、退号和 HIS 回写；
- 预问诊、随访、风险自评、健康自测、锦旗和表扬信。

旧端存在请求代码或页面，不能作为新端写入契约。缺少幂等、owner、撤回或审核规则时，继续保留状态页。

### 批次 D：支付与医保最后处理

微信支付、医保授权、1101/6201/6202/6301/6203/6401、结算、退款、查单和 HIS 回写统一最后处理。必须有金额单位、订单 owner、幂等键、回调/查单状态机、失败补偿和真实沙箱/生产验收；当前只读费用页不得跳入支付流程。

## 四、每批的完成定义

只有同时满足以下条件，才将矩阵状态从“迁移中/部分迁移”改为“已完成”：

1. 新端页面和 API contract 已注册，字段白名单与患者归属明确；
2. 成功、空结果、未授权、暂时故障和未知响应均有稳定读模型；
3. 服务端日志只记录低敏业务字段，并能用 requestId 与客户端链路关联；
4. 自动化测试、类型检查、运行包构建通过；
5. 真实环境页面、HTTP requestId、服务端日志和 provider 结果完成同链验收；
6. 文档、发布候选和旧服务共存证据同步更新。

## 五、当前明确不做的事情

- 不修改旧 Python 项目、旧 `8001` 服务、旧数据库和 Redis；
- 不因为“先全量迁移”而复制旧端 provider URL、完整身份证号、卡号、`patId` 或 token 到小程序；
- 不把状态页、Toast、空列表或本地计算结果包装成真实医疗业务成功；
- 不强制结束微信开发者工具进程，不删除被锁定的 live `dist/`；
- 不与另一会话负责的众阳自动化文件冲突。
