# 五个低风险业务域横向逻辑审计（2026-08-26）

> 本文是当前迁移候选的跨域逻辑审计，不是生产验收证明。
> 审计对象为 `dee4803fb94ad50c59c9ef8fda996bc0f37427c6`，对应小程序功能提交 `dee4803f`、40 个原生页面、`307 pass / 0 fail / 3511 expect()`。
> 线上服务端仍为 `8eb51b5f`，当前 pending 运行包尚未替换微信开发者工具占用的 live `dist`；旧 Python `8001`、旧数据库和旧 Redis 不在本次修改范围内。

## 1. 审计结论

五个低风险域已经形成代码层的完整闭环：页面、API、service/domain、adapter 或持久化、错误语义、低敏日志和文档均有落点，`pnpm readonly:audit` 已通过。

这只说明“新项目没有把页面、接口和业务边界拆散”，不表示以下事实已经完成：

- 当前 pending 运行包已经发布到微信开发者工具或上传微信；
- 当前候选已经取得真机页面、客户端 `requestId`、服务端 `traceId`、Provider 低敏请求号的关联证据；
- 报告真实详情、附件、临床病历、预约写入、费用支付、医保授权或 HIS 回写已经开放。

## 2. 五个业务域的横向落点

| 业务域 | 原生页面 | 当前可用子集 | 必须保持的不变量 | 当前未完成能力 |
| --- | --- | --- | --- | --- |
| 就诊人目录 | `patient-select`、首页患者卡片 | owner-scoped 目录、同步、显式切换、刷新、失效选择处理 | 小程序只保存内部 opaque `patientId`；未确认当前 owner 时不能沿用旧患者；临床不可用记录不能进入业务请求 | 新增/绑定、实名关系、二维码和真实账号切换证据 |
| 预约目录与历史 | `appointment-directory`、`appointment-records`、`missed-appointments`、今日摘要 | 科室/排班只读；在线/全部历史；爽约由服务端明确状态派生；本地分批展示 | `scope` 只能是服务端认可的 `online|all`；本地展开不是 Provider 分页；未知状态不能变成爽约；不触发锁号、写入或取消 | 当前候选真机证据、预约写入、锁号、取消、详情和 HIS 回写 |
| 报告 | `report-directory`、`report-detail` | 近 30 天目录；受限 opaque 详情入口；患者切换和重试 | 详情必须同时校验 owner、当前患者、opaque `reportId` 和引用 TTL；列表不能冒充病历或附件授权 | 真实 LIS/PACS/ECG 详情、附件短期授权、体检报告和真机证据 |
| 门诊费用 | `outpatient-payment` | 待缴/已缴费用只读目录；金额由服务端展示模型转换；本地分批展示 | 金额仅用于展示；列表不代表最终结算；点击费用不得调起微信支付、医保授权、订单或 HIS 写回 | 费用明细、支付/医保状态机、查单、退费和真机证据 |
| 普通资料 | `profile`、全局资料快照 | 昵称、性别、年龄、邮箱 GET/PUT；版本冲突可见；全局单飞初始化 | 普通资料不等于实名、手机号、微信身份或患者档案；409 不得静默覆盖；账号切换不能展示上一账号资料 | 真实微信读写、受控 409 真机证据、头像和实名独立 contract |

## 3. 跨页面共用逻辑

### 3.1 会话和资料只初始化一次

小程序启动时由 `App.onLaunch` 触发全局资料初始化；页面只消费同一个全局快照。页面在明确 `idle` 状态下才接管同一个 Promise，不能因为页面切换再次登录或重复拉取用户资料。

会话失效时只清理当前会话派生的患者和资料状态，不能把旧 token、旧 `patientId` 或旧资料重新提交；持久化暂时不可用时保留可恢复的登录边界，不把故障误报为未授权。

### 3.2 当前就诊人必须显式且可追溯

首页、就诊、报告、费用、挂号记录和临床/服务入口消费同一个当前就诊人上下文。页面进入时如果没有已确认患者，只能进入统一选择页或稳定错误态；“当前微信用户”不能自动冒充就诊人。

切换患者后，旧页面请求的结果必须因患者代际变化而丢弃。患者选择页只返回内部 opaque 引用，不把众阳 `patId`、卡号或身份证号写入路由和本地缓存。

### 3.3 查询状态不能把错误变成空态

五个域都必须区分 `requesting`、成功有数据、成功空结果、未授权、参数错误、暂时故障和契约异常。加载、错误和空态使用稳定容器，避免先显示“正在查询”再把卡片整体撑高或把 Provider 超时误显示成“未查询记录”。

其中“空”只能表示服务端明确确认当前 owner/患者/时间范围没有可展示记录；依赖未配置、持久化故障、Provider 超时和响应结构错误不能落到空态。

### 3.4 只读域禁止副作用

预约目录点击号源、报告点击摘要、费用点击记录、患者刷新和资料读取都不能隐式创建订单、锁号、预约、医保授权、支付、文件下载授权或 HIS 写回。任何副作用能力必须先建立独立 contract、幂等键、最终状态查询、补偿和回滚证据。

### 3.5 日志只保留关联，不保留敏感正文

每个小程序请求生成独立 `x-request-id`；服务端把它归一化为 `traceId`，业务事件和 Provider 低敏请求号沿用同一关联链。日志不得写入 Authorization、微信 code、身份证号、完整卡号、患者姓名、报告正文、费用明细或 Provider 原始响应。

页面验收至少需要同时记录页面动作、客户端 `requestId`、服务端 `traceId`/业务事件和 Provider 低敏请求号；缺任一层，只能记为代码或服务器链路证据。

## 4. 当前可执行的横向队列

| 队列 | 继续动作 | 停止条件 | 当前状态 |
| --- | --- | --- | --- |
| A 只读闭环 | 先发布同一 pending 候选，再一次采集患者、预约、报告、费用和普通资料证据 | 发现 sourceRevision 不一致、owner/患者串线、敏感日志或状态误判 | 代码闭环通过，真实证据待采集 |
| B 健康内容 | 收集审核 bundle、版本、撤回和临床责任人，继续保持 fail-closed | 没有审核版本或内容来源不明 | 只读代码骨架存在，正式内容未放行 |
| C 临床读取 | 分别冻结门诊病历、住院、医生关系、问诊/导诊 contract | 缺字段白名单、授权、患者映射或脱敏样例 | 页面外壳已迁移，业务读取未放行 |
| D 患者/便民 | 分别处理新增绑定、协议、签名、预问诊、随访、锦旗/表扬信 | 缺同意、幂等、撤回、文件安全或医护读取规则 | 安全页面已迁移，写入未放行 |
| E 外部入口 | 冻结客服、问诊、订阅和互联网医院 allowlist、短期 ticket、回跳 | URL、受众、退出或日志边界不明确 | 安全壳已迁移，外部会话未放行 |
| F 支付医保 | 最后建立微信支付、医保授权、查单、退费和 HIS 回写状态机 | 金额、状态、回调、幂等或补偿任一缺失 | 全部关闭，不与只读费用混接 |

## 5. 本轮验证命令

```powershell
pnpm readonly:audit
pnpm migration:audit
pnpm migration:boundary:audit
pnpm migration:breadth:audit
pnpm logging:audit
pnpm docs:audit
pnpm format:check
pnpm --filter @hospital/miniprogram test
pnpm --filter @hospital/miniprogram typecheck
pnpm --filter @hospital/api typecheck
```

以上命令证明的是源码、静态台账、日志登记、格式和自动化回归；发布、真实 Provider、公网、微信真机和支付医保验收仍必须分别留证，不能由测试数字代替。

## 6. 新会话接续入口

先阅读本审计、[`legacy-page-matrix.md`](legacy-page-matrix.md)、[`native-page-migration-status.md`](native-page-migration-status.md) 和 [`../release/next-readonly-business-acceptance-plan-2026-08-26.md`](../release/next-readonly-business-acceptance-plan-2026-08-26.md)。

接续时优先横向推进 A/B/C/D/E 中最先取得正式材料的队列；没有正式 provider、临床或微信能力材料时，只补契约、关闭态、日志和验收台账，不制造假数据。F 队列继续最后处理。
