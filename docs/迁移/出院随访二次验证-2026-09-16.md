# 出院随访二次验证（2026-09-16）

## 结论

本次按旧服务源码重新核对出院随访的表单范围、患者上下文、读取条件、提交字段和覆盖语义，并对新端关闭态及临床契约门禁进行了验证。结论是：旧端存在真实业务页面和 CRUD，但数据边界不足以直接迁移；新端当前保持关闭态，没有读取旧随访答案，也没有发起随访写入，P1-14 继续保持未完成。

本项只涉及出院随访，不修改旧服务、数据库、Provider、线上配置或支付、医保、混合支付、自费支付、退费、收银台及支付相关 HIS 回写代码。

## 1. 旧服务源码证据

### 1.1 表单和入口范围

旧端入口固定展示 8 套表单，源码位于 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/pagesB/health/discharge_followup.vue:136-153`：

1. 高平市人民医院一级随访记录表；
2. 高平市人民医院四级手术随访记录表；
3. 心内科四级手术随访记录表；
4. 神经内科四级手术随访记录表；
5. 高平市人民医院二级回访登记表；
6. 高平市人民医院日间手术随访记录表；
7. 高平市人民医院四级手术二次回访登记表；
8. 高平市人民医院日间手术二级回访登记表。

入口从 `SelCard` 缓存提取 `patId` 等患者信息，见同文件 `:155-190`；随后以 `requestChannel: "3"` 查询预约记录并只保留 `status === 3`，见同文件 `:214-239`。该条件只能证明旧端用“已就诊预约”作为展示筛选，不能证明它等同于权威出院事件。

### 1.2 旧端读取、提交和回填

旧详情页按照 `tableName` 选择不同表单组件，见 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/pagesB/health/discharge_followup_detail.vue:20-95`。页面提交时从路由或缓存获取 `patId`，从缓存获取 `user_id`，将表单内容序列化后提交 `user_id`、`pat_id`、`table_name`、`content`，见同文件 `:140-199`。

详情读取按 `pat_id + table_name` 查询，使用返回的第一条记录解析并回填；没有内容才显示提交按钮，见同文件 `:240-289`。旧客户端接口声明也确认了相同字段和路径：

- `GET /convenience/discharge-follow-up/list`，见 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/api/modules/health.ts:318-357`；
- `POST /convenience/discharge-follow-up/create`，见同文件 `:359-387`。

旧 Python 服务创建时先校验认证用户与请求 `user_id` 一致，再只按 `user_id + pat_id` 查找；存在记录就更新，不存在才新增，见 `/Users/yxswy/Documents/GitHub/hospital/app/api/v1/module_convenience/discharge_follow_up/service.py:12-29`。列表查询强制附加当前认证用户，并支持 `pat_id`、`table_name` 过滤，见同文件 `:31-67`。模型仅有 `user_id`、`pat_id`、`table_name`、`content`，见 `/Users/yxswy/Documents/GitHub/hospital/app/api/v1/module_convenience/discharge_follow_up/model.py:9-18`。

因此旧服务没有可用于新端迁移的出院事件 ID、住院 episode、随访任务 ID、表单版本、提交序号、幂等键、撤回状态或医护读取授权边界。尤其不能把旧端的覆盖记录当作多个出院或多个随访任务的可靠历史。

## 2. 新端二次验证

- `apps/miniprogram/src/pages/discharge-followup/discharge-followup.ts:1-4` 仅注册 `registerClinicalContentSurfacePage("discharge-followup")`，没有表单、旧接口或答案写入调用。
- `apps/miniprogram/src/services/clinical-content-surface.ts:71-85` 明确要求唯一出院事件、任务版本，且不能让多个出院共用任务。
- `apps/miniprogram/src/services/clinical-content-surface.ts:119-133` 将问卷版本、患者授权、幂等、医护读取、临床审核、失败回退和撤回列为 contract；` :180-221` 只注册关闭态页面和患者上下文加载。
- 当前新端没有出院事件查询、任务生成、八套表单 schema/version、答案 repository、撤回接口或医护读取路由；因此未将旧端 `createDischargeFollowUp` 接入新端，也未导入旧答案。

## 3. 本次验证结果

### 3.1 静态代码和契约门禁

执行：

```text
pnpm clinical:contract:audit
```

结果：通过。门禁报告门诊就诊记录为 `contract-pending / registered-fail-closed`，住院信息和电子导诊单为 `contract-pending / unregistered`；临床内容实现仍保持 fail-closed。命令同时报告本机 Node `v26.8.1`，项目声明版本为 `24.12.0`，该版本差异不改变本次静态结论。

### 3.2 小程序相关验收测试

执行：

```text
bun test apps/miniprogram/scripts/acceptance.test.ts --test-name-pattern 'clinical|inpatient|patient context'
```

结果：8 pass、149 filtered、0 fail、76 expects。测试覆盖临床关闭态、共享患者上下文、无伪造公共记录、读取失败清理旧模型和上下文刷新生命周期；它证明关闭态边界没有被当前代码破坏，不等于出院随访业务已在 DevTools、真机或生产完成。

## 4. 分层验收状态

| 层级 | 状态 | 依据/缺口 |
| --- | --- | --- |
| 旧端表单和接口 | 已确认 | 固定 8 套表单、列表/创建接口和详情回填源码已核对 |
| 旧端覆盖行为 | 已确认 | 旧服务按 `user_id + pat_id` 查找并覆盖 |
| 新端静态代码 | 已确认 | 仅关闭态页面，无随访读写调用 |
| 临床 contract 门禁 | 已通过 | 当前实现继续 fail-closed |
| 出院事件和 episode 来源 | 未确认 | 缺权威事件、状态和患者 owner 映射 |
| 八套表单适用规则 | 未确认 | 缺科室/手术/人群/时间窗口和任务生成规则 |
| 表单 schema/version | 未确认 | 缺题目、选项、必填项、敏感字段及版本发布策略 |
| 提交、修改、幂等、撤回 | 未确认 | 旧服务无对应安全语义，需新 contract |
| 患者授权和医护读取 | 未确认 | 缺代填规则、最小读取范围和审计要求 |
| Provider / staging | 未验收 | 当前无可执行业务链路和样例 |
| DevTools / 真机 / 生产 | 未验收 | 需 contract 完成后按页面矩阵补证据 |

## 5. 处理决定

1. P1-14 保持未完成，不复制旧端八套表单，不迁移或覆盖旧答案。
2. 不把已完成预约 `status=3` 当成出院事件，不把 `user_id`、`pat_id` 或 `table_name` 当新端任务唯一键和授权凭证。
3. 下一步必须先冻结出院事件、任务生成、表单版本、患者授权、幂等、撤回、医护读取、审核和保留策略，再按八套表单逐项实现。
4. 只有在代码、生成包、DevTools、真机及真实后端证据分别具备后，才能将本项从 TODO 勾选完成。
