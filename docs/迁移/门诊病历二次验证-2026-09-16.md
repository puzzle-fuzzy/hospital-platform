# 门诊病历二次验证（2026-09-16）

## 结论

P1-09 仍未完成。旧端页面实际只调用 `out-visit-records` 的近 30 天门诊就诊摘要；`out-emrs` 仅在 API 文件声明，未发现门诊页面调用。新端已实现摘要安全子集和 fail-closed 读模型，但没有 Provider、公网、DevTools dist、真机和生产同链证据，也没有完整病历正文依据。

本次只做非支付门诊摘要的源码与 fixture 测试，没有调用 Provider，没有实现正文、结构化内容或附件，也没有触碰支付/医保代码。

## 旧服务依据

- `hospital-app/src/pagesB/health/electronic_record.vue:87-113` 实际调用 `POST /msun-middle-aggregate-clinic/v1/out-visit-records`，查询当前患者近 30 天到当天的摘要。
- `hospital-app/src/api/modules/medicalRecord.ts:112-127` 仅声明 `GET /msun-middle-aggregate-clinic/v1/out-emrs`；对旧端页面源码检索未发现实际调用。
- 旧页面直接展示多个 Provider 字段并在异常时清空列表，不能把这些字段或错误处理直接作为新公共 contract。

## 新端依据

- `apps/api/src/modules/medical-records/index.ts:18-52`、`service.ts:51-189` 只接受内部患者 ID、受限日期并按 owner 映射 Provider 患者；`packages/adapters/src/zhongyang-medical-records.ts:16-229` 固定旧摘要请求形状。
- `packages/domain/src/medical-records.ts:4-155` 和 `packages/contracts/src/index.ts:512-533` 公开旧记录卡所需的门诊摘要和当前患者展示快照白名单，不公开 `regId`、`patId`、身份证或 Provider 原始对象。
- `apps/miniprogram/src/pages/medical-record/medical-record.ts:91-220` 只展示摘要；页面明确提示病历正文、附件和住院病历尚未开放。
- 当前 `ZHONGYANG_MEDICAL_RECORDS_READY=false`，继续保持安全关闭态。

## 本轮旧端字段补漏

旧页面记录卡实际展示 `patName`、`sexName`、`patAge`、`maritalStatusName`。新端现已在适配器映射为 `patientName`、`patientSex`、`patientAge`、`maritalStatus`，并在 domain、公共 contract 和小程序二次白名单中保留；页面按字段存在性展示这四项。Provider 主键、身份证和原始对象仍不进入公共响应。

本轮 domain 2 项、adapter 4 项、API 2 项及小程序回归 439 项通过；`build:dev`、`runtime:verify:dev` 通过。当前 development 候选为 `sourceRevision=workspace-sha256:461f40c3ab9859a7a363ceba731fdd5f80190b3ea651546fd47cb464c6765ff9`、`generatedAt=2026-09-16T15:16:37.248Z`、52 页。该结果仍不代表真实 Provider 或临床业务验收。

## 可复现验证

执行：

```text
bun test packages/adapters/src/zhongyang-medical-records.test.ts apps/api/src/modules/medical-records/service.test.ts
bun test apps/miniprogram/scripts/acceptance.test.ts --test-name-pattern 'medical-record|门诊病历|outpatient reads|report and outpatient'
```

结果：adapter/service 6 pass、0 fail、17 expect calls；小程序相关验收 4 pass、153 filtered、0 fail、38 expect calls。

本地测试证明的是摘要字段、旧端患者快照字段、日期窗口、患者映射、异常类型和页面关闭文案；不证明 Provider 合同或真实临床数据。

## 未完成与放行条件

需取得 `out-visit-records` 当前 Provider 版本的成功、合法空、拒绝、超时、分页、排序和字段样例，并用患者 A/B/无映射患者验证 owner 归属、同日边界和日志脱敏，完成同候选 API、公网、DevTools、真机和生产证据后，业务方才能决定是否打开摘要 gate。

若业务确实需要正文，必须先确认 `out-emrs` 是否真实启用，再独立冻结正文/结构化内容、附件、短期引用、权限、撤回和审计 contract；不能因为 API 声明或页面标题而新增正文，也不能复用报告数据。

## 当前候选复核（2026-09-17）

重新核对旧页展示字段后未发现新的安全展示缺口。门诊 adapter/API 回归 6 pass、0 fail、17 assertions；小程序门诊病历/临床壳验收 4 pass、0 fail、84 assertions。未调用 Provider、未导入旧病历、未实现正文/附件；`ZHONGYANG_MEDICAL_RECORDS_READY=false` 保持，当前 development 包指纹和构建状态记录在根 `TODO.md`。

## 当前顺序复核（2026-09-17）

重新读取旧 `electronic_record.vue`、旧 `ZY.ts` 和 `medicalRecord.ts` 后确认，旧页面实际只有近 30 天 `out-visit-records` 门诊摘要请求；`out-emrs` 仅声明未被门诊页面调用。当前新端已覆盖旧卡片可确认的科室、医生、就诊时间、院区、就诊类型、收费类别、患者展示快照和诊断字段，并按 owner-scoped 患者映射与 30 天窗口 fail-closed；没有发现可在现有 contract 下安全新增的功能。

本轮定向回归共 13 pass、0 fail、62 assertions：`zhongyang-medical-records` adapter 4/10、medical-records API 2/7、domain 2/6、miniprogram acceptance 2/27、dashboard 3/12。未调用 Provider、未导入旧病历、未实现正文/附件或结构化临床结论；`ZHONGYANG_MEDICAL_RECORDS_READY=false` 继续保持。Provider 正式 contract、真实患者映射、公网/DevTools/真机/生产同链证据仍缺，因此 P1-09 不勾选。当前 development 包没有因本轮源码未变而重建，其最近一次来源指纹、构建与 runtime 校验仍以根 `TODO.md` 的记录为准。
