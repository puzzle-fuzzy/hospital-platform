# 风险评估与健康自测二次验证（2026-09-16）

## 结论

本次把 P1-15 拆成“风险评估”和“健康自测”两条独立链路，重新核对旧端页面、旧客户端接口、旧 Python 配置/评分与新端实现。结论不变：旧端确有可执行表单、题库和评分，但风险记录是覆盖式患者记录，健康自测是固定整数 ID 的题库和服务端评分，均不能在缺少临床 contract 的情况下直接迁移。新端风险评估保持关闭；健康自测只保留不保存、不分级、不诊断的 BMI 与血压安全数值子集。

本项只涉及风险评估和健康自测，不修改旧服务、旧数据库、Provider、线上配置或支付、医保、混合支付、自费支付、退费、收银台及支付相关 HIS 回写代码。

## 1. 旧服务真实行为复核

### 1.1 风险评估

旧端风险入口展示跌倒、压力性损伤和疼痛三类表单，见 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/pagesB/health/risk_self_evaluation.vue:72-81,91-103,124-130`。跌倒表单实际包含跌倒史、排泄、高风险药物、导管携带、活动能力和认知 6 组题目，提交时将答案组装为 `content[]`，并从缓存取 `user_id`、`pat_id` 后调用 `createRiskAssessment`，见 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/pagesB/health/risk_form_fall.vue:18-95,106-177,214-300`。

旧客户端声明列表和创建接口分别为 `GET /convenience/risk-assessment/list` 与 `POST /convenience/risk-assessment/create`，请求含 `user_id`、`pat_id`、`table_name`、`content`，见 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/api/modules/health.ts:232-237,279-312`。旧 Python service 创建时校验认证用户，按 `user_id + pat_id` 查找，存在则更新覆盖，不存在才新增；列表再强制按当前认证用户过滤，见 `/Users/yxswy/Documents/GitHub/hospital/app/api/v1/module_convenience/risk_assessment/service.py:13-62`。模型只有 `user_id`、`pat_id`、`table_name`、`content`，见同目录 `model.py:10-18`。

因此旧数据没有评估实例 ID、表单/规则版本、适用人群、阈值引用、授权主体、幂等键、撤回状态或临床复核状态。不能原样接入旧覆盖写入，也不能假设三类评估天然共享同一个患者记录。

### 1.2 健康自测

旧端首页展示 9 个入口，ID 为 1 到 9；其中肥胖指数和血压指数走独立计算器，其余入口通过 `NAME_TO_TYPE` 进入通用题目页，见 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/pagesB/health/health_test.vue:79-151,258-290`。通用题目页读取题目后按题目 ID 组装答案并调用评分接口，结果页展示返回的 `level`、`description`、`tips`，见 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/pagesB/health/self_test_question.vue:30-43,53-89,107-196`、`self_test_result.vue:18-76,107-166`。

旧客户端的题目和评分接口为 `GET /knowledge/selftest/questions/{id}` 与 `POST /knowledge/selftest/assessment`，见 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/api/modules/selfTest.ts:1-53`。旧 Python `QUESTION_CONFIGS`/`RESULT_CONFIGS` 实际包含 7 套题库，见 `/Users/yxswy/Documents/GitHub/hospital/app/api/v1/module_knowledge/selftest/config/list.py:1-35`；controller 按 `1..len(QUESTION_CONFIGS)` 读取配置并按题目分值和结果闭区间选择等级、描述和提示，见 `/Users/yxswy/Documents/GitHub/hospital/app/api/v1/module_knowledge/selftest/controller.py:16-99`。例如糖尿病阈值见 `config/diabetes.py:103-110`，肺功能结果区间见 `config/lung.py:52-57`。

这是旧端真实可执行的评分链路，但没有不可变规则版本、临床审核记录、适用人群证明、答案/结果撤回与失效、保留策略或结果解释审计。工程侧不能仅凭配置存在就确认医学文案和阈值仍然可用。

## 2. 新端二次验证

- `apps/miniprogram/src/pages/risk-evaluation/risk-evaluation.ts:1-4` 只注册 `registerClinicalContentSurfacePage("risk-evaluation")`，没有题目读取、评分或写入；临床页面仍为关闭态。
- `apps/miniprogram/src/pages/health-test/health-test.ts:39-43,88-124` 只调用本地 BMI 公式和血压输入校验；页面不读患者、不写服务端、不保存医疗记录，也不输出风险等级、疾病建议或分诊结论。
- `apps/miniprogram/src/services/health-safe-calculators.ts:1-6,21-29,63-114` 固定 `local-non-diagnostic-v1`，只定义工程输入范围、公式和格式化规则；代码明确禁止把它当临床指南或分级规则。
- `apps/miniprogram/src/services/clinical-content-surface.ts:135-153,180-221` 要求题库、阈值、适用人群、免责声明、临床审核、回滚和审计先完成，页面注册和患者上下文保持 fail-closed。

当前没有新端风险评估 API、评分 domain、规则 bundle、题库 API、结果持久化或审核 bundle。旧端 `selfTest.ts` 接口声明没有被误当成新端实现。

## 3. 本次验证结果

### 3.1 相关单元与边界测试

执行：

```text
bun test apps/miniprogram/src/services/health-safe-calculators.test.ts apps/miniprogram/src/services/migration-coverage.test.ts tools/migration-boundary-catalog.test.ts
```

结果：通过（以当前仓库测试收集结果为准）；覆盖 BMI/血压边界、`local-non-diagnostic-v1` 规则版本、健康自测只开放安全数值子集，以及风险评估和健康自测不能共用开放开关。测试证明当前安全边界，没有证明临床题库、分级或风险评估已经迁移。

### 3.2 新端小程序验收

执行：

```text
bun test apps/miniprogram/scripts/acceptance.test.ts --test-name-pattern 'clinical|inpatient|patient context'
```

结果：8 pass、150 filtered、0 fail、117 expects。覆盖临床关闭态、患者上下文和失败清理；不包含真实题库、真实评分、Provider、DevTools、真机或生产业务验收。

### 3.3 临床契约门禁

执行：

```text
pnpm clinical:contract:audit
```

结果：通过。风险评估相关实现继续保持 `contract-pending`/关闭态；门禁报告显示当前临床域实现仍 fail-closed。命令报告本机 Node `v26.8.1`、项目声明 `24.12.0`，版本差异需在正式复现时消除。

## 4. 分层验收状态

| 层级 | 风险评估 | 健康自测 |
| --- | --- | --- |
| 旧端事实 | 3 类入口，跌倒 6 组题目，列表/创建并按 `user_id + pat_id` 覆盖 | 7 套题库、题目读取、服务端评分和结果展示；首页另有 2 个计算器 |
| 新端静态代码 | 关闭态原生外壳，无读写调用 | BMI/血压安全本地子集，无题库、分级或结果持久化 |
| 规则/题库版本 | 未冻结 | 未冻结 |
| 临床审核和适用人群 | 未冻结 | 未冻结 |
| 患者授权、幂等、撤回、保留 | 未冻结 | 未冻结 |
| Provider / staging | 未验收 | 未验收 |
| DevTools / 真机 / 生产 | 未验收 | 未验收 |

## 5. 处理决定

1. P1-15 保持未完成，风险评估与健康自测继续使用独立开关和独立 contract。
2. 不迁移旧 `user_id + pat_id` 覆盖式风险记录，不复制旧题库、评分区间、医学建议或旧结果缓存。
3. 保留 BMI/血压非诊断安全子集；若要开放分类、风险等级、分诊或建议，必须先建立临床审核的规则 bundle 和结果解释 contract。
4. 待规则版本、适用人群、授权、幂等、撤回/失效、保留、审计、Provider 和多环境验收证据齐全后，再分别按真实旧端范围实现。

## 6. 入口与题库承载复核（2026-09-17）

- 旧端健康自测 9 个入口（动脉血管、2 型糖尿病、心脏功能、肺功能、心理年龄、老年痴呆、心理压力、肥胖指数、血压指数）与新端 `health-test.ts` 的 9 个入口一致；前 7 个进入独立原生题目承载，后 2 个进入 BMI/血压安全工具。
- 旧 Python `QUESTION_CONFIGS` 的 7 套题库在新端 `self-test-question.ts` 均有对应题库结构；新端只允许逐题填写和完整性校验，结果按钮明确提示未生成风险结论，没有复制旧评分阈值或旧结果文案到业务结果链。
- 本次安全子集/迁移边界回归：12 pass、0 fail、71 assertions；没有调用旧题库/评分接口、没有写入答案或结果。P1-15 继续未勾选，待题库版本、临床审核及结果 contract 齐备后再开放评分。

## 当前顺序复核补充（2026-09-17）

重新核对三类风险量表、七类自测题流和 BMI/血压工具，确认旧题目/选项已有原生承载；结果按钮仍不生成风险结论，BMI/血压仅保留 `local-non-diagnostic-v1` 数值计算和格式校验。

定向回归共 11 pass、0 fail、83 assertions：安全计算器 6/17、医疗服务入口 1/55、临床关闭态 4/11。未调用旧题库/评分接口、未写入答案或结果、未导入旧数据；题库/规则版本、临床审核、适用人群、结果授权和审计 contract 仍缺，因此 P1-15 不勾选。
