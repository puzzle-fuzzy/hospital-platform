# 入院/预约前预问诊二次验证（2026-09-16）

## 结论

P1-13 的两条旧链路均确认存在“提交意图”，但不是同一个服务：入院预问诊走旧 Python CRUD，预约前预问诊走旧小程序指向的外部接口。两条链路都把健康敏感文本和旧患者/预约标识交给旧系统，且缺少新平台必须的问卷版本、授权、幂等、撤回和医护读取 contract。新端两个页面继续保持关闭态是正确迁移结果，本次不复制旧题目、不接旧外部 URL、不新增写入 API。

本次只读复核没有修改旧服务、旧数据库、Provider、生产配置、支付或支付相关 HIS 回写代码。

## 1. 入院预问诊：旧服务确有覆盖写入

| 事实 | 旧源码证据 | 迁移含义 |
| --- | --- | --- |
| 题目和敏感字段 | `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/pagesB/health/admission_preconsultation.vue:128-203` | 固定 10 题包含疾病史、用药、手术医院和恢复情况，不能按普通页面字段直接复制 |
| 历史读取 | 同文件 `:290-337` | 读取旧 API 的第一条 `content`，按数组位置回填，没有任务/问卷版本选择规则 |
| 提交入参 | 同文件 `:340-404` | 从缓存取 `user_id/pat_id`，提交 `{user_id, pat_id, content}` |
| 旧 API | `/Users/yxswy/Documents/GitHub/hospital/app/api/v1/module_convenience/admission_preconsultation/controller.py:17-48` | 提供列表和提交路由，依赖旧用户认证 |
| 覆盖语义 | 同模块 `service.py:15-65` | 按 `user_id + pat_id` 查找，存在则覆盖、否则新增 |
| 存储模型 | 同模块 `model.py:9-17` | 只有 `user_id/pat_id/content`，没有 episode、版本、撤回、幂等和医护权限 |

## 2. 预约前预问诊：旧端只确认了外部调用意图

- 旧小程序定义 6 个症状/病程/过敏/烟酒/传染病/家族史问题，见 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/pagesB/health/pre_visit.vue:80-130`。
- 页面从缓存读取 `patId/patCardNo/hospitalId`，从路由读取 `hisRegisterId`，见同文件 `:143-167`；提交时以 `medicalCardNumber/registerId/hospitalId` 和自由文本组装请求，见 `:220-274`。
- 旧客户端只声明外部 `POST /msun-hzzn-app-config/v1/saveBeforeVisitRecord`，见 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/api/modules/health.ts:181-203`；旧 Python 仓库中未发现该外部服务的 controller、数据模型、成功/拒绝/超时最终状态或读取权限。

因此不能把旧 `patId`、`patCardNo` 或 `hisRegisterId` 当新平台授权凭证，也不能在没有外部正式 contract 的情况下通过兼容请求制造“已迁移”。

## 3. 新端二次验证

| 页面/层级 | 当前代码依据 | 结果 |
| --- | --- | --- |
| 入院预问诊 | `apps/miniprogram/src/pages/admission-preconsultation/admission-preconsultation.ts:1-4` | 仅注册 `registerClinicalContentSurfacePage`，不读取旧答案、不提交问卷 |
| 预约前预问诊 | `apps/miniprogram/src/pages/pre-visit/pre-visit.ts:1-4` | 仅注册统一临床内容关闭态，不接旧外部保存接口 |
| 关闭态 contract 提示 | `apps/miniprogram/src/services/clinical-content-surface.ts:31-53,119-133,180-221` | 入院页要求住院事件+问卷版本；预约前页要求具体预约上下文；两者均要求授权、幂等、撤回和临床审核 |
| 迁移覆盖 | `apps/miniprogram/src/services/migration-coverage.ts:165-172,195-201` | 仍归入 `clinical-content-write`，不代表写入已实现 |

## 4. 二次验证命令

- `pnpm clinical:contract:audit`：通过；临床域仍为 contract-pending/unregistered，代码保持 fail-closed。
- `bun test apps/miniprogram/scripts/acceptance.test.ts --test-name-pattern 'clinical|inpatient|patient context'`：8 pass、150 filtered、0 fail、117 expect calls；证明关闭态和患者上下文边界，不证明问卷读写可用。
- `pnpm docs:audit`、`pnpm todo:audit`、`pnpm migration:fact:audit`：均通过。

## 5. 未验证和处理决定

仍缺：住院 episode/预约权威来源、问卷版本和字段白名单、患者授权、答案覆盖/修改/撤回、重复提交和超时后最终状态、医护读取权限、外部服务正式响应、内容审计和真机同链证据。敏感答案不能写入普通日志、页面 fixture、报告、病历或风险记录。

P1-13 保持未完成：继续使用关闭态；不注册问卷 API，不复制 `user_id/pat_id`，不接入未确认的外部保存接口。未来材料齐备后，必须先建独立 contract 和版本化任务模型，再进行 staging、回滚、Provider、公网和真机验收；本项不得解锁任何支付或费用链路。
