# P2 占位能力结论性收口（2026-09-16）

## 结论

P2-01、P2-02、P2-03、P2-04 已完成“事实收口”，不是把这些能力实现完成：

- 快递：旧端只有假患者/本地空数组和查询 TODO，没有真实物流接口；不属于当前可迁移业务。
- 订阅：旧端开关、搜索、折叠和“设置已保存”都只是页面内存，没有 `wx.requestSubscribeMessage` 或服务端保存；不属于当前可迁移的微信订阅链路。
- 患者地址：旧 64 个页面和旧 action 清单没有地址页面或地址 API；新端 `patient-address` 现明确标记为“未来新需求”，不再伪装成旧服务迁移入口。
- 采血预约：旧端只有硬编码患者、固定院区、空项目和“功能开发中”，没有采血 Provider 号源/预约/取消调用；不复用普通门诊号源。

本次没有新增 Provider、微信、物流、地址或采血接口，没有发起外部请求，也没有触碰支付、医保、退费、收银台、账单或支付相关 HIS 回写代码。

## 旧服务代码依据

- 快递：`/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/pagesB/patient/express.vue:59-85` 使用本地默认患者、`BOUND_PATIENTS`/`CURRENT_PATIENT` 和空 `expressList`；患者切换处的查询仍是 TODO。
- 订阅：`/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/pagesB/user/subscription_message.vue:170-218` 只构造本地分类/开关并在确认时 Toast，没有微信授权或服务端持久化调用。
- 地址：旧仓库页面、页面注册和 API 搜索没有患者地址管理页面或接口；旧 `medicalRecord.ts` 中的 `addressName` 是住院字段，不是患者地址业务来源。
- 采血：`/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/pagesB/hospital/bloodAppointment.vue:45-101` 为固定空态、硬编码患者/院区和开发中 Toast，没有号源请求。

## 当前新端代码依据

- 快递 `apps/miniprogram/src/pages/patient-express/patient-express.ts:40-47,84-133` 只读取 owner-scoped 当前患者并展示明确物流未开放状态，不读取旧缓存中的物流或 Provider 患者号。
- 订阅 `apps/miniprogram/src/pages/patient-subscription/patient-subscription.ts:68-79,281-300` 将项目 `enabled` 固定为 `false`，不调用微信订阅授权，页面只保留安全展示和状态入口。
- 地址 `apps/miniprogram/src/services/feature-navigation.ts:251-259` 现在明确写出“未来新需求”和“旧服务没有对应”，`apps/miniprogram/src/services/migration-coverage.test.ts` 增加无旧来源断言；没有地址页面、API 或 action 入口。
- 采血 `apps/miniprogram/src/pages/blood-appointment/blood-appointment.ts:103-150` 只读取当前患者、展示院区准备中/服务未开放，不复用普通预约号源。

## 可复现验证

执行：

```text
bun test apps/miniprogram/scripts/acceptance.test.ts --test-name-pattern 'legacy express|blood appointment|subscription page'
bun test apps/miniprogram/src/services/migration-coverage.test.ts
```

结果：占位页面验收 3 pass、154 filtered、0 fail、35 expect calls；迁移覆盖聚合测试 14 pass、0 fail、76 expect calls。

测试证明的是“旧端真实占位行为没有被伪造成新业务”，不证明物流、微信订阅、患者地址或采血服务存在。

## 后续边界

如果业务未来确实需要这些能力，必须分别取得物流来源和患者归属、微信模板/授权时机/业务事件、地址字段与删除规则、采血项目/院区/号源/预约状态等正式材料，再按独立 contract 实现。当前不能因为状态页或计划型 FeatureKey 存在就新增请求、持久化或成功提示。
