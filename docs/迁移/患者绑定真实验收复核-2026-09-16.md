# 患者绑定真实验收复核（2026-09-16）

## 结论

P1-01 仍未完成，当前保持绑定能力的安全关闭态。本次复核确认新端已有一条受 owner、幂等和配置门禁约束的服务端链路，但仓库测试只能证明平台代码边界，不能替代当前机构 Provider、旧服务用户授权、真机和生产验收。

本次没有调用众阳患者绑定或生产业务接口，也没有改变任何支付、医保、收银台、账单或支付相关 HIS 回写代码与配置；旧服务仅接受了一个明确无效的微信登录探针码，未产生有效认证结果。

本轮仅做了不带业务参数的连通性探针：`https://gpsrmyy.meiyi.pro/` 返回 HTTP 200，`https://test-hp.meiyi.pro/api/v1` 根路径返回 HTTP 404。两者不能证明任何登录、卡类型、查档、建档、绑卡或最终目录确认事实；没有发送患者绑定业务 POST，也没有接触真实患者数据。

本轮又向旧服务明确发送了一个无效的微信登录探针码（不是真实用户 code）：`POST https://test-hp.meiyi.pro/api/v1/system/auth/login/wechat` 返回 `success=false`、`code=-1`、`status_code=500`、`data=null`，消息为“换取openid异常: code无效”；未返回 token、unionId 或患者字段。该结果只确认旧认证端点当前可达且无效 code 的错误包络，不能证明有效登录、unionId 一致性或患者绑定能力，也未触发患者建档/绑卡。

本次再次检查当前项目环境，仅记录配置状态而不打印值：`.env` 已写入众阳基础地址、由字典探针得到的 `orgId=10389`、`hospitalId=10389001`、`cardTypeId=3`，以及旧小程序 production 配置中的旧服务认证根地址；但患者绑定 gate 仍为 `false`、众阳授权 token 配置项为空，旧服务用户登录/unionId 一致性尚未验证；当前未发现正在运行的新 API 进程。因此没有条件进行真实绑定回归，绑定能力继续保持 fail-closed。本轮对官方卡类型字典执行了不带患者字段、且未使用有效 Authorization 的只读请求，返回 HTTP 200、`success=true`、11 条字典项，均显示 `hospitalId=10389001`、`orgId=10389`，其中 `cardTypeId=3` 为“身份证”。该响应只能作为当前上游默认字典的低风险事实，不能证明用户级授权、患者端自助绑卡或建档/绑卡 contract。

## 旧服务可迁移事实

- 旧客户端 `hospital-app/src/api/modules/ZY.ts:17-75` 只声明患者档案查询、建档和绑卡调用；它没有提供命令幂等、最终确认、owner 绑定或失败补偿语义。
- 旧页面 `hospital-app/src/pagesB/patient/patientAdd.vue:81-111,199-256` 把查档、建档和绑卡串在一次提交中；查档异常会继续走建档，建档后再绑卡。页面还把证件号、固定卡类型和卡号假设混在一起，不能作为新端字段 contract。
- 旧页面的协议勾选和普通用户资料副作用也没有版本化同意、独立授权或患者档案更新 contract；这些边界继续由 `docs/迁移/患者绑定契约草案.md:104-129` 约束。

因此，本次没有为了“补齐旧页面”直接复制旧接口或把旧端异常分支迁移到新端。

## 新端代码核对

- 小程序 `apps/miniprogram/src/pages/patient-binding/patient-binding.ts:110-163` 只提交展示姓名、手机号、身份证号、同意事实和旧服务登录码；它不提交 `openid`、`unionId`、Provider 患者号或 Provider 地址。
- API `apps/api/src/modules/patients/index.ts:31-96` 从当前会话解析 owner，绑定和同步要求受限的 `idempotency-key`；患者目录 GET 也不接受客户端 owner 参数。
- 服务 `apps/api/src/modules/patients/binding-service.ts:183-323` 先校验身份证、同意和 owner，再通过旧服务授权链核对 unionId，调用 Provider gateway，最后用独立的同步幂等键刷新目录；当前还会用 Provider 返回的 HIS 患者引用反查 owner-scoped 临床映射，只有确认本次患者已进入目录才返回成功。目录未出现时只重试只读同步，不重复建档或绑卡。
- Provider adapter `packages/adapters/src/zhongyang-patient-binding.ts:10-24,205-295` 将档案查询、建档和绑卡路径分开，并要求 `orgId`、`hospitalId`、当前机构卡类型和真实 `cardNo`；不再把身份证号当作医疗卡号。
- 配置门禁 `packages/config/src/index.ts:568-603,1090-1097,1139` 要求绑定 gate、目录 gate、HTTPS Provider 地址、机构/院区/卡类型及旧服务认证地址同时满足；缺少任何一项时不得误开写入。

## 可复现验证

执行：

```text
bun test apps/api/src/modules/patients/binding-service.test.ts packages/adapters/src/zhongyang-patient-binding.test.ts
```

历史记录结果为 8 pass、0 fail、31 expect calls；本轮按当前用户工作树重新执行 Provider adapter、绑定服务和运行配置回归，结果为 21 pass、0 fail、104 assertions。两者都只证明本地代码边界，后者是当前候选源码的最新结果。

已覆盖：

1. 身份证派生出生日期/性别，并使用独立同步幂等键；
2. 未同意或非法身份证在下游调用前拒绝；
3. 旧服务授权 JWT 只进入众阳上下文；
4. 首次目录确认失败时的确认窗口重试；
5. 旧服务身份 unionId 与当前 owner 不一致时拒绝；
6. 已有档案只查档并绑卡；
7. 明确无档案时才建档再绑卡；
8. 绑卡返回成功但目录仍为空/缺少本次 HIS 患者映射时，不伪造成功，只在确认窗口内重试同步。

本次新增的“绑定后 HIS 引用反查”回归覆盖：目录确认成功、目录短暂未出现后重试、目录始终未出现时拒绝成功；绑定服务/患者目录/Provider/持久化定向测试共 56 pass、0 fail、192 assertions。该结果仍是本地替身和代码边界证据，不是实际 Provider 验收。
8. 相同 owner/幂等键的并发提交只产生一次 Provider 操作，材料冲突复用幂等键会拒绝。

另执行 API 组合边界回归：

```text
bun test apps/api/src/app.test.ts
```

结果：55 pass、0 fail、386 expect calls。回归同时确认未认证请求先于实名 payload 校验返回 401；已认证绑定请求才进入绑定服务，服务上下文收到受限幂等键，响应仅保留平台患者目录的脱敏字段，不返回 Provider 患者号、完整卡号或 owner 标识。

这组测试没有证明 Provider 真正接受当前机构参数，也没有证明旧服务认证、数据库持久化、真机提交、生产日志和回滚流程。

小程序患者绑定入口另执行：

```text
pnpm --filter @hospital/miniprogram exec bun test scripts/acceptance.test.ts --test-name-pattern 'patient binding|患者绑定|patient-binding|wx.login'
```

结果：2 pass、0 fail、38 assertions。该回归确认入口使用一次性 `wx.login` code，未把旧 JWT、Provider 患者号或原始 Provider 地址交给小程序；仍不替代微信开发者工具/真机提交验收。当前 development 包已生成到用户指定目录并通过 `runtime:verify:dev`，但本机开发者工具仍打开旧仓库包，且 CLI 因 Service Port 未开启无法自动切换，因此没有新增 DevTools 业务结论。

本轮同时复核运行配置：`ZHONGYANG_PATIENT_BINDING_READY=false`、`ZHONGYANG_PATIENT_DIRECTORY_READY=false`，众阳授权 token 配置项为空；未发送患者绑定写入请求。

对旧服务配置目录的再次只读核对显示：旧服务生产环境文件只有通用微信、MBS 和支付侧配置，旧小程序环境只有 AppID 与医院 ID；未发现可用于患者绑定的 Provider token、患者专用认证配置、映射文件或测试用户凭证。旧后端代码中声明的可选认证字段不等于实际授权。该核对没有输出密钥值，也没有调用患者绑定写接口。

## 开发者工具页面复核（2026-09-16 14:00-14:01）

开发者工具已从旧项目 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/dist/dev/mp-weixin` 切换到当前新项目 `/Users/yxswy/Documents/GitHub/hospital-platform/apps/miniprogram` 并执行普通编译。新首页的“就诊人绑定”入口可达：先打开 `pages/patient-select/patient-select`，再打开 `pages/patient-binding/patient-binding`；实名表单实际显示姓名、手机号、身份证号、协议勾选和“提交添加”。本次只做页面访问，没有填写、勾选或提交，不产生患者绑定写入。

开发者工具返回患者选择页时短暂出现“当前微信账号暂无已绑定的就诊人 / 就诊人服务正在完善中，暂时无法新增就诊人”的关闭态，随后首页重新加载当前平台目录并恢复脱敏当前就诊人展示；该现象只记录为当前模拟会话的页面状态变化，不作为 Provider 绑定成功或失败结论。当前配置 gate 仍为 `false`。

该次工具运行还确认首页底部 Tab 为“医疗服务 / 就诊 / 互联网医院 / 我的”，没有“智能陪诊”标题；进入 `pages/consultation/consultation` 后能看到当前就诊人/院区上下文和兼容历史摘要，日志记录 `/appointments/records` HTTP 200。返回 `pages/electronic-consultation/electronic-consultation` 后能看到电子导诊记录、加载更多入口和“缴费账单 / 病历查询 / 住院预约”三个入口，页面同时保留“旧服务兼容版、实时导诊与执行状态未开放”的明确提示。上述均是页面渲染和当前平台只读请求证据，不替代真实外部问诊会话、Provider、真机或生产验收。

开发者工具报告 1 个 `app.json` schema warning：`navigateToMiniProgramAppIdList`。该配置与旧端医保小程序跳转白名单及现有静态测试相关，本轮不擅自删除；后续需要在不破坏旧端跳转边界的前提下单独确认工具版本兼容性。

页面复核还发现患者选择页从新增页返回时的加载时序问题：清空目录后同步尚未完成，旧代码会提前退出 loading，短暂渲染“暂无已绑定的就诊人”。现已改为保持 loading，直接复用带页面令牌的同步流程，并在同步结束后退出 loading；定向回归 2 pass、0 fail、57 assertions，development 包已重新生成并通过运行校验（52 页，`sourceRevision=workspace-sha256:a103eb598a7e64172bd60ac356694ec0d7f4c218109be43ddef4d2dfa91e2b83`）。该修正不改变绑定 gate，也没有产生患者绑定写入。

修正后的完整小程序回归为 439 pass、0 fail、4818 assertions；开发者工具重新普通编译后新首页仍可加载，底部 Tab 保持“医疗服务 / 就诊 / 互联网医院 / 我的”。这只证明当前候选代码和页面状态边界，没有把模拟会话中的患者目录或 HTTP 200 当作真实 Provider 绑定完成。

## 未完成与放行条件

仍缺少以下实际证据：

- 当前机构 2.1.55 卡类型字典中的 `cardTypeId`，以及 2.1.51/2.1.52 是否允许患者端自助建档和绑卡的书面确认；
- 可在测试环境使用的旧服务用户认证配置与一次脱敏的授权交换结果，且必须确认交换出的 unionId 与当前平台 owner 一致；
- 查档无记录、重复档案、超时/5xx、建档成功后绑卡失败、绑卡结果未知和最终查询的 Provider 响应样例；
- 测试环境数据库/命令恢复与重复点击验证，以及小程序真机上成功、拒绝、超时和重新同步表现；
- 生产发布后的 owner 归属、Provider requestId、审计事件和目录快照证据。

在这些证据到达前，P1-01 继续标记为“代码具备，真实验收阻塞”，不打开 `ZHONGYANG_PATIENT_BINDING_READY`，不修改旧服务，不新增患者编辑/解绑能力。

## 装配层安全补强（2026-09-17）

本次回归发现：旧服务认证根地址即使存在，也不应在患者绑定网关未配置时被患者绑定服务使用。已在 `apps/api/src/application.ts` 收紧默认装配，只有 `patientBindingGateway` 与 `patientProviderAuthorizationGateway` 同时存在时，才把旧服务认证传入绑定服务；缺少绑定网关时先抛出 `DependencyNotConfiguredError("patient-binding")`，不会产生旧服务认证请求。

`apps/api/src/application.test.ts` 新增装配层回归：注入会抛错的旧认证探针，调用关闭态绑定，结果为 `DependencyNotConfiguredError`，认证调用次数为 `0`。定向 API 回归共 70 pass、0 fail、436 assertions，`pnpm typecheck` 13/13 通过。该测试不使用真实患者、不调用 Provider、不导入旧数据；患者绑定 gate 仍保持关闭，真实 owner/Provider/真机/生产证据仍未验证。
