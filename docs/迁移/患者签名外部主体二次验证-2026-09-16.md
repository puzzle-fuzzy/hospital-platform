# 患者签名外部主体二次验证（2026-09-16）

## 结论

本次重新核对旧端患者签名页面的患者列表来源、外部小程序跳转参数、成功/失败处理，并验证新端是否仍存在外部调用或患者标识外发。旧端确有 `navigateToMiniProgram`，但目标主体、path、回跳、签名结果和授权协议均无法由当前仓库证实；旧端还把客户端患者 ID 和姓名放进 `extraData`。新端使用 owner-scoped 脱敏患者目录，点击只保留关闭态提示，不调用外部小程序、不上传签名、不改变全局当前患者。P1-18 继续保持未完成。

本项只涉及患者签名外部入口，不修改旧服务、旧数据库、Provider、线上配置或支付、医保、混合支付、自费支付、退费、收银台及支付相关 HIS 回写代码。

## 1. 旧服务真实行为复核

旧页面先展示患者列表，选择患者后在微信小程序环境调用 `uni.navigateToMiniProgram`，见 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/pagesB/patient/patient_signature.vue:20-83,96-105,104-130`。跳转参数为硬编码 `appId=wx0b76c9904392518f`，没有明确 `path`，并把 `patientId`、`patientName` 放入 `extraData`；成功回调为空，失败只打印并 Toast，没有签名任务、回跳状态或结果校验。

旧列表还包含本地伪患者，并可从 `BOUND_PATIENTS` 任意字段映射患者 ID、姓名、性别和手机号，见同文件 `:97-101,139-149`。这些字段不是已证明的 owner-scoped 医院患者读模型，不能作为签名主体或外部授权凭证。

当前仓库没有目标小程序主体资质、正式入口 path、最小字段白名单、签名结果回传、回跳校验、文件访问和撤回协议。不能从旧 appId、跳转成功或目标页面可打开推导签名业务完成。

## 2. 新端二次验证

- `apps/miniprogram/src/pages/patient-signature/patient-signature.ts:1-19,70-129` 通过 `loadPatients()` 读取当前账号 owner-scoped 患者目录，并在会话轮换和页面返回时重新加载；不会从本地示例患者恢复列表。
- `apps/miniprogram/src/pages/patient-signature/patient-signature.ts:50-61,132-142` 只把受控关系翻译为展示文案；患者可用时更新页面选中态并提示“签名功能尚未开放”，不可用时拒绝选择。
- 同文件没有 `navigateToMiniProgram`、旧 appId、`extraData`、签名文件上传或回跳处理；`patient-signature.wxml:20-77` 只显示脱敏卡号、关系、状态和协议/进度入口。
- `apps/miniprogram/src/services/feature-navigation.ts:278-286` 明确签名仍待用途、文件安全、同意、撤回和外部会话 contract。

当前已验证的只是安全展示和关闭态，不是外部签名业务。

## 3. 本次验证结果

执行：

```text
bun test apps/miniprogram/scripts/acceptance.test.ts --test-name-pattern 'patient signature'
```

结果：通过；覆盖真实页面落点、owner-scoped 患者目录、脱敏卡号、协议入口，并断言新端不存在 `navigateToMiniProgram` 和旧硬编码 appId。测试证明新端没有恢复旧外发边界，不代表外部签名主体或生产签名链路已验收。

同时复核了 `apps/miniprogram/src/services/migration-coverage.test.ts` 中患者签名覆盖断言，确认该入口仍归入患者写入 contract，而非被误报为已经完成的外部能力。

## 4. 分层验收状态

| 层级 | 状态 | 依据/缺口 |
| --- | --- | --- |
| 旧端跳转 | 已确认 | 微信跳转、硬编码主体、患者 ID/姓名外传、空 success 回调 |
| 外部主体/path/用途 | 未确认 | 缺主体资质、正式入口和环境对应关系 |
| 新端静态代码 | 已确认 | owner-scoped 脱敏列表和关闭提示，无外部调用 |
| 会话/授权/防重放 | 未冻结 | 缺一次性短期会话、用途绑定、撤销和回跳校验 |
| 签名结果/文件安全 | 未冻结 | 缺结果真实性、访问范围、删除和撤回规则 |
| Provider / staging | 未验收 | 当前没有可执行业务 adapter |
| DevTools / 真机 / 生产 | 未验收 | 需外部 contract 完成后补同主体证据 |

## 5. 处理决定

1. P1-18 保持未完成，不恢复硬编码 appId，不复制 `extraData.patientId/patientName`。
2. 继续保留新端脱敏患者目录、会话轮换清理和关闭态，不把外部页面打开当签名完成。
3. 待主体、path、一次性短期会话、授权、回跳、失败/撤回、文件安全和审计协议齐全后，再实现独立外部签名 adapter 与结果状态机。
4. 只有代码、生成包、DevTools、真机和生产证据分别具备后，才能勾选完成。

## 6. 当前回归记录（2026-09-17）

患者签名关闭边界与实时/固定 WebView 组合回归通过；新端仍不调用外部小程序、不外发旧患者标识。目标主体、path、短期签名会话、回跳和签名结果协议仍未确认，P1-18 继续关闭。

## 当前顺序复核补充（2026-09-17）

患者签名仍只展示 owner-scoped 脱敏患者并保持关闭态，不恢复旧硬编码 appId、`extraData.patientId/patientName` 或未知 path。未调用外部主体、未导入旧数据；目标主体、path、短期签名会话、回跳和签名结果协议仍未确认，因此 P1-18 不勾选。
