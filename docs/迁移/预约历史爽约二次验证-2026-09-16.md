# 预约历史与爽约二次验证（2026-09-16）

## 结论

P1-06 仍未完成。当前新端已按 `online/all` 分开表达历史范围，并只把服务端归一化的 `missed` 展示为爽约；本轮已将在线历史窗口改为与旧端一致的前后三个日历月，月底保留旧 JavaScript 的溢出语义，爽约查询仍有意保持过去 90 天安全边界。当前也没有同候选版本的 Provider、公网、DevTools dist、真机和生产证据。

本次只做非支付预约历史的静态和测试复核，没有调用 Provider，也没有触碰费用、支付、医保、退费、收银台或支付相关 HIS 回写。

## 旧服务与新端依据

- 旧端 `hospital-app/src/pagesB/user/my_registration.vue:186-220` 的在线/全部两个标签分别使用 `requestChannel=3/4`；在线查询按设备日期前后各三个月，全部查询不传日期。
- 旧端 `hospital-app/src/pagesB/user/miss_appointment.vue:208-233` 使用渠道 3、不传日期，并在客户端按旧 Provider 数值 `status === 4` 筛选爽约。
- 新端 `apps/miniprogram/src/pages/appointment-records/appointment-records.ts:213-318,367-393` 将标签映射为 `online/all`；`apps/miniprogram/src/services/dashboard-service.ts` 按 `Asia/Shanghai` 计算前后三个日历月并保留月底溢出；`packages/adapters/src/zhongyang-appointments.ts:1256-1313` 集中映射 Provider 渠道和日期；`apps/miniprogram/src/pages/missed-appointments/missed-appointments.ts:106-236` 只消费服务端 `missed` 枚举并使用过去 90 天窗口。
- 新端服务层和领域模型拒绝越权患者引用、非法状态和不安全 Provider 字段，见 `apps/api/src/modules/appointments/service.ts:956-1057,1079-1127`、`packages/domain/src/appointments.ts:916-1029`。

## 本次验证

执行：

```text
bun test packages/adapters/src/zhongyang-appointments.test.ts apps/api/src/modules/appointments/service.test.ts
bun test apps/miniprogram/scripts/acceptance.test.ts --test-name-pattern 'appointment records|missed appointments|derives missed'
```

结果：

- Provider adapter/API 预约目录与记录测试：54 pass、0 fail、172 expect calls；
- 小程序历史/爽约相关验收：5 pass、152 filtered、0 fail、55 expect calls。

测试覆盖的是 fixture 和源码边界，不证明当前 Provider 状态值、无日期查询是否可接受、真实记录分页或生产数据。

### 院区展示字段补漏（2026-09-17）

对照旧端记录卡实际使用的 `hospitalAreaName`，新端已将该可展示字段从 Provider `hospitalAreaName` 经过 adapter、domain/API 白名单校验后传到小程序；“我的挂号”显示院区，“爽约记录”将院区与科室位置分开显示。该字段不包含患者号、预约号、支付字段或身份证信息。

本次补漏回归：domain/预约 adapter 共 33 pass、0 fail、89 assertions；预约历史/爽约小程序筛选回归 20 pass、0 fail、245 assertions。development 包已重新构建并通过 `runtime:verify:dev`，当前 `pageCount=52`、`sourceRevision=workspace-sha256:b93d161f61ed50ba5fc5ff8b9188fdaf07a64de6da5f91b8b732a71c5c3e0f0b`。这些结果仍不替代 Provider 状态语义和真实历史数据验收，旧历史数据没有导入。

## 未完成与放行条件

仍需确认：

1. 旧端与新端均已使用“前后各三个月”日历窗口；仍需确认 Provider `endDate` 包含规则及月底溢出日期是否被当前合同接受；
2. 爽约是否允许旧端无日期的无界查询，以及 Provider 的保留期、分页和上限；
3. Provider 状态 0/1/3/4/5/6/7 的当前含义、未知状态和取消/爽约边界；
4. Provider 预约号到平台 opaque 详情引用的关联命中、重复/缺号/分页行为；
5. 同一候选版本的成功、空、拒绝、超时、切换患者/会话、公网、DevTools dist、真机和生产 trace 证据。

在证据齐全前，P1-06 继续保持未勾选；不把单元测试、空列表或客户端 `status === 4` 规则当作业务完成，也不扩大查询窗口。

## 详情交互复核（2026-09-17）

重新核对旧端 `my_registration.vue` 卡片点击和 `registration_detail.vue` 展示范围后，新端 `appointment-records` 已将列表中经 contract 校验的安全摘要导航至 `appointment-detail`。平台本地预约携带 owner-scoped `appointmentId`，详情页再读取服务端真实详情；Provider 历史摘要只进入只读详情版式，不携带旧 `patId/appointmentInfoId`，不显示取消或支付动作。预问诊仅对有平台预约引用的记录进入，院内导航继续使用受控静态科室位置弹层。

本次未调用 Provider、未执行取消/预约/支付，也未导入旧历史数据。页面回归和类型检查已通过；Provider 状态/日期边界、公网、DevTools、真机和生产同链证据仍缺，因此 P1-06 继续保持未完成。

本轮还补回了旧详情页地址区的“去导航”动作：新 `appointment-detail` 通过 `services/department-location.ts` 复用预约记录页的静态位置白名单和精确优先匹配，点击后展示科室位置弹窗；没有实时路线、未审核楼层或 Provider 写入。预约/导航定向回归 23 pass、0 fail、310 assertions，development 包重新构建并通过运行校验。该功能补漏不改变 P1-06 的 Provider 状态、日期窗口和真机/生产证据门槛。

## 详情导航注释与运行包复核（2026-09-17）

预约记录页此前有一段过时注释，称卡片点击会提示“详情暂未开放”；当前实际实现已经区分平台预约详情和 Provider 历史只读摘要。本轮已将注释改为当前真实行为说明，未改变数据范围或写入边界。预约记录、爽约和二级动作定向回归 6 pass、0 fail、93 assertions；随后重新构建 development 包并通过 `runtime:verify:dev`，包包含 52 个页面脚本，source=`workspace-sha256:cc0fba3`。旧历史数据未读取或导入，Provider 状态/日期范围及真机、生产同链证据仍缺。
