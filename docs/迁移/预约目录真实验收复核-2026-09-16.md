# 预约目录真实验收复核（2026-09-16）

## 结论

P1-04 仍未完成。平台侧预约目录、排班和号源只读代码及测试闭环良好，本轮已补充当前上游的无患者字段只读探针；源码对照已将旧端 `first-depts=3`、排班/医生/号源 `=4` 的渠道差异落到新 adapter，但仍必须由当前 Provider 请求/响应确认业务等价性。当前未打开任何写入 gate。

本次只做源码复核和隔离测试，没有触碰支付、医保、退费、收银台、账单或支付相关 HIS 回写代码。

## 旧服务行为

- 旧接口声明位于 `hospital-app/src/api/modules/appointment.ts:40-72,75-185,188-353`，包含 `first-depts`、`scheduling-depts`、`scheduling-doctors`、`schedulings`、排班详情和 `sources`。
- 旧页面实际调用位于 `hospital-app/src/pagesB/hospital/registration.vue:135-147,186-212`、`department_select.vue:440-458,555-572`、`doctor_card.vue:278-301`、`timeslot_source.vue:98-153`。
- 旧页面字段和状态不能直接作为新端公开模型；Provider ID、停诊状态、号源数和日期窗口都需要服务端重新校验。

## 新端代码核对

- API 路由在 `apps/api/src/modules/appointments/index.ts:84-98,162-232`；小程序目录、排班和号源入口在 `apps/miniprogram/src/pages/appointment-directory/appointment-directory.ts:87-185`、`appointment-schedule.ts:151-348`、`timeslot-source.ts:24-99`。
- `packages/adapters/src/zhongyang-appointments.ts:1056-1253` 固定服务端请求渠道、日期窗口和 opaque 引用，并以 `usableSourceNum` 作为已确认号源字段；旧端 `scheduleStatus` 已映射为有限的 `availabilityStatus`，只有 `open` 且有余号才允许进入号源页，缺失状态为 `unknown`，类型异常整批拒绝。未知二级科室、重复排班号、无效日期和不能形成安全读模型的响应会在触网前或映射时拒绝。
- 新端医生模式现通过独立 `/appointments/doctor-schedules` 读取旧端 `scheduling-doctors`，adapter 只在内部展开外层医生字段和 `schedulingList`；该接口缺少 `scheduleStatus` 时保留 `unknown`，小程序只显示观测余号，进入具体号源前仍由日期模式重新读取 `schedulings`。日期模式和具体号源仍不复用医生接口的未知状态。
- `apps/api/src/modules/appointments/service.ts:229-232,818-874` 对目录/号源读取使用配置 gate、短期服务器快照和受限日志；服务层不会把非法字段、未知渠道或快照失败静默转成成功。

## 可复现验证

### Provider 只读探针（2026-09-16，渠道修正前历史证据）

通过当前项目 `ZHONGYANG_BASE_URL` 直接调用新端已实现的 adapter，只带服务端生成的探针 trace/idempotency，不带患者、`patId`、号源写入或支付字段；当前 `.env` 未配置有效众阳 Authorization。该探针执行时排班/号源 adapter 尚固定使用 `requestChannel=3`，早于本轮按旧端源码拆分为目录树 `3`、排班/号源 `4` 的修正，因此以下结果仅是修正前历史观察，不能证明当前渠道 `4` 的返回等价性。观察结果如下：

| 操作 | 日期/范围 | 结果 |
| --- | --- | --- |
| `first-depts` | — | HTTP 业务成功；21 个一级分组、75 个二级科室 |
| `scheduling-depts` | 2026-09-16 至 2026-09-16 | HTTP 业务成功；0 个可预约科室 |
| `scheduling-depts` | 2026-09-17 至 2026-09-23 | HTTP 业务成功；61 个可预约科室 |
| `schedulings` | 2026-09-17 至 2026-09-23，取首个 Provider 科室 | HTTP 业务成功；9 条排班、318 个可用号源，9 条均映射为 `availabilityStatus=open` |

Provider 响应未观察到独立的外部 request ID，`requestJson` 因此回退使用本次平台 trace；这些 trace 只能关联本次探针，不能冒充 Provider 原生请求号。该历史探针证明上游在当时渠道 `3` 的这组无患者只读请求下返回了可解析目录/排班，不证明按旧端对齐后的 `requestChannel=4` 在所有排班/号源端点均业务等价，也不证明当前候选 API、DevTools dist 或真机链路一致。

### 当前上游渠道 4 只读探针（2026-09-17）

本次直接对当前 `.env` 中的 `ZHONGYANG_BASE_URL` 发起无患者字段的 HTTPS GET，只读取目录、排班和号源，不调用锁号、预约、取消、支付、医保或任何写入接口；当前授权 token 未配置，因此请求也未注入用户级患者身份。仅记录脱敏后的状态、数量和字段键，不保存原始 Provider 响应。

| 操作 | 请求渠道/范围 | 结果 |
| --- | --- | --- |
| `first-depts` | `requestChannel=3`，不带患者字段 | HTTP 200，`success=true`，返回 21 个一级目录条目 |
| `scheduling-depts` | `requestChannel=4`，2026-09-17 至 2026-09-23 | HTTP 200，`success=true`，返回 61 个可预约科室条目；首条包含 `deptId/deptName/roomAddress/professionalName/usableSourceNum` 等字段 |
| `schedulings` | `requestChannel=4`，首个安全科室，`scheduleType=1` | HTTP 200，`success=true`，返回 9 条排班；首条包含医生、职称、专长、科室位置、日期、时段、状态和号源字段 |
| `sources/{hisScheduleId}` | `requestChannel=4`，取首条排班的内部 Provider 排班引用 | HTTP 200，`success=true`，返回 40 个号源条目；首条仅记录 `serialNumber/workDate/workTime` 等字段键 |

这组探针证明当前上游在渠道 `4` 下能返回可解析的目录/排班/号源响应，并与新 adapter 当前固定渠道一致；它仍不能单独证明字段业务语义、正式 Provider contract、患者归属、平台 API 同链、DevTools dist、真机或生产验收。P1-04 继续保持未完成。

### `scheduling-doctors` 独立医生目录复核（2026-09-17）

继续使用同一批 `scheduling-depts` 返回的首个安全科室，直接对当前上游执行无患者字段的 HTTPS GET：`scheduling-doctors?requestChannel=4&startDate=2026-09-17&endDate=2026-09-23&deptId=<受控科室>`。请求未注入用户 token，也未调用锁号、预约、取消、支付、医保或其它写入接口。

- Provider 返回 HTTP 200、`success=true`，医生条目数为 5。
- 医生条目包含 `docId/docName/postTitleName/introduce/specialty/doctorPic/workDate/totalUsableNum/schedulingList` 等旧端医生卡字段；嵌套 `schedulingList` 包含 `hisScheduleId/totalNum/usableNum/shiftName/registrationFee/timeGroupFlag` 等排班字段。
- 当前新端医生模式仍通过平台 `/appointments/schedules` 读取 `schedulings` 扁平排班，再由 `groupAppointmentDoctorCards` 聚合。因此当前代码已覆盖“有排班医生”的卡片、职称、介绍/擅长、头像、日期和余号展示，但没有独立承诺旧端 `scheduling-doctors` 返回的零可约医生、`totalUsableNum` 和完整 `workDate` 集合语义；Provider 两种返回的正式等价性也尚未由院方/Provider contract 确认。

本次只记录数量和字段键，不保存原始响应。该探针补充了旧端接口存在及当前上游可读的事实，但不替代正式字段合同、平台 API 同链、DevTools/真机/生产验收；在明确医生卡投影合同前不开放任何预约写入，P1-04 继续保持未完成。

### 当前代码回归复核（2026-09-17）

按 TODO 顺序重新执行预约目录 adapter、预约 service、预约目录视图和 dashboard 白名单回归：共 105 pass、0 fail、335 assertions。回归覆盖目录树、三级门诊、医生嵌套排班、日期模式、号源、`requestChannel` 分流、空结果、停诊/未知状态、非法日期、重复标识、`usableSourceNum`、快照过期/写入失败、Provider trace 及客户端展示白名单。测试全部使用受控 fixture；没有锁号、预约、取消、支付、医保或旧数据导入。

本次复核不改变 `ZHONGYANG_APPOINTMENT_DIRECTORY_READY`，也不把代码测试或既有无患者只读探针写成真实业务完成。P1-04 仍等待正式 Provider 字段合同、平台同链、当前候选版本 DevTools/真机和生产证据。

### 当前候选开发者工具只读页面（2026-09-16 14:12 CST）

使用当前源码项目 `/Users/yxswy/Documents/GitHub/hospital-platform/apps/miniprogram` 打开的微信开发者工具页面，不是旧项目或旧 `dist`：

- 首页“预约挂号”先进入 `pages/hospital-list/hospital-list`，院区卡片的“去挂号”再进入 `pages/appointment-directory/appointment-directory`。
- 预约目录页面实际渲染了标题“选择科室”、导诊入口、科室/医生搜索框及科室列表（截图中可见美容外科、儿科、口腔等）。
- 开发者工具日志记录了平台 `GET /appointments/department-tree` HTTP 200；这只能证明当前候选包到平台目录接口的页面读取链路成功，不等于 Provider 业务成功、可挂号或号源已确认。
- 选择“内科”→“内科专家门诊”后进入 `pages/appointment-schedule/appointment-schedule`；页面初次读取和点击一次“重新加载”均请求 `GET /api/v2/appointments/schedules?startDate=2026-09-16&endDate=2026-09-23&departmentId=201124` 并返回 HTTP 502。平台日志 requestId 分别为 `mp-mu46lzvp-udrqaxdp`、`mp-mu46mbrg-vx4rbc57`，页面两次均正确展示“预约信息暂时无法获取，请稍后再试”，未伪造排班或号源。
- 本次没有点击具体号源、确认页、锁号、预约、费用或支付动作；排班/号源的 Provider 同链证据仍未完成，当前 502 需要服务端/上游继续排查。

执行：

```text
bun test packages/adapters/src/zhongyang-appointments.test.ts apps/api/src/modules/appointments/service.test.ts
```

结果：本轮预约 adapter 独立回归 26 pass、0 fail、65 assertions，预约 service 独立回归 29 pass、0 fail、111 assertions；此前小程序目录、日期视图回归仍为 72 pass、0 fail、212 expect calls。所有 Provider adapter 状态映射和客户端目录白名单均使用受控 fixture 验证。

已覆盖目录、排班、号源、空结果、Provider 业务失败、非法日期、重复 ID、`usableSourceNum`、快照过期/写入失败、Provider trace 和未知请求字段等边界。测试均使用受控 fixture，不代表当前 Provider、公网、开发者工具、真机或生产返回。

本轮渠道修正后重新生成用户指定 development 包：`build:dev` 与 `runtime:verify:dev` 通过；随后补齐院区展示字段并再次生成，当时 `build-info.json` 为 `sourceRevision=workspace-sha256:b93d161f61ed50ba5fc5ff8b9188fdaf07a64de6da5f91b8b732a71c5c3e0f0b`、`generatedAt=2026-09-16T16:26:20.448Z`、`pageCount=52`。该记录是当时的历史候选，不是当前最新运行包；它只证明当时运行包可生成和校验，不替代 Provider/DevTools/真机验收。

### 医生卡展示补漏（2026-09-17）

对照旧端 `department_select.vue:34-91,470-533` 后发现，旧“按医生挂号”卡片实际展示医生职称，以及 `introduce || specialty` 作为描述；新端排班 contract/domain 虽已保留 `titleName/introduction/expertise`，原医生卡聚合却只展示姓名、头像、余号和出诊日期。本轮已将这两个受控展示字段补到 `AppointmentDoctorCard`、医生卡聚合和 `appointment-schedule.wxml/.wxss`，不引入 Provider 原始字段、不改变预约写入。

验证结果：`appointment-directory-view` 与 `dashboard-service` 定向回归 47 pass、0 fail、149 assertions；小程序 typecheck、`build:dev`、`runtime:verify:dev` 通过。当时 development 包为 52 页，`sourceRevision=workspace-sha256:828b235b5d6fa8e90dc2cb3eb12d1911dedb00091a749d0c8430543bb22d8750`；后续 P1-11/P1-12 等补漏已继续重建，最新候选以 TODO.md 的“当前 development 运行包复核”为准。这只是源码/运行包展示补漏，不替代 Provider contract、平台同链、DevTools、真机或生产验收；P1-04 gate 继续关闭。

### 医生模式独立接口补齐（2026-09-17）

为对齐旧端 `department_select.vue` 的按医生模式，新服务新增只读 `/appointments/doctor-schedules` 路由和客户端请求入口。真实众阳 adapter 固定调用 `scheduling-doctors`，将外层医生信息和嵌套 `schedulingList` 映射为现有排班白名单；小程序医生卡改用该入口，日期模式仍调用 `schedulings`。由于 `scheduling-doctors` 响应没有 `scheduleStatus`，映射状态保持 `unknown`，页面展示“当前观测余号”，不允许医生卡直接进入号源；点击后以日期接口重新读取状态和号源。

验证结果：众阳 adapter 27 pass、0 fail、69 assertions；预约 service/API 定向回归通过；小程序排班视图/服务回归 48 pass、0 fail、152 assertions，API 与小程序 typecheck 通过。该功能补齐只读接口和页面调用，不执行锁号、预约、取消、支付或旧数据迁移；Provider 正式 contract、平台同链、DevTools/真机/生产证据仍缺，P1-04 继续保持未完成。

随后使用新 `createZhongyangAppointmentGateway().listDoctorSchedules()` 对当前 `ZHONGYANG_BASE_URL` 做真实只读映射复核：先通过渠道 4 的 `scheduling-depts` 取得受控科室，再调用 `scheduling-doctors`；未注入患者 token，未执行任何写操作。adapter 成功映射 9 条嵌套排班，输出只保留受控排班键，`trace.operation=appointment-doctor-schedules`；未保存原始响应。该结果只证明当前上游样本可被新 adapter 解析，不替代正式 Provider contract、患者归属、平台 API 同链、DevTools/真机或生产验收。

## 未完成与放行条件

仍需当前机构确认并留存同一候选版本的：

1. 排班/号源端点 `requestChannel=4` 与 Provider 当前合同的业务含义、`first-depts=3` 的适用边界；
2. `first-depts` 与 `scheduling-depts` 的层级/可预约关系，停诊、替诊和号源字段的正式语义；
3. 当前日期窗口、分页/快照有效期、空目录和 Provider 失败响应；
4. 一次成功、空、拒绝、超时和会话切换的 Provider requestId/traceId，及同版本公网、DevTools dist 和真机证据。

在这些证据到达前，保持 `ZHONGYANG_APPOINTMENT_DIRECTORY_READY` 关闭，不把本地测试或已有探针结果当作真实号源验收。

## 挂号类型名称展示补漏（2026-09-17）

继续对照旧端 `doctor_card.vue:130-132`，确认按医生/按日期排班卡都可展示 `registerClassName`。新端已将该字段按 `registerClassName → registrationClassName` 经过 adapter、domain/contract 和小程序白名单后展示；没有迁移旧端费用字段，也没有新增号别筛选或预约写入。定向回归 41 pass、0 fail、313 assertions，小程序全量回归 442 pass、0 fail、4862 assertions，`pnpm typecheck` 13/13 包通过，development 包 52 页且通过运行包校验。该补漏只证明受控只读展示，Provider 正式字段合同、平台同链、DevTools/真机/生产证据仍缺，P1-04 继续未完成。

## 医生卡排序补漏（2026-09-17）

旧端 `department_select.vue:505-553` 先按 `docId` 聚合医生，再按每位医生首条记录的 `visitCount` 降序排列；`party` 则被旧端明确写死为 `false`，接口没有党员标识，因此不迁移该徽章。新端此前只保留 `scheduling-doctors` 返回顺序，可能导致同一业务目录的医生卡顺序与旧端不同。

本轮在 `packages/adapters/src/zhongyang-appointments.ts` 的 `scheduling-doctors` adapter 内记录每位医生首条 `visitCount`，稳定降序重排后再展开嵌套排班；该字段只用于 adapter 内排序，不进入公共 API 或小程序。新增回归“众阳旧端医生目录按首条 visitCount 保持医生卡访问量顺序”：2 pass、0 fail、5 assertions。未调用 Provider 写入、未扩展费用/党员字段、未导入旧数据；P1-04 仍等待正式字段 contract、平台同链、真机和生产证据。
