# 我的医生关系二次验证（2026-09-16）

## 结论

P1-07 仍未完成。新端关系服务的 owner 隔离、医生目录复核、关注/取消关注幂等和未来排班窗口测试已通过，但旧端存量关系是否导入、医生关系失效规则以及旧端后续排班 `requestChannel=4` 与新端目录渠道 `3` 是否等价，仍没有当前 Provider/业务证据。

本次只验证非支付的医生关系能力，没有调用 Provider，没有处理预约写入、费用、支付、医保、退费、收银台或支付相关 HIS 回写；本轮补充了详情排班入口的状态门禁。

## 旧服务依据

- 旧服务路由在 `/Users/yxswy/Documents/GitHub/hospital/app/api/v1/module_convenience/my_doctor/controller.py:17-63`，提供列表、创建关注和 GET 删除取消关注。
- 旧客户端 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/api/modules/user.ts:38-110` 允许提交 `user_id`、数值 `doctor_id` 及客户端医生快照；旧页面 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/pagesB/hospital/doctor_card.vue:278-306,364-449` 关注前查询关系，后续排班请求使用 `requestChannel=4`。
- 旧关系是用户级客户端快照，旧接口没有医生目录版本、失效时间或跨候选迁移指纹；不能把旧行直接复制成新端关系事实。

## 新端依据

- `apps/api/src/modules/my-doctors/index.ts:23-107` 从 Bearer 会话解析 owner，不接受旧 `user_id`；输入只允许 opaque `doctorId`。
- `apps/api/src/modules/my-doctors/service.ts:100-248` 关注前通过未来 7 日预约目录确认医生存在，列表和取消均按 owner 与医生 ID 作用域处理，关系写入使用幂等上下文。
- 新端小程序已经有真实医生页和 API 调用，静态入口由 `apps/miniprogram/scripts/acceptance.test.ts:2103-2122` 保护；页面不再直接提交旧端医生资料快照。
- 本轮补回旧端医生名片的“查看简介”交互：新端详情页增加原生底部简介弹层和关闭动作，内容仍只来自当前服务端医生读模型的 `introduction/expertise`，不新增外部请求或旧数据读取。
- `my-doctor-detail` 详情排班入口现与预约排班页统一要求 `availabilityStatus=open` 且余号大于 0；`stopped/unknown` 只展示停诊/待确认并阻止进入号源页，避免把余号字段单独当成可预约事实。
- 新端排班采用预约目录 adapter 的受控字段与渠道；当前仍没有 Provider 请求/响应证明它等价覆盖旧端后续排班渠道，也没有库存关系导入决策。

## 可复现验证

执行：

```text
bun test apps/api/src/modules/my-doctors/service.test.ts
bun test apps/miniprogram/scripts/acceptance.test.ts --test-name-pattern 'my doctor|my-doctor|doctor relation'
```

结果：API 关系服务 5 pass、0 fail、15 expect calls；小程序医生页静态验收 1 pass、161 filtered、0 fail、14 expect calls。

本轮继续验证（2026-09-16）：小程序组合回归 `233 pass、0 fail、3011 assertions`，类型检查 `12/12 package` 通过；新增的账号切换回归要求 `my-doctor` 和 `my-doctor-detail` 在会话事件后清空旧关系/排班/关注态，并以新 owner 重读；本轮定向源码验收再次锁定详情页的 `open` 状态门禁。该验证仍是源码与 fixture 边界，不是 Provider、真机或生产业务证据。

本轮补漏回归（2026-09-16 14:45）：医生详情页“查看完整简介”及关闭弹层的源码验收已通过；小程序回归 `439 pass、0 fail、4824 assertions`，`build:dev` 和 `runtime:verify:dev` 通过。当前 development 运行包为 `sourceRevision=workspace-sha256:f2dea75ec67c94a02e98e961d1733b279aff01a363ced723250a46593f12404a`、`generatedAt=2026-09-16T14:45:12.491Z`、52 页。该包仍不替代 Provider、真机或生产业务证据。

测试证明代码边界，不证明旧关系库存量、当前 Provider 医生目录、公网、DevTools dist、真机或生产接受。

## 未完成与放行条件

仍需：

1. 旧关系记录的数量、owner 映射、医生 ID 映射和“导入/只读兼容/人工复核/不导入”决定；
2. 当前医生目录的稳定 ID、在职/科室变更/下线和关系失效语义；
3. 旧排班渠道 4 与新端目录渠道 3 的当前 Provider 对照；
4. 空目录、医生不存在、重复关注、取消不存在、Provider 拒绝/超时及会话切换的同候选版本证据；
5. 不触发预约写入的公网、DevTools、真机和生产 trace/审计验证。

在这些证据到达前，不导入旧客户端快照，不改变渠道，不把关注成功 Toast 或关系单测标作迁移完成。

## 挂号类型名称展示补漏（2026-09-17）

继续对照旧端 `doctor_card.vue:130-132`，确认医生排班卡展示 Provider 的 `registerClassName`（例如“专家号”）。新端本轮已将它作为非敏感、非费用的可选只读字段映射为 `registrationClassName`，贯穿众阳预约 adapter、预约 domain/contract、小程序 `dashboard-service` 白名单，并在预约排班页和“我的医生”详情排班卡展示；旧端 `registrationFee` 没有迁入。

验证：相关 adapter、dashboard、页面 acceptance 定向回归 41 pass、0 fail、313 assertions；小程序全量回归 442 pass、0 fail、4862 assertions；`pnpm typecheck` 13/13 包通过；development 包重建并通过 `runtime:verify:dev`，52 页，当前 `sourceRevision=workspace-sha256:ed89df29f00c877e3765ccb198dff0a7a079f6d49aee7f27995239c1ff667cc9`。本轮不调用 Provider 写入、不执行关注/取消、不导入旧关系或历史数据。Provider 正式字段合同、空/拒绝/超时和同候选版本 DevTools、真机、生产证据仍缺，P1-07 继续未完成。

## 院区展示字段补漏（2026-09-17）

对照旧 `doctor_card.vue` 的门诊排班卡片和当前众阳排班响应，确认 `hospitalAreaName` 是独立于科室位置的展示事实。新端现已将该可选字段从 Provider adapter 经过预约 domain/contract 与小程序客户端白名单传到页面：医生详情显示院区和科室位置，预约排班列表显示院区；缺失时保持不显示，不用固定医院名称补值。

本轮回归：预约 domain/adapter 33 pass、0 fail、89 assertions；预约/我的医生小程序定向验收 21 pass、0 fail、284 assertions；contracts、domain、adapters、API 类型检查通过。development 包重建与 `runtime:verify:dev` 结果以根 TODO 的最新指纹为准。该补漏不打开关注/预约写入、不导入旧关系或旧历史；Provider 关系失效语义、真实关注/取消、真机和生产同链证据仍缺，P1-07 继续未完成。

## 关注读取失败不阻断医生详情（2026-09-17）

旧端 `doctor_card.vue:248-249,365-380` 将排班读取与关注状态读取分开处理，关注列表失败时仍展示医生和排班。新端原先在 `my-doctor-detail.ts` 使用 `Promise.all`，导致关注服务暂不可用时排班详情也进入错误态。本轮改为 `Promise.allSettled`：排班是必要读取，关注列表是附加读取；关注失败只记录转换后的客户端错误并按未关注展示，关注/取消操作仍独立反馈，不把失败伪装成关注成功。

验证：小程序 typecheck 通过；医生/预约目录定向 acceptance 5 pass、0 fail、156 assertions。该补漏不调用 Provider、不导入旧关系、不打开关注写入；真实关系、Provider、真机和生产证据仍缺，P1-07 gate 继续关闭。

随后按当前源码重新生成用户指定 development 包，`build:dev` 与 `runtime:verify:dev` 均通过；`build-info.json` 记录 52 页、`sourceRevision=workspace-sha256:3d72ef9baf31917189424bb20e463c684501d69ae961d83eb42970f90dc9670c`、`generatedAt=2026-09-16T17:04:05.345Z`。运行包来源校验不替代关注关系、Provider、真机或生产验收。

## P1-07 顺序复核（2026-09-17）

重新对照旧医生名片的关注/取消、简介弹层、未来七日日期切换、排班号别、停诊/无号状态和预约入口，当前新端均已有对应功能；挂号费仍不进入公共 contract，等待正式金额合同。独立回归：我的医生 service 5 pass、0 fail、15 assertions；预约排班/医生视图 3 pass、0 fail、17 assertions；众阳医生/排班 adapter 8 pass、0 fail、20 assertions；小程序医生页面 acceptance 1 pass、0 fail、39 assertions。

本轮未调用 Provider 写入、未导入旧关系。真实关注/取消、Provider contract、旧关系失效语义及真机/生产证据仍缺，P1-07 保持未完成。
