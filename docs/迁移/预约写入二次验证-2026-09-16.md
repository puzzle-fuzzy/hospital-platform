# 非支付预约写入二次验证（2026-09-16）

## 结论

P1-05 仍未完成。新端确实有预约占位、创建、取消和 owner-scoped 详情的代码，但当前仓库无法从旧页面或本地 fixture 得到锁号 TTL、释放、费用/来源字段和 Provider 未确定状态的完整合同；本轮已按旧端源码将号源明细/实际费用读取渠道对齐为 `4`，但未改变锁号、创建、取消的未确认边界。本次没有调用 Provider，没有打开 `ZHONGYANG_APPOINTMENT_WRITES_READY`，也没有修改支付代码。

## 代码与旧服务对照

- 旧端只在 `hospital-app/src/api/modules/appointment.ts:355-393` 声明锁号、费用、执行预约和取消接口；确认页实际提交的 `patId`、身份资料、金额、排班号、号源号、`registerSource`、`settleWay` 等见 `hospital-app/src/pagesB/hospital/confirm_registration.vue:199-215,237-272`，取消见 `registration_detail.vue:380-423`。
- 旧端未发现确认页实际调用 `lockSourcesApi` 的证据，因此不能从页面行为推导锁号生命周期。旧端费用读取使用 `requestChannel=4`，新端号源明细/费用读取现已按旧端使用 `4`；锁号、创建、取消的渠道仍需当前 Provider 证据解决。
- 新端 API 路由在 `apps/api/src/modules/appointments/index.ts:100-160,234-255`；小程序只提交平台 opaque 的患者、排班、号源或 hold 引用，见 `apps/miniprogram/src/services/api-client.ts:2786-2831,3014-3066`。
- `apps/api/src/modules/appointments/write-service.ts:445-760` 负责服务端读取快照、患者映射、号源和费用、hold 过期、幂等、重复预约及本地记录；`:762-838` 通过 Provider 映射取消，并保留既有支付关联的取消前 fail-closed 检查。本次没有修改或验证支付边界。
- `packages/adapters/src/zhongyang-appointment-writes.ts:448-604,606-842` 在服务端重新解析号源并拼装 Provider 请求；锁号、费用、预约和取消都依赖当前 adapter 固定渠道和尚未完成的 Provider 合同。

## 可复现验证

执行：

```text
bun test packages/adapters/src/zhongyang-appointment-writes.test.ts apps/api/src/modules/appointments/write-service.test.ts
```

本次新增 `packages/adapters/src/zhongyang-appointment-writes.test.ts` 4 项测试，与 `apps/api/src/modules/appointments/write-service.test.ts` 合计 5 pass、0 fail、18 expect calls；`pnpm --filter @hospital/adapters typecheck` 和新增测试文件的 Biome 检查均通过。新增测试覆盖 19 位 Provider ID 保真、相等时间点不发送时间范围、锁号请求 body/trace、创建请求字段和非支付 `isPay=0`、取消映射，以及 unionId 绑定后再查档的患者资料解析；它们是本地 fake response 的请求形状证据，不能替代锁号 TTL、释放、Provider envelope 的正式合同或真实验收。

这个结果仍是缺口证据：本地 adapter/service 测试已补齐一部分请求形状和边界，但不能把它们包装成写入业务已验收，也不应为了填测试数量猜造 Provider 响应。

## 放行条件

必须先取得受控 staging 的锁号、费用、创建、取消和详情/查询请求响应，冻结渠道、字段、金额单位、TTL、释放、成功/拒绝/占用/超时/未知状态与最终确认；再补齐不依赖真实 Provider 的幂等、过期、重复、取消和未确定状态测试，最后用同一候选版本完成公网、DevTools dist、真机和数据库/Provider trace 关联验证。

在此之前，预约写入继续保持关闭；不把页面跳转、HTTP 200、旧端接口声明或本地预约记录当作 Provider 成功。

## 当前代码回归补充（2026-09-17）

- `packages/adapters/src/zhongyang-appointment-writes.test.ts`：5 pass、0 fail、18 assertions。
- `apps/api/src/modules/appointments/write-service.test.ts`：5 pass、0 fail、13 assertions。
- 小程序预约相关 acceptance（按 appointment/预约/hold/挂号/cancel/取消筛选）：22 pass、0 fail、259 assertions。

本轮仍未调用 Provider 写入接口，以上结果只证明当前请求边界、幂等/过期/重复预约/支付关联拦截和页面链路的本地实现；锁号生命周期、Provider 最终状态、当前渠道合同和真实预约闭环仍未验证。

## 跨系统失败恢复缺口（2026-09-17）

状态机复核确认：Provider 锁号或预约创建可能先成功，随后本地 `insertHold` 或 `insertRegistration` 持久化失败。当前 `AppointmentWriteGateway` 没有已确认的释放锁号、撤销未落库预约或按幂等键查询补偿 contract，旧端源码也没有可执行的锁号调用/回滚证据。不能用本地幂等键把两个系统伪装成原子事务；正式实现前必须冻结 Provider 补偿接口、最终状态查询、告警和重试规则。

本轮未调用 Provider 写入、未触发锁号/预约/取消、未修改 gate；该缺口作为 P1-05 的放行条件保留。

## 挂号详情导航补漏（2026-09-17）

旧 `registration_detail.vue` 的预约详情地址区包含“去导航”按钮；新端此前只有地址文字。本轮在 `apps/miniprogram/src/pages/appointment-detail/appointment-detail.ts/.wxml/.wxss` 补回该动作，并抽取 `apps/miniprogram/src/services/department-location.ts`，与预约记录页共用同一份已审核静态科室位置匹配逻辑。详情页和历史记录页均只展示静态位置弹窗：精确匹配优先，未匹配时显示空态，不生成楼层、不打开实时路线，也不携带 Provider 标识。

小程序预约/导航定向回归 23 pass、0 fail、310 assertions；`build:dev` 与 `runtime:verify:dev` 通过，development 包为 52 页，`sourceRevision=workspace-sha256:46cc8052c7905d3aa242dbec8286e07905d2de82555cc1db2b42bd28be07b14c`。本次没有调用 Provider 写入、锁号、预约、取消或支付，也没有导入旧历史数据。P1-05 的真实写入合同、锁号生命周期、跨系统补偿和同候选版本真机/生产证据仍缺，继续保持关闭。

## 确认页展示字段补漏（2026-09-17）

旧端确认页 `confirm_registration.vue:45-65` 实际展示“预约号别”；旧医生卡/排班上下文也保留院区信息。此前新端从号源页进入确认页时只传递科室、医生、日期、时段和序号，导致真实 `registerClassName`/院区字段没有进入确认页。

本轮已将服务端已白名单校验的 `registrationClassName` 与 `hospitalAreaName` 从 `timeslot-source` 传入 `confirm-registration`，确认页按字段存在性展示“预约号别”和“就诊院区”。缺失时不拼接旧端的演示号别，不恢复费用或 Provider 原始患者字段；预约写入仍由既有 gate 控制。

新增小程序 acceptance 1 pass、0 fail、34 assertions；小程序类型检查通过，development 包重新生成并通过 `runtime:verify:dev`。本轮没有调用 Provider 写入、没有锁号/预约/取消或支付，也没有导入旧数据；P1-05 真实写入 contract、锁号生命周期和跨系统补偿仍未验证。

## 写入后详情展示字段持久化补漏（2026-09-17）

继续沿 P1-05 核对时发现：新端排班 contract 已有 `registrationClassName` 与 `hospitalAreaName`，但 MySQL 短期排班快照和本地预约记录此前没有保存它们；真实预约写入后再次读取详情会丢失旧端确认页/详情页的号别与院区展示。本轮新增 `0049_appointment_display_context.sql`，让 MySQL 快照、预约记录、预约注册响应、挂号详情和挂号支付页贯穿这两个可选白名单字段；内存仓储也复用同一领域模型。字段缺失时仍不拼接演示文本，不保存 Provider 标识或患者敏感字段。

服务层“预约注册保留排班号别和院区到写入结果”回归通过；预约写入服务 4 项定向回归 10 assertions，MySQL 快照/SQL 相关回归 12 项 39 assertions，持久化与 API/小程序类型检查通过；小程序预约 acceptance 2 项 45 assertions 通过。development 包已重新生成，`runtime:verify:dev` 通过，52 个页面，当前 source=`workspace-sha256:f479482`。

本轮仍未执行 Provider 锁号、预约、取消、支付或医保调用，也未导入旧历史数据。migration 0049 需要在受控环境实际执行并验证旧数据库兼容性；Provider 正式写入合同、锁号 TTL/释放、跨系统补偿和真实预约闭环仍未验证，P1-05 继续保持关闭。
