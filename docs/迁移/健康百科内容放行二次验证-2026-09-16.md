# 健康百科内容放行二次验证（2026-09-16）

## 结论

健康百科的工程迁移链路已经具备，但真实内容不能放行。旧服务确实提供健康百科目录、症状查疾病、疾病详情和药品详情；当前新端也有对应的版本化只读 API、bundle 校验、staging 单事务导入和 published-only 读取。然而当前唯一健康知识源快照仍为 `publicationState=not-approved`、`publishable=false`，严格质量审计仍失败，独立审核 bundle 不存在。

本轮完成了源快照、导入边界、API fail-closed 和现有测试的二次验证；没有修改源快照、旧数据库、Provider、发布状态或患者端开放 gate。P1-12 保持未完成。

## 1. 旧服务真实来源

旧 Python 服务挂载健康百科路由见 `/Users/yxswy/Documents/GitHub/hospital/app/api/v1/module_knowledge/__init__.py:1-11`，具体目录、症状、疾病和药品路由见 `/Users/yxswy/Documents/GitHub/hospital/app/api/v1/module_knowledge/health/controller.py:15-203`。旧小程序实际调用见：

- `health_encyclopedia.vue:241-247,280-429`：目录、部位/症状和关联疾病；
- `search_result.vue:396,834-868`：症状查疾病和结果跳转；
- `disease_detail.vue:172-210`：疾病详情；
- `drug_detail.vue:129-162`：药品详情。

旧模型来源为 `knowledge_crowd`、`knowledge_department`、`knowledge_part`、`knowledge_symptoms`、`knowledge_disease`、`knowledge_drug` 和关系表，见 `/Users/yxswy/Documents/GitHub/hospital/app/api/v1/module_knowledge/health/model.py:12-182`。`knowledge_tips` 是另一个未定义内容类型，本轮已在 [`健康贴士来源与范围复核-2026-09-16.md`](健康贴士来源与范围复核-2026-09-16.md) 中单独处理，不能混入本 bundle。

## 2. 当前源快照复核

输入为 `.local/health-knowledge/legacy-source-snapshot.json`，导出器和审计只读，不进入 Git。`pnpm health:source:audit` 当前结果：

| 项目 | 结果 |
| --- | ---: |
| `sourceValid` | `true` |
| `publicationState` | `not-approved` |
| `publishable` | `false` |
| 知识条目 | 15,668 |
| 疾病详情 / 药品详情 | 8,509 / 1,207 |
| 疾病关系 / 部位症状 / 症状疾病 | 16,259 / 5,910 / 19,776 |
| 疾病-药品重复名称组 | 6 |
| 旧正文控制字符 | 115 |
| 被清理文本字段 | 10 |
| 未定义旧来源 | 1（`knowledge_tips`） |

质量摘要与正文重算一致，但一致只证明审计数字没有脱节，不证明医学内容可发布。`pnpm health:source:audit -- --strict` 按预期失败，严格门禁仍阻止质量问题和未审核源进入发布流程。

## 3. 新端实现和 fail-closed 复核

| 层级 | 代码依据 | 二次结论 |
| --- | --- | --- |
| HTTP contract | `apps/api/src/modules/knowledge/index.ts:29-221` | 保留旧患者端路径语义，使用 Bearer 会话、camelCase/opaque ID 和受限查询；没有健康内容写入和 `tips` 路由 |
| 领域 bundle | `packages/domain/src/knowledge-import.ts:94-271,780-817` | 拒绝未知字段、跨类型关系、缺失详情、无时区时间、隐藏控制字符和不可点击药品引用；不接收 `knowledge_tips` |
| 源导出 | `packages/persistence/scripts/health-knowledge-source-export.ts:91-99,516` | 固定记录 `ignoredLegacySources=["knowledge_tips"]`，不伪造其映射 |
| staging 导入 | `packages/persistence/scripts/health-knowledge-bundle-import.ts:20-180` | 只接受显式 staging 环境和确认参数，校验后单事务导入，失败回滚 |
| 患者读取 | `packages/persistence/src/mysql-health-knowledge-repository.ts:305-351` | 只读取唯一 `published` 窗口；无 published 内容或窗口重叠时 fail-closed |
| 小程序 | `apps/miniprogram/src/pages/health-encyclopedia/health-encyclopedia.ts:108-229`、`health-knowledge-search.ts:47-92`、`health-knowledge-detail.ts:58-135` | 只消费带 publication 元数据的只读模型；源未审核时不得展示旧快照 |

当前没有 `.local/health-knowledge/reviewed-bundle.json`，也没有可用于患者端的 published 数据库版本。页面/API 存在只证明代码落点，不证明内容已迁移。

## 4. 二次验证命令

- `pnpm health:source:audit`：通过源结构和摘要一致性审计，但返回 `publishable=false`。
- `pnpm health:source:audit -- --strict`：预期失败，`strictPassed=false`；继续保留质量/审核阻塞。
- `bun test packages/persistence/scripts/health-knowledge-source-export.test.ts packages/domain/src/knowledge-import.test.ts packages/persistence/scripts/health-knowledge-bundle-import.test.ts packages/persistence/src/health-knowledge-import.test.ts`：22 pass、0 fail、70 expect calls。
- `bun test apps/api/src/modules/knowledge/service.test.ts apps/api/src/modules/knowledge/index.test.ts`：9 pass、0 fail、32 expect calls。
- `bun test apps/miniprogram/src/services/api-client.test.ts --test-name-pattern '健康知识'`：需作为客户端错误/响应边界复核；它不替代内容审核、Provider、真机或生产验收。

本轮在用户指定仓库重新执行上述源审计：默认模式返回 `sourceValid=true`、`strictPassed=true`、`publishable=false`；显式 `--strict` 返回 `strictPassed=false` 并以退出码 1 结束。源快照可读只代表输入可审计，仍不代表旧正文可发布；本轮没有导入旧数据或改变发布状态。

## 5. 放行前阻塞和下一步

1. 内容责任人处理 6 组重复关系、115 个控制字符和 10 个清理字段，并保留可追溯的处理决定；不能直接在源快照上手改数字。
2. 生成独立审核 bundle，补齐 `contentVersion`、`status`、`reviewedAt`、`reviewerRef`、固定免责声明、`effectiveFrom`/`effectiveTo` 和完整关系集。
3. 由临床审核确认疾病/药品正文、药品关系可点击性、适用范围和下线/撤回规则；工程工具不能推断医学正确性。
4. 先执行只读 bundle check，再用 `DEPLOY_ENV=staging` 做显式确认的单事务导入，验证失败回滚和重叠发布窗口。
5. 在 staging 验证目录、症状查病、疾病详情、药品详情同一 contentVersion 读取后，再采集同候选版本的公网、Provider、DevTools、真机和生产证据。

在以上证据齐备前，健康百科继续保持 `not-approved` 和 fail-closed；不得复制旧快照为 published，不得把 `knowledge_tips` 或健康自测结果塞进百科正文，也不得因本次复核打开患者端健康内容。

## 旧端首字母索引交互补漏与当前复核（2026-09-17）

旧端健康百科的症状、疾病列表使用首字母分组/索引；新端此前将安全条目平铺展示。本轮在小程序展示层新增 `groupHealthKnowledgeItems`，按服务端 `initialLetter` 分组，缺失值归入 `#` 并置于末尾；症状和疾病两种列表均使用同一分组结构，未根据名称自行推断医疗内容。

本轮健康百科视图/API 回归 18 pass、0 fail、49 assertions；domain/persistence bundle、发布和仓储回归 40 pass、0 fail、114 assertions；小程序菜单/健康百科页面验收 2 pass、0 fail、53 assertions。默认 `pnpm health:source:audit` 返回 `sourceValid=true`、`publishable=false`；显式 `pnpm health:source:audit -- --strict` 返回 `strictPassed=false` 并以退出码 1 结束。当前 development 包已按本轮源码重建并通过 `runtime:verify:dev`，指纹记录在根 `TODO.md`。未导入旧内容、未生成 published 版本、未打开患者端健康百科；P1-12 继续未完成。

## 部位已选数量补漏（2026-09-17）

旧端部位菜单会在每个部位名称旁显示该部位已选症状数量。新端此前只显示底部“已选症状”总数，本轮增加当前发布版本内已加载的“部位 → 症状 ID”映射，并在选择或取消症状后重算 `partSelectionCounts`；页面仅在症状 Tab 显示数量徽标，疾病目录不复用该状态。没有导入旧内容，也没有根据名称推断关系。

小程序健康百科菜单验收 1 pass、0 fail，健康视图 9 pass、0 fail，miniprogram typecheck、`build:dev` 和 `runtime:verify:dev` 均通过。当前 development 包为 52 页，`sourceRevision=workspace-sha256:16489415d62620346f876d9392688cb929a8cdeac40de78a41b6e05ebebb6f46`，`generatedAt=2026-09-16T17:16:42.855Z`。内容审核、published bundle、Provider/公网/DevTools/真机证据仍缺，P1-12 继续保持未完成。

## 当前顺序复核补充（2026-09-17）

重新核对旧健康百科目录、症状查疾病、疾病详情和药品详情页面，确认旧“去搜索”仅为演示 Toast，当前新端已覆盖有可靠来源的目录、首字母分组、部位选择计数、症状查询和详情白名单；未发现可在审核 bundle、质量清理和发布证据缺失时安全新增的功能。

本轮定向回归共 53 pass、0 fail、209 assertions：domain/import 18/43、persistence 16/62、API 9/32、健康知识视图 9/17、小程序菜单/百科入口 1/55。`health:source:audit` 默认仍为 `publishable=false`，`--strict` 仍按预期失败；未导入旧百科数据、未生成 published 版本、未开放患者端内容，因此 P1-12 不勾选。
