# 实时就诊二次验证（2026-09-16）

## 结论

本次重新核对旧端“今日就诊”的 WebSocket、叫号队列位置查询、重连和 token/patient 参数，与未来/历史预约摘要的边界；同时验证新端是否恢复了旧实时调用。旧端实时链路真实存在，但缺消息版本、事件 ID、游标补偿、订阅授权和队列最终一致性 contract。新端只读取 owner-scoped 预约摘要，明确把实时就诊状态标记为未开放，不创建 WebSocket、不调用队列接口、不显示叫号位置。P1-19 继续保持未完成。

本项只涉及实时就诊/陪诊能力，不修改旧服务、旧数据库、Provider、线上配置或支付、医保、混合支付、自费支付、退费、收银台及支付相关 HIS 回写代码。

## 1. 旧服务真实行为复核

旧 WebSocket 封装位于 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/api/ws.ts:1-128`，连接地址为 `VITE_APP_WS_API + "/webSocket/online/message"`。有 token 时既设置 `Authorization: Bearer ...`，又把 `token` 和 `patId` 放入 URL 查询参数，见同文件 `:66-88`；错误/关闭时最多自动重连 5 次，见 `:31-45,113-126`。

旧就诊页在今日标签建立连接，解析数组中的 `messages`，再拼接科室、事件、时间、地点、预约号等页面字段，见 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/pages/consult/consult.vue:214-287`。收到叫号通知后再查询队列位置并回填，见同文件 `:320-337` 及旧 `ws.ts:180-196`。切换标签时关闭连接，但页面 `onHide` 关闭逻辑被注释，见 `consult.vue:430-433`。

旧代码没有形成可核验的统一消息 schema/version、事件唯一 ID、游标、断线补偿、重放去重、患者/就诊事件绑定、服务端订阅范围或队列位置最终一致性规则。未来/历史预约读取是另一条链路，预约日期和状态不能等同于当日排队、叫号或就诊动态。

## 2. 新端二次验证

- `apps/miniprogram/src/pages/consult/consult.ts:169-173,236-291` 只调用 `loadAppointmentRecords`，固定业务日快照并按今日/未来/历史分组，没有 WebSocket 或队列调用。
- `apps/miniprogram/src/pages/consult/consult.ts:31-71` 保留三个标签和预约摘要页大小；`apps/miniprogram/src/services/consult-record-view.ts:1-63` 只按日期把预约事实分组，不生成临床实时状态。
- `apps/miniprogram/src/pages/consult/consult.wxml:24-62` 使用“预约摘要”和“实时就诊状态暂未开放”文案，空态也明确当前只展示已确认的就诊摘要。
- 当前没有新端 WebSocket client、事件 repository、queue adapter 或实时 API route；不会把旧 URL token/patId 或旧消息字段拼接逻辑带回小程序。

## 3. 本次验证结果

### 3.1 就诊摘要与边界测试

执行：

```text
bun test apps/miniprogram/src/services/consult-record-view.test.ts apps/miniprogram/scripts/acceptance.test.ts --test-name-pattern 'consult'
```

结果：`consult-record-view.test.ts` 为 5 pass、0 fail、16 expects；小程序验收中匹配 `consult` 的 3 项为 3 pass、155 filtered、0 fail、58 expects。覆盖当天预约只作为摘要、三标签页面、实时关闭、无 WebSocket/队列直连、空态文案和固定预约摘要边界。测试证明新端没有误报实时能力，不代表 WebSocket、队列 Provider 或真机连接已验收。

### 3.2 当前候选开发者工具只读验收（2026-09-16 14:15 CST）

在当前源码项目 `/Users/yxswy/Documents/GitHub/hospital-platform/apps/miniprogram` 的开发者工具中，从底部“就诊”进入 `pages/consult/consult`。页面实际显示“今日就诊 / 未来就诊 / 历史就诊”三个标签；当前今日和未来为空，历史展示了预约摘要、医生、地点、日期、时间、就诊序号和“已取消”状态，并提供加载更多入口。

本次平台读取为 `GET /appointments/records`，HTTP 200，requestId=`mp-mu46nlox-cg4z3b19`。页面底部仍明确提示“实时就诊状态暂未开放”。本次未建立 WebSocket、未查询队列位置、未填写或提交任何医疗动作；该结果只证明当前候选包的预约摘要和关闭态可渲染，不证明实时就诊业务已完成。

## 4. 分层验收状态

| 层级 | 状态 | 依据/缺口 |
| --- | --- | --- |
| 旧端 WebSocket/队列 | 已确认 | 今日消息、叫号位置和自动重连代码已核对 |
| 旧端消息/授权 contract | 未确认 | 无版本、事件 ID、游标补偿和订阅授权依据 |
| 新端预约摘要 | 已确认 | owner-scoped 只读，固定业务日，三标签分组 |
| 新端实时就诊 | 未实现 | 明确关闭，无 WebSocket/队列调用 |
| 事件/队列/审计 contract | 未冻结 | 缺事件来源、关联键、保留和脱敏规则 |
| Provider / staging | 未验收 | 当前没有可执行业务链路 |
| DevTools / 真机 / 生产 | 未验收 | 需 contract 完成后补连接、断线和最终状态证据 |

## 5. 处理决定

1. P1-19 保持未完成，不复制旧 WebSocket、URL token/patId、队列直连或消息字段拼接。
2. 保留新端预约摘要和“实时就诊状态暂未开放”边界，不把“今日”标签当实时就诊。
3. 待事件状态机、认证、订阅范围、游标补偿、去重、队列关联、后台/断线生命周期和审计 contract 齐全后，再实现独立实时服务。
4. 只有代码、生成包、Provider/staging、DevTools、真机和生产证据分别具备后，才能勾选完成。

## 6. 当前回归记录（2026-09-17）

`consult` 仍仅保留 owner-scoped 预约历史摘要，不包含 WebSocket、旧 token/patId URL 或队列查询调用；实时事件、认证、游标补偿和真机/生产证据仍未确认，P1-19 继续关闭。

## 当前顺序复核补充（2026-09-17）

`consult` 仍仅保留 owner-scoped 预约历史摘要，不建立 WebSocket、不查询队列、不外发旧 token/patId URL。未调用实时 Provider 或提交医疗动作；事件状态机、认证、游标补偿、患者/就诊关联和真机/生产证据仍缺，因此 P1-19 不勾选。
