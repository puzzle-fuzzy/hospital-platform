# 报告详情动作二次验证（2026-09-16）

## 结论

云影像、分享、复诊三个入口的旧端行为不同，不能因为按钮都存在就按同一种业务迁移：云影像是旧端任意 URL 下载，分享只有待实现提示，复诊只有通用页面跳转。新端目前保留了受 owner、患者、会话和短期引用保护的附件候选链路，分享保持关闭，复诊只跳通用预约目录且不自动创建预约。真实 Provider 资源映射、外部分享 contract 和报告关联复诊仍未验收，P1-11 保持未完成。

本次复核没有修改旧服务、Provider、生产配置或支付/医保/收银台代码，也没有执行报告附件真实下载或外部分享。

## 旧端行为复核

| 动作 | 旧端源码行为 | 迁移判断 |
| --- | --- | --- |
| 云影像 | `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/pagesB/health/report_detail.vue:34-43,365-396` 保留下载入口；无可用 URL 时提示“暂无云影像”，有 URL 时经 `proxyForward` 下载并保存 | 新端保留入口和明确空结果提示，但只允许服务端已授权的 opaque 附件，不恢复任意 URL |
| 分享 | 同文件 `:44-51,398-402` 只有 `showToast("分享功能待实现")` | 没有可迁移的分享业务，不实现伪 token、永久链接或任意 URL |
| 复诊 | 同文件 `:184-197,404-409` 只导航 `/pages/consult/consult`，不传报告、患者或预约关联 | 只能迁移通用入口事实，不能宣称报告复诊或自动预约已完成 |

旧报告接口的声明见 `/Users/yxswy/Documents/GitHub/hospital/hospital-app/src/api/modules/ZY.ts:80-145`；它不能证明四类报告都提供可下载资源，也没有独立的附件授权 contract。

## 新端当前边界复核

| 层级 | 当前代码事实 | 状态 |
| --- | --- | --- |
| 详情读取 | `apps/miniprogram/src/pages/report-detail/report-detail.ts:96-218` 先校验当前患者和会话，再读取服务端详情；错误会清空旧读模型 | 代码具备，待真实报告证据 |
| 附件入口 | 同文件 `:280-350` 只消费服务端返回的 `attachmentId`，按 PDF/图片走 `wx.openDocument`/`wx.previewImage`，会话变化和失败时清理状态 | 安全候选子集，待 Provider 资源映射 |
| 云影像 | 同文件 `:352-362` 优先打开服务端附件列表中的图片，无图片时回退第一项 PDF；不接受旧端 `pdfUrl` 直接外发 | 代码具备，待真实资源 |
| 服务端代理 | `apps/api/src/modules/reports/index.ts:43-73` 和 `apps/api/src/modules/reports/service.ts:848-997` 按 owner、患者、报告/附件引用、TTL、origin、MIME 和 20 MiB 边界重新校验 | 结构通过，未做真实 Provider/公网验收 |
| 分享 | `report-detail.ts:359-362` 调用 `navigateToFeatureStatus("report-share")`；`apps/miniprogram/src/services/feature-navigation.ts:428-434` 保持关闭 | 安全关闭 |
| 复诊 | `report-detail.ts:364-368` 只导航 `pages/appointment-directory/appointment-directory`，不传 reportId、不自动提交预约 | 通用入口保留，报告关联未实现 |

## 测试和门禁

- `bun test packages/adapters/src/zhongyang-reports.test.ts apps/api/src/modules/reports/service.test.ts`：51 pass、0 fail、173 expect calls。
- `bun test apps/miniprogram/scripts/acceptance.test.ts --test-name-pattern 'report detail|report.*attachment|report.*actions|report and outpatient'`：11 pass、146 filtered、0 fail、65 expect calls。
- 报告目录/详情和附件测试只证明 owner/patient/session/字段边界和关闭态，不证明 Provider 资源存在或真实文件可打开。

本轮补充旧端空附件行为回归：顶部云影像入口在无附件时仍可点击并提示“暂无可用报告附件”；有附件时继续保持图片优先、PDF 兜底。这只校正页面交互，不证明真实附件资源、MIME 或下载链路已验收。

本轮源码变更已重新生成用户指定 development 包：`pnpm --filter @hospital/miniprogram build:dev` 和 `pnpm --filter @hospital/miniprogram runtime:verify:dev` 均通过；`build-info.json` 记录 `sourceRevision=workspace-sha256:ff6b1500ea1efc9b293ae7077a6ae5727946a0cc7a9e7d292db5437535046c09`、`generatedAt=2026-09-16T15:08:36.533Z`、`pageCount=52`。报告详情相关回归为 439 pass、0 fail、4826 assertions；这仍只证明源码和运行包边界，不证明 Provider、DevTools、真机或生产业务完成。

## 未验证和放行条件

1. 当前 Provider 对 LIS/PACS/ECG/PEIS 四类报告的真实资源字段、资源类型、Content-Type、大小、过期和授权样例；
2. 旧 `reportImgPath/reportPdfPath/pdfUrl` 到新 `attachments` 的逐字段映射和空/拒绝/超时/错误 MIME 结果；
3. 附件 origin allowlist、短期引用、撤回、会话切换、跨患者和真实真机打开的同链证据；
4. 分享受众、患者授权、脱敏字段、TTL、防重放、撤回和访问审计 contract；
5. 报告复诊的目标（科室/医生/互联网问诊）、可携带摘要、患者上下文、重复预约和取消规则；
6. 当前候选源码、dist、开发者工具、公网、Provider requestId、真机和生产证据一致性。

在上述材料齐备前，附件代理继续保持依赖未配置即失败，分享继续关闭，复诊继续只作为用户主动进入通用预约目录的安全跳转。不能把本地 fixture、HTTP 200、页面可进入、附件按钮存在或单元测试通过写成报告业务完成。

## LIS 专家意见字段补漏（2026-09-17）

旧端 LIS 明细卡实际展示 `expertOpinion`（专家意见），新端此前未保留该可选字段。本轮已将其加入 LIS detail 的 domain/contract、众阳 adapter 和小程序详情模板，限制为受控文本（4096 字符），缺失时不补值，也不改变报告解读、分享、复诊或附件 gate。

本轮报告 adapter/domain/API 回归 57 pass、0 fail、187 assertions；报告详情及相关视觉边界小程序验收 12 pass、0 fail、107 assertions；contracts、domain、adapters、API、miniprogram 类型检查通过。development 包已重新生成并通过 `runtime:verify:dev`，最新指纹记录在根 `TODO.md`。未调用 Provider、未导入旧报告数据；真实 Provider 字段、临床责任、附件资源、真机和生产证据仍缺。

## 患者上下文展示补漏（2026-09-17）

旧端报告详情的图文报告基本信息会显示患者姓名。新端详情页在请求前和响应提交前都已经确认当前 owner、当前患者和会话代际，但之前没有把这份当前患者目录中的脱敏名称展示出来。本轮增加 `selectedPatientName`，仅来自当前患者目录的 `displayName`；会话重置、详情错误和患者不匹配时清空，WXML 只在有值时显示“患者姓名”。这不是 Provider 报告字段，也不导入旧报告数据，不扩大报告详情、附件或分享权限。

小程序报告详情定向验收 2 pass、0 fail；miniprogram typecheck、`build:dev` 和 `runtime:verify:dev` 均通过。当前 development 包为 52 页，`sourceRevision=workspace-sha256:2c7092a6c345d2abb06b7ddf10056433d09c63124cc120bb0547e673064198df`，`generatedAt=2026-09-16T17:12:58.234Z`。真实 Provider/公网/DevTools/真机/生产同链证据仍缺，P1-11 继续保持未完成。

## 当前候选重新生成（2026-09-17）

本轮再次执行 `build:dev`、`runtime:verify:dev`，报告 adapter/API 回归 51 pass、0 fail、173 assertions，小程序报告相关定向验收 17 pass、0 fail、140 assertions，全仓 `pnpm typecheck` 13/13 包通过。development 包当前为 52 页，`sourceRevision=workspace-sha256:bec20b7c481db6766dca24bf9856c68d068ba38c8344a29c0962acb2c49eb387`，`generatedAt=2026-09-16T17:40:17.166Z`。这些结果只证明源码、白名单和运行包边界；未调用 Provider、未下载真实附件、未开放分享/复诊，也未导入旧报告数据，P1-11 继续等待真实资源和运行证据。

## 当前顺序复核补充（2026-09-17）

重新核对旧报告详情动作与当前实现，确认云影像仍只从 owner/patient/短期 opaque 引用进入服务端附件代理；PACS 附件按当前 adapter 的受控顺序展示，分享仍为明确关闭态，复诊只进入通用预约目录且不自动创建预约。

本轮定向回归共 71 pass、0 fail、320 assertions：报告 adapter 24/57、API service 27/116、小程序报告验收 17/140、报告日期/范围回归 3/7。未调用 Provider、未下载真实附件、未导入旧报告数据；真实资源 allowlist/TTL、分享受众与脱敏、复诊关联、真机/生产同链证据仍缺，因此 P1-11 不勾选。
