# 后台监控、任务、文件与便民运营不迁移记录（2026-09-16）

## 结论

P3-02 在当前非支付迁移范围内形成明确处置：旧服务中确有监控、定时任务、通用文件和便民服务路由，但当前新管理端没有对应的完整运营闭环。没有生产使用清单、运营 owner、RBAC/审计矩阵和 staging 账号时，不复制接口、不新建管理写入、不删除旧数据；各域暂由旧服务保留或由已有患者端迁移项独立控制。该决定完成的是不迁移/保留记录，不代表后台监控、任务、文件或便民运营功能已实现。

本记录只覆盖非支付运营能力。旧 `common` 下涉及医保/支付/结算的路由排除，不读取、不修改、不验收；本记录没有触碰支付、医保、退费、收银台、账单支付或支付相关 HIS 回写代码。

## 1. 旧服务实际路由证据

旧 FastAPI 总路由挂载 `monitor`、`common`、`application`、`convenience`，见 `/Users/yxswy/Documents/GitHub/hospital/app/api/v1/__init__.py:8-18`。

### 1.1 监控

`module_monitor/__init__.py:3-17` 挂载 cache、online、server、resource 四组路由：

- cache controller 提供缓存读取和删除操作，见 `/Users/yxswy/Documents/GitHub/hospital/app/api/v1/module_monitor/cache/controller.py:15-165`；
- online controller 提供在线用户查询和踢出操作，见 `module_monitor/online/controller.py:17-89`；
- server controller 提供服务器监控读取，见 `module_monitor/server/controller.py:14-36`；
- resource controller 提供资源读取及相关管理操作，见 `module_monitor/resource/controller.py:26-260`。

这些是运维控制面，不是患者小程序迁移的只读业务；旧权限、主机运行环境和操作审计不能从页面存在推断。

### 1.2 定时任务

`/Users/yxswy/Documents/GitHub/hospital/app/api/v1/module_application/__init__.py:3-11` 挂载 `job`。旧 controller 实际包含任务详情/列表/创建/更新/删除/导出、清理日志、暂停/恢复/重启，以及任务日志和详情，见 `module_application/job/controller.py:22-240`。这涉及调度器、重试、停启和日志保留，不应在新管理端凭接口名称直接重建。

### 1.3 通用文件

旧 `common/file/controller.py:14-56` 提供文件上传和下载，并允许下载后删除；上传/下载由 `common:file:upload`、`common:file:download` 权限保护，但当前源码仍需进一步确认文件类型、病毒扫描、路径隔离、访问主体和保留周期。该文件域与支付/医保共用 `common` 总模块，因此本轮只登记非支付文件消费者，不接入旧上传/下载接口。

### 1.4 便民服务

旧 `module_convenience/__init__.py:3-19` 挂载表扬信、电子锦旗、风险评估、我的医生、出院随访和入院预问诊。患者端真实行为已分别在 P1-07、P1-13、P1-14、P1-15、P1-17 记录；旧服务是患者 API，不等同于已存在的后台运营台。当前没有证据证明有一个独立、可授权的审核/运营管理界面可以直接迁移。

## 2. 当前新端对照

`apps/admin/src` 当前文件只有 `App.tsx`、`LogPanel.tsx`、`api.ts`、`raw-logs.ts`、`server.ts`、`types.ts` 及对应测试/样式；`apps/admin/src/App.tsx:607-691` 的管理菜单只有日志和范围外入口。`apps/admin/src/server.ts:572-624` 也没有 monitor、job、file 或 convenience 运营路由。

新 API 的日志只读入口 `apps/api/src/modules/admin/logs.ts:169-218` 是受令牌保护的安全日志查询/详情/内部 ingest，不是监控、任务、文件或便民运营管理闭环。患者端 `my-doctors` 等新 API 也不能被当作后台审核和运营接口。

## 3. 处置矩阵

| 域 | 旧端事实 | 当前处置 | 保留/删除规则 | 解锁条件 |
| --- | --- | --- | --- | --- |
| monitor | cache/online/server/resource 运维路由 | 保留旧服务，当前不复制到新管理端 | 不删除旧监控数据/能力；由现有运维责任人继续维护 | 监控指标、主机权限、RBAC、审计、告警和 staging |
| application/job | 任务控制和任务日志路由 | 保留旧调度器，当前不新建控制面 | 不停止、不重建、不清理旧任务日志 | 任务 inventory、幂等/重试/停启语义、权限和回滚 |
| common/file（非支付） | 上传/下载/可选删除 | 保留既有消费者，当前不提供新管理上传 | 不批量搬迁或删除文件；逐域确认 owner 和保留周期 | 消费者清单、MIME/大小/病毒/路径、访问和删除审计 |
| convenience | 患者端反馈/临床/关系 API | 患者端按 P1 独立迁移；当前不凭空创建运营台 | 旧数据不导入、不删除；新端关闭态继续有效 | 审核状态机、公开脱敏、医护/运营 RBAC、撤回和审计 |
| common 下支付/医保/结算 | 属于范围外能力 | 本轮排除 | 不读取、不修改、不验收 | 另行授权和独立任务 |

## 4. 当前验收状态

| 层级 | 状态 |
| --- | --- |
| 旧路由存在和职责 | 已确认 |
| 新管理端完整闭环 | 未实现 |
| 非支付保留/不迁移决策 | 已记录 |
| 生产使用和责任人 | 未确认 |
| RBAC/审计/保留 contract | 未冻结 |
| monitor/job/file/convenience staging/生产 | 未验收 |

## 5. 处理决定

1. P3-02 以本记录完成当前范围内的“不迁移记录”：旧服务继续保留，禁止复制、删除或用当前日志页冒充运营闭环。
2. P1 的患者端迁移项继续独立推进；患者端有 API 不代表后台具备审核、处理、撤回和失败重试能力。
3. 未来若确认某域仍在生产使用，必须先指定 owner 并补 RBAC、审计、列表/详情/状态、失败重试和回滚，再建立新 API/管理端。
4. 本记录没有修改旧服务、数据库、Provider、线上运行环境或支付/医保代码。
