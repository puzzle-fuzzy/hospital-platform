<view class="page">
	<web-view wx:if="{{cashierUrl}}" src="{{cashierUrl}}" binderror="onWebViewError"></web-view>
	<view wx:else class="error-card">
		<text>{{error || '正在准备医保收银台'}}</text>
	</view>
</view>
