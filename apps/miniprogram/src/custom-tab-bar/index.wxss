/*
	底栏只在这个官方共享组件中固定一次。
	页面不再声明 position: fixed 的底栏，也不再自己判断当前 Tab，避免
	“我的”页面打开后再创建一份独立底栏。
*/
.legacy-tabbar {
	position: fixed;
	right: 0;
	bottom: 0;
	left: 0;
	z-index: 20;
	display: flex;
	align-items: flex-start;
	justify-content: space-around;
	box-sizing: border-box;
	height: calc(130rpx + env(safe-area-inset-bottom));
	padding-bottom: env(safe-area-inset-bottom);
	border-top: 1rpx solid #eeeeee;
	background: #ffffff;
}

.legacy-tabbar-item {
	display: flex;
	align-items: center;
	justify-content: flex-start;
	box-sizing: border-box;
	flex: 1 1 0;
	width: auto;
	height: 130rpx;
	color: #2c2c2c;
	flex-direction: column;
	font-size: 20rpx;
	line-height: 1;
	padding-top: 18rpx;
}

.legacy-tabbar-item-active {
	color: #3d6df6;
	font-weight: 500;
}

.legacy-tabbar-icon {
	display: block;
	width: 48rpx;
	height: 48rpx;
	margin-bottom: 8rpx;
}
