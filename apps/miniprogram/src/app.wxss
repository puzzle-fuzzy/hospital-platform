/*
 * 旧端的视觉基线集中在这里：医疗蓝、浅灰画布、白色内容卡片。
 * 页面样式只能消费这些 token，避免原生小程序逐页出现新的蓝色和灰色。
 */
page {
	height: 100%;
	overflow: hidden;
	--hospital-primary: #3d6df6;
	--hospital-primary-strong: #4b8eff;
	--hospital-primary-soft: #f2f6ff;
	--hospital-primary-border: #85aeff;
	--hospital-canvas: #f5f5f5;
	--hospital-nav: #f8f8f8;
	--hospital-surface: #ffffff;
	--hospital-text: #333333;
	--hospital-muted: #999999;
	--hospital-subtle: #666666;
	--hospital-border: #eeeeee;
	--hospital-danger: #ff4444;
	--hospital-success: #16a34a;
	background: var(--hospital-surface);
	color: var(--hospital-text);
	font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", "PingFang SC", sans-serif;
}

/*
	四个主 Tab 使用独立的内容滚动视口。微信默认会让 page 根节点整体滚动，
	这会把固定 custom-tab-bar 一并带入滚动边界，表现为页面和底栏同时出现
	滚动条。根节点锁住后，只有这个 scroll-view 滚动，底栏始终覆盖在视口底部。
*/
.tab-page-scroll {
	box-sizing: border-box;
	width: 100%;
	height: 100vh;
}

button::after {
	border: 0;
}

swiper,
scroll-view {
	box-sizing: border-box;
}

/*
	患者查询页的加载、空结果和错误状态共用同一块空间。

	只设置 min-height 不足以稳定布局：空态图片、说明和重试动作会把内容
	继续向下撑开，导致“加载中”切换到“暂无记录”时页面突然跳高。这个类
	只负责空间契约，颜色、圆角和页内间距仍由各页面按旧端视觉基线声明。
*/
.query-state-shell {
	box-sizing: border-box;
	height: 380rpx;
	min-height: 380rpx;
	overflow: hidden;
}

.query-state-shell-column {
	display: flex;
	align-items: center;
	justify-content: center;
	flex-direction: column;
}
