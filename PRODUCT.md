# Product

## Register

product

## Users

高平医院患者及其家属，主要在微信小程序中使用。用户通常在移动网络和碎片时间内完成
就诊人切换、预约查询、挂号记录查看、爽约记录查看和门诊费用查询；他们不应需要理解
众阳、HIS、医保或新旧服务的技术边界。

## Product Purpose

这是高平医院原有患者端小程序的 TypeScript/Bun 重制版本。它以旧端的页面结构、资源和
交互习惯为视觉基线，把患者端只读业务逐步迁移到新的 API，同时在真实 provider 契约未
确认前明确关闭写入、支付、医保和结算能力。成功标准是：患者能清楚知道当前就诊人、
查询状态和数据是否真实可用，页面在加载、空数据、错误和切换期间保持稳定且不误导。

## Brand Personality

可靠、克制、亲切。信息要像医院服务窗口一样清楚直接，减少等待期间的不确定感；保留
旧小程序熟悉的医疗蓝、浅灰画布、白色内容卡片和原有图标，不用装饰性动画或夸张视觉
打断患者完成查询。

## Anti-references

- 不把网络错误、依赖未配置或会话失效显示成“暂无记录”。
- 不在爽约记录中自动弹出或嵌入选择就诊人模块，避免用户误以为这是爽约数据的一部分。
- 不使用加载态和空态高度不同的占位卡片，不让查询结果到达时页面突然跳高。
- 不把支付、医保、预约写入或 HIS 回写的入口伪装成已经可用的按钮。
- 不为了视觉效果引入渐变背景、过度圆角、强阴影或与旧端不一致的图标体系。

## Design Principles

1. 先表达业务事实，再表达装饰：患者、状态、日期和恢复动作必须有清晰层级。
2. 状态稳定优先：加载、合法空结果和错误使用同一空间契约，数据到达不改变页面骨架。
3. 显式上下文优先：切换患者是明确动作；未知患者上下文不能静默换人或自动打开另一模块。
4. 旧端熟悉感优先：保留既有页面布局、资源、颜色和文案语义，只在层级和可读性不足处收敛。
5. 能力边界必须诚实：没有 provider/HIS/支付证据的功能保持关闭，并给出可理解的下一步。

## Accessibility & Inclusion

使用中文清晰文案，避免只依赖颜色表达状态；触摸目标保持足够尺寸，长姓名、科室和时间
允许截断而不撑破布局；减少不必要动画，加载动画只表达等待状态。错误状态应提供重试，
但不能把未确认的医疗数据或服务故障降级成空结果。
